# Правила каталога Obsidian против нынешнего состояния плагина

> **Зачем этот файл.** Заказчик решил 2026-09-08: правила каталога Obsidian
> соблюсти уже сейчас, хотя раздача пока идёт узкому кругу через BRAT
> (PRD 15.0, Р-4). Решение сделало правила **требованием**, а не пожеланием.
> Значит первым делом их надо выписать списком против того, что в плагине
> есть, — иначе любая правка «под каталог» будет догадкой.

**Правила сняты с документации 2026-09-08, состояние — с кода в тот же день.**
Каждая строка «как у нас» получена командой, а не чтением: команда написана
рядом, чтобы число можно было переснять, а не поверить ему.

Откуда правила:

| Страница | Что в ней |
|---|---|
| `docs.obsidian.md/Plugins/Guidelines` | руководство по разработке: безопасность, ресурсы, тексты интерфейса, команды, workspace, vault, редактор, стили, TypeScript |
| `docs.obsidian.md/Community+directory/Submission+requirements+for+plugins` | шесть требований к подаче: `fundingUrl`, `minAppVersion`, описание, `isDesktopOnly`, id команд, остатки образцового кода |
| `docs.obsidian.md/Community+directory/Developer+policies` | политика: что запрещено, что требует раскрытия в `README`, имя и товарный знак, лицензия, форки |
| `docs.obsidian.md/Reference/Manifest` | поля манифеста и какие из них обязательны |

---

## 1. Итог одной таблицей

Оценка одна из трёх: **сходится**, **расходится** (надо править) и **решение
заказчика** (правило допускает оба ответа, выбор его).

| № | Что требует каталог | Как у нас | Оценка |
|---|---|---|---|
| П1 | не обращаться к глобальному `app`, брать `this.app` | одно место: `pkm_v2/TagWheel/tagwheel.js:274` читает `globalThis.app` | **расходится** (под З3) |
| П2 | не логировать без нужды | `console.error` — 78 (все в `catch`, это следы отказа), `console.warn` — 10, **`console.log` / `info` / `table` — 15**, и восемь из них в окне отладки полос | **расходится**, дёшево |
| П3 | не пользоваться `innerHTML` / `outerHTML` / `insertAdjacentHTML` | одно место: `src/ui/tagwheel_scroller_overlay.js:183`, `list.innerHTML = ""` | **расходится**, одна строка |
| П4 | никакого `new Function` и `eval` | ноль в рантайме; запрещено линтером для `.ts` | сходится |
| П5 | ресурсы плагина освобождаются при выгрузке | `onunload` закрывает журнал, снимает `<style>`, выгружает хранилище. **Сессия TagWheel не закрывается**, а она держит `window.addEventListener('keydown', …, true)` | **расходится** — это Д-2 разбора готовности |
| П6 | обработчики окна ставить через `registerDomEvent` | `registerDomEvent` не зовётся ни разу; одно прямое `window.addEventListener` — `tagwheel.js:2082` | **расходится** (под З3), лечится вместе с П5 |
| П7 | не задавать хоткеи по умолчанию | ни одной команде (З7) | сходится |
| П8 | `getActiveViewOfType` вместо `workspace.activeLeaf` | четыре места, **все четыре под З3**: `pkm_runtime_v2.js:47`, `status_date.js:1340`, `status_tags.js:1460`, `tagwheel.js:279` | **расходится** (под З3) |
| П9 | `Editor` вместо `Vault.modify` для открытой заметки | правки строки, на которой стоит курсор, идут через `Editor` и транзакции CodeMirror; `vault.modify` до открытой заметки не доходит | сходится |
| П10 | `Vault.process` вместо `Vault.modify` в фоне | **четыре места в `transform_feature.js`** (2550, 2551, 2557, 2558): режимы `overwrite` и `add_to_note` читают заметку через `vault.read` и пишут `vault.modify` — двумя вызовами вместо одного `process` | **расходится**, дёшево |
| П11 | `FileManager.processFrontMatter` для frontmatter | свойства заметки собираются **из шаблона**, а не из заметки: в режиме `add_to_note` над существующим файлом `templateContent` пуст, блок YAML не строится вовсе, и к телу дописывается только текст. Чужой frontmatter плагин не читает и не правит | сходится |
| П12 | Vault API предпочтительнее Adapter API | `vault.adapter` — 15 мест, и **все пятнадцать про папку плагина внутри `.obsidian/`**: файлы текстов, служебный файл правил, журнал разработчика, копия конфига версии 1. Vault эту папку не индексирует вовсе (10.13.26 Ф5), и Vault API до неё не достаёт | сходится **с оговоркой**: причина есть и записана |
| П13 | не обходить все файлы, чтобы найти один по пути | обходов ради одного пути нет. `getMarkdownFiles()` зовётся дважды, и оба раза ради **списка**: шаблоны в папке и список заметок для подсказки ввода | сходится |
| П14 | `normalizePath()` для путей, которые ввёл человек | **не зовётся ни разу.** Путей от человека три: `Templates folder`, `New notes folder`, папка копий настроек. `collectTemplateOptions` нормализует их своей строкой из четырёх `replace` | **расходится** — второе объявление правила (У-32) |
| П15 | `updateOptions()` при смене расширений редактора | **не зовётся ни разу.** `src/ui/editor/mount.js` обходит открытые заметки сам и на каждой шлёт `reconfigure` своих четырёх `Compartment` | **расходится по букве**, см. раздел 5 |
| П16 | никаких инлайновых стилей, всё классами | **182 места**: `transform_feature.js` — 116, `tagwheel_scroller_overlay.js` — 42, `ui/editor/decorations.js` — 23, `custom/dom.ts` — 1 | **расходится**, крупно |
| П17 | брать цвета из переменных темы Obsidian | в `styles.css` 2657 строк и **три** шестнадцатеричных цвета, все три — объявления своих переменных типа Field (`--io-type-tag`, `--io-type-link`, `--io-type-element`) | сходится |
| П18 | `const` и `let` вместо `var` | объявлений `var` — **1560**, и **1490 из них под З3**: `tagwheel_core.js` 778, `tagwheel.js` 407, `line_pipeline.js` 180, `status_line_runtime_unified.js` 68, `token_graph_unified.js` 46, `pkm_runtime_v2.js` 10, `field_model.js` 1. Вне З3 — **70**: `tagwheel_scroller_overlay.js` 66 и `editor_visuals_config.js` 4 | **расходится**; правило Р4 («существующий JS не переписывается») с этим спорит |
| П19 | `async` / `await` вместо цепочек `Promise` | три `.then(`, и ни один не цепочка вместо `await`: очередь записи журнала, продолжение той же очереди и намеренное «не ждать» в редакторе Fields | сходится **с оговоркой** |
| П20 | заголовки в настройках — только если разделов больше одного | 36 групп на семи вкладках | сходится |
| П21 | в заголовках настроек не должно быть слова «settings» | два заголовка: **`Setting ids`** и **`Settings backup`** | **решение заказчика**: тексты согласованы (Р8), переименование — его слово |
| П22 | текст интерфейса — Sentence case, не Title Case | держит гейт прототипа со списком исключений: сущности плагина пишутся с заглавной в любом месте фразы (Р9, решение заказчика) | сходится **с объявленным исключением** |
| П23 | `setHeading` вместо `<h1>` / `<h2>` | своих `h1`–`h6` в панели ноль: заголовок группы рисует платформа по полю `heading` декларативного API | сходится |
| П24 | папки вместо россыпи `.ts` в корне | `src/` разложен по `core`, `features`, `ui`; в корне лежат четыре файла рантайма и точка входа | сходится |
| П25 | убрать имена из образца (`MyPlugin`, `SampleSettingTab`) | ноль вхождений | сходится |
| П26 | `id` манифеста: строчные буквы и дефисы, без `obsidian`, не заканчивается на `plugin` | `inline-overhaul` | сходится |
| П27 | `name` манифеста: короткое, своё, только базовая латиница | `inlineOverhaul` | сходится по букве правила; **см. вопрос ниже** |
| П28 | `version` по semver `x.y.z` | `0.1.0-beta.4` | сходится |
| П29 | `minAppVersion` — минимальная версия, на которой плагин работает | `1.13.0`, и это верно: панель построена на декларативном API 1.13 | сходится |
| П30 | описание: до 250 знаков, с точкой, без эмодзи, начинается с действия | 85 знаков, точка есть, эмодзи нет. Начинается не с действия: «Unified inline workflow platform: …» | **решение заказчика**, текст его |
| П31 | `author` обязателен | `Kuzne` | сходится |
| П32 | `authorUrl` и `fundingUrl` — только настоящие адреса, иначе поля не заводить | оба не заведены; пин `manifest_publish_tests.js` держит запрет и говорит, чем он снимается | сходится (В-75 открыт) |
| П33 | `isDesktopOnly: true` — если используются API Node или Electron | стоит `true`, а `require("fs")` и `require("path")` в рантайме — **ноль** | **решение заказчика**: мобильные отложены его словом (Р-2) |
| П34 | `README.md` описывает, зачем плагин и как им пользоваться | есть, 14 КБ; сторожит пин `docs_terms_tests.ts` в обе стороны | сходится |
| П35 | `LICENSE` с ясным указанием лицензии | MIT, в корне | сходится |
| П36 | id команды не повторяет id плагина | ни одна не повторяет (T7); держит `command_ids_tests.ts` | сходится |
| П37 | релиз с тегом, равным версии манифеста, три вложения | делает `release.yml`: сверка тега с манифестом, шесть проверок, поимённая сверка трёх вложений | сходится |
| П38 | не прятать назначение кода обфускацией | сборка не минифицируется вовсе (Р-1, его решение) | сходится |
| П39 | ни рекламы, ни телеметрии на стороне человека | сеть не зовётся ниоткуда: `fetch`, `requestUrl` и `XMLHttpRequest` в рантайме — ноль | сходится |
| П40 | плагин не обновляет себя и свои зависимости | обновление делает BRAT, плагин в это не лезет | сходится |
| П41 | доступ к файлам вне vault раскрывается в `README` | вне vault плагин не читает и не пишет ничего | сходится |
| П42 | имя не должно наводить на мысль, что плагин официальный | слова `Obsidian` в имени нет | сходится |

---

## 2. Расхождения по цене

Восемь строк. Порядок — по цене, а не по важности: дешёвое стоит сделать
сразу, потому что сделанное остаётся сделанным.

### Р1. `innerHTML` в оверлее скроллера — одна строка

`src/ui/tagwheel_scroller_overlay.js:183`: `target.list.innerHTML = ""`.
Файл **не под З3**. Правило Obsidian требует `el.empty()`, и ровно это здесь и
нужно: строка чистит список, а не вставляет разметку. Линтер запрещает
`innerHTML` только в `.ts` — значит запрет надо распространить на `.js`
рантайма, иначе следующая такая строка приедет молча.

**Чем закрепить:** сплошной обход рантайма в `bootstrap_loader_tests.js` — ни
одного `innerHTML`, `outerHTML`, `insertAdjacentHTML`.

### Р2. Пятнадцать шумных записей в консоль

`console.log`, `console.info` и `console.table` — 15 мест:

| Где | Сколько | Что это |
|---|---|---|
| `src/features/strip_debug_api.js` | 8 | окно отладки полос: человек сам зовёт его из консоли |
| `src/core/config_write.js` | 3 | «копия конфига версии 1 сохранена», «служебный файл уехал» — разовые события переезда |
| `src/features/plugin_bootstrap.js` | 2 | `loaded` при каждом запуске и печать сводки |
| `pkm_v2/TagWheel/tagwheel.js` | 1 | запасной путь сообщения, когда `Notice` недоступен (под З3) |
| `src/ui/settings/texts_runtime.ts` | 1 | не код, а слово `console.log` в объяснении |

**`console.error` из этих 78 не считаю расхождением:** правило про
«unnecessary logging», а след отказа в `catch` нужен именно затем, чтобы
человек мог прислать его в отчёте. Стоит другого — правила, **какой** отказ
говорит вслух, а какой молчит; это Д-4 разбора готовности, и оно уже стоит в
очереди.

**Что править:** `plugin_bootstrap` печатает `loaded` при каждом запуске — это
и есть образцовый «unnecessary». Остальное живёт за тумблером журнала
разработчика или за прямым вызовом человека, и это законно; надо только
убедиться, что за тумблером, а не рядом с ним.

### Р3. `normalizePath` не зовётся ни разу

Путей, которые вводит человек, три: `Templates folder`, `New notes folder` и
папка копий настроек. Дальше они идут в `getAbstractFileByPath` и
`createFolder` как есть, а `collectTemplateOptions` приводит их к виду своей
строкой из четырёх `replace`.

**Чем это платит человек:** путь с обратной косой, с двойной косой или с
косой на конце — это разные строки для vault и одна для человека. Папка
создастся не там, где он ждал, либо создастся вторая рядом.

**Цена:** один помощник и три места вызова. Своя нормализация снимается — это
второе объявление правила, которое уже есть у платформы (У-32).

### Р4. Выгрузка плагина не закрывает панель TagWheel

Это **Д-2** разбора готовности, и заказчик уже поставил его в очередь. Правило
каталога делает его не «хорошо бы», а требованием: перехват `keydown` на
стадии capture переживает выключение плагина.

**Цена:** три строки в `onunload`. Файл под З3 — нужен вопрос.

### Р5. Прямой `window.addEventListener` вместо `registerDomEvent`

Одно место — `tagwheel.js:2082`, и это тот же обработчик, что в Р4. Правило
каталога называет и способ: `registerDomEvent` снимает обработчик сам при
выгрузке, и тогда Р4 чинится не тремя строками в `onunload`, а сменой одной.

**Но у этой замены есть цена, которой нет у Р4:** `registerDomEvent` живёт у
компонента плагина, а сессия TagWheel заводится в движке под З3, куда объект
плагина приходит не всегда. Разбирать это надо вместе с Р4, одним заходом, и
спрашивать разрешение один раз.

### Р6. Глобальный `app` и `workspace.activeLeaf` — пять мест, все под З3

| Место | Что там |
|---|---|
| `pkm_v2/TagWheel/tagwheel.js:274` | `globalThis.app` как запасной путь к приложению |
| `pkm_v2/TagWheel/tagwheel.js:279` | `app_.workspace.activeLeaf` |
| `pkm_runtime_v2.js:47` | `ws.activeLeaf.view` |
| `pkm_v2/status_date.js:1340` | `activeLeaf?.view?.editor` с откатом на `activeEditor` |
| `pkm_v2/status_tags.js:1460` | то же |

Два последних уже умеют читать `activeEditor` — правило требует именно его
(точнее `getActiveViewOfType`), а `activeLeaf` у них стоит **первым**. То есть
починка — это не новая ветка, а порядок двух существующих.

**Цена:** по строке на место, пять мест, все под З3. Разрешение спрашивается
после разбора (У-9).

### Р7. Сто восемьдесят два инлайновых стиля

Самое крупное расхождение и единственное, которое я **не советую** брать целиком.

| Файл | Мест | Под З3 |
|---|---|---|
| `src/features/transform_feature.js` | 116 | нет |
| `src/ui/tagwheel_scroller_overlay.js` | 42 | нет |
| `src/ui/editor/decorations.js` | 23 | нет |
| `src/ui/settings/custom/dom.ts` | 1 | нет |

**Почему не целиком.** Часть этих стилей — не оформление, а **вычисленная
величина**: положение оверлея скроллера, ширина полосы тега, отступ виджета.
Такое в класс не переносится вообще: значение известно только во время
отрисовки. Правило каталога про **захардкоженное** оформление, и оно право
ровно там, где стиль — цвет, шрифт или отступ, известный заранее.

**Значит работа делится на две.** Сначала пересчитать 182 места по признаку
«величина или оформление» — это разбор, а не правка. Потом перенести в классы
то, что оформление, а вычисленному оставить свой путь через переменные CSS,
как уже сделано для цвета полос (У-68).

**Цена разбора:** один заход. Цена правки — по числу, которое разбор даст, и
раньше него называть её значило бы гадать.

### Р8. Запись в чужую заметку идёт `read` плюс `modify` вместо `process`

`src/features/transform_feature.js`, четыре места: режим `overwrite`
(строки 2550, 2551) и режим `add_to_note` (2557, 2558). Оба читают заметку
целиком `vault.read`, собирают новое содержимое и пишут `vault.modify`; ещё
два вызова — откат, возвращающий прочитанное.

**Чем это платит человек.** Между чтением и записью заметка может измениться —
он сам её и правит в соседней вкладке. `Vault.process` делает то же одним
атомарным ходом: читает, отдаёт функции и пишет результат, не давая двум
записям наступить друг на друга. Правило каталога называет ровно этот случай —
«фоновая правка заметки, которая не открыта».

**Цена:** четыре места, файл **не под З3**. Откат остаётся откатом: он и сейчас
пишет заранее прочитанный текст, и после перехода на `process` не меняется.

**Запасного пути не нужно:** `Vault.process` объявлен в
`node_modules/obsidian/obsidian.d.ts:7510`, а `minAppVersion` у нас `1.13.0` —
то есть метод есть у каждого, кто плагин поставит. Заводить откат на `modify`
значило бы завести ветку, которую никто не выполнит (У-90).

**Чем закрепить:** `transform_runtime_tests.js` — оба режима коллизии на
подделке vault, где `process` записывает, что́ ему передали функцией: правка
обязана прийти как **функция от прочитанного**, а не как готовая строка, иначе
атомарность потеряна при живом вызове `process`.

---

## 3. Что решает заказчик, а не я

Четыре места, где правило допускает оба ответа. Каждое — вопрос ему, и ни одно
не правится молча.

1. **Два заголовка со словом «settings»** — `Setting ids` и `Settings backup`
   (П21). Правило говорит «avoid», а не «forbidden»; тексты панели согласованы
   им (Р8), и переименование — его слово. Замены, которые не теряют смысл:
   `Ids of the panel rows` и `Backup`.
2. **Имя плагина `inlineOverhaul`** (П27). Требование каталога оно выполняет:
   базовая латиница, коротко, своё, слова `Obsidian` нет. Но в списке
   каталога все имена читаются как имена — `Dataview`, `Templater`, — а
   `inlineOverhaul` начинается со строчной и склеен из двух слов. Это вопрос
   узнаваемости, не соответствия, и имя он уже назначал (10.13.42).
3. **Описание в манифесте** (П30). Требование — начинать с действия. Наше
   начинается с того, **что** это, а не что оно делает. Вариант, который
   правилу отвечает: «Carries status, dates and links inside a single line, and
   turns that line into a note.»
4. **`isDesktopOnly: true` при нулевом использовании Node** (П33). Правило
   просит ставить флаг, когда API Node используется; у нас не используется, и
   формально флаг лишний. Но снять его значит пустить плагин на телефон, где
   весь его способ работы — клавиатурный. Мобильные отложены его словом (Р-2),
   и до тех пор `true` — честный ответ, а не расхождение.

---

## 4. Расходится по букве, но не по делу

Два правила, где наш ответ на вопрос правила верный, а способ — не тот, что
названо в руководстве. Их стоит держать в списке именно затем, чтобы на review
не отвечать наспех.

### Б1. `updateOptions()` не зовётся: расширения пересобираются по открытым заметкам

Правило: «Use `updateOptions()` to update all editors» при смене расширений
редактора. У нас четыре `Compartment` (`tagwheelHeader`, `tagVisual`, `strip`,
`sourceMarks`), и `src/ui/editor/mount.js` обходит открытые заметки,
дожидаясь у каждой `cm.dispatch` с `reconfigure` своего отсека.

**Почему так, а не по правилу.** `updateOptions()` заставляет редакторы
перечитать расширения, зарегистрированные плагином, — то есть работает на
уровне «все расширения сразу». `Compartment.reconfigure` меняет **один** отсек
и не трогает остальные три; именно это и нужно, когда человек поменял цвет
полос, а виджеты заголовка панели переставлять незачем.

**Что бы стоило сделать всё-таки:** позвать `updateOptions()` **рядом** —
не вместо. Он единственный, кто достаёт до редакторов, которых нет в списке
листов (всплывающие окна, popover заметки). Цена — одна строка, и проверить её
из репозитория нечем: списка листов у заглушки не бывает.

### Б2. `var` в движках: 1490 объявлений под З3

Правило требует `const` и `let`. Правило проекта Р4 говорит обратное:
существующий код на JS не переписывается. Спор решается ценой: переписать
1490 объявлений в `tagwheel_core.js` и `tagwheel.js` — это диффы поперёк всего
файла, у которого поведение снято проверками не целиком, а цена ошибки —
молчаливая потеря текста человека (15.5).

**Мой ответ на review, если спросят:** правило про новый код, и новый код его
соблюдает — вне З3 объявлений `var` осталось семьдесят, и все в двух файлах.
Семьдесят — это работа на один заход, и её стоит сделать; 1490 — нет.

---

## 5. Чем это закрепляется

Список без сторожа стареет молча — это в проекте куплено дважды (У-64, У-71).
Поэтому у каждого расхождения выше названо, чем оно закрепляется, а сам список
получает **проверку на симптом**, а не на память:

`tests/regression/catalog_rules_tests.ts` — сплошным обходом рантайма, по
одному утверждению на измеримое правило: `innerHTML` и родня, `new Function` и
`eval`, `window.addEventListener` мимо `registerDomEvent`, `console.log` вне
журнала разработчика, глобальный `app`, `activeLeaf` первым, `vault.modify`,
пути от человека без `normalizePath`, число инлайновых стилей.

**Числа в утверждениях — не «должно быть ноль», а «не больше, чем сейчас»**, и
у каждого такого «не больше» обязан быть положительный контроль (У-88): пусто
верхнюю границу выполняет лучше всех. Поэтому обход обязан находить хотя бы
одно место каждого вида, иначе он мерит пустоту.
