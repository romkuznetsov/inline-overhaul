# Приняты его словом в цикле 95 (тесты 1 и 5 цикла 94)

Тест 1 — «перенеси задачу в бэклог»: `docs/dev/REMAINING_WORK.md`, раздел 4.

### 367у. `Keep sub-fields` — **сделано 2026-09-25, вечер** (`В-224`, `В-228`, тест 5 цикла 93)

Переключатель под `Fields to keep`, путь `transform.inline2note.sourceProcessing.keepSubFields`, умолчание `Off`. `On` — у отмеченного Field на строке остаются и дочерние Values, копия уходит в заметку. Прогоном до правки подтверждено: сегодня дочернее Value отмеченного Field уходит со строки — это поведение `Off`. Закреплено `parent_navigator_tests.js` с отрицательным контролем `Off` и мутацией.

### 333у. Буллит вернулся: 28 против 54 — **разобрано 2026-09-24, ночь, без правки**

Выключение плагина снимает его стили, и Obsidian на этом сбрасывает весь кэш отступов (`css-change` → `clearCache`, `app.js` 1.13.7) — поэтому оно лечит, но не называет виновного. Кэш хранится по номеру строки и при промахе берёт запись другой строки с началом той же длины. Версия «Source mode» ваши 28 точек не объясняет. **Как проверить:** тест 2 — вернётся ли дефект без плагина. Разбор — PRD 10.13.264.

### 329у. Висячий отступ: ваш `outerHTML` назвал режим — **разобрано 2026-09-23, вечер, без правки**

**Ваши данные к тесту 6.** У строки с дефектом отступ 10 точек, после правки 22. Разница 12 — ровно отступ знака списка в Live Preview (`0.75em`) против Source mode (`0`) в `app.css` Obsidian 1.13.7: строку замерили, когда редактор был в Source mode или переключался, а кэш замера сбрасывает только правка строки. Плагин режим не трогает. **Как проверить:** тест 3 — переключить режим туда и обратно, с плагином и без. Разбор — PRD 10.13.259.

### 326у. Перенос длинной строки под буллитом — **разобрано 2026-09-23, день, без правки**

**Ваше замечание** («Новое пишите сюда»): строки переноса уходят левее буллита и возвращаются после правки строки. Отступ ставит и перемеряет сама Obsidian (`app.js` 1.13.7): при перемерке стирает `text-indent` и `padding-inline-start` и оставляет стёртыми, если замер не удался; правка строки заставляет перемерить. На вашей строке нет ни одного нашего узла, плавающая кнопка у вас выключена, наших правил отступа строки в коде нет. Воспроизвести без вас нечем — просьба в тесте 6 (день без плагина или `outerHTML` строки). Разбор — PRD 10.13.256.
