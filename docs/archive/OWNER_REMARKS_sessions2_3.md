# Замечания заказчика: сессии 2 и 3, 2026-09-01 (закрыты)

Обе сессии закрыты 2026-09-01. Живая сводка — `docs/OWNER_REMARKS.md`.
Здесь полный разбор каждого пункта: где задавалось поведение, файл и функция,
цена правки и чем она закреплена. Открывать, когда нужна формулировка пункта
или причина, по которой сделано именно так.

---

# Сессия 2 — поведение в редакторе, 2026-09-01

Источник: **`test-vault/Test 1.md`**, блок «Проверка в инлайн записи». Файл
замечаний сменился: `Test.md` остался с реестром сессии 1, а новые замечания
вы пишете в `Test 1.md`. Перечитывать надо оба.

**Нумерация здесь моя и служебная.** Адресом считается ваш текст, `И-N` — ярлык
для ссылок из других документов.

**Разбор всех пунктов закончен 2026-09-01** — все места, где задаётся
поведение, файл и функция по каждому, цена и чем будет закреплено. Порядок
«сначала разбор, потом разрешение» вы закрепили в тот же день по задаче 5.3.1.

## Что изменилось в самом блоке

| Реестр | В `Test 1.md` | Итог |
|---|---|---|
| И-1, И-2.1, И-2.2, И-3, И-4 | ваши пункты 1, 2.1, 2.2, 3, 4 | живые |
| **И-2.3** `Empty bubble width` | не повторён | оставлен открытым, причина найдена |
| **И-2.4** Tag Bars не работают в записи | нет; в пункте 1.4.2.1 вы пишете «в самой заметке все работает нормально» | **снят вашим словом**, остаётся только предпросмотр в панели (сессия 3, 1.4.2.1) |

## Разрешение по З3, данное 2026-09-01

Вы разрешили тронуть файлы движка ради **трёх** правок: И-1, И-3 и И-4. Это
исключения **пятое, шестое и седьмое** к З3. Каждое закреплено парой пинов —
поведение и исходник, — как все четыре предыдущих. Список исключений —
`CLAUDE.md` и PRD 3.3.

**Все три сделаны 2026-09-01, тем же заходом.** Тронуто ровно то, о чём шла
речь: одна строка уведомления, один аргумент в `applySelection` и один
помощник в `buildTagTokenKeyMap`. Ни один из четырёх остальных проходов,
найденных разбором, править не пришлось — и это не удача, а следствие того,
что разбор был доведён до конца до правки, а не после.

## Реестр

### И-1 — уведомление при каждом открытии TagWheel

`[механика]` Всплывает `TagWheel: режим активирован (left)`.

**Все места (одно).** `pkm_v2/TagWheel/tagwheel.js:1769`, `runTagWheel` —
последняя строка успешного открытия. Шва наружу нет: `makeTagWheelNotice`
(строка 22) берёт `Notice` из `globalThis` (строка 273), плагин ничего не
передаёт. Копии в `main.js` нет — `tagwheel.js` грузится из vault.

**З3:** да. **Цена:** 1 строка, 1 файл.
**Чем закрепляется:** поведение — `tests/TagWheel/tagwheel_tests.js`
(открытие на поддельном `Notice` не даёт ни одного сообщения); исходник —
`tests/regression/bootstrap_loader_tests.js` (строки с этим текстом нет).
**Статус:** **сделано 2026-09-01**, вами не проверено.

### И-2.1 — строка TagWheel не выделяется цветом

`[новая функция, не починка]` Это Н-4 из PRD, раздел 3b.

**Все места.**

| Место | Файл, функция | З3 |
|---|---|---|
| `==` ставятся при `rules.ui.activePanel.useHighlight === true` | `pkm_v2/TagWheel/tagwheel_core.js:2754–2761`, `renderControlLine` | да, **править не нужно** |
| ветку `ui` в документ правил кладёт | `src/features/rules_markdown_builder.js:73`, `buildRulesShapeFromConfig` — `ui: slice(behaviorCfg, "ui")` | нет |
| ветки нет в умолчаниях | `main.js`, `DEFAULT_CONFIG.pkm.behavior` | нет |
| контрола нет нигде | прототип, `src/ui/settings/schema/visual.ts`, группа `tagwheel` | нет |
| заливку рисует по `==…==` | `main.js:4133 buildTagwheelHeaderDecorations`, `TagwheelFillWidget` | нет |

Подтверждено на вашем vault: блок `tagwheel-ui` сгенерированных правил — `{}`,
поэтому `panelCfg` равен `null` и ветка не выполняется никогда. Цвет заливки у
вас уже задан (`visual.tagWheel.fillColor: #f1e596`) — не хватало ровно `==`.

**Ловушка.** Внутри `activePanel` есть свой `showMarkers` — это **не** тумблер
`Show tag markers`, а обёртки `{TW} … {/TW}` вокруг строки. Писать в
`activePanel` можно только `enabled` и `useHighlight`.

**Ваше решение 2026-09-01:** **тумблер в `Visual → TagWheel`**, рядом с
`Background`. Варианты «всегда включено» и «признать ушедшим» отклонены.

**З3:** нет — движок уже умеет, дописывается только запись значения.
**Цена:** ~10 строк в `rules_markdown_builder.js`, ~8 строк умолчаний и клампа
в `main.js`, контрол в прототипе плюс генерация схемы.
**По Р13 начинается описанием в PRD 10.13.**
**Чем закрепляется:** поведение — `tests/TagWheel/tagwheel_tests.js`
(`renderControlLine` оборачивает в `==` при включённой настройке и не
оборачивает при выключенной) и `tests/regression/settings_layer_tests.ts`;
исходник — `bootstrap_loader_tests.js` (строитель правил кладёт `useHighlight`
и **не** кладёт `showMarkers`).
**Статус:** **сделано 2026-09-01** — тумблер `Highlight the line` в `Visual → TagWheel`, по умолчанию выключен. Вами не проверено.

### И-2.2 — прозрачность блоков и размер текста достаются только тегам

`[дефект]` Emoji-elements и links остаются неизменными.

**Все места, и все вне запрета.**

| Место | Файл, функция |
|---|---|
| сканер токенов строки — `const rx = /#\S+/g` | `main.js:3637`, `buildTagVisualDecorations` |
| второй фильтр: `if (!hasVisualOverride) continue` — тег без заданного цвета тоже не получает ни прозрачности, ни размера | `main.js:3769`, там же |
| где применяется | `main.js:3480–3542`, `TagVisualTokenWidget.toDOM` |
| зона левый/правый считается по позиции относительно разделителей, к виду токена не привязана | `main.js`, `resolveTagVisualZone` |

**Опасность.** Нынешний механизм — `Decoration.replace` виджетом: токен
заменяется своим `span`. Для `[[test1]]` это убьёт родную ссылку Obsidian.
Ссылкам и элементам нужна `Decoration.mark` со стилем, а не замена.

**Ваше решение 2026-09-01:** распространить на **теги, эмодзи-элементы и
ссылки**; ваш текст между разделителями не трогается.

**З3:** нет. **Цена:** ~30–40 строк в `main.js`.
**Чем закрепляется:** `tests/regression/tag_visual_render_tests.ts` — строка с
тегом, элементом и ссылкой в левом блоке отдаёт три диапазона с одной
прозрачностью, у ссылки — `mark`, а не `replace`; плюс пин на то, что сканер
один и берёт три вида токенов.
**Статус:** **сделано 2026-09-01**, вами не проверено.

### И-2.3 — `Empty bubble width` не виден в записи

`[дефект]` Причина не в конфиге и не в применении: **формулы две**.

| Где | Формула | При вашем `emptyBubblePct: 50` |
|---|---|---|
| панель | `styles.css:309` — `calc(30px * var(--io-empty-x))` | 15 px, на 180 % — 54 px |
| запись | `main.js:3522` — `max(6, round(horizontalPaddingPx * 2 * emptyScale))`, где `horizontalPaddingPx` = 5 при ширине пузыря 80 % | **6 px**, на 180 % — 18 px |

Вся шкала 50…180 % умещается в записи в 6…18 px, а нижняя треть упирается в
предел и не двигается вовсе. Режим включён: у `#/1`, `#/2`, `#/3` стоит
`visibility: "empty"`.

**З3:** нет. **Цена:** 1–2 строки в `main.js`. По Р8 нормативен прототип,
поэтому запись приводится к панельной формуле; вашего решения не требует.
**Чем закрепляется:** `tag_visual_render_tests.ts` — ширина пустого пузыря при
50/100/180 % совпадает с панельной; плюс пин на то, что формула одна.
**Статус:** **сделано 2026-09-01** — заметка считает пустой пузырь тем же числом, что и панель. Вами не проверено.

### И-3 — префикс при выходе из цикла: хоткей и TagWheel расходятся

`[дефект]` Разошлись на **одном аргументе**.

| Место | Файл, функция | Роль |
|---|---|---|
| **корень** | `pkm_v2/TagWheel/tagwheel.js:1079–1085`, `applySelection` | зовёт `resolveOffPrefixFlagsUnified` с литералом `clearedOwnCheckbox: false` |
| эталон, ход 1 | `pkm_v2/status_tags.js:1957` | передаёт `targetSelectionClearedByAction && fieldHasAnyCheckboxRule(...)` |
| эталон, ход 2 | `pkm_v2/status_tags.js:2198` | то же |
| недостающий второй проход | в `tagwheel.js` нет вызова `enforceOffModeFinalPrefixUnified`; у хоткея он есть дважды (`status_tags.js:1987`, `:2415`) | добивает префикс после финализации |
| общее решение, не меняется | `src/core/pkm_line_finalize_unified.js:1722 resolveOffPrefixFlagsUnified`, `:173 buildPrefixUnified` | при `clearedOwnCheckbox` даёт буллит, иначе сохраняет исходный чекбокс |
| третий проход, полей-дат | `pkm_v2/status_date.js` этих флагов не считает вовсе | ещё одна асимметрия, вами пока не виденная |

На вашем конфиге воспроизводится точно:
`prefixRules.checkboxByFieldValue.type` = `{"#todo": "[ ]", "#note": "[n]",
"#source": "[s]"}`, `freeRoam.offPrefix: false`, все поля в режиме `off`.

**З3:** да. **Цена:** 1 строка плюс помощник «у поля есть правило чекбокса»
(~10 строк) в `tagwheel.js`; при необходимости — вызов
`enforceOffModeFinalPrefixUnified` (~8 строк) там же.
**Чем закрепляется:** поведение — `tests/regression/runtime_unified_parity_tests.js`
(тот же файл уже держит паритет двух ходов): `- [ ] #todo || text` при выходе
даёт одну и ту же строку хоткеем и через TagWheel; исходник —
`bootstrap_loader_tests.js` (`clearedOwnCheckbox` вычисляется, а не стоит
литералом).
**Статус:** **сделано 2026-09-01**, вами не проверено. Второй половины — вызова `enforceOffModeFinalPrefixUnified` — не понадобилось: расхождение закрылось одним аргументом.

### И-4 — Field типа link не следует Order

`[дефект]` Проходов **четыре**, и первый найденный был не тем (У-9).

| № | Место | Файл, функция | Роль |
|---|---|---|---|
| 1 | **корень** | `src/core/pkm_rules_runtime_helpers.js:821`, `buildTagTokenKeyMap` | карта «токен → ключ Order» строится только по корзине тегов (`rules.leftMode`); `[[test1]]` в неё не попадает |
| 2 | следствие | `src/core/pkm_rules_runtime_helpers.js:588`, `reorderSegmentTokensByOrder` | токен без ключа уходит в `unknown`, а `unknown` дописывается после всех упорядоченных |
| 3 | второй проход | `pkm_v2/TagWheel/tagwheel.js:800`, `relocateTagLikeByOrder` | берёт только `rules.leftMode.fields` — ссылки в переносе по Order не участвуют |
| 4 | третий проход | `pkm_v2/TagWheel/tagwheel_core.js:2455–2457`, `buildTags` | токен ссылки кладётся в `otherManaged`, а тот выливается в слот `otherTags` |
| 5 | четвёртый | `pkm_v2/TagWheel/tagwheel_core.js:385 getTechOrder` → `:77 getPrimaryFieldIds` | при пустом `inlineLayout` (у вас `{}`) раскладка собирается из первых трёх полей корзины тегов; ссылке места в ней нет |

Потребителей карты трое — `tagwheel.js:767`, `status_tags.js:566` и `:670`,
`status_date.js:931`, — поэтому починка места № 1 чинит и TagWheel, и хоткеи.

**Расхождение в вашем тексте, проверить перед показом.** Вы пишете «Мой order —
importance (tag), type (tag), project (link)», но ожидаемую строку приводите как
`#/1 [[test1]] #todo`, то есть ссылка второй. При порядке importance → type →
project правильной была бы строка, названная «реальностью». В сгенерированных
правилах на 11:52 стоит `order.left: [Importance, type, Project]`. На разбор это
не влияет — ссылка не участвует в упорядочивании вовсе, — но порядок на экране
стоит сверить.

Отдельно: `Project` у вас `dependsOn: "type"` с
`enabledForParentValues: ["#todo"]` и лежит в корзине ссылок, а Block ему
назначен слева. Тот же шов, что закрывался исключением № 3 (Н-3), но там речь
шла про сторону, а здесь про место внутри стороны.

**З3:** да. **Цена:** ~6 строк в `pkm_rules_runtime_helpers.js`; если мало —
ещё ~4 в `buildTags` и ~2 в `relocateTagLikeByOrder`.
**Чем закрепляется:** поведение — `tests/TagWheel/tagwheel_tests.js` на вашей
раскладке, с печатью получившейся строки (У-8), плюс
`tests/regression/block_placement_tests.ts`; исходник —
`bootstrap_loader_tests.js` (`buildTagTokenKeyMap` читает и `rightMode`).
**Статус:** **сделано 2026-09-01**, вами не проверено. Хватило места № 1 —
общего помощника: остальные три прохода чинить не пришлось, потому что
перестановка по Order идёт после них и теперь знает ссылку.

---

# Сессия 3 — приёмка панели, 2026-09-01

Источник: `test-vault/Test 1.md`, блок «Новая сессия». Это ваша приёмка
раздела 7а: четырнадцать пунктов, адрес — ваша нумерация. Под З3 не попадает
ничего, разрешений не требуется. Разбор первого прохода: где лежит и что уже
видно чтением.

| Ваш пункт | Где живёт | Что найдено |
|---|---|---|
| 1.1.1 tip и tipbox у хедеров не появились | `src/ui/settings/to_definitions.ts:209`, `groupToDefinition` | **дефект подтверждён.** `group.tip` не читается вовсе: в определение уходят только `heading` и `intro`. Прототип свои подсказки рисует (`settings_prototype.html:2468`) — значит расхождение с нормативом, а не задумка |
| 1.2.1.1 `Command reference` → `Commands & Hotkeys` | прототип `:1455`, `schema/keyboard.ts:62`, `README.md` (2 места), `instructions.md` (5 мест), `settings_layer_tests.ts:187` | переименование; документы правятся тем же коммитом, сторожит `docs_terms_tests.ts` |
| 1.2.1.2 заливка строк-рубрикаторов, три ступени яркости | `custom/command_reference.ts`, `styles.css` | вёрстка |
| 1.3.1 карандаш: `Rename` ничего не делает | **`main.js:2023`** против `custom/fields_model.ts:288` | **дефект подтверждён, корень найден.** Панель разрешает `/^[a-z0-9_\- ]+$/i`, первый проход нормализации (`main.js:2014`) — тоже, а второй (`main.js:2023`) требует `/^[a-z0-9_-]+$/`: без заглавных и без пробелов. Всё остальное молча откатывается к исходному ключу, поэтому имя и остаётся прежним. Правило объявлено в трёх местах, два расходятся |
| 1.3.2.1 tip у `Property` | `schema/pkm.ts`, `custom/yaml_property.ts` | текст |
| 1.3.2.2 `✕` сдвигает выпадающий список | `custom/yaml_property.ts`, `styles.css` | вёрстка: кнопка внутрь поля, а не рядом |
| 1.3.2.3 `tags` пишется списком даже при «одном значении» | `src/features/transform_feature.js`, `buildYamlMapFromContext` | **это не предпросмотр, а то, что пишется в заметку.** Правило: свойство, в которое пишут два и более Field, принудительно становится списком. У вас `type → tags` и `Project → tags`. Конфликт двух настроек, а не ошибка — нужен ваш выбор, чья берёт |
| 1.3.2.4 кавычки у значения | `transform_feature.js:891` и `:894` — всегда `JSON.stringify` | тоже пишется в заметку. Документация Obsidian проверяется до правки |
| 1.4.1.1 `add tag` в нижней левой границе таблицы | `custom/user_tags.ts`, `styles.css` | вёрстка |
| 1.4.2.1 в предпросмотре полос не рисуется третья | `custom/previews.ts:432 drawNode`, дерево — `schema/custom_texts.ts:114` | по коду третья дорожка есть: дерево трёхуровневое, условие `lane < cap`, CSS считает три. Чтением не воспроизведено — нужен прогон панели на вашем `data.json`. Ваше «в заметке всё нормально» снимает И-2.4 |
| 1.4.3.1.1 нет опции, что делать с tree | `schema/transform.ts:103`, строка `content-sublines` | **опция есть** — `Sub-lines (tree) behavior`, `Leave them` / `Take them along`. Она стоит в группе `Note content`, а не в `Source line`; в подсказке `Source line` про это даже написано. Вопрос не в функции, а в том, что она лежит не там, где вы её ищете |
| 1.4.3.1.2 live preview у `Source line` | новый блок рядом с `custom/source_fields.ts` | новая функция, по Р13 начинается описанием в PRD 10.13 |
| 1.4.4.1 добавить Field целиком | — | это **1.6.6.1**: решено вами 2026-08-31, не написано |
| 1.4.4.2 `Move to folder` | — | это **1.6.6.4**: решено вами 2026-08-31, не написано |

Последние два — не новые замечания, а те самые «решено, но не начато». Вместе с
`Line above is header` и подсветкой обработанной строки очередь описаний в 10.13
подпирается теперь и вашей приёмкой.

## Что сделано 2026-09-01

**Тринадцать пунктов из четырнадцати.** Все правки в
`docs/AWAITING_OWNER_CHECK.md`, раздел 7б, со ссылкой на то, чем каждая
закреплена. Коротко о том, что оказалось не тем, чем выглядело:

- **1.3.1 (переименование).** Мест с правилом имени оказалось **три**, а не
  два: третье — `getOrderStrictName` в `main.js`, оно подменяло имя ключом уже
  в заметке конфигурации. Нашлось не чтением, а пином на совпадение правил.
- **1.3.2.3 (`tags` списком).** Это не выбор между двумя настройками, а дефект:
  догадка «два Field в одном свойстве — значит список» стояла **после** ручной
  настройки и молча её перекрывала, хотя подсказка контрола обещает обратное.
  Вашего решения не потребовалось.
- **1.3.2.4 (кавычки).** Документация Obsidian проверена: текстовое свойство
  пишется без кавычек, обязательны они у внутренней ссылки. Так и сделано —
  плюс там, где без кавычек YAML прочитается иначе (число, `true`, двоеточие).
- **1.4.3.1.1 (дерево).** Опция была, но лежала в `Note content`. Перенесена в
  `Source line`, где вы её искали.
- **1.3.2.1 (подсказка у `Property`).** Отменяет ваше же решение 2026-08-27 —
  «?» здесь не ставить. Сделано по последнему слову; прежнее решение записано
  рядом с кодом, чтобы третий круг не начался с нуля.

**Два пункта не сделаны, и вот почему:**

| Пункт | Почему |
|---|---|
| 1.4.2.1, третья полоса в предпросмотре | **не воспроизводится.** На вашем `data.json` значения 1, 2 и 3 дают 2, 4 и 7 полос и дорожки `0`, `0–1`, `0–1–2`. Пин с печатью получившегося стоит. Нужен ваш взгляд после переустановки |
| 1.4.3.1.2, live preview у `Source line` | **сделан позже тем же днём**, 10.13.10. Оказался не новой функцией, а потерянной при переезде: старая панель показывала «Before / After» одной строкой, движок для него цел. Теперь это дерево — строка и две дочерние, как вы и просили |
