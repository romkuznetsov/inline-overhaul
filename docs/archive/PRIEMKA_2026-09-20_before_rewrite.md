# Промпт новой сессии

Скопируйте этот блок целиком в новую сессию.

```
Новая сессия по inline Overhaul. Прошлая, 67-я, шла четырьмя заходами и прошла его очередь целиком: четыре назначенных пункта, одиннадцать замечаний трёх заходов и заказ из интервью. Десять коммитов, семь шагов зелёные (`npm test` — 87 из 87), обход строки 0 расхождений из 234, браузерный шаг зелёный при 50 подменах, мёртвых ключей в его `data.json` 0 из 348.

ДВА ЕГО РЕШЕНИЯ ПРИ ЗАКРЫТИИ — ПРОЧТИ ИХ ПЕРВЫМИ

**Выкладка остановлена его словом** 2026-09-19, вечер: «пока не выкладывай на gh, хочу оттестировать перед релизом». Это третья такая остановка. Шаг 4 завершения сессии **не выполняется**, `git push` не делается ни по ходу, ни при закрытии; локальные коммиты — как обычно, нужны. Снимается только его новым словом. До остановки в тот день в `main` ушли два коммита — исключение к З3 № 132 и контрол `Sub-field works without parent`.

**Выпуск теперь требует полного листа приёмки:** «выкладывать релиз только если в файле приёмка все чекбоксы проверок заполнены, в противном случае — выполнять доработку по комментариям». Условие считается командой до вопроса: `grep -c "^- \[ \]" "test-vault/Приёмка.md"`. Больше нуля — вопрос о выпуске не задаётся вовсе. Правило — `docs/VERSIONING.md` раздел 3, шаг — `docs/SESSION_END.md` пункт 5, правило 160 в `CLAUDE.md`.

ЧТО ДЕЛАТЬ ПЕРВЫМ

Открыть `test-vault/Приёмка.md` и прочитать **его галочки и строки `💬`**. Пустых чекбоксов на закрытии сессии было тринадцать: `S1`, `S4`…`S11`. По его правилу предмет этой сессии — **доработка по его комментариям**, а не выбор из очереди: комментарий в листе есть назначенная работа. Если он не написал ничего и галочек не поставил — работа берётся из `docs/REMAINING_WORK.md`: раздел 1ф (находки с готовым разбором — `Ф-1`, `Ф-2`, `Ф-5`, `Ф-7`, `Ф-8`), раздел 3 (ждёт его глаза). Из разделов 2 и 4 без его слова не начинать.

ЧТО СДЕЛАНО И ЖДЁТ ЕГО ГЛАЗА

`S1` контрол `Stripe direction`; `S4` окно «что изменилось» — нумерованные пункты по строке каждый, все пропущенные выпуски, ссылка в двух местах; `S5` тумблер `Autosave`; `S6` автокопия — только настройки, уведомление и коллаут; `S7` кегль и уровень написанного в Block; `S8` «что изменилось» словами человека; `S9` мёртвые ветки его `data.json`; `S10` панель больше не дописывает значение дочернего Field; `S11` контрол `Sub-field works without parent` — три положения ряда `Child Field` и ряд `Parent Value` при `Show always`. Отчёты `252у`…`263у`, разборы PRD 10.13.204…10.13.215.

ЕГО ОТВЕТЫ ИНТЕРВЬЮ ПОСЛЕДНЕГО ЗАХОДА, ВСЕ ИСПОЛНЕНЫ

`В-150` «добавь контрол для этой опции» — вместо переворота его прежнего решения о предусловии заведена настройка у каждого Field; прежнее поведение стало умолчанием `After parent`. `В-151` — родителя дописывать или нет, вторым рядом, умолчание «не дописывать». `В-152` «и команды и панель» — разрешение читает одно объявление предусловия. `В-153` «про края списка контрол не нужен» — снято мерой: его «револьверный выбор значений» уже работает так у каждого Field и в обе стороны.

ТРИ ВЕЩИ, КОТОРЫЕ ЭТА СЕССИЯ СТОИЛА ЕМУ ПИСЬМА

**Кегль своего узла берётся у того, кто рисует** (У-235, правило 157): пузырь считался от литерала 14 при тексте Obsidian 16, и «при 100 текст в Block такой же, как текст строки» не выполнялось ни у кого. **Обход по литералам считает бухгалтерию чтением** (У-236, правило 158): первый обход мёртвых ключей объявил его файл чистым и не нашёл ту самую ветку, ради которой заказ и возник. **Новая настройка поля едет через перечни** (У-237, правило 159): `Sub-field works without parent` писалась, хранилась, показывалась — и команда молчала, потому что сборка поля для движков перечисляет свойства по именам; нашёл стенд на его конфиге, а не набор.

ПРОЧТИ СОСТОЯНИЕ, А НЕ ВСПОМИНАЙ ЕГО
  repo/CLAUDE.md — правила до первого действия, целиком
  test-vault/Приёмка.md — этот файл, и `Test.md` с `Test 1.md` рядом
  repo/docs/REMAINING_WORK.md — шапка, раздел 1ф (находки), 3 (его глаз), 4 (отложенное)
  repo/docs/OWNER_REMARKS.md — сессия 67, четыре захода и закрытие: его слова дословно
  repo/docs/OPEN_QUESTIONS.md — открытых нет; `В-150`…`В-153` отвечены с разбором
  repo/docs/AWAITING_OWNER_CHECK.md — живые отчёты; сколько их, скажет `grep -c "^### [0-9]*у\." docs/AWAITING_OWNER_CHECK.md`
  repo/docs/VERSIONING.md — знак, момент, условие выпуска и кто нажимает
  repo/docs/SESSION_END.md — завершение: пять шагов, шаг 4 сейчас остановлен его словом
  repo/docs/Z3_EXCEPTIONS.md — адрес каждого исключения, открывать перед правкой файла под З3
  repo/docs/BROWSER_GATE.md — подробности седьмого шага

С ЧЕГО НАЧАТЬ: С ПРОГОНА, А НЕ С ЧТЕНИЯ

`npm test` — 87 из 87. Дальше `node tools/line_matrix.js`: 0 расхождений из 234, и это главная мера; с ключом `--split` их четыре, это находка `Ф-8`. Браузерный шаг гонять по одному.

КАК ПРОВЕРЯТЬ, ЧТОБЫ НЕ БЫЛО РЕГРЕССИЙ

Семь зелёных шагов (седьмой — `npm run gate:browser`, гонять по одному). Мутация до того, как правка объявлена сделанной, и у самой мутации две проверки: бьёт ли она в предмет (У-151, У-222) и осталась ли программа исполнимой (У-175). Обход строки при любой правке строки — двумя заходами. Новая форма дефекта дописывается в обход тем же заходом (У-185). **Заводя настройку поля, пересчитай перечни на её дороге** (правило 159): два разбора настроек, перенос на определение поля, сборка поля для движков и подделки нормализатора в проверках.

**Временные файлы — только в scratchpad.** Патчи писать файлом, а не heredoc, и текст с обратными кавычками — инструментом правки файла, а не через оболочку (правило 122). **`git checkout -- <файл>` не применять** (У-10): откат мутаций — копией файла.

ДВЕНАДЦАТЬ СТЕНДОВ, БЕЗ КОТОРЫХ БУДЕШЬ УГАДЫВАТЬ
  node tools/line_bench.js list|cmd <id> <строка>|panel <left|right> <строка> [клавиши]
  node tools/line_matrix.js [--split] — все его Fields обеими дорогами, две сводки
  node tools/line_stress.js [curve|where] — строки из внешнего мира; `where` — профиль V8
  node tools/form_divergence.js — расхождение между копиями правила
  node tools/rule_copies.js — какие правила объявлены больше одного раза
  node tools/rule_copy_shapes.js — род каждого имени, вердикт по форме
  node tools/undo_bench.js — Ctrl+Z на настоящей истории CodeMirror
  node tools/dead_keys.js — ветки его `data.json`, у которых нет читателя
  node tests/browser/check_editor.js [подмена] — подложка, пузыри, выравнивание
  node tests/browser/check_tagwheel.js [подмена] — настоящая сессия панели
  node tools/coverage_map.js <папка> — что из рантайма не исполняет ни один прогон
  node tools/migration_report.js <файл> — что стало с его `data.json` при переезде

Стенд на его настройках берётся с копией: `IO_DATA=<путь к копии data.json> node tools/line_bench.js …` — так проверялась настройка `Show always`, не трогая его файл.

ЧТО МОЖНО И ЧТО НЕЛЬЗЯ

Читать всё, править документы и проверки — можно всегда. Код вне З3 — можно. Движки под З3 — по постоянному разрешению В-120 при нулевом измеренном расхождении, иначе его словом. Каждое новое исключение записывается строкой в `docs/Z3_EXCEPTIONS.md` И абзацем в PRD 3.3 одним заходом, и тем же заходом — числительное в `ORDINALS` проверки `z3_exceptions_split_tests.js`. Локальные коммиты — нужно. **`git push` — нельзя: остановлен его словом 2026-09-19.** Релиз — нельзя: и по остановке, и по его новому условию (в листе есть пустые чекбоксы).

ЧТО СДАВАТЬ

По каждому куску: строка в `docs/AWAITING_OWNER_CHECK.md` тем же заходом, строка в листе приёмки с ответом на «как проверить» — если нажимать есть что, локальный коммит, `npm run install:test` в конце (и он же с путём к `test5`: он проверяет там). В листе приёмки предложения не разрывать переносом строки — его слово.
```

# Приёмка

**Очередь пройдена целиком, четырьмя заходами.** Ваши четыре пункта, одиннадцать замечаний и заказ из интервью сделаны — строки `S1`…`S11` ниже.

**Последним заходом сделано два куска.** `S10`: панель больше не дописывает значение дочернего поля, у которого родителя на строке нет, — это было по вашему слову «не надо дописывать». `S11`: ряд `Child Field` стал выбором из трёх, и при `Show always` под ним появляется ряд `Parent Value`.

**Третий контрол из вашего заказа я не делал, и вы с этим согласились:** описанный вами револьвер (`last-1 < last < 0 > first > second`) уже работает так у каждого поля и в обе стороны — это измерено командой на ваших настройках.

**Наружу ничего не выложено после вашего слова** «пока не выкладывай на gh»: работа лежит локальными коммитами. Ваше правило про выпуск записано — пока в этом листе есть пустой чекбокс, про релиз я не спрашиваю, а беру в работу ваши комментарии.

**Как отмечать:** работает - галочка, строку `💬` можно не трогать. Не работает или есть замечание - чекбокс не ставьте, пишите в `💬`. Пропустили - напишите `мимо`. Мои строки помечены `→`.

# S. Полоса Block: порядок, имена, сторона

- [x] **S1.** 🕐 Новый контрол `Stripe direction`: полоса только слева, только справа или с обеих сторон
	- → Стоит прямо под тумблером полосы, три значения — `Left`, `Right`, `Both`. Умолчание `Both`, то есть пока вы сами не выберете сторону, ничего не изменится
	- → **Как проверить, за минуту:** `Visual → Inline appearance`, включите `Color the Block with Stripe` и поставьте `Stripe direction` = `Left`. В картинке предпросмотра и в заметке с тегами по обе стороны текста полоса должна остаться только у левого Block; `Right` — только у правого
	- 💬[[Скриншоты]] - замечание к tags-block-fill
	- → **Ваши скриншоты разобраны, строка `S7`.** Причин было две: кегль в Block считался от четырнадцати точек, записанных в коде, а у вас текст шестнадцать; и выравнивание «по середине» браузер считает от высоты буквы `x`, а не от написанного. Обе сняты, промах на браузерном шаге стал нулевым на тридцати парах
	- → **Сам контрол `Stripe direction` при этом ждёт вашей галочки здесь:** он про сторону полосы, а не про кегль
- [x] **S2.** 🕐 Пять имён полосы — вашими словами
	- → `Color the Block with Stripe`, `Stripe color`, `Stripe opacity`, `Stripe height`, `Stripe width`. Прежние подписи остались словами для поиска: напечатаете `Band width` — панель найдёт `Stripe width`
	- → **Как проверить, за минуту:** открыть `Visual → Inline appearance` и прочитать пять строк полосы; заодно поискать в панели `Band width` и убедиться, что строка находится
	- 💬
- [x] **S3.** 🕐 Порядок в `Line view` — ваш прежний, вернул
	- 💬я сказал, что нужно сделать в заданной мной очередности - `tags-text-size-left`, `tags-text-size-right` должны быть над `Color the Block with Stripe`
	- → **Вернул, и вы были правы.** Порядок такой: прозрачность левого Block, прозрачность правого, кегль левого, кегль правого, полоса со всеми своими строками — ровно та последовательность, которую вы назвали пунктом 3
	- → **Что я сделал не так:** ваше «tags-block-fill должен быть над tags-text-size-left» я прочёл как «выше в списке» и переставил. Фраза читалась двояко, и надо было спросить — это стоило бы одной строки интервью. Записал правилом: когда новое слово переворачивает прежнее, а причина не названа, спрашиваю до правки
	- → **Как проверить, за минуту:** открыть `Visual → Inline appearance` и посмотреть на порядок строк в разделе `Line view`
- [ ] **S4.** 🕐 Окно «что изменилось»: нумерация, одна строка на пункт, пропущенные выпуски и ссылка
	- → Причина разрывов была в самом `CHANGELOG.md`: Obsidian показывает одиночный перенос строки переносом (это прочитано в его коде, а не угадано), а файл был свёрстан по восемьдесят знаков. Теперь пункт — одна строка, у каждого номер, и весь файл переписан лаконично: 106 пунктов
	- → Окно теперь показывает **все** пропущенные выпуски, новые сверху, не больше пяти за раз; дальше ссылка на полный рассказ
	- → **Как проверить прямо сейчас, за минуту:** `General → Help → Changelog → Open` — откроется `CHANGELOG.md` на GitHub. Это тот же текст, который покажет окно после обновления
	- → **Само окно** по-прежнему видно только на следующем обновлении плагина: оно показывается один раз на версию. Ссылка в нём стоит строкой `Full changelog on GitHub`
	- 💬
- [x] **S5.** 🕐 `Autosave` — копия настроек при открытии плагина
	- → По вашему ответу: копия снимается при запуске Obsidian, если `data.json` разошёлся с последней автокопией. Тумблер в `Advanced → Backup`, выключен по умолчанию — в ваш vault плагин без спроса не пишет
	- → Копия максимальная (все вкладки и хоткеи), в имени `_autosave`, внутри раздел `What changed` над блоком настроек, держится десять новых
	- → **Как проверить, за две минуты:** включите `Autosave`, закройте Obsidian, поменяйте любое число в `.obsidian/plugins/inline-overhaul/data.json` Блокнотом и откройте Obsidian снова. В папке копий появится заметка с `_autosave` в имени, а в ней — строка про ту самую настройку
	- → **И второй раз подряд копия не появится:** файл уже совпадает с последней автокопией. Это признак, что сравнение работает
	- 💬сейчас autosave слишком чувствительный - он делает автосохранение, даже если изменилась активная панель в настройках ui.activeSettingsTab - это лишнее, должно автосохраняться только при изменении настроек (изменение значений контролов)
	- 💬я хочу, чтобы при создании autosave об этом возникало уведомление - сейчас все происходит молча
	- 💬я изменил data.json, но при открытии плагина я этих изменений не увидел в настройках (я изменил в data.json)           "wikilinks": {
            "Project": {
              "bySection": {
                "Project": {
                  "defaults": [
                    "[[test1]]",
                    "[[test3]]" (было "[[test2]]")
            - 💬у меня в плагине отображается [[Скриншоты]]
    - 💬хочу, чтобы в заметке бэкапа текст "Write anything here. The plugin never reads this part, so nothing you write here changes what comes back" был коллаутом
	- → **Все три замечания сделаны, строка `S6`.** Копия больше не снимается от переключения вкладки панели: причина была в одной строке отбора — сравнение брало «всё, кроме чужих вкладок», а ветка с открытой вкладкой не принадлежит ни одной из них. Уведомление о снятой копии появилось, текст в заметке стал коллаутом
	- → **Про `data.json`, в котором вы не увидели своей правки: дефекта нет, и вот чем это измерено.** Плагин ваш файл принял — правка в нём осталась. Но ветка `pkm.fields.taxonomy.tagWheelConfig.wikilinks…` не читается **никем**: во всём коде слово `taxonomy` встречается в двух файлах, а `bySection` — ни в одном. Значения Field `Project`, которые панель вам показала, лежат в `pkm.fields.links.fields[].values[]`, и там было ровно `[[test1]]` и `[[test2]]`
	- → **Эту ветку я снял:** это кеш конфиг-заметки TagWheel, которую вы сняли решением 2026-09-03, — 589 знаков без читателя, и они успели ввести вас в заблуждение. Уйдёт на первой же записи настроек; вернуть можно из любой копии, включая ваши автокопии. Один раз после обновления увидите лишнюю автокопию — это она и есть
	- 💬я хочу, чтобы ты проверил, что в data.json и в генерируемых файлах бэкапа нет других мертвых веток. В целом ты ранее выполнял аудит кода, я ожидал, что после его завершения и выполнения всех выявленных проблем подобных ситуаций не будет

- [ ] **S6.** 🕐 Автокопия: только настройки, уведомление и коллаут
	- → Копия снимается, когда изменились **настройки**. Открытая вкладка панели настройкой не считается — от неё копии больше нет
	- → При снятой копии всплывает `Settings autosaved to …` с путём заметки. Если копия не понадобилась, плагин молчит
	- → В самой заметке строка `Write anything here…` теперь коллаут
	- → **Как проверить, за две минуты:** при включённом `Autosave` откройте Obsidian, походите по вкладкам настроек, закройте и откройте снова — новой заметки быть не должно. Потом поменяйте любой контрол, перезапустите Obsidian — заметка появится, и всплывёт уведомление с её путём
	- 💬ок, но сейчас в заметке autosave текст в `# What changed` очень технический и не понятен для пользователя (`pkm.fields.order.left: list of 4 → list of 5`). Должно быть понятно - указывай название настроек и что конкретно произошло. В этом примере я переместил field `Cat` из right block в left block. 
	- 💬

- [x] **S7.** 🕐 Кегль и уровень написанного в Block — по вашим скриншотам
	- → При 100 % текст в left и right Block теперь ровно того же кегля, что текст между разделителями, и стоит на том же уровне. Причин было две: кегль считался от четырнадцати точек, записанных в коде (а у вас шестнадцать), и выравнивание «по середине» браузер считает от высоты буквы `x`, а не от написанного
	- → Кегль берётся у **своей строки**: у заголовка он свой, и Block следует ему
	- → **Как проверить, за минуту:** поставьте `Left Block text size` и `Right Block text size` = 100 и посмотрите на строку с тегами по обе стороны текста — буквы в Block и буквы между разделителями должны быть одного размера и на одной линии. Потом поставьте 50: значение должно остаться по середине полосы, как вы просили раньше
	- 💬
- [ ] **S8.** 🕐 «Что изменилось» в автокопии — словами, а не путями
	- → Ваш пример теперь читается так: `Field «Cat» moved from the Right Block to the Left Block`. Обычная настройка — `«Opacity of the Left Block» (Visual → Inline appearance): 100 → 40`
	- → Имена берутся из самой панели, а не из отдельного списка: переименуете строку — изменится и текст в заметке
	- → **Как проверить, за две минуты:** при включённом `Autosave` переставьте любое поле между Block и перезапустите Obsidian. В новой заметке копии, в разделе `What changed`, должна стоять строка про это поле
	- 💬[[Settings 2026-09-19 20-13-12_autosave]] - смотреть setting `pkm.fields.tags.fields` - это непонятно для пользователя ты должен написать название контрола (как в настройках плагина), чтобы пользователь понял что конкретно изменилось. Также группируй элементы если настройки находятся в дочерних контролах (например, изменение цветов value делается в рамках изменения контрола field - значит лог изменения цвета текста value должен быть в sub строке относительно field, и на одном уровне с другим дочерним действием - например, изменения fill)
	- → **Сделано, и проверено на вашей же паре заметок.** Взял `Settings 2026-09-19 19-58-50_autosave` и `Settings 2026-09-19 20-13-12_autosave` и прогнал их через новый разбор — вышло так:
	- → `- Field «Cat_sub»` → `- «Child Field»: After parent → Show always` → `- Value «#med» added`; и отдельной группой `- Field «Cat»` → `- Value «#med»` → `- «Text color»: not set → #1dcd41`
	- → **Три правки.** Ветка определений Fields больше не описывается длиной списка — сказано, у какого Field появилось значение. Контролы названы так, как подписаны в панели, и стоят внутри своего значения, а значение внутри своего Field. И `Child Field` сказан вашими словами (`After parent → Show always`), а не `off → on`
	- → **Убрал и шум:** панель пишет строку значения целиком, и вы получали по четыре строки `not set → empty` на каждое заведённое значение. Правка, у которой обе стороны — умолчание, в отчёт больше не идёт
	- → **Как проверить, за две минуты:** при включённом `Autosave` заведите у любого Field новое значение и покрасьте ему текст, потом перезапустите Obsidian. В новой заметке копии, в разделе `What changed`, значение должно стоять отступом под своим Field, а цвет — отступом под значением, и ни одного пути вида `pkm.fields…` там быть не должно
- [x] **S9.** 🕐 Мёртвых веток в вашем `data.json` не осталось
	- → Было 35 листьев, которых не читает никто: ключи конфиг-заметки, состояние старой панели, тумблеры вида редактора Fields и три листа приставок. Все сняты — плагин их больше не создаёт. Стало 0 из 346
	- → **По дороге нашлось важное:** Transform читал старую форму правил приставок, и три настройки (`Decide by`, `Field order source`, `Parent or child wins`) до него не доезжали. Починено
	- → **Как проверить, за минуту:** откройте `data.json` и поищите `configNote`, `visualSubTab` или `orderShowInfoTips` — их там больше быть не должно (уйдут при первой же записи настроек). Ваши настройки при этом на месте
	- 💬

- [x] **S10.** 🕐 Панель больше не дописывает значение дочернего Field
	- → По вашему слову. Строка со значением дочернего поля, у которого родителя рядом нет, больше не получает это значение вторым разом в правый Block
	- → Обход строки на ваших настройках: было 6 расхождений из 234, стало 0
	- → **Как проверить, за минуту:** напишите строку `- #new :: текст` (значение `Cat_sub` без `Cat`), откройте на ней TagWheel и закройте. Строка должна остаться такой же
	- → **Вторая половина вашего слова — про прокрутку sub-values без родителя — спрошена интервью:** она отменяет ваше же прежнее решение, по которому команда в этом случае говорит `Cat_sub waits for Category`
	- 💬

- [ ] **S11.** 🕐 Дочернее поле может работать без родителя
	- → По вашему заказу. `Settings → Tags & PKM → Fields`, выберите `Cat`: ряд `Child Field` стал списком из трёх — `Show always`, `After parent` (как было, умолчание) и `Hide`
	- → При `Show always` под ним появляется ряд `Parent Value`: `Leave the line alone` (умолчание) или `Add the parent Value`
	- → **Как проверить, за минуту:** поставьте `Cat` в `Show always`, напишите строку `- текст` и нажмите `Cat_sub next` несколько раз: значения дочернего поля должны листаться подряд, а на пустом месте круга строка возвращаться к `- текст`. Переключите `Parent Value` в `Add the parent Value` — вместе со значением появится и `#work`
	- → **Третий контрол, про края списка, я не делал:** револьвер, который вы описали, уже так и работает — проверьте `Cat_sub previous` с пустой строки, он даёт последнее значение
	- 💬для теста я добавил в field=category несколько subvalues `#med`, `#old` и io-field-child=show always. Однако в tagwheel в sub-field есть только значение `#new` (которое было раньше). При этом если сначала выбрать соответствующий родительский токен в field=cat в tagwheel, то после этого в field=cat-sub уже отображаются `#med` и `#old`
	- → **Вы правы, починено.** Родителя при `Add the parent Value` дописываем мы сами, и на следующем нажатии плагин считал его вашим выбором — отбирал по нему, а у выбранного родителя ребёнок один. Отсюда и `#new` в одиночестве, и то, что после выбора родителя руками всё выглядело правильно
	- → **Заодно снялись ещё два:** команда печатала долистанное значение дважды (`- #home #med #med`), и на пустом месте круга наш родитель уходил со строки, а знак задачи от его значения оставался
	- → **Как проверить, за минуту:** нажмите `Cat_sub next` четыре раза подряд с пустой строки `- текст`. Должно идти `#work #new` → `#home #med` → `#health #old` → снова `- текст`, и то же самое в TagWheel стрелкой вверх. А на строке `- #home текст` первым должен встать `#med` — ребёнок того родителя, которого выбрали вы

# N. Ждёт вашей проверки

- [ ] **N4.** 🕐 Окно «что изменилось» больше не показывает разметку
	- → Заголовки становятся заголовками, списки списками. Своего разборщика markdown в плагине не появилось: чужую разметку разбирает тот, чья она
	- → **Как проверить:** окно открывается один раз на версию, поэтому увидеть его можно только на следующем обновлении плагина. Отдельной кнопки у него нет
	- 💬стало лучше, но все равно плохо - [[Скриншоты]]. 
		- Текст с разрывами на предложениях
		- префикс исправлений должен быть нумерацией (чтобы было видно сколько пунктов изменено)
		- текста много - нужны буллиты с понятным, но лаконичным описанием - чтобы было понятно обычному человеку что изменилось
	- этот changelog должен дублировать changelog.md в репозитории (т.е. если пользователь откроет changelog.md он должен видеть то же самое). Сделай так, чтобы они зеркалились, при необходимости скорректируй changelog.md чтобы он соответствовал моим запросам (т.е. changelog.md должен накапливаться при выходе новых релизов, новые версии сверху, а changelog в osidian показывает только изменения последней версии (или лучше N версий с последнего обновления плагина (т.е. если последнее обновление было 2 релиза назад, то в этом чейнджлоге должны показываться изменения в этих двух версиях)). Также в плагине должна быть ссылка на changelog.md в репозитории github (т.е. при нажатии пользователь сразу может открыть). Я не очень опытный в github, возможно я изобретаю велосипед - тогда подскажи более оптимальный и рациональны вариант
	- → **Сделано целиком, строка `S4`.** Причина разрывов была не в окне: `CHANGELOG.md` был свёрстан по восемьдесят знаков, а Obsidian показывает одиночный перенос строки переносом — это прочитано в его коде. Файл переписан в 106 нумерованных пунктов по строке каждый, окно теперь показывает все пропущенные выпуски, ссылка есть и в окне, и кнопкой `General → Help → Changelog`
	- → **Один вопрос остался вам:** ссылка сейчас ведёт на сам файл в репозитории, а можно на страницу релизов. Задан интервью в конце сессии
- [x] **N5.** 🕐 Кегль Block разведён на две стороны
	- → **Ваше замечание записано** и стоит в очереди следующей сессии как `З-10`: `tags-block-fill` со своими дочерними контролами встанет над `tags-text-size-left`. Это правка порядка, который вы назвали в прошлый раз, и новое слово сильнее старого - прежнюю последовательность я возвращать не буду
	- → **Сам кегль при этом уже разведён:** `Left Block text size` и `Right Block text size`, каждая действует только на свою половину строки. Ваши 70 встали с обеих сторон
	- → **Как проверить, за две минуты:** `Visual → Inline appearance`, поставьте слева 140, справа 50 и посмотрите на строку в заметке с тегами по обе стороны текста. Должны разъехаться только Blocks, и каждый по своему числу
	- 💬
- [ ] **N9.** 🕐 Структура репозитория: в корне не осталось ни одного файла плагина
	- → `main.js`, `navigation_runtime.js`, `pkm_runtime_v2.js`, папка `pkm_v2` и `styles.css` уехали в `src`; папка `build` - в `tools`; `media` и `showcase.md` - в `docs`. Папка `docs` похудела с 27 записей до 21, ни один файл не выброшен
	- → **Всё выложено:** девять коммитов в `main`, CI зелёный со всеми семью шагами
	- → **Как проверить, за минуту:** откройте страницу репозитория на GitHub и посмотрите на список в корне
	- 💬[[Скриншоты]] (# 2) - в корне много файлов .json .mjs - можно их тоже спрятать в подпапки?
	- → **Посчитал прогоном, а не на глаз:** на странице в корне двадцать записей, и служебных файлов шесть. Четыре из шести переехать не могут — их ищут в корне те, кто их читает: `manifest.json` и `versions.json` читают Obsidian и BRAT, `package.json` с замком — npm. Переехать могут `tsconfig.json` и `eslint.config.mjs`, и цена — флаг с путём в каждой команде плюс редакторы, которые перестанут находить их сами
	- → **Решаете вы,** вопрос задан интервью в конце сессии. Там же третий вариант: убрать из корня ещё и `CLAUDE.md` — это мой рабочий файл, вам он не нужен

# C. Настройки

- [x] **C1.** 🕐 Настройки, изменённые снаружи, больше не затираются
	- → **Ваш заказ про автоматическую копию записан** и стоит в очереди следующей сессии как `З-11`. Вы сами написали, что набрасываете варианты, - поэтому первым будет разбор, а не код: у вашего описания три развилки, и от них зависит всё остальное. Когда снимать копию (на каждую запись - это сотни файлов при одной протяжке слайдера; при открытии плагина - ваш второй вариант; при приёме чужого файла - ровно тот случай, ради которого вы просите), что писать в строку «что изменилось» и сколько копий хранить. Принесу разбор и один вопрос
	- → **Сама починка уже сделана:** плагин больше не зависит от часов Obsidian. Перед тем как положить своё в файл, он смотрит, что там лежит, и если не то, что писал сам, - принимает диск, бросает свою запись и говорит об этом
	- → **Как проверить способом попроще, за минуту:** откройте `.obsidian/plugins/inline-overhaul/data.json` этого vault в Блокноте, поменяйте любое число и сохраните. Уведомление должно всплыть сразу
	- → **И ваш способ, за две минуты:** скопируйте `data.json` из `test5` поверх файла в `test-vault` при запущенном Obsidian. Уведомления в этот момент не будет - его и не должно быть, Obsidian о таком файле молчит, - но дальше сделайте **любую** правку в панели настроек. В этот момент плагин увидит чужой файл, примет его, скажет `Settings changed on disk…`, и панель покажет настройки из `test5`
	- 💬

# F. Документы на чтение

- [ ] **F3.** 🕐 `README.md` и `instructions.md` приведены к новой панели
	- → Оба дополнены всем, что появилось за последние сессии, включая файл текстов и то, что переключается, а что нет
	- → **Как проверить, за три минуты:** открыть оба файла и посмотреть, не обещают ли они того, чего нет, и не молчат ли о том, что есть
	- 💬 не смотрел

- [ ] **F4.** 🕐 `FEATURES.md` - лист того, что плагин умеет
	- → Новый документ в корне репозитория, по-английски. Ссылка первой строкой README
	- → **Как проверить, за пять минут:** открыть и посмотреть, не обещает ли он того, чего нет, и не молчит ли о том, что для вас важно
	- 💬

# Новое пишите сюда

Я разберу и заведу строками с чекбоксами. Назначенной работы сейчас нет: все ваши пункты и все замечания этой сессии закрыты, открытых вопросов к вам тоже нет.
