# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Сессия 45, 2026-09-13, вечер и ночь — закрыта его словом «ок работает».**
Обе живые строки листа закрыты галочками: `S29` (знак списка, знак заголовка и
артефакты на хедере) и `S15` (поведение строки, `Ctrl+Z`, текст из двух цифр,
подложка на перенесённой строке). Шестнадцать отчётов унесены в
`docs/archive/AWAITING_OWNER_CHECK_session45.md`.

**Что осталось живым:** `F3` и `F4` — два документа, по которым проверять нечем,
кроме чтения, и он их пока не смотрел.

### Раздел 130у — **подтверждён заказчиком 2026-09-13**

Строка `S32` закрыта его галочкой: шаг 3 снятия служебного файла правил принят.
Отчёт унесён в `docs/archive/AWAITING_OWNER_CHECK_session46.md`.

### 135у. Пустой слот под текст — при любом знаке списка — **сделано 2026-09-14, ночь**

**Строка листа `S34`, вторая половина.** Найдено тем же обходом.

**Что было.** Строка `- ` после шага по полю давала `-  :: #/1`, а `* ` —
`* :: #/1`. Разница в одном пробеле, и это не мелочь: два пробела между началом
строки и разделителем — то место, куда встанет ваше слово. У строк, начатых
звёздочкой, плюсом или номером, этого места не было.

**Что изменилось.** Слот держится при любом знаке списка. Старые строки, где он
схлопнут, чинятся сами при следующем действии над ними.

### 134у. Знак списка человека — не только дефис — **сделано 2026-09-14, ночь**

**Строка листа `S34`.** Нашлось обходом по следу вашего замечания, а не вами:
разбирая, где решается судьба начала строки, я прогнал все знаки списка, а не
один.

**Что было.** Строка `* текст` после шага по элементу-дате давала
`- 📅… * || текст`: звёздочка уезжала в зону значений, а строка получала чужой
дефис. То же с `+`, `1.` и `1)`. Это тот же класс, что ваш пример `1. test`
→ `- test`, только в другом движке.

**Что изменилось.** Строка, начатая любым знаком списка, остаётся такой.
Заодно плагин научился видеть список с номером через скобку (`1) текст`) — для
Obsidian это список, а для нас был текст.

**Чего это не меняет.** На дефисе не изменилось ничего: он и был единственным,
который работал.

### 133у. Знак задачи переживает команду поля — **сделано 2026-09-14, ночь**

**Строка листа `S33`.** Ваше замечание закрыто, и условие оказалось шире, чем
выглядело.

**Что было.** Строка `- [ ] ` после `importance next` давала `-  :: #/1`: знак
задачи пропадал. Пропадал он не только у важности и не только у пустого
чекбокса — у любой команды поля, у которого своих знаков нет, и с любым знаком,
включая `[N]` и `[!]`, то есть вместе со значением Field `type`. И ещё в одном
месте: когда шаг по кругу снимал последнее значение и строка сворачивалась к
началу, знак уходил вместе с ним — `- [ ] #work || ` после `Cat previous`
давала `- `.

**Почему прошлая починка не дотянулась.** Правило «сохранить знак» стояло верно
и работало на строке с текстом. Но знак к этому правилу приезжает разбором
строки, а разбор по дороге правился: «на строке нет содержимого — знак убрать».
Правило решало судьбу того, чего уже не было. Отсюда и расхождение между вашим
экраном и моим отчётом.

**Что изменилось.** Строка, начатая как задача, задачей и остаётся. Снятое
значение уносит **свой** знак и не трогает чужой: `- [x] #todo || ` после шага
`type` до конца круга даёт `- [x] `, а `- [ ] #todo || ` — `- `, потому что
`[ ]` у вас и есть вид значения `#todo`.

**Чего это не меняет.** Строка без знака задачи не получает его ниоткуда. Режим
`Free roam: minimal` не тронут — там знак принадлежит самому Field, он рисует им
своё значение; ваше замечание было про `strict`.

**Как проверено.** Обход всех ваших Fields обеими дорогами
(`node tools/line_matrix.js`) получил две новые исходные строки — со знаком
задачи, с текстом и без: было четыре расхождения между командой и панелью,
стало ноль из 72. Обход команд на вашем `data.json` показал 50 изменившихся
мест, и все 50 — про знак; обход панели — два. Четыре новые проверки поведения,
каждая с парой «свой знак уносится / чужой остаётся», и пять подмен, каждая
краснеет на своём утверждении. Семь шагов зелёные.

### 132у. Чекбокс пропадает на строке без текста — **найдено 2026-09-13, закрыто 133у**

**Его замечание, дословно:** «заметил баг - при активации command importance next
исходный префикс строки изменился с чекбокса на буллит - а не должен был. Ты
отчитывался, что это уже починено (io-field-behavior=strict,
placement-bullet-strict=on)».

**Воспроизведено стендом на его `data.json`**, и условие оказалось у́же, чем
выглядело: чекбокс пропадает **только на строке без текста**.

| Строка до | `importance next` | чекбокс |
|---|---|---|
| `- [ ] ` | `-  :: #/1` | **потерян** |
| `- [ ] test` | `- [ ] test :: #/1` | цел, и цел все пять шагов цикла |
| `- [x] test` | `- [x] test :: #/1` | цел |
| `- [ ] #todo` | `- [ ] #todo \|\|  :: #/1` | цел |

**Что это значит.** Прошлая починка была **половинной**: она закрыла случай с
текстом, а случай пустой строки остался — отсюда и расхождение между его
экраном и моим отчётом (У-164).

**Проба повторяется одной командой:**
`node tools/line_bench.js cmd importance-next "- [ ] " 6`.

**Не начато:** он завершил сессию тем же сообщением, которым принёс замечание.
Это первая работа новой сессии.

### 131у. Седьмой шаг проверок вернулся на сервер — **сделано 2026-09-13**

**Строки в листе у этого пункта нет, и это правильно** (У-84): нажать нечего, на
экране не меняется ничего. Отчёт.

**Что это за шаг.** Вид панели и подложек в заметке проверяет настоящий
браузер — единственная проверка, которая меряет точки, а не структуру. Вы
решили 11 сентября поставить её на сервер вместе с остальными шестью; она
упала там в тот же час, оставаясь зелёной у меня, и с тех пор гонялась руками.

**Почему это тянулось два дня.** Я перебирал окружение — восемь шрифтов,
семнадцать ширин окна, — вместо того чтобы прочитать журнал упавшего прогона.
Журнал лежал на сервере всё это время и назвал причину за один взгляд.

**Чем оказалось.** Проверка считала дефектом «подложка дошла до правого края».
Но подложка правого Block по своему же правилу растёт наружу за конец
написанного на 8.8 точки — значит на строке, где текст кончается в десяти
точках от края, **правильная** подложка края достигает. На Linux шрифт шире,
текст кончался ближе к краю — и проверка кричала на здоровой странице. У себя
это воспроизводится при ширине окна 472 точки: окно, в котором видно, шириной
семь точек, и перебор в него не попал.

**Что сделано.** Вопрос переписан так, что величины машины в нём нет вовсе:
подложка перенесённой строки выходит за написанное не дальше, чем подложки
строк без переноса — на них дефекта быть не может, и они эталон с той же
страницы. Шаг возвращён на сервер: **семь шагов из семи зелёные**, и все 49
подмен на сервере краснеют, как и у меня.

**Ваше слово на выкладку потрачено ровно на это.** Рабочая ветка выложена
разово, ради прогона; `main` не тронут, релиза нет.

### Разделы 77у, 78у, 79у, 80у, 82у, 89у, 90у, 91у, 92у, 94у, 95у, 96у, 99у, 100у — **подтверждены заказчиком 2026-09-12**

Строки `S3`, `S4`, `S5`, `S11`, `S12`, `S13`, `S14`, `S16`, `S17`, `S18` и `S19`
закрыты галочками. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions39_40.md`.

### Разделы 37у, 39у, 40у — **подтверждены заказчиком 2026-09-08, вечер**

Четыре решения про каталог Obsidian (`K1`, `K2`), `Del` на пустой строке
(`S1`) и тумблер `Step out of the word` (`S2`) — все четыре строки закрыты
галочками, замечаний под ними нет. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### Разделы 93у, 97у, 98у, 101у, 114у, 117у, 119у, 121у, 122у, 123у, 124у, 125у, 126у, 127у, 128у, 129у — **подтверждены заказчиком 2026-09-13**

Строки `S15` и `S29` закрыты его галочками: «ок работает». Шестнадцать отчётов
унесены в `docs/archive/AWAITING_OWNER_CHECK_session45.md` — там поведение
строки, `Ctrl+Z`, строка-заголовок и подложка на перенесённой строке.

### Отчёты 52у–88у — **унесены в архив 2026-09-13**

Работа 9–11 сентября, у которой **нечего нажать**: ревизия кода семью заходами,
тихие отказы, мёртвый код, два выпуска беты, лист возможностей, сокращение
служебного файла правил. Подтверждается она словом, а не галочкой, и слова
этого пока не было — поэтому строки остаются здесь, а разборы уехали в
`docs/archive/AWAITING_OWNER_CHECK_reports_2026-09-09_11.md`.

**Почему унесены.** Этот файл читается каждую сессию целиком, а разборы
занимали в нём три четверти объёма и не менялись с 11 сентября (У-31).

| Отчёт | О чём |
|---|---|
| 58у | Служебный файл правил сокращён на 28 тысяч знаков |
| 83у, 86у | подтверждены его галочками 2026-09-12 |
| 85у, 87у, 88у | подтверждены его галочками 2026-09-12 |
| 84у | Кто теряет вторую половину значения элемента |
| 81у | Панель TagWheel по Order: разбор, и он оказался неверен |
| 72у, 75у, 76у | подтверждены его галочками 2026-09-12 |
| 74у | Ревизия кода, заход седьмой, и решение по шву TagWheel |
| 73у | Ревизия кода, заход шестой: фикстуры, которые не могут увидеть дефект |
| 71у | Ревизия кода, заход пятый: числа документов против измеренных |
| 70у | Ревизия кода, заход четвёртый: запреты без образца |
| 69у | Ревизия кода, три захода |
| 68у | Выпуск `0.1.0-beta.6` |
| 67у | Лист возможностей |
| 66у | `var` вне движков — ноль, и новых не завести |
| 65у | Правило «где взять активный редактор» сведено к одному объявлению |
| 64у | Мёртвый код снят, копии правил сведены к одному объявлению |
| 63у | Тихие отказы под З3, третий кусок: 27 → 0, долг закрыт |
| 62у | Тихие отказы под З3, второй кусок: 60 → 27 |
| 61у | Тихие отказы под З3, первый кусок: 69 → 60 |
| 60у | Выпуск `0.1.0-beta.5` в GitHub |
| 59у | Все 69 тихих отказов под З3 разобраны |
| 50у, 53у, 54у, 55у, 57у | подтверждены его галочками 2026-09-10 |
| 56у | Тихие отказы вне З3 разобраны |
| 52у | Бриф для LLM на проектирование PKM |

### Разделы 44у, 45у, 48у, 49у, 51у — **в архиве**

Пять отчётов по строкам `S6` и `S7`, **перекрытых позднейшими заходами**. Обе
строки заказчик закрыл галочками 2026-09-10, и последние отчёты по ним (50у,
53у, 57у) унесены в `docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.
Причины дефектов и то, чем правки закреплены, —
`docs/archive/AWAITING_OWNER_CHECK_sessions27_29.md`.

### 38у. Браузер в наборе — **сделано 2026-09-08, вечер**

Ваш ответ: «добавить сейчас отдельным куском», в CI пока не включать.

**Что теперь есть.** Седьмой шаг проверок — `npm run gate:browser`. Он
открывает прототип панели настоящим Chromium и спрашивает **вычисленные
величины**, а не дерево: видит ли браузер открытую подсказку, во всю ли ширину
своего блока она встала, какой контраст у текста шапки против получившейся
заливки, в одну ли строку встала подпись слайдера. На все семь вкладок — 151
подсказка, 7 шапок, 7 подписей.

**Почему это важно.** Заглушка, на которой работают остальные проверки, не
считает ни одного пикселя. За неделю через неё прошли и были найдены **только
вашими глазами**: шесть подсказок в скрытом узле, подсказки шириной с колонку,
шапки серым по серому и знак `%`, уехавший на вторую строку. Дерево каждый раз
было верным.

**Гейт проверяет сам себя.** Он гоняет свои проверки дважды: на нынешнем виде
они обязаны пройти, а на четырёх подменах — упасть. Подмена возвращает панель
в то состояние, из которого вы уже приносили замечание.

**Что он нашёл в первый прогон — вопрос В-92.** Подсказка шапки списка Fields
раскрыта на 188 точек: она занимает свою колонку целиком, а колонка узкая. И
это та самая подсказка, в которую унесён смысл `Left Block` и `Right Block`.

**Проверять тут нечего — это отчёт.** Смотреть придётся одно: ответ на В-92.

### Разделы 25у–36у — **в архиве**

Унесены 2026-09-08 вечером, по итогу сессии 26. Четыре раздела заказчик
закрыл галочками (30у, 31у, 32у, 35у — подсказки у всех подписей, приглушённая
заливка шапок, три починки и перевод панели), остальные четыре — отчёты, в
которых проверять нечего: список правил каталога (29у), закрытие фазы 6 (33у),
три расхождения с каталогом (34у) и разбор мёртвого вне З3 (36у).
Всё вместе — `docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 28у. Решено и не начато

Ни одна строка ниже проверки не ждёт: они ждут работы. Осталось то, у чего срок
назначен его же словом.

- **Минификация — в самом конце**, перед выходом наружу: «нужно упростить
  работу по отлавливанию багов». Вопрос В-95 отвечен 2026-09-10: бету **не
  минифицировать**. Срок остаётся за ним. **Не начато.**
- **Мобильные устройства** — отложены его словом, реализовать позже. **Не
  начато.**
- **Снятие инлайн-элементов при экспорте заметки наружу** — первый пункт его
  файла идей; разбор назвал его хорошим кандидатом в ближайший релиз. Решения
  ещё не было. **Не начато.**

**Долгов, закреплённых числом, больше нет.** Тихие отказы (Д-4, было 105 → 69)
закрыты целиком 2026-09-10: список `DEBT` в `silent_failures_tests.ts` пуст, и
теперь роняет проверку любое новое безымянное молчание. Выпуск наружу, который
стоял здесь «не начато», сделан дважды: `0.1.0-beta.5` 2026-09-10 и
`0.1.0-beta.6` 2026-09-11.

**Долг по инлайновому оформлению закрыт** (Р7, 48 из 48) и подтверждён галочкой
на строке `V2` 2026-09-10; отчёт — в архиве проверенного. Абзац про «182
инлайновых стиля» удалён, а не унесён: две трети того числа лежали в мёртвой
функции, а остаток перенесён (У-31, У-117).

### Разделы 1у–24у — **в архиве**

Всё, что заказчик закрыл галочками до сессии 24, унесено в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`. Открывать, когда нужно
вспомнить, чем закреплена старая правка или когда её сделали.

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него сделаны и подтверждены заказчиком; разборы — в архиве
реестра: `Floating button`, симметричный цикл `Move right`, подсветка
обработанной строки, три пункта 1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа
свойства через `app.metadataTypeManager`.

**Остатка у фазы 6 больше нет.** Последний пункт закрыт 2026-09-08: манифест и
снятие команды `Open settings` были сделаны 2026-09-06, `dist/` вышел из
репозитория тогда же, а половина пункта про `main.js` потеряла предмет — после
разбора это 159 строк исходника, а не артефакт сборки. Живого в пункте
оказалась одна вещь, и её в задании не было: последняя заглушка на месте
модуля, `fields_editor_legacy.js`. Разбор — PRD, раздел 11.

Абзац про «остаток фазы 6 — снятие моста модулей» удалён 2026-09-08, а не
унесён в архив: он описывал состояние, которого больше нет (У-31). Моста нет
с 2026-09-07 — проверено поиском: файла `vault_module_bridge.js` не
существует, вызовов моста в рантайме ноль. В архиве этот абзац продолжал бы
врать.

**Остальное решённое и не начатое — раздел 28у выше**, и оно перечислено там,
а не здесь: этот раздел про работу, о которой заказчик не просил, а тот — про
ту, которую он назначил.
