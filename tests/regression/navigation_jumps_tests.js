"use strict";
/**
 * Переходы по заголовкам и шаг курсора внутри строки (D2, D3).
 *
 * Разрешение В-17 от 2026-09-02, девятое исключение к З3: тронуть
 * `navigation_runtime.js` ради двух правок навигации. Разбор — в
 * `docs/dev/OWNER_REMARKS.md`, сессия 4, раздел E.
 *
 * Проверка на НАСТОЯЩИХ функциях плагина: `jumpToHeader` и `navigateInline`
 * берутся из `navigation_runtime.js`. Подделан только **редактор** — команды
 * работают с открытой заметкой, и другого способа позвать их вне Obsidian нет
 * (У-1). Подделка решений не принимает: она держит строки, курсор и запись
 * обратно.
 *
 * Что закреплено:
 *
 *   1. текст выше первого заголовка — такая же секция, только без строки
 *      заголовка, и переходы работают внутри неё (решение В-20);
 *   2. выше первой строки после frontmatter переход не идёт;
 *   3. шаг `sentence` в строке без конца предложения уходит в начало и конец
 *      зоны, а не пересобирается по словам.
 */

const path = require("path");

/*
 * `navigateInline` ставит курсор через `window.setTimeout`: в Obsidian это
 * окно есть, в Node его нет. Подменяется ровно то, что вызывается, и до
 * загрузки модуля — иначе он падает на первом же шаге.
 */
if (typeof globalThis.window === "undefined") {
  globalThis.window = { setTimeout: (fn, ms) => setTimeout(fn, ms) };
}

const nav = require(path.join(__dirname, "..", "..", "src", "navigation_runtime.js"));

let passed = 0;
function ok(what) { passed++; console.log("  ok " + what); }
function assertArrayEq(actual, expected, name) {
  const a = JSON.stringify(actual);
  const e = JSON.stringify(expected);
  if (a !== e) throw new Error(name + ": expected " + e + ", got " + a);
  passed++;
  console.log("  ok " + name);
}

function assertEq(actual, expected, name) {
  if (actual !== expected) throw new Error(`${name}: expected ${JSON.stringify(expected)} got ${JSON.stringify(actual)}`);
}

/**
 * Редактор на несколько строк: только то, что зовут переходы.
 *
 * Курсор ставится асинхронно (`setCursorRobustCentered` повторяет установку
 * через микрозадачу и таймер), поэтому проверка читает его после ожидания.
 */
function fakeEditor(text, cursor) {
  const lines = String(text).split("\n");
  const state = { lines, cursor: { ...cursor } };
  return {
    lastLine: () => state.lines.length - 1,
    getLine: (n) => state.lines[n],
    getCursor: () => ({ ...state.cursor }),
    setCursor: (p) => { state.cursor = { ...p }; },
    setValue: (v) => { state.lines = String(v).split(String.fromCharCode(10)); },
    getScrollInfo: () => ({ top: 0, left: 0 }),
    scrollTo: () => {},
    setSelection: () => {},
    getSelection: () => "",
    setLine: (n, t) => { state.lines[n] = t; },
    /*
     * Правка отрезка — настоящая. Пустышкой она быть не может: перемещение
     * строк пишет именно ею (пункт 11 его замечаний 2026-09-20), и подделка,
     * которая молча ничего не делает, показала бы «команда промолчала» там,
     * где команда сработала (У-45).
     */
    replaceRange: (insert, from, to) => {
      const head = state.lines.slice(0, from.line);
      const tail = state.lines.slice(to.line + 1);
      const first = String(state.lines[from.line] || "").slice(0, from.ch);
      const last = String(state.lines[to.line] || "").slice(to.ch);
      state.lines = head.concat((first + String(insert) + last).split("\n"), tail);
    },
    posToOffset: (p) => p.ch,
    offsetToPos: (ch) => ({ line: 0, ch }),
    getValue: () => state.lines.join("\n"),
    focus: () => {},
    scrollIntoView: () => {},
    at: () => ({ ...state.cursor }),
  };
}

/** Дать курсору доехать: установка повторяется через таймер. */
const settle = () => new Promise((r) => setTimeout(r, 80));

const NOTE = [
  "первая строка вводного текста",
  "вторая строка вводного текста",
  "",
  "## Первый заголовок",
  "строка первой секции",
  "",
  "## Второй заголовок",
  "строка второй секции",
].join("\n");

const WITH_YAML = ["---", "tags: [a]", "---"].concat(NOTE.split("\n")).join("\n");

const CFG = {
  enabled: true,
  centerCursor: false,
  jumpMode: "edge",
  edgeMode: "start-end",
  jumpCursorPosition: "start",
};

async function jump(text, line, direction, over) {
  const ed = fakeEditor(text, { line, ch: 0 });
  nav.jumpToHeader(ed, direction, Object.assign({}, CFG, over || {}));
  await settle();
  return ed.at().line;
}

(async function run() {
  /* ---- D2: текст выше первого заголовка — секция ------------------------ */

  /*
   * Курсор на второй строке вводного текста. Раньше он считался стоящим в
   * ПЕРВОЙ секции — то есть ниже себя, — и переход назад уводил его на
   * нулевую строку. Теперь он в безымянной секции, и переход назад ведёт к её
   * началу.
   */
  assertEq(await jump(NOTE, 1, "up"), 0,
    "назад из вводного текста — к началу своей же секции");

  /* А из её начала наверх идти некуда: она первая. */
  assertEq(await jump(NOTE, 0, "up"), 0,
    "из начала безымянной секции переход назад не двигает курсор");

  /*
   * Вперёд из вводного текста: `alternate start and end` сначала ведёт к
   * концу своей секции, потом в следующую. Конец безымянной секции — её
   * последняя написанная строка, а не пустая.
   */
  assertEq(await jump(NOTE, 0, "down"), 1,
    "вперёд из начала безымянной секции — к её концу");
  assertEq(await jump(NOTE, 1, "down"), 4,
    "с конца безымянной секции — в следующую секцию");

  /*
   * Назад из первой настоящей секции — в безымянную, а не в нулевую строку.
   * Строка 4 — единственная написанная строка первой секции, то есть сразу и
   * её начало, и её конец: `alternate start and end` уводит из неё в
   * предыдущую секцию, а её конец — строка 1. Прежний код отдал бы 0.
   */
  assertEq(await jump(NOTE, 4, "up"), 1,
    "назад из первой секции — к концу безымянной, а не в начало заметки");
  ok("D2: текст выше первого заголовка участвует в переходах как секция");

  /* ---- D2: выше первой строки после frontmatter не прыгаем -------------- */

  /*
   * У заметки со свойствами безымянная секция начинается после `---`. В
   * прошлой версии откат вёл на строку `yamlEnd`, то есть на закрывающий
   * `---`, и курсор оказывался в свойствах.
   */
  assertEq(await jump(WITH_YAML, 4, "up"), 3,
    "назад из вводного текста — на первую строку после свойств, а не в них");
  assertEq(await jump(WITH_YAML, 3, "up"), 3,
    "и дальше вверх переход не идёт");
  assertEq(await jump(WITH_YAML, 1, "down"), 3,
    "из свойств вперёд — на первую строку после них");
  assertEq(await jump(WITH_YAML, 1, "up"), 1,
    "а вверх из свойств идти некуда, и курсор стоит");
  ok("D2: выше первой строки после свойств заметки переход не идёт");

  /* ---- D2: «строка за строкой» на той же безымянной секции -------------- */

  assertEq(await jump(NOTE, 1, "up", { jumpMode: "line" }), 0,
    "строка за строкой: назад по вводному тексту");
  assertEq(await jump(NOTE, 0, "up", { jumpMode: "line" }), 0,
    "и на его первой строке переход не двигает курсор");
  ok("D2: режим «строка за строкой» работает в безымянной секции");

  /* ---- D3: шаг `sentence` без конца предложения ------------------------- */

  /*
   * Заказчик: «при in-line-step=sentence, если в строке нет конца
   * предложения, поведение как при word, а должен прыгать в начало и конец».
   *
   * Проверяется по остановкам: из середины зоны шаг влево обязан привести к
   * её началу, а не к предыдущему слову.
   */
  const SENT = "- один два три четыре пять";
  const step = async (from, direction) => {
    const ed = fakeEditor(SENT, { line: 0, ch: from });
    nav.navigateInline(ed, direction, { delim: "||", trailingMarkers: [] },
      { stepMode: "sentence", boundaryJump: false, onBoundary: "stay" });
    await settle();
    return ed.at().ch;
  };

  const zoneStart = SENT.indexOf("один");
  const zoneEnd = SENT.length;
  const middle = SENT.indexOf("три");

  assertEq(await step(middle, "left"), zoneStart,
    "без конца предложения шаг влево ведёт в начало зоны, а не к слову");
  assertEq(await step(middle, "right"), zoneEnd,
    "и шаг вправо — в её конец");
  ok("D3: шаг `sentence` без точки уходит в начало и конец зоны");

  /*
   * И обратная сторона: с концом предложения шаг по-прежнему считает
   * предложения, а не зону целиком. Иначе правка подменила бы один режим
   * другим.
   */
  const DOTS = "- один два. три четыре. пять";
  const ed = fakeEditor(DOTS, { line: 0, ch: DOTS.length });
  nav.navigateInline(ed, "left", { delim: "||", trailingMarkers: [] },
    { stepMode: "sentence", boundaryJump: false, onBoundary: "stay" });
  await settle();
  assertEq(ed.at().ch, DOTS.indexOf("четыре.") + "четыре".length,
    "с точками шаг влево встаёт на конец предыдущего предложения");
  ok("D3: с концом предложения режим остаётся собой");

  /* ---- D1: где перемещение заголовка молчит ---------------------------- */

  /*
   * Заказчик: «перемещение заголовков не работает — когда я нахожусь на
   * строке-хедере, Move line up/down не делает ничего (при обоих вариантах
   * move-lines-heading)». На его `Демо строки.md` это не воспроизвелось:
   * движок двигает заголовок в обе стороны.
   *
   * Прогон по формам заметки нашёл ровно две, где движок молчит **в обе
   * стороны**, и обе — на любом заголовке, не только на среднем:
   *
   *   1. выше строки стоит НЕЗАКРЫТЫЙ кодовый блок. Разбор считает
   *      ограждения выше курсора и при нечётном числе считает строку кодом.
   *      По правилам Markdown это верно: незакрытое ограждение и правда
   *      делает кодом всё ниже. Но молчит команда так же, как при дефекте;
   *   2. в заметке один заголовок. Переставлять его некуда: и вверх, и вниз
   *      искать нечего.
   *
   * Пин держит границу: если однажды молчать начнёт что-то ещё, он покраснеет
   * на той форме, а не на всех сразу.
   */
  const moveLineOn = (lines, at, dir, headerMode) => {
    const ed = fakeEditor(lines.join("\n"), { line: at, ch: 0 });
    const before = ed.getValue();
    nav.moveLine(ed, dir, {
      enabled: true, headerMode, noSelectionMode: "line-only",
      crossSectionAllowed: true, highlightMovedLines: false,
    });
    return before !== ed.getValue();
  };

  const both = (lines, at) => [
    moveLineOn(lines, at, "up", "move-as-line"),
    moveLineOn(lines, at, "down", "move-as-line"),
    moveLineOn(lines, at, "up", "move-with-section"),
    moveLineOn(lines, at, "down", "move-with-section"),
  ];

  /* Обычная заметка: средний заголовок двигается всеми четырьмя способами. */
  assertEq(both(["## A", "a1", "## B", "b1", "## C", "c1"], 2).join(","),
    "true,true,true,true",
    "средний заголовок обычной заметки двигается в обе стороны");

  /* Закрытый кодовый блок выше не мешает. */
  assertEq(both(["## A", "```js", "code", "```", "## B", "b1", "## C", "c1"], 4).join(","),
    "true,true,true,true",
    "закрытый кодовый блок выше на перемещение не влияет");

  /* Незакрытый — мешает, и в обе стороны, в любом режиме. */
  assertEq(both(["## A", "```js", "code", "## B", "b1", "## C", "c1"], 3).join(","),
    "false,false,false,false",
    "ниже незакрытого ограждения команда молчит: строка считается кодом");

  /*
   * Единственный заголовок заметки: секцию переставлять некуда, а **строку**
   * есть куда — она уходит вниз, за `a1`, как любая другая строка. Это и есть
   * правка D1: при `Headers only` заголовок ведёт себя как обычная строка
   * (заказчик, 2026-09-02). До неё цель считалась «за соседний заголовок того
   * же уровня» в обоих режимах, соседнего не было — и команда молчала.
   */
  assertEq(both(["## A", "a1", "a2"], 0).join(","), "false,true,false,false",
    "единственный заголовок: как строка идёт вниз, как секция — некуда");

  /*
   * Края. Как **строка** заголовок ходит туда, куда ходит любая строка: у
   * последнего заголовка ниже стоит его собственная `b1`, и вниз он идёт за
   * неё. Как **секция** ему вниз некуда: следующей секции нет.
   */
  assertEq(both(["## A", "a1", "## B", "b1"], 0).join(","), "false,true,false,true",
    "первый заголовок: вверх некуда, вниз двигается");
  assertEq(both(["## A", "a1", "## B", "b1"], 2).join(","), "true,true,true,false",
    "последний заголовок: как строка ходит в обе стороны, как секция — только вверх");
  ok("D1: граница молчания — незакрытое ограждение и секция, которую некуда переставить");

  /*
   * D1, само поведение: при `Headers only` заголовок сдвигается **ровно на
   * одну строку**, а при `Headers with their sections` переезжает вместе со
   * своей секцией. Раньше оба режима считали цель одинаково, и заголовок
   * перелетал через целую секцию в обоих: «перемещается как-то непонятно».
   */
  const moveText = (lines, at, dir, headerMode) => {
    const ed = fakeEditor(lines.join("\n"), { line: at, ch: 0 });
    nav.moveLine(ed, dir, {
      enabled: true, headerMode, noSelectionMode: "line-only",
      crossSectionAllowed: true, highlightMovedLines: false,
    });
    return ed.getValue().split("\n");
  };

  const MOVE_NOTE = ["## A", "a1", "a2", "## B", "b1"];

  assertArrayEq(moveText(MOVE_NOTE, 0, "down", "move-as-line"),
    ["a1", "## A", "a2", "## B", "b1"],
    "Headers only: заголовок вниз — ровно на одну строку");
  assertArrayEq(moveText(MOVE_NOTE, 3, "up", "move-as-line"),
    ["## A", "a1", "## B", "a2", "b1"],
    "Headers only: заголовок вверх — тоже на одну строку");

  assertArrayEq(moveText(MOVE_NOTE, 0, "down", "move-with-section"),
    ["## B", "b1", "## A", "a1", "a2"],
    "Headers with their sections: заголовок уезжает вместе со своей секцией");



  /* ---- перемещение выделенного текста внутри строки --------------------- */

  /*
   * Заказчик (свободное замечание, 2026-09-02):
   *
   *   исходная строка   `- [ ] #/1 #todo || 123 || 📅2026-09-02 20:44`
   *   влево получалось  `- [ ] #/1 #123 || todo || 📅2026-09-02 20:44`
   *   ожидалось         `- [ ] #/1 #todo 123 ||  || 📅2026-09-02 20:44`
   *   ещё раз влево     `- [ ] #/1 123 #todo ||  || 📅2026-09-02 20:44`
   *
   * Единицей перескока был набор «символов слова», поэтому от тега бралось
   * `todo`, а решётка оставалась на месте. Теперь единица — то, что стоит
   * между пробелами, и это же правило заказчик и описал в своём ожидании: с
   * первого нажатия текст перепрыгивает разделитель, со второго — тег
   * целиком. Ссылка ведёт себя так же: `[[test1]]` переставляется вся.
   *
   * Одиннадцатое исключение к З3, разрешение 2026-09-02.
   */
  /*
   * **Замена документа целиком роняет прогон, и это сторож, а не украшение.**
   *
   * Его замечание 2026-09-22: «при переносе выделенного текста move left\right
   * прыгает экран». `Editor.setValue` у Obsidian — это замена всего документа
   * (`app.js` 1.13.7), а CodeMirror держит прокрутку за опорным местом и
   * переносит его через изменение: место внутри заменённого отрезка уезжает в
   * его начало, то есть в начало заметки.
   *
   * Подделка, у которой `setValue` работает, к этому слепа: текст выходит тот
   * же, и прогон зелёный (У-45). Поэтому здесь она **громко отказывает**, и
   * любая новая дорога переноса, написанная заменой документа, роняет набор в
   * том же прогоне, где её завели. Прокрутку как таковую меряет браузер —
   * `node tools/move_scroll_bench.js`.
   */
  const WHOLE_DOC_WRITE = "перенос текста написал документ целиком — прокрутка уедет в начало заметки";

  const moveTextIn = (line, phrase, direction, presses, over, lineFormat) => {
    let text = line;
    for (let n = 0; n < presses; n++) {
      const at = text.indexOf(phrase);
      if (at < 0) throw new Error("переносимый текст пропал: " + text);
      const state = { text, from: { line: 0, ch: at }, to: { line: 0, ch: at + phrase.length } };
      const ed = {
        lastLine: () => 0,
        getLine: () => state.text,
        getValue: () => state.text,
        setValue: () => { throw new Error(WHOLE_DOC_WRITE); },
        getSelection: () => state.text.slice(state.from.ch, state.to.ch),
        getCursor: (which) => (which === "to" ? { ...state.to } : { ...state.from }),
        setCursor: () => {},
        setSelection: (a, b) => { state.from = { ...a }; state.to = { ...b }; },
        posToOffset: (p) => p.ch,
        offsetToPos: (ch) => ({ line: 0, ch }),
        listSelections: () => [{ anchor: state.from, head: state.to }],
        getScrollInfo: () => ({ top: 0, left: 0 }),
        scrollTo: () => {},
        setLine: (_n, t) => { state.text = String(t); },
        replaceRange: (insert, x, y) => {
          state.text = state.text.slice(0, x.ch) + String(insert) + state.text.slice(y.ch);
        },
        focus: () => {},
        scrollIntoView: () => {},
      };
      nav.moveSelection(ed, direction, Object.assign({
        enabled: true, inlineEnabled: true, inlineMoveMode: "word",
        prefixCyclerEnabled: false, indentFallbackEnabled: false,
      }, over || {}), lineFormat);
      text = state.text;
    }
    return text;
  };

  const TAGGED = "- [ ] #/1 #todo || 123 || 📅2026-09-02 20:44";
  /* Порядок — тот, что заказчик и выписал. Двойного пробела на месте текста
     при этом не остаётся: зазор переезжает вместе с ним. */
  assertEq(moveTextIn(TAGGED, "123", "left", 1),
    "- [ ] #/1 #todo 123 || || 📅2026-09-02 20:44",
    "текст перепрыгивает разделитель целиком, а не влезает в тег");
  assertEq(moveTextIn(TAGGED, "123", "left", 2),
    "- [ ] #/1 123 #todo || || 📅2026-09-02 20:44",
    "второе нажатие переставляет тег целиком, вместе с решёткой");
  ok("перемещение текста не разрывает тег (пример заказчика посимвольно)");

  const LINKED = "- [ ] [[test1]] || 123 || конец";
  assertEq(moveTextIn(LINKED, "123", "left", 2),
    "- [ ] 123 [[test1]] || || конец",
    "ссылка переставляется целиком, а текст не уезжает внутрь скобок");
  ok("перемещение текста не разрывает ссылку");

  /* Обратная сторона: в обычном тексте единица осталась словом. */
  assertEq(moveTextIn("одно два три", "три", "left", 1), "одно три два",
    "в обычном тексте перескок остался словом");
  ok("в обычном тексте поведение не изменилось");

  /* ---- шаг до знака и переезд пробела (его пункт 7, 2026-09-22) ---------
   *
   * Его слова: «если в предложении `(слово1 слово2 слово3)` выделить `слово2`
   * и move right, то получается `(слово1 слово3) слово2`, а я бы хотел, чтобы
   * перенос был до символа… Если я в этом же примере переношу вторую скобку
   * влево, то получаю `(слово1 слово2 )слово3`, а ожидал
   * `(слово1 слово2) слово3`». И условие: «я хочу испытывать комфорт от этой
   * команды, чтобы мне не приходилось руками корректировать текст».
   *
   * Правил два, и оба спрашивают про знак, а не про перенос вообще: сосед
   * отдаёт свой край из скобок и знаков препинания отдельным шагом, а пробел
   * перед парой и пробел между её половинами меняются местами вместе с
   * половинами — но только когда одна из половин целиком из таких знаков.
   * Пробелов правило не добавляет и не убирает.
   */
  const BRACKETED = "(слово1 слово2 слово3)";
  assertEq(moveTextIn(BRACKETED, "слово2", "right", 1), "(слово1 слово3 слово2)",
    "первый шаг вправо обязан меняться со словом, а не со словом и скобкой");
  assertEq(moveTextIn(BRACKETED, "слово2", "right", 2), "(слово1 слово3) слово2",
    "второй шаг выносит слово за скобку, и пробел переезжает за ней");
  assertEq(moveTextIn(BRACKETED, ")", "left", 1), "(слово1 слово2) слово3",
    "скобка влево прилипает к слову слева, а пробел уходит направо");
  assertEq(moveTextIn(BRACKETED, "слово2", "left", 1), "(слово2 слово1 слово3)",
    "открывающая скобка остаётся на месте: слово меняется со словом");

  /*
   * Отрицательные контроли — то, что правило трогать не должно. Без них
   * «сосед отдаёт край» было бы зелёным и у разбора, который рвёт всё подряд.
   */
  assertEq(moveTextIn("- [ ] [[test1]] || 123 || конец", "123", "left", 2),
    "- [ ] 123 [[test1]] || || конец",
    "у ссылки края парные — она по-прежнему переставляется целиком");
  assertEq(moveTextIn("- [ ] #/1 #todo || 123 || 📅2026-09-02 20:44", "123", "left", 2),
    "- [ ] #/1 123 #todo || || 📅2026-09-02 20:44",
    "решётка и косая к знакам края не относятся: тег целый");
  /*
   * **Этот контроль отменён его словом 2026-09-22**, и отменено то, что писал
   * я, а не он: «слово в своих скобках — одно целое» было моим правилом,
   * заведённым ради `[[test1]]`. Он попросил обратного: «перемещение сбоит —
   * перепрыгивает через скобки», и выписал три нажатия, которых ждёт. Ссылку
   * теперь бережёт признак ссылки, а скобки вокруг прозы — обычный край.
   */
  assertEq(moveTextIn("(слово) два", "два", "left", 1), "(слово два)",
    "скобка — шаг своего права: слово входит в группу, а не перепрыгивает её");
  assertEq(moveTextIn("одно два три", "три", "left", 1), "одно три два",
    "в обычном тексте промежутки одинаковы, и правило ничего не меняет");
  ok("его пункт 7: шаг до знака, пробел переезжает со знаком, принятое не тронуто");

  /* ---- скобка — шаг своего права (его замечание 2026-09-22) -------------
   *
   * Его слова: «при выделении одного слова перемещение работает нормально, но
   * если выделить несколько слов, то перемещение сбоит — перепрыгивает через
   * скобки». И тут же — чего он ждёт, по нажатиям:
   *
   *   `слово2 слово3 (слово1)`  →  `(слово2 слово3 слово1)`
   *                             →  `(слово1 слово2 слово3)`
   *                             →  `(слово1) слово2 слово3`
   *
   * **Число выделенных слов тут ни при чём, и это измерено:** на одном слове
   * шаг перепрыгивал группу так же. Его пример описывал форму, а не условие
   * (У-269).
   */
  const GROUPED = "слово2 слово3 (слово1)";
  assertEq(moveTextIn(GROUPED, "слово2 слово3", "right", 1), "(слово2 слово3 слово1)",
    "первое нажатие входит за открывающую скобку, а не перепрыгивает группу");
  assertEq(moveTextIn(GROUPED, "слово2 слово3", "right", 2), "(слово1 слово2 слово3)",
    "второе меняется со словом внутри группы");
  assertEq(moveTextIn(GROUPED, "слово2 слово3", "right", 3), "(слово1) слово2 слово3",
    "третье выносит фразу за закрывающую скобку, и пробел переезжает за ней");
  /* Одно слово — тот же путь: условие не в числе слов. */
  assertEq(moveTextIn(GROUPED, "слово3", "right", 1), "слово2 (слово3 слово1)",
    "одно слово входит в группу тем же шагом");
  /* И зеркало: влево тот же путь в обратную сторону. */
  assertEq(moveTextIn("(слово1) слово2 слово3", "слово2 слово3", "left", 1),
    "(слово1 слово2 слово3)",
    "влево фраза входит за закрывающую скобку");
  assertEq(moveTextIn("(слово1) слово2 слово3", "слово2 слово3", "left", 3),
    "слово2 слово3 (слово1)",
    "и тремя нажатиями выходит с другой стороны");

  /*
   * **Отрицательные контроли — то, что шагом не разбирается.** Без них
   * «скобка — шаг своего права» было бы зелёным и у правила, которое рвёт
   * ссылку пополам: `[текст подпись](адрес)` — это уже не разметка.
   */
  assertEq(moveTextIn("текст [[test1]] хвост", "текст", "right", 1),
    "[[test1]] текст хвост",
    "wikilink — один знак смысла: шаг берёт его целиком");
  assertEq(moveTextIn("текст [подпись](https://a.b) хвост", "текст", "right", 1),
    "[подпись](https://a.b) текст хвост",
    "и разметка ссылки тоже: до 2026-09-22 шаг рвал её на `[текст подпись](…)`");
  assertEq(moveTextIn("текст #todo хвост", "текст", "right", 1), "#todo текст хвост",
    "у тега краёв нет вовсе — он и не был предметом правила");
  ok("его замечание 2026-09-22: скобка — шаг своего права, ссылка остаётся целой");

  /* ---- слово не втыкается в оформление строки (его замечание 2026-09-22) --
   *
   * Его слова к тесту 9: «смотри в `💥В. Тест` вторую и третью строку — при
   * переносе текста влево, оно втыкается в префикс». Измерено до правки: на
   * третьем нажатии выходило `-слово3 ( слово1 слово2)` и
   * `- [ ]слово3 ( слово1 слово2)`, на четвёртом слово перепрыгивало сам знак
   * (`слово3- (…`, `- [слово3 ] (…`, `1слово3. (…`).
   *
   * Причин было две, и правка тоже из двух половин: у переноса не было стены
   * на оформлении начала строки, а открывающая скобка уносила пробел не в ту
   * сторону — знак держится за текст справа, а правило знало одну сторону.
   */
  const LIST_BRACKETED = "- (слово1 слово2 слово3)";
  assertEq(moveTextIn(LIST_BRACKETED, "слово3", "left", 3), "- слово3 (слово1 слово2)",
    "слово выходит из скобки за знак списка и остаётся отделено от него пробелом");
  assertEq(moveTextIn(LIST_BRACKETED, "слово3", "left", 4), "- слово3 (слово1 слово2)",
    "знак списка тексту не принадлежит: дальше шагать некуда");
  assertEq(moveTextIn("- [ ] (слово1 слово2 слово3)", "слово3", "left", 4),
    "- [ ] слово3 (слово1 слово2)",
    "задача целая: ни слово внутрь скобок чекбокса, ни чекбокс в сторону");
  assertEq(moveTextIn("1. (слово1 слово2 слово3)", "слово3", "left", 4),
    "1. слово3 (слово1 слово2)",
    "номер списка — тоже оформление строки, и он остаётся номером");
  assertEq(moveTextIn("  > - (слово1 слово2 слово3)", "слово3", "left", 4),
    "  > - слово3 (слово1 слово2)",
    "цитата и отступ стоят перед знаком списка и переносом не задеваются");
  assertEq(moveTextIn("# (слово1 слово2 слово3)", "слово3", "left", 4),
    "# слово3 (слово1 слово2)",
    "решётка заголовка — оформление, а не сосед по строке");
  /*
   * Открывающая скобка держится за текст справа и в середине строки: там
   * дефект выглядел как `текстслово3 ( слово1)` и к оформлению отношения не
   * имел вовсе.
   */
  assertEq(moveTextIn("текст (слово3 слово1)", "слово3", "left", 1), "текст слово3 (слово1)",
    "открывающая скобка не отрывается от своего содержимого");
  /*
   * Отрицательные контроли к стене: она стоит на оформлении, а не на всём
   * подряд, и закрывающий знак свою сторону не поменял.
   */
  assertEq(moveTextIn("- один два три", "три", "left", 2), "- три один два",
    "внутри строки перенос идёт как шёл, до самого первого слова текста");
  assertEq(moveTextIn("слово1, слово2 слово3", "слово2", "left", 1), "слово1 слово2, слово3",
    "запятая держится за текст слева — сторона у закрывающего знака прежняя");
  assertEq(moveTextIn("(слово1 слово2 слово3)", "слово3", "left", 4), "слово3 (слово1 слово2)",
    "строка без оформления: стеной остаётся её начало");
  ok("его замечание 2026-09-22: перенос не втыкается в оформление строки");

  /*
   * Тот же вопрос у посимвольного шага, и там он стоил бы дороже: с
   * включённым `Step out of the word` буква уезжала **через перевод строки** в
   * соседнюю строку — `- один\n- два` давало `- одиндв\n- а`. Многострочный
   * редактор нужен именно для этого: однострочная подделка такого не видит.
   */
  const moveInDoc = (text, phrase, direction, presses, over) => {
    const state = { text: String(text), from: null, to: null };
    const lines = () => state.text.split("\n");
    const offsetOf = (p) => {
      const ls = lines();
      let at = 0;
      for (let i = 0; i < p.line; i++) at += ls[i].length + 1;
      return at + p.ch;
    };
    const posOf = (off) => {
      const ls = lines();
      let at = 0;
      for (let i = 0; i < ls.length; i++) {
        if (off <= at + ls[i].length) return { line: i, ch: off - at };
        at += ls[i].length + 1;
      }
      return { line: ls.length - 1, ch: ls[ls.length - 1].length };
    };
    const at = state.text.indexOf(phrase);
    if (at < 0) throw new Error("переносимого текста нет в строке: " + text);
    const line = state.text.slice(0, at).split("\n").length - 1;
    const lineStart = state.text.lastIndexOf("\n", at - 1) + 1;
    state.from = { line: line, ch: at - lineStart };
    state.to = { line: line, ch: at - lineStart + phrase.length };
    const ed = {
      lastLine: () => lines().length - 1,
      getLine: (n) => lines()[n],
      getValue: () => state.text,
      setValue: () => { throw new Error(WHOLE_DOC_WRITE); },
      getSelection: () => state.text.slice(offsetOf(state.from), offsetOf(state.to)),
      getCursor: (which) => (which === "to" ? { ...state.to } : { ...state.from }),
      setCursor: () => {},
      setSelection: (x, y) => { state.from = { ...x }; state.to = { ...y }; },
      posToOffset: offsetOf,
      offsetToPos: posOf,
      listSelections: () => [{ anchor: state.from, head: state.to }],
      getScrollInfo: () => ({ top: 0, left: 0 }),
      scrollTo: () => {},
      setLine: () => {},
      replaceRange: (insert, x, y) => {
        const at = offsetOf(x);
        const end = offsetOf(y);
        state.text = state.text.slice(0, at) + String(insert) + state.text.slice(end);
      },
      focus: () => {},
      scrollIntoView: () => {},
    };
    for (let n = 0; n < presses; n++) {
      nav.moveSelection(ed, direction, Object.assign({
        enabled: true, inlineEnabled: true, inlineMoveMode: "char",
        inlineWordEscape: true, prefixCyclerEnabled: false, indentFallbackEnabled: false,
      }, over || {}), null);
    }
    return state.text;
  };

  assertEq(moveInDoc("- один\n- два", "дв", "left", 3), "- один\n- два",
    "посимвольный шаг не уводит буквы в соседнюю строку через её знак списка");
  assertEq(moveInDoc("- один\n- два", "ин", "right", 3), "- один\n- два",
    "и за конец строки — тоже: соседняя строка не его текст");
  assertEq(moveInDoc("- [ ] один", "од", "left", 3), "- [ ] один",
    "чекбокс посимвольным шагом не разбирается");
  assertEq(moveInDoc("- один два", "ди", "left", 1), "- дион два",
    "внутри строки посимвольный шаг работает как работал");
  assertEq(moveInDoc("- один два три", "два", "right", 1, { inlineMoveMode: "word" }),
    "- один три два",
    "многострочная подделка даёт тот же ответ, что однострочная");
  ok("стена строки стоит и у посимвольного шага");

  /* ---- часть слова уезжает за пределы слова (замечание 2026-09-08) ------
   *
   * Заказчик: «при выборе части слова выделенный текст двигается только в
   * пределах слова, в котором он был. Я хочу, чтобы он не был ограничен и мог
   * двигаться посимвольно в любом направлении, в т.ч. за пределы слова».
   *
   * Это был не шаг, а **отказ**: при `auto`, когда сосед по направлению не
   * словесный, функция возвращала `noop` — клавиша не делала ничего. Теперь
   * отказ снимается тумблером `Step out of the word`; умолчание выключено,
   * решение заказчика того же дня.
   *
   * Проверяются оба направления и оба положения тумблера: пин на одно
   * положение зелен и у настройки, которая до движка не доехала (У-56).
   */
  {
    const AUTO = { inlineMoveMode: "auto" };
    const FREE = { inlineMoveMode: "auto", inlineWordEscape: true };

    /* Внутри слова шаг посимвольный, и это работало и до правки. */
    assertEq(moveTextIn("text more", "ex", "left", 1, AUTO),
      "extt more", "внутри слова часть слова двигалась и раньше");

    /*
     * На краю слова решает тумблер. Выключен — строка не меняется вовсе;
     * включён — буквы уезжают за пробел, ровно как он и просил.
     */
    assertEq(moveTextIn("text more", "xt", "right", 1, AUTO),
      "text more", "с выключенным тумблером часть слова остаётся в своём слове");
    assertEq(moveTextIn("text more", "xt", "right", 1, FREE),
      "te xtmore", "с включённым тумблером буквы уезжают за пробел");

    /* И в обратную сторону — тем же тумблером. */
    assertEq(moveTextIn("more text", "te", "left", 1, AUTO),
      "more text", "влево на краю слова выключенный тумблер тоже держит");
    assertEq(moveTextIn("more text", "te", "left", 1, FREE),
      "morete xt", "влево с включённым тумблером буквы уезжают за пробел");

    /*
     * Положительный контроль на сам тумблер: там, где отказа не было, он не
     * меняет ничего. Иначе «включил — поехало» объяснялось бы не им.
     */
    assertEq(moveTextIn("one two three", "three", "left", 1, FREE),
      "one three two", "целое слово по-прежнему прыгает словами");

    /*
     * И шов: сборщик правил обязан **назвать** ключ. Он перечисляет поля по
     * одному, и настройка, которую он не назвал, до движка не доезжает молча
     * — этим уже куплен У-56. Заодно проверяется умолчание: выключено, то
     * есть без настройки поведение прежнее.
     */
    const picked = nav.pickMoveSelectionCfg({}, {});
    assertEq(picked.inlineWordEscape, false, "умолчание тумблера — выключен");
    assertEq(nav.pickMoveSelectionCfg({ inlineWordEscape: true }, {}).inlineWordEscape, true,
      "сборщик правил не назвал ключ: настройка до движка не доедет");
    ok("часть слова уезжает за пределы слова только с тумблером, в оба направления");
  }

  /* ---- Continue past a Separator у переноса текста ----------------------
   *
   * Замечание заказчика 2026-09-04: у курсора внутри строки такая опция есть,
   * а у переноса текста не было. Проверяются **оба** положения тумблера, и
   * включённое — то же принятое поведение из его примера 2026-09-02.
   */
  {
    const SEP = { separator1: "::", separator2: "::" };
    const LINE = "- #todo :: купить хлеб :: 📅2026-09-04";

    /* Выключен: за первый разделитель влево не уходит. */
    assertEq(
      moveTextIn(LINE, "купить", "left", 1, { inlineBoundaryJump: false }, SEP),
      LINE,
      "при выключенном тумблере текст не уезжает за первый Separator");
    /* И за второй вправо тоже. */
    assertEq(
      moveTextIn(LINE, "хлеб", "right", 1, { inlineBoundaryJump: false }, SEP),
      LINE,
      "при выключенном тумблере текст не уезжает за второй Separator");
    /* Внутри своей зоны при этом двигается как обычно. */
    assertEq(
      moveTextIn(LINE, "хлеб", "left", 1, { inlineBoundaryJump: false }, SEP),
      "- #todo :: хлеб купить :: 📅2026-09-04",
      "внутри своего текста перенос работает и при выключенном тумблере");
    ok("выключенный Continue past a Separator держит текст между разделителями");

    /*
     * **Та же граница у строки без зоны тегов** (замечание заказчика
     * 2026-09-11). Первого разделителя в строке нет, и прежде границ не
     * возникало вовсе: фраза уезжала за второй, в хвост с датой. Образец
     * разводит разделители нарочно — на одинаковых случая не бывает (У-147).
     */
    const SPLIT = { separator1: "||", separator2: "::" };
    const NOSEP1 = "- [ ] купить хлеб :: 📅2026-09-04";
    assertEq(NOSEP1.indexOf("||"), -1, "контроль: первого разделителя в строке нет");
    assertEq(
      moveTextIn(NOSEP1, "хлеб", "right", 1, { inlineBoundaryJump: false }, SPLIT),
      NOSEP1,
      "текст не уезжает за второй Separator и там, где первого в строке нет");
    assertEq(
      moveTextIn(NOSEP1, "хлеб", "left", 1, { inlineBoundaryJump: false }, SPLIT),
      "- [ ] хлеб купить :: 📅2026-09-04",
      "а внутри своей зоны перенос работает как обычно");
    ok("строка без зоны тегов: перенос текста тоже держится второго Separator");

    /* Включён: поведение прежнее — текст перепрыгивает разделитель. */
    assertEq(
      moveTextIn(LINE, "купить", "left", 1, { inlineBoundaryJump: true }, SEP),
      "- #todo купить :: хлеб :: 📅2026-09-04",
      "при включённом тумблере текст перепрыгивает разделитель, как было");
    ok("включённый Continue past a Separator оставляет прежнее поведение");

    /* Умолчание — включено: принятое поведение не отменяется молча. */
    assertEq(
      moveTextIn(LINE, "купить", "left", 1, {}, SEP),
      "- #todo купить :: хлеб :: 📅2026-09-04",
      "по умолчанию перенос ходит через разделитель, как до правки");
    ok("умолчание тумблера сохраняет принятое 2026-09-02 поведение");

    /* Посимвольный режим слушает тот же тумблер. */
    assertEq(
      moveTextIn(LINE, "купить", "left", 1, { inlineMoveMode: "char", inlineBoundaryJump: false }, SEP),
      LINE,
      "посимвольный перенос тоже стоит у разделителя");
    ok("тумблер один на оба шага переноса");
  }

  /* ---- Курсор на прибытии: `End of your text` ---------------------------
   *
   * Замечание заказчика 2026-09-04: при `Cursor on arrival` = `End of your
   * text` курсор встаёт в конец строки, а не перед вторым Separator.
   *
   * Проверка спрашивает **позицию курсора**, а не текст функции. Прежний пин
   * (`bootstrap_loader_tests.js`) сверял строку исходника про «configurable
   * separator1 fallback» и был зелёный всё время, пока режим не работал ни у
   * кого: разделители в `pickJumpCfg` не приходили, значение по умолчанию
   * `||` в строке заказчика не находилось, и функция отдавала конец строки.
   */
  {
    const LINE = "- #todo :: купить хлеб :: 📅2026-09-04";
    const NOTE_SEP = ["## Раздел один", LINE, "", "## Раздел два", "хвост"].join("\n");
    const AT_END = LINE.length;
    /* Ожидание выписано отдельно от того, из чего его считает движок (У-5):
       конец слова «хлеб» — это позиция перед пробелом и вторым `::`. */
    const BEFORE_SECOND = LINE.indexOf(" :: 📅");
    assertEq(LINE.slice(0, BEFORE_SECOND), "- #todo :: купить хлеб",
      "фикстура: до второго разделителя стоит именно текст человека");

    const jumpTo = async (lineFormat) => {
      const ed = fakeEditor(NOTE_SEP, { line: 4, ch: 0 });
      nav.jumpToHeader(ed, "up", Object.assign({}, CFG, {
        edgeMode: "end", jumpCursorPosition: "section-end",
      }), lineFormat);
      await settle();
      return ed.at();
    };

    const own = await jumpTo({ separator1: "::", separator2: "::" });
    assertEq(own.line, 1, "переход пришёл не на ту строку");
    assertEq(own.ch, BEFORE_SECOND,
      "курсор должен встать перед вторым Separator, а не в конец строки");
    ok("End of your text ставит курсор перед вторым Separator (разделители заказчика)");

    /* Разделители разные: второй ищется вторым, а не первым. */
    const LINE2 = "- #todo || купить хлеб :: 📅2026-09-04";
    const NOTE2 = ["## Раздел один", LINE2, "", "## Раздел два", "хвост"].join("\n");
    const ed2 = fakeEditor(NOTE2, { line: 4, ch: 0 });
    nav.jumpToHeader(ed2, "up", Object.assign({}, CFG, {
      edgeMode: "end", jumpCursorPosition: "section-end",
    }), { separator1: "||", separator2: "::" });
    await settle();
    assertEq(ed2.at().ch, LINE2.indexOf(" :: 📅"),
      "при разных разделителях второй ищется вторым");
    ok("второй Separator ищется вторым Separator, а не первым");

    /*
     * **Первого разделителя в строке нет, а второй есть** — замечание
     * заказчика 2026-09-11. У него `||` и `::`, и строка без зоны тегов
     * первого разделителя не содержит вовсе: прежде ветка искала только его и
     * уводила курсор в конец строки, за хвост с датой.
     *
     * Положительный контроль стоит первым: на одинаковых разделителях этого
     * случая не бывает — `first` находит тот же знак, — и проверка была бы
     * зелёной от отсутствия предмета (У-147).
     */
    const LINE4 = "- [ ] 1244 :: 📅2026-09-04";
    const NOTE4 = ["## Раздел один", LINE4, "", "## Раздел два", "хвост"].join("\n");
    const endOf = async (lineFormat) => {
      const ed = fakeEditor(NOTE4, { line: 4, ch: 0 });
      nav.jumpToHeader(ed, "up", Object.assign({}, CFG, {
        edgeMode: "end", jumpCursorPosition: "section-end",
      }), lineFormat);
      await settle();
      return ed.at().ch;
    };
    assertEq(LINE4.indexOf("||"), -1,
      "контроль: первого разделителя в строке нет — иначе правленая ветка не выполняется (У-56)");
    /* Метки подаются так же, как их подаёт команда: по ним хвост значений
       отличается от текста человека. */
    const MK4 = [String.fromCodePoint(0x1F4C5)];
    assertEq(await endOf({ separator1: "||", separator2: "::", markers: MK4 }), "- [ ] 1244".length,
      "строка без первого разделителя: курсор встаёт перед вторым, а не в конец строки");
    assertEq(await endOf({ separator1: "::", separator2: "::", markers: MK4 }), "- [ ] 1244".length,
      "на одинаковых разделителях ответ тот же, но приходит он другой веткой");
    ok("строка без зоны тегов: конец текста — второй Separator");

    /*
     * Шаг курсора внутри строки на тех же разделителях. До 2026-09-11 правила
     * навигации несли **один** разделитель, зона текста угадывалась по метке
     * даты, и при выключенном переходе через разделители курсор уходил в
     * хвост: «при `Move cursor right in line` курсор прыгает в конец строки».
     */
    {
      const LINE5 = "- [ ] #todo || text123 :: 📅2026-09-04 10:21";
      /*
       * **Правила строит функция плагина, а не эта проверка** (У-4). Первая
       * версия писала их литералом, и мутация «второй разделитель не доезжает
       * до правил» её не роняла: проверка держала свой ответ вместо ответа
       * продукта. Конфиг — из фикстуры через настоящую миграцию (правило 2).
       */
      const fsMod = require("fs");
      const normalize = require(path.join(__dirname, "..", "..", "src", "core", "config_normalize.js"));
      const cfg5 = normalize.migrateConfig(JSON.parse(
        fsMod.readFileSync(path.join(__dirname, "..", "fixtures", "config_v1_realistic.json"), "utf8")));
      cfg5.pkm.lineFormat.separator1 = "||";
      cfg5.pkm.lineFormat.separator2 = "::";
      const rules = nav.buildNavigateRules(cfg5);
      assertEq(rules.delim, "||", "первый разделитель доехал до правил навигации");
      assertEq(rules.delim2, "::", "и второй доехал тоже");
      const walk = async (navRules) => {
        const ed = fakeEditor(LINE5, { line: 0, ch: 0 });
        const seen = [];
        for (let i = 0; i < 5; i++) {
          nav.navigateInline(ed, "right", navRules, { stepMode: "word", boundaryJump: false, onBoundary: "stay" });
          await settle();
          const at = ed.at().ch;
          if (seen.length && at === seen[seen.length - 1]) break;
          seen.push(at);
        }
        return seen;
      };
      /*
       * Конец текста — **сразу за последней буквой**, а не на зазоре перед
       * вторым разделителем. Так отвечает общее правило, которым ставят курсор
       * все движки PKM, и так его ждёт заказчик: «должен был `|| ывааы|`»
       * (`S4` 2026-09-12). Прежде здесь стоял индекс самого разделителя — то
       * есть зазор считался частью текста, — и это было второе объявление того
       * же правила.
       */
      const END_OF_TEXT = LINE5.indexOf(" ::");
      const withBoth = await walk(rules);
      assertEq(withBoth[withBoth.length - 1], END_OF_TEXT,
        "с двумя разделителями шаг вправо кончается на конце текста");
      /* Контроль: без второго разделителя предмета нет — курсор уходит дальше. */
      const onlyFirst = await walk(Object.assign({}, rules, { delim2: "" }));
      if (!(onlyFirst[onlyFirst.length - 1] > END_OF_TEXT)) {
        throw new Error("контроль: без второго разделителя курсор обязан уходить за конец текста, а он встал на "
          + onlyFirst[onlyFirst.length - 1]);
      }
      ok("шаг курсора внутри строки кончается на втором Separator, а не в хвосте с датой");
    }

    /* Разделителей в строке нет — конец строки, как и было. */
    const NOTE3 = ["## Раздел один", "просто строка", "", "## Раздел два", "хвост"].join("\n");
    const ed3 = fakeEditor(NOTE3, { line: 4, ch: 0 });
    nav.jumpToHeader(ed3, "up", Object.assign({}, CFG, {
      edgeMode: "end", jumpCursorPosition: "section-end",
    }), { separator1: "::", separator2: "::" });
    await settle();
    assertEq(ed3.at().ch, "просто строка".length,
      "в строке без разделителей курсор остаётся в конце");
    ok("строка без разделителей ведёт себя как прежде");

    /* Разделители не переданы вовсе: старое поведение, а не падение. */
    const noFormat = await jumpTo(undefined);
    assertEq(noFormat.ch, AT_END,
      "без разделителей режим отдаёт конец строки, а не падает");
    ok("без переданных разделителей поведение прежнее");
  }

  /* ---- H7: «строка за строкой» считает заголовок обычной строкой ------- */

  /*
   * Замечание заказчика H7 от 2026-09-04: «heading-jumps-mode при line by line
   * перепрыгивает через хедеры, но в этом режиме он должен считать хедеры как
   * обычную линию».
   *
   * Спрашивается **номер строки, на которую встал курсор**, а не текст правки:
   * прежний код ходил внутри секции, а её содержимое начинается со строки
   * после заголовка, — и заголовок оставался пропущен. Строки `NOTE`:
   * 0-1 вводный текст, 2 пусто, 3 первый заголовок, 4 строка, 5 пусто,
   * 6 второй заголовок, 7 строка.
   */
  {
    const line = (from, dir) => jump(NOTE, from, dir, { jumpMode: "line" });

    assertEq(await line(1, "down"), 3,
      "вниз с последней строки вводного текста — на первый заголовок");
    assertEq(await line(3, "down"), 4,
      "с заголовка — на строку под ним");
    assertEq(await line(4, "down"), 6,
      "с последней строки секции — на следующий заголовок, а не через него");
    assertEq(await line(6, "down"), 7,
      "и дальше на строку под ним");
    assertEq(await line(7, "down"), 7,
      "в конце заметки переход не двигает курсор");

    assertEq(await line(7, "up"), 6,
      "вверх со строки — на её заголовок");
    assertEq(await line(6, "up"), 4,
      "с заголовка — на последнюю строку предыдущей секции");
    assertEq(await line(4, "up"), 3,
      "и снова на заголовок, а не через него");
    assertEq(await line(3, "up"), 1,
      "с первого заголовка — в вводный текст");
    ok("H7: в режиме «строка за строкой» заголовок — такая же остановка");

    /* Верхняя граница прежняя: выше первой строки после свойств не идём (В-20). */
    assertEq(await jump(WITH_YAML, 3, "up", { jumpMode: "line" }), 3,
      "выше первой строки после свойств заметки переход не идёт");
    ok("H7: решение В-20 про свойства заметки не тронуто");
  }

  /* ---- правила навигации приезжают из настроек, а не из файла ----------- */

  /*
   * Служебного файла правил навигация больше не касается (PRD 10.13.52, П-8,
   * шаг первый; 2026-09-11).
   *
   * **Что здесь стояло.** Два утверждения про чтение этого файла: что правила
   * в папке плагина берутся запасным путём через адаптер — `.obsidian/**`
   * vault не индексирует, и `getAbstractFileByPath` такой путь не находит
   * никогда, — и что при отсутствии файла ошибка громкая. Предмета у них не
   * стало, и пойти за предметом они не могут: чтения файла в навигации нет
   * вовсе (У-94). Оба случая, от которых они стерегли, теперь невозможны по
   * устройству, а не по проверке.
   *
   * **Что спрашивается вместо них** — то же самое по существу: доехал ли
   * разделитель до навигации. Источником стал конфиг, и берётся он из
   * фикстуры через настоящую миграцию плагина (правило 2 раздела
   * «Проверки»), а не пишется здесь руками.
   */
  {
    const fs = require("fs");
    const normalize = require(path.join(__dirname, "..", "..", "src", "core", "config_normalize.js"));
    const cfg = normalize.migrateConfig(
      JSON.parse(fs.readFileSync(path.join(__dirname, "..", "fixtures", "config_v1_full.json"), "utf8")));

    const rules = nav.buildNavigateRules(cfg);
    /*
     * Положительный контроль (У-88, У-147): у фикстуры разделитель `//`, а
     * умолчание навигации — `||`. Сверка «приехало то, что записано» на
     * значении, равном умолчанию, прошла бы и при не приехавшем вовсе.
     */
    assertEq(cfg.pkm.lineFormat.separator1, "//", "в фикстуре разделитель отличается от умолчания навигации");
    assertEq(rules.delim, "//", "разделитель приехал из настроек");
    ok("правила навигации собираются из настроек");

    /* Ни vault, ни файла здесь нет вовсе — и это устройство, а не везение. */
    const bare = normalize.migrateConfig({ schemaVersion: 2 });
    assertEq(nav.buildNavigateRules(bare).delim, bare.pkm.lineFormat.separator1,
      "у конфига без правок разделитель тоже свой, а не зашитый в навигацию");
    ok("файла правил на этом пути нет ни одного");
  }

  /* ---- прокрутка при перемещении строки (10.13.36) --------------------- */

  {
    /*
     * Заказчик: «мне не нравится, что при использовании move-lines экран
     * прыгает, причем произвольно — иногда центрирует место перемещения
     * линии, иногда прыгает так, что перемещаемая линия находится наверху
     * экрана».
     *
     * Разбор нашёл, что прокруткой распоряжалась платформа, а свой код до
     * неё не доезжал: он спрашивал `editor.getViewport()`, которого у
     * редактора Obsidian нет вовсе, и выходил первой же строкой. Поэтому
     * подделка редактора здесь **не даёт `getViewport`** — как и настоящая:
     * подделка добрее браузера ловит не тот дефект (У-45).
     *
     * Записан не «был ли вызов», а **что именно уехало в платформу**: y-режим
     * прокрутки и позиция. Пин на факт вызова остался бы зелёным, если
     * перепутать местами верх и низ.
     */
    const viewEditor = (lines, at) => {
      const ed = fakeEditor(lines.join("\n"), { line: at, ch: 0 });
      const seen = { effects: [], scrollTops: [], frames: [] };
      const dom = { scrollTop: 500, scrollLeft: 0 };
      /* Класс представления: статический `scrollIntoView` живёт на нём, и
         именно так до него дотягивается движок (своего импорта у него нет). */
      function View() {}
      View.scrollIntoView = (pos, opts) => ({ pos, y: opts && opts.y });
      const cm = Object.create(View.prototype);
      cm.scrollDOM = dom;
      cm.dispatch = (tr) => { seen.effects.push(tr && tr.effects); };
      cm.dom = { ownerDocument: { defaultView: {
        requestAnimationFrame: (fn) => { seen.frames.push(fn); return 1; },
      } } };
      ed.cm = cm;
      ed.replaceRange = () => {
        /* Платформа прокручивает сама: каждая её транзакция несёт
           `scrollIntoView: true`. Здесь это и изображается — прокрутка
           уезжает, и вернуть её обязан наш код. */
        dom.scrollTop = 900;
      };
      return { ed, seen, dom, at };
    };

    const LINES = ["один", "два", "три", "четыре", "пять"];
    const move = (cfg, at, dir) => {
      const h = viewEditor(LINES, at === undefined ? 2 : at);
      nav.moveLine(h.ed, dir || "down", Object.assign({
        enabled: true, headerMode: "move-as-line", noSelectionMode: "line-only",
        crossSectionAllowed: true, highlightMovedLines: false,
      }, cfg));
      return h;
    };

    const down = move({ keepInView: true, viewPosition: "center" });
    const effects = down.seen.effects.filter(Boolean);
    if (!effects.length) throw new Error("движок не попросил платформу прокрутить вовсе");
    assertEq(effects[effects.length - 1].y, "center",
      "по центру обязано уехать режимом center");

    assertEq(move({ keepInView: true, viewPosition: "top" }).seen.effects.filter(Boolean).pop().y,
      "start", "«по верху» обязано уехать режимом start");
    assertEq(move({ keepInView: true, viewPosition: "bottom" }).seen.effects.filter(Boolean).pop().y,
      "end", "«по низу» обязано уехать режимом end");

    /* Значения нет — читается как «по центру», а не как поломка. */
    assertEq(move({ keepInView: true }).seen.effects.filter(Boolean).pop().y,
      "center", "без выбранной позиции остаётся центр");

    /*
     * Выключено — экран остаётся там, где был. Одной синхронной записи мало:
     * платформа применяет свою прокрутку в проходе измерения, то есть после
     * нашей записи, — поэтому проверяется и то, что вторая запись назначена
     * на следующий кадр, и то, что она действительно возвращает прокрутку.
     */
    const held = move({ keepInView: false });
    assertEq(held.seen.effects.filter(Boolean).length, 0,
      "выключенная настройка всё равно попросила платформу прокрутить");
    assertEq(held.dom.scrollTop, 500, "прокрутка не вернулась на место сразу");
    if (!held.seen.frames.length) throw new Error("вторая запись прокрутки на следующий кадр не назначена");
    held.dom.scrollTop = 900;
    held.seen.frames.forEach((fn) => fn());
    assertEq(held.dom.scrollTop, 500,
      "прокрутка, отданная платформе после нашей записи, осталась чужой");

    ok("прокрутка при перемещении строки: три позиции уезжают режимами, выключенная держит экран");
  }

  /* ---- где встаёт строка на экране после перехода (10.13.37) ----------- */
  {
    /*
     * Заказ заказчика 2026-09-06: «такая же опция, как у перемещения строки».
     * Прежняя строка звала обёртку Obsidian с `center = true` и другого
     * положения не знала.
     *
     * Записан не факт вызова, а **режим**, уехавший в платформу: пин на
     * «прокрутили» остался бы зелёным, если перепутать верх и низ (У-58).
     * Троттлинг здесь снят нулём: он про повторные нажатия, а не про выбор
     * положения, и с ним второй вызов подряд молча не доедет.
     */
    const revealEditor = () => {
      const ed = fakeEditor(NOTE, { line: 0, ch: 0 });
      const seen = { effects: [] };
      function View() {}
      View.scrollIntoView = (pos, opts) => ({ pos, y: opts && opts.y });
      const cm = Object.create(View.prototype);
      cm.dispatch = (tr) => { seen.effects.push(tr && tr.effects); };
      ed.cm = cm;
      return { ed, seen };
    };

    const jumped = async (over) => {
      const h = revealEditor();
      nav.jumpToHeader(h.ed, "down", Object.assign({}, CFG, {
        centerCursor: true, centerDelayMs: 0, centerThrottleMs: 0,
      }, over || {}));
      await settle();
      return h.seen.effects.filter(Boolean);
    };

    const centered = await jumped({ viewPosition: "center" });
    if (!centered.length) throw new Error("переход не попросил платформу прокрутить вовсе");
    assertEq(centered.pop().y, "center", "«по центру» обязано уехать режимом center");
    assertEq((await jumped({ viewPosition: "top" })).pop().y,
      "start", "«по верху» обязано уехать режимом start");
    assertEq((await jumped({ viewPosition: "bottom" })).pop().y,
      "end", "«по низу» обязано уехать режимом end");
    /* Значения нет — читается как «по центру», то есть как было до заказа. */
    assertEq((await jumped({})).pop().y,
      "center", "без выбранной позиции остаётся центр");
    /* Выключенный тумблер не прокручивает вовсе — он и раньше не прокручивал. */
    assertEq((await jumped({ centerCursor: false, viewPosition: "top" })).length, 0,
      "выключенный тумблер всё равно попросил платформу прокрутить");

    ok("прокрутка при переходе по заголовкам: три положения уезжают разными режимами");
  }

  /* ---- пустой слот текста у строки с одним разделителем ---- */

  /*
   * Заказчик 2026-09-06: Field из правого Block активирован на пустой строке,
   * и прыжок внутри строки ставит курсор после разделителя, а надо — в то место, где
   * слово началось бы: `- | :: 📅2026-09-06 10:21`.
   *
   * Записан номер столбца, который видит человек, а не намерение кода: разница
   * между верным и неверным ответом здесь в один символ, и он решает всё:
   * напечатанное в позиции справа слипается с разделителем.
   */
  {
    const MARKER = String.fromCodePoint(0x1F4C5);
    const RULES = { delim: "::", trailingMarkers: [MARKER], dateRegexSrc: "\\d{4}-\\d{2}-\\d{2}" };
    const land = async (text, from, direction) => {
      const ed = fakeEditor(text, { line: 0, ch: from });
      nav.navigateInline(ed, direction, RULES,
        { stepMode: "word", boundaryJump: false, onBoundary: "stay" });
      await settle();
      return ed.at().ch;
    };

    const BULLET = "-  :: " + MARKER + "2026-09-06 10:21";
    const BOX = "- [ ]  :: " + MARKER + "2026-09-06 10:20";

    /* После `- ` и до пробела перед разделителем — столбец 2. */
    assertEq(await land(BULLET, 0, "right"), 2,
      "курсор обязан встать в пустой слот текста, а не вплотную к разделителю");
    assertEq(await land(BULLET, BULLET.length, "left"), 2,
      "и с конца строки — туда же");
    assertEq(await land(BOX, 0, "right"), 6,
      "со снятым чекбоксом — сразу за ним, а не у разделителя");

    /* Обратная сторона: строка с текстом ходит, как ходила. */
    const WITH_TEXT = "- один два :: " + MARKER + "2026-09-06 10:21";
    assertEq(await land(WITH_TEXT, 0, "right"), 2,
      "у строки с текстом начало зоны прежнее");
    assertEq(await land(WITH_TEXT, WITH_TEXT.length, "left"), "- один два".length,
      "и конец зоны прежний — перед разделителем");

    ok("пустой слот текста: курсор встаёт туда, где начнётся слово");
  }

  /* ---- курсор при разных разделителях: шесть случаев заказчика ---- */

  /*
   * Замечание 2026-09-12, строка `S4`: «при прыжках курсор часто встаёт в
   * неправильное место строки». Шесть примеров, снятых с его экрана, и у
   * каждого он написал, где курсор оказался и где должен был.
   *
   * **Почему этого не видели прежние проверки.** У них один разделитель на
   * обе роли (`delim: "::"`), а у заказчика `||` и `::` — и правило, верное
   * при совпадающих, при разных промахивается (У-147). Здесь разделители
   * разведены нарочно.
   *
   * Где кончается текст человека, знает **одно** объявление —
   * `getCursorAtTextEnd` в `pkm_macro_shared.js`, — и оно на всех шести
   * случаях отвечает ровно то, что написал заказчик. Проверка спрашивает
   * поведение команд, а не его.
   */
  {
    const MARKER = String.fromCodePoint(0x1F4C5);
    const LF = { separator1: "||", separator2: "::" };
    const RULES = { delim: "||", delim2: "::", trailingMarkers: [MARKER], dateRegexSrc: "\\d{4}-\\d{2}-\\d{2}" };
    const JUMP = {
      centerCursor: false, jumpMode: "line", edgeMode: "start",
      jumpCursorPosition: "section-end", centerDelayMs: 0, centerThrottleMs: 0,
    };

    const jumpDown = async (lines, fromLine) => {
      const ed = fakeEditor(lines.join("\n"), { line: fromLine, ch: 0 });
      nav.jumpToHeader(ed, "down", JUMP, LF);
      await settle();
      return ed.at();
    };

    const jumpDownWith = async (lines, fromLine, cursorMode) => {
      const ed = fakeEditor(lines.join("\n"), { line: fromLine, ch: 0 });
      nav.jumpToHeader(ed, "down", { ...JUMP, jumpCursorPosition: cursorMode }, LF);
      await settle();
      return ed.at();
    };

    const step = async (line, from, direction, stepMode) => {
      const ed = fakeEditor(line, { line: 0, ch: from });
      nav.navigateInline(ed, direction, RULES,
        { stepMode: stepMode || "sentence", boundaryJump: false, onBoundary: "stay" });
      await settle();
      return ed.at().ch;
    };

    /* 1. Пустой слот в конце строки: курсор за первым разделителем, а не до него. */
    const L1 = MARKER + "2026-09-12 09:10 || ";
    const p1 = await jumpDown(["первая строка", L1], 0);
    assertEq(p1.ch, L1.length, "прыжок: пустой слот текста в конце строки");

    /* 2. Строка без первого разделителя: курсор в слоте, а не вплотную к знаку списка. */
    const L2 = "-  :: " + MARKER + "2026-09-12 12:06";
    const p2 = await jumpDown(["первая строка", L2], 0);
    assertEq(p2.ch, 2, "прыжок: пустой слот текста между знаком списка и вторым разделителем");

    /* 3. Оба разделителя: курсор в слоте между ними, а не вплотную к первому. */
    const L3 = "- [N] " + MARKER + "2026-09-12 09:07 ||  :: #/2 #note";
    const p3 = await jumpDown(["первая строка", L3], 0);
    assertEq(p3.ch, L3.indexOf("||") + 3, "прыжок: пустой слот текста между двумя разделителями");

    /*
     * 3а. `Start of your text` — четвёртое положение той же строки панели,
     * заказанное 2026-09-12. Прыжок обязан встать в начало текста человека, то
     * есть сразу за значениями, а не в начало строки и не в её конец.
     *
     * Рядом стоит `End of your text` на той же строке: без него утверждение
     * было бы зелёным и у плагина, который нового положения не знает и
     * откатывается на начало строки — на этой строке начало текста и начало
     * строки разные места, но проверку это не спасает, если сравнивать не с
     * чем.
     */
    const L3a = "- [ ] " + MARKER + "2026-09-12 09:07 #/1 || мой текст :: #note";
    const p3aStart = await jumpDownWith(["первая строка", L3a], 0, "section-start");
    assertEq(p3aStart.ch, L3a.indexOf("мой текст"),
      "прыжок при `Start of your text`: начало текста человека, сразу за значениями");
    const p3aEnd = await jumpDownWith(["первая строка", L3a], 0, "section-end");
    assertEq(p3aEnd.ch, L3a.indexOf(" :: #note"),
      "контроль: `End of your text` на той же строке ведёт в другое место — в конец текста");

    /*
     * 4. Шаг вправо из зоны значений уходит **в текст**, и куда именно —
     * говорит настройка `Cursor position after jumping`.
     *
     * Решение заказчика 2026-09-12 (В-107): «два режима будут определять, в
     * начало или конец моего текста курсор должен попадать», и действует этот
     * выбор на оба хода — на прыжок и на шаг. До него шаг всегда вставал в
     * начало, а заказчик в своём примере ждал конец сразу.
     *
     * Обе стороны спрашиваются подряд, и вторая — положительный контроль:
     * утверждение «курсор в конце» было бы зелёным и у плагина, который
     * настройку не читает вовсе, потому что конец — умолчание.
     */
    const stepWith = async (line, from, direction, cursorMode) => {
      const ed = fakeEditor(line, { line: 0, ch: from });
      nav.navigateInline(ed, direction, RULES,
        { stepMode: "sentence", boundaryJump: false, onBoundary: "stay", jumpCursorPosition: cursorMode });
      await settle();
      return ed.at().ch;
    };

    const L4 = "- [ ] " + MARKER + "2026-09-11 23:46 #/1 || ывааы";
    const L4_TEXT_START = L4.indexOf("ывааы");
    const from4 = L4.indexOf("#/1") + 3;
    assertEq(await stepWith(L4, from4, "right", "section-end"), L4.length,
      "при `End of your text` шаг вправо из зоны значений обязан вести в конец текста");
    assertEq(await stepWith(L4, from4, "right", "section-start"), L4_TEXT_START,
      "контроль: при `Start of your text` тот же шаг обязан вести в его начало");

    /* 5. То же от начала значения, и обе стороны настройки. */
    const L5 = "- [ ] " + MARKER + "2026-09-12 09:02 #/1 || 123";
    const L5_TEXT_START = L5.indexOf("123");
    assertEq(await stepWith(L5, 6, "right", "section-end"), L5.length,
      "шаг вправо от значения при `End of your text`: конец текста");
    assertEq(await stepWith(L5, 6, "right", "section-start"), L5_TEXT_START,
      "контроль: при `Start of your text` — его начало");
    assertEq(await stepWith(L5, L5_TEXT_START, "right", "section-start"), L5.length,
      "шаг изнутри зоны настройкой не трогается: следующая остановка — конец текста");

    /*
     * И ход по словам настройка не отменяет: там остановки — сами слова, и
     * первое из них и есть место, куда человек шагает.
     */
    assertEq(await step(L5, 6, "right", "word"), L5_TEXT_START,
      "в режиме `Word` шаг вправо по-прежнему встаёт на первое слово текста");

    /* 6. Шаг влево у строки без первого разделителя: слот, а не место у знака. */
    const L6 = "-  :: " + MARKER + "2026-09-12 12:05";
    assertEq(await step(L6, 3, "left"), 2,
      "шаг влево: пустой слот текста, а не место вплотную ко второму разделителю");

    /*
     * 7. Лишние пробелы перед вторым разделителем: один из них остаётся
     * **слева** от курсора.
     *
     * Замечание заказчика 2026-09-12: «при прыжке курсор встал
     * `… || 123 1231|  :: 👤111`, а должен был `… || 123 1231 | :: 👤111` — то
     * есть после последнего слова, чтобы не приходилось делать дополнительный
     * arrow right на пустое место».
     *
     * Конец текста считается по слоту, а лишние пробелы лежат **за** слотом —
     * и курсор оставался вплотную к последнему слову: следующее набранное
     * слово к нему и приклеивалось.
     *
     * Рядом стоят обе границы правила, и они же положительный контроль: при
     * одном пробеле и при нуле курсор не двигается вовсе. Его слова: «при
     * прыжках не создавать пробел, если его не было изначально». Без них
     * утверждение было бы зелёным и у плагина, который просто уводит курсор
     * вплотную ко второму разделителю всегда.
     */
    const L7 = "- " + MARKER + "2026-09-12 18:59 || 123 1231  :: #/1 #note";
    const p7 = await jumpDown(["первая строка", L7], 0);
    assertEq(p7.ch, L7.indexOf(" :: "),
      "прыжок: при двух пробелах перед вторым разделителем один обязан остаться слева от курсора");

    const L7one = "- " + MARKER + "2026-09-12 18:59 || 123 1231 :: #/1 #note";
    const p7one = await jumpDown(["первая строка", L7one], 0);
    assertEq(p7one.ch, L7one.indexOf(" :: "),
      "контроль: при одном пробеле курсор остаётся там же, где стоял — пробел не заводится");

    const L7none = "- " + MARKER + "2026-09-12 18:59 || 123 1231:: #/1 #note";
    const p7none = await jumpDown(["первая строка", L7none], 0);
    assertEq(p7none.ch, L7none.indexOf("::"),
      "контроль: пробела не было — курсор встаёт вплотную ко второму разделителю");

    ok("курсор при разных разделителях: семь случаев заказчика");
  }

  /* ---- метка Field доезжает до навигации ---- */

  /*
   * У заказчика четыре именованных правила дат пусты: поле-дата у него не одно
   * из них, а свой Field со своей меткой. Без метки навигация считает
   * единственный разделитель первым и уводит курсор в хвост с датой.
   * Спрашивается результат чтения, а не факт разбора.
   *
   * **Источником стали настройки** (PRD 10.13.52, П-8, шаг первый,
   * 2026-09-11): раньше здесь подавалась рукописная заметка правил. Конфиг
   * берётся из фикстуры через настоящую миграцию плагина — и та же фикстура
   * даёт положительный контроль, без которого утверждение ниже ничего не
   * значит: именованные правила дат в ней пусты, и метке взяться больше
   * неоткуда, кроме списка Field.
   */
  {
    const fs = require("fs");
    const normalize = require(path.join(__dirname, "..", "..", "src", "core", "config_normalize.js"));
    const cfg = normalize.migrateConfig(
      JSON.parse(fs.readFileSync(path.join(__dirname, "..", "fixtures", "config_v1_realistic.json"), "utf8")));
    const MARKER = String.fromCodePoint(0x1F4C5);

    const named = (cfg.pkm.behavior && cfg.pkm.behavior.dateRules) || {};
    assertEq(Object.keys(named).length, 0, "в фикстуре именованные правила дат пусты — метке взяться больше неоткуда");

    const rules = nav.buildNavigateRules(cfg);
    if (rules.trailingMarkers.indexOf(MARKER) === -1) {
      throw new Error("метка Field не доехала до навигации: " + JSON.stringify(rules.trailingMarkers));
    }
    ok("метка своего Field берётся из списка Field, а не только из четырёх правил дат");

    /* И она впрямь решает, куда встанет курсор: те же правила через прыжок. */
    const LINE = "-  " + rules.delim + " " + MARKER + "2026-09-06 10:21";
    const ed = fakeEditor(LINE, { line: 0, ch: LINE.length });
    nav.navigateInline(ed, "left", rules, { stepMode: "word", boundaryJump: false, onBoundary: "stay" });
    await settle();
    assertEq(ed.at().ch, 2, "с правилами из настроек курсор всё равно встаёт в слот текста");
    ok("путь целиком: настройки → метка → положение каретки");
  }

  /*
   * **Форма строки для прыжков: метки приезжают от движка, а не «если вдруг»**
   * (10.13.167).
   *
   * Вопрос здесь не к движку, а к **слою команд**: `getJumpLineShape` спрашивала
   * `typeof rt.buildNavigateRules === "function"` и на «нет» отдавала форму
   * строки **без меток элементов** — то есть ровно то поведение, которым был
   * замечен `S4`: прыжок «в конец вашего текста» уезжал за дату. Молча.
   *
   * Исполнял эту функцию **ни один прогон**: счётчик на её охране за весь
   * набор показал ноль. Поэтому водитель написан вместе с починкой (правило
   * 124): команда прыжка зовётся так же, как её зовёт Obsidian, — с движком
   * аргументом.
   */
  {
    const registry = require(path.join(__dirname, "..", "..", "src", "features", "command_registry.js"));
    const notices = [];
    const plugin = {
      app: { setting: { open: () => {}, openTabById: () => {} } },
      manifest: { id: "inline-overhaul" },
      notice: (m) => { notices.push(String(m || "")); },
      getConfig: () => ({}),
    };
    const defs = registry.buildNavigationCommandDefs(plugin);
    const jump = defs.find((d) => d && d.id === "jump-next");
    if (!jump) throw new Error("команда прыжка вперёд не собралась вовсе");

    const cfg = { pkm: { lineFormat: { separator1: "||", separator2: "::" } } };
    const navCfg = { jumpToHeader: { enabled: true, mode: "sections" } };

    /* Движок целиком: форма обязана донести метки до самого прыжка. */
    const seen = [];
    const rtFull = {
      jumpToHeader: (_ed, _dir, _opts, shape) => { seen.push(shape); },
      buildNavigateRules: () => ({ trailingMarkers: ["\u{1F4C5}", "\u{1F464}"] }),
    };
    jump.run({}, navCfg, cfg, rtFull);
    assertEq(seen.length, 1, "прыжок не позвал движок");
    assertArrayEq(seen[0].markers, ["\u{1F4C5}", "\u{1F464}"],
      "метки элементов не доехали до формы строки прыжка");
    assertEq(seen[0].separator1, "||", "разделитель человека до формы прыжка не доехал");

    /*
     * А без сборки правил команда обязана сказать человеку, а не молча прыгнуть
     * по другой форме строки. Это положительный контроль в другую сторону: без
     * него зелёное значило бы «метки есть всегда».
     */
    const rtHalf = { jumpToHeader: () => { seen.push(null); } };
    const before = seen.length;
    jump.run({}, navCfg, cfg, rtHalf);
    assertEq(seen.length, before, "без сборки правил прыжок всё равно состоялся");
    assertEq(notices.length, 1, "человеку не сказано, что движок навигации не доехал");
    ok("прыжок: метки доезжают до формы строки, а без движка человек читает отказ");
  }

  console.log("\n" + passed + " проверок пройдено");
})().catch((e) => { console.error(e); process.exit(1); });
