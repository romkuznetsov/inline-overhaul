# Тестирование · inline Overhaul

> [!info] Где мы сейчас · 2026-09-23, ночь, последний выпуск `0.7.0`
> **Сделаны обе задачи из вашей очереди, по порядку, и работа по всем вашим ответам интервью.** Выбиралка знака (тесты 1 и 2, `Escape` сворачивает её первым), дочернее поле под `Alt` — и без значения у родителя (тест 3), панель по бренд-буку: цвета типов, знак, тон текстов (тест 4).
> **Тесты 1–3 прошлой заметки приняты и убраны.** Тест про `Ctrl+Y` убран по вашему слову «убери этот таск, я не могу его воспроизвести».
> **Выкладки не было:** работа лежит пятью локальными коммитами. Выложу, когда скажете.
> **Бадж версии в README не врёт:** он берёт последний выпуск у GitHub, а там `0.7.0`. «0.6» — это кеш сервиса баджей в первые часы после выпуска.

> [!tip]- Как устроен тест · разверните один раз
> Под заголовком — **галочка** и строка `💬`: принято, или что не так. Ниже четыре свёрнутых блока — описание, журнал, настройки, действие — и три раздела:
> **🎯А** — какой станет строка после действий, **🛠️Б** — эталонная исходная строка, **💥В** — её копия, на которой работаете вы.
> Трогайте только **В**. Расхождение между В и А разбираю я.
> При закрытии сессии я убираю принятое, чищу В, обновляю журнал.

## 1. Выбиралка знака в окне Binder
- [ ] принято
- 💬в целом хорошо, но есть ряд комментариев
	- сейчас по умолчанию в textbox Inserts стоит символ `→`, замени его на текст `Type anything or choose below`  
	- переименуй `Text faces` → `Kaomoji`, `Symbols` → `Symbol`
	- поменяй очередность табов на `Symbol`, `Emoji`, `Kaomoji`
	- хочу, чтобы ты добавил больше опций в `Symbol`, `Emoji` (например, чтобы было столько же эмодзи, сколько есть в эмодзи windows 11), то же с `Symbol` - должно быть больше популярных, чтобы пользователю не нужно было искать эти элементы где-то еще 
	- сейчас в `Symbol` и `Emoji` все элементы представлены одним списком. Я хочу, чтобы ты добавил рубрикаторы (текстовые строки - своего рода хедеры) в `Symbol` и `Emoji`, чтобы тематические значения находились рядом. Например в `Symbol` можно сделать рубрику `Arrows` (это для примера, подумай сам над рубриками) - в которой будут находиться `→`, `←`, `⤷`, `⇄` и т.д. Эти рубрикаторы должны лишь визуально разделять элементы (т.е. я представляю это так - что в текущем picker элементов появятся текстовые названия сепараторов, под которыми будут элементы. Т.е. должна по прежнему остаться одна скроллящаяся форма со всеми элементами в табах `Symbol`, `Emoji`, но элементы в них структурированы под хедерами рубрик)
	- в модальном окне добавления команды в Binder у контролов (`inserts`, `command name`, `description`) нет tips - добавь, чтобы была единая логика tip во всем плагине

> [!question]- Описание задачи
> Ваш пункт 9: в окне новой команды Binder у `Inserts` — выпадающий список с вкладками эмодзи, символов и текстовых рожиц, и имя команды должно подбираться само: «при `→` должно быть `Arrow right`». Вписать своё можно по-прежнему.
> **Сделано.** Нажатие в поле `Inserts` раскрывает под ним выбиралку: вкладки `Emoji`, `Symbols`, `Text faces`, поиск по имени во всех трёх и сетка с прокруткой. Выбрали знак — он лёг в поле, выбиралка свернулась, а в `Command name` встало его имя.
> **Имя держится, пока вы его не тронули.** Выбрали другой знак — имя сменилось вслед. Написали своё — окно его больше не трогает. У своего текста имя тоже предлагается: `Insert <текст>`.
> **`Escape` — по вашему ответу:** первый сворачивает только выбиралку, второй закрывает окно.

> [!example]- Журнал
> **Цикл 82 · 2026-09-23, ночь**
> - выбиралка — один модуль на оба места, как вы ответили («одна на оба места»)
> - восемь подмен, каждая краснеет; в настоящем браузере проверены раскрытие, поиск, выбор и сворачивание
> - по вашему ответу: `Escape` сворачивает выбиралку, а не окно; четыре подмены

> [!settings]- Настройки
> `Keyboard` → `Binder` → кнопка `Add command`

> [!action]- Действие
> 1. Перезапустить плагин: выключить и включить `inlineOverhaul` в `Community plugins`.
> 2. Открыть `Keyboard` → `Binder` и нажать `Add command`.
> 3. Нажать в поле `Inserts`: под ним обязана раскрыться выбиралка с тремя вкладками и поиском.
> 4. Открыть `Symbols` и нажать `→`: в `Inserts` встаёт `→`, в `Command name` — `Arrow right`, красная рамка у `Inserts` пропадает.
> 5. Снова нажать в `Inserts`, в поиске набрать `rocket` и выбрать `🚀`: имя обязано смениться на `Rocket`.
> 6. Вписать в `Command name` своё имя, затем выбрать другой знак: ваше имя обязано остаться.
> 7. Стереть `Inserts` и набрать руками `см. выше`: имя обязано стать `Insert см. выше` (если своё имя из шага 6 стерто).
> 8. Снова раскрыть выбиралку и нажать `Escape`: свернуться обязана только она, окно остаётся. Второй `Escape` закрывает окно — поэтому перед ним:
> 9. Нажать `Add` и проверить, что команда появилась в палитре под выбранным именем.

### 🎯А. Ожидаемый результат
—

### 🛠️Б. Эталонная исходная строка
—

### 💥В. Тест
—

---

## 2. Выбиралка эмодзи у знака Field
- [ ] принято
- 💬учти комментарии из пункта 1 про эмодзи (увеличения количества, рубрикатор)

> [!question]- Описание задачи
> Ваш пункт 10: у знака Field типа `Emoji` — «чтобы при нажатии на панель ввода открывался выпадающий список с эмодзи — пользователь может ткнуть в понравившийся, а может сам вставить что хочет».
> **Сделано той же выбиралкой**, но с одной вкладкой — эмодзи: знак Field должен быть одним знаком. Поиск по имени есть и здесь. Выбранный знак пишется в настройки той же записью, что и набранный руками.

> [!example]- Журнал
> **Цикл 82 · 2026-09-23, ночь**
> - та же выбиралка, одна вкладка; запись выбранного знака сверена с записью набранного — путь и причина совпадают
> - прототип панели поправлен тоже: знак Field он рисует сам

> [!settings]- Настройки
> `Tags & PKM` → `Fields` → Field типа `Emoji` (например, `Due`) → раздел `Value` → `Emoji-prefix`

> [!action]- Действие
> 1. Перезапустить плагин.
> 2. Открыть Field `Due` и нажать в поле `Emoji-prefix`: под ним обязана раскрыться сетка эмодзи с поиском, без вкладок.
> 3. Набрать в поиске `calendar` и выбрать `📅`: знак обязан встать в поле, сетка свернуться.
> 4. Нажать в поле ещё раз и нажать в соседнее поле `Value format`: сетка обязана свернуться.
> 5. Вставить в поле знак руками — как раньше; это обязано работать.
> 6. Если знак менялся — вернуть свой прежний.

### 🎯А. Ожидаемый результат
—

### 🛠️Б. Эталонная исходная строка
—

### 💥В. Тест
—

---

## 3. Дочернее поле под Alt
- [ ] принято
- 💬 есть проблемы
	- при скроллинге в sub-field в строке (при io-field-child=show when press alt) при каждом нажатии на arrow up/down возникает уведомление `Tagwheel is open on this line: finish it with enter or close with esc first`. Вероятно дело в том, что у меня на `alt + arrow up` стоит бинд на команду `Importance next`. Смотри следующий буллит
	-  :: #test2 #test2-2 - у меня сейчас бинд на открытие tagwheel `alt + arrow down`, когда я пытаюсь скроллить subfield при зажатом alt вниз - у меня закрывается tagwheel. Думаю нужно изменить логику - чтобы sub-field активировался не удержанием кнопки alt, а однократным нажатием (первое нажатие открывает sub-field активного field, второе нажатие закрывает его)
- 💬хочу изменить логику - чтобы при нажатии alt открывалось sub-field любого field вне зависимости от io-field-child=show when press alt (исключение - io-field-child=hide (тогда field вообще не появляется) и io-field-child=show always (оно всегда видно и без зажатия alt). Т.е. по сути мое изменение логики относится только к io-field-child=after parent). А у fields с io-field-child=show when press alt - это поле возникало ТОЛЬКО при нажатии alt

> [!question]- Описание задачи
> Ваша задача `З-36`: дочернее поле показывается в панели, пока зажат `Alt`, «не переключателем»; отпустили — снова скрыто, выбранное значение остаётся на строке. И пункт 11: это положение контрола `Child Field` — `Show when press Alt`.
> **Сделано.** У `Child Field` четвёртое положение. В tagWheel такое поле скрыто; зажали `Alt`, стоя на родителе, — поле встаёт рядом, на него можно шагнуть стрелкой и выбрать значение. Отпустили — поле скрыто, курсор возвращается на родителя, значение остаётся на строке.
> **Без значения у родителя — тоже**, по вашему ответу «показать все дочерние»: под `Alt` поле встаёт со всеми дочерними значениями, выбранное ложится на строку без родителя. Команды поля работают, как при `After parent`.
> **Правка задела файлы движка** (`tagwheel.js`, `tagwheel_core.js`, `pkm_order_config.js`) — работу назначили вы. Поэтому здесь вопрос: **оставить ли сделанное**. Нет — откатывается одним коммитом.

> [!example]- Журнал
> **Цикл 82 · 2026-09-23, ночь**
> - поле прячется только в показе панели; чистка значений и раскладка строки его видят, как прежде
> - `Alt`, отпущенный за пределами окна (после `Alt+Tab`), снимается следующим нажатием
> - девять подмен в проверках и две в браузере, все красные
> - по вашему ответу: `Alt` открывает поле и без значения у родителя

> [!settings]- Настройки
> `Tags & PKM` → `Fields` → `Importance` → таблица `Values`: одно значение сделать дочерним к `#high` (стрелкой в колонке `Level`)
> `Tags & PKM` → `Fields` → `Importance` → `Behavior` → `Child Field` = `Show when press Alt`

> [!action]- Действие
> 1. Перезапустить плагин.
> 2. У `Importance` добавить значение (например, `#urgent`) и стрелкой в колонке `Level` сделать его дочерним к `#high`.
> 3. В `Behavior` поставить `Child Field` = `Show when press Alt`.
> 4. Поставить курсор на строку `💥В` и открыть tagWheel слева. На полосе обязан стоять `Importance` со значением `#high`, а дочернего поля — нет.
> 5. Встать на `Importance` и зажать `Alt`: рядом обязано появиться дочернее поле.
> 6. Не отпуская `Alt`, нажать `→`, чтобы встать на дочернее поле, и `↑`, чтобы выбрать `#urgent`.
> 7. Отпустить `Alt`: дочернее поле обязано исчезнуть с полосы, курсор — встать на `Importance`.
> 8. Нажать `Enter`: в строке обязано стоять и `#high`, и `#urgent`.
> 9. Вторая строка `💥В` — без родителя: открыть tagWheel, встать на `Importance` (пустой), зажать `Alt`, шагнуть `→` на дочернее поле, выбрать `#urgent`, отпустить, `Enter`. В строке обязан стоять один `#urgent`, без `#high`.
> 10. Поставить галочку, если оставляем; написать в `💬`, если откатываем.

### 🎯А. Ожидаемый результат
- #high #urgent :: строка для Alt
- #urgent :: вторая строка без родителя

### 🛠️Б. Эталонная исходная строка
- #high :: строка для Alt
- вторая строка без родителя

### 💥В. Тест
- #high :: строка для Alt
- вторая строка без родителя

---

## 4. Панель по бренд-буку
- [ ] принято
- 💬очень большое пустое расстояние у знака сверху и снизу - уменьши
	- ![[Pasted image 20260923102237.png]]
- 💬 у знака каретка не мерцает - можно ли сделать как в github, чтобы она мигала?
- 💬 поменяй очередность вкладок, чтобы Transform был перед Visual
- 💬 

> [!question]- Описание задачи
> Ваш пункт 1 из `Ideas.md` и выбор в интервью: три бесспорных пункта бренд-бука; запрет своих цветов в панели (З6) остаётся.
> **Цвета типов Field.** Чип `Tag` — янтарь бренда с тёмным текстом (белый на янтаре не читается), чип `Link` — сталь. У `Emoji` цвета в бренде нет, он прежний.
> **Знак.** Готовый знак `inlineOverhaul` в начале вкладки `General`, как вы выбрали; тёмный вариант — в тёмной теме, на телефоне знак прячется.
> **Тон.** 22 подсказки переписаны по правилам тона бренд-бука: без «you will», без пассива, без восклицания. Смысл прежний.
> **Вопрос здесь:** знак стоит **над** вводным коллаутом `Before you start`, а ваше правило К1 говорит, что коллаут — первый блок вкладки. Галочка — оставить так; `💬` «под коллаут» — переставлю.

> [!example]- Журнал
> **Цикл 82 · 2026-09-23, ночь**
> - разбор бренд-бука для панели — `docs/dev/BRAND_PANEL_ANALYSIS.md`
> - цена места знака пересчитана до правки и спрошена заново
> - сторож тона держит ноль по всем 1 572 строкам панели

> [!settings]- Настройки
> `General` — начало вкладки
> `Tags & PKM` → `Fields` — чипы типа в списке и в заголовке Field
> `Tags & PKM` → предпросмотр строки над списком Fields

> [!action]- Действие
> 1. Перезапустить плагин.
> 2. Открыть настройки: на `General` сверху обязан стоять знак `inlineOverhaul`, под ним — коллаут `Before you start`.
> 3. Прокрутить вниз: знак обязан уехать вместе со страницей; на других вкладках его нет.
> 4. Переключить Obsidian на тёмную тему: знак обязан смениться на тёмный вариант.
> 5. Открыть `Tags & PKM` → `Fields`: чип `Tag` — янтарный с тёмными буквами, чип `Link` — стальной, `Emoji` — зелёный, как был.
> 6. Навести на «?» у `Binder`: подсказка начинается прежними словами, а про описание сказано «Afterwards you change only the description».
> 7. Поставить галочку, если знак оставляем над коллаутом; написать `💬`, если переставить под него.

### 🎯А. Ожидаемый результат
—

### 🛠️Б. Эталонная исходная строка
—

### 💥В. Тест
—

---

## Новое пишите сюда
Всё, что заметите по ходу, — сюда, своими словами и без порядка. Я разберу и заведу отдельными тестами.
- 💬иногда в заметке возникает визуальный баг на длинной (многострочной) линии с буллитом - текст второй и далее строк смещается левее буллита. Этого быть не должно 
	- пример бага
		- ![[Pasted image 20260923133150.png]]
	- после совершения действия в этой строке (т.е. не просто клика или перемещения курсора, а именно редактирования - ввод пробела, символа и т.п.) отображение строки восстанавливается к нормальному состоянию 
		- ![[Pasted image 20260923133217.png]]
- 💬сейчас у обязательных для заполнения полей красная рамка видна не сразу а только после того, как я совершу какое-то действие в экране. Я хочу, чтобы у таких полей рамка была красная всегда, до тех пор, пока в нем не появятся значение (перекрашивать в серый только после появления значения)
	- только открыл экран, курсор находится в этой строке ввода (рамка серая)
		- ![[Pasted image 20260923101811.png]]
	- совершил любое действие на экране (рамка красная)
		- ![[Pasted image 20260923101928.png]]
## Мои вопросы к вам

Все вопросы интервью этого цикла отвечены вами сразу, и работа по ответам сделана тем же заходом. Открытым остаётся одно — оно в тесте 4: знак над вводным коллаутом или под ним.

> [!note]- Служебное · промпт для новой сессии Claude
> Это не вам. Наведите курсор на блок ниже, нажмите кнопку копирования в его углу и вставьте в новую сессию.
>
> ```
> Новая сессия по inline Overhaul. Прошлый цикл, 82-й, закрыт ночью 2026-09-23: обе задачи его очереди и работа по всем его ответам интервью сделаны, выкладки не было.
>
> ЧТО ПРОЧИТАТЬ ДО ПЕРВОГО ДЕЙСТВИЯ
>
> `CLAUDE.md` целиком. Затем `test-vault/testing.md`: его галочки, строки `💬` и **весь текст**, он правит заметку прямо в ней и вписывает задачи между блоками. Плюс `test-vault/Test.md` и `test-vault/Test 1.md`. Затем `docs/dev/OPEN_QUESTIONS.md`, раздел «Открытые»: вопросы `В-195`…`В-198` отвечены им тем же заходом; открытый вопрос один — знак над коллаутом или под ним, он стоит тестом 4. Правите что-либо из того, что читает человек снаружи, — сперва `.claude/skills/repo-docs/SKILL.md`.
>
> КАК УСТРОЕН ЦИКЛ — ЕГО РЕШЕНИЕ `В-175`, 2026-09-21
>
> Вход — ссылка на эту заметку. Порядок: **сперва то, что он пометил дефектом, потом новые задачи**. Вопросы — интерактивным интервью и **после** починок. Затем, не спрашивая, идёт процедура завершения (`docs/dev/SESSION_END.md`). **`git push` в неё не входит.** Ответил на интервью — работа по ответу идёт тем же заходом.
>
> ГДЕ МЫ СТОИМ
>
> **Последний выпуск — `0.7.0` от 2026-09-22.** В `## Unreleased` шесть пунктов — всё, что сделано циклом 82. Локально лежат пять коммитов поверх выложенного.
>
> Цикл 82 сделал две его задачи и работу по его ответам. Выбиралка знака (`320у`, `В-182`): один модуль `char_picker.ts` на окно новой команды Binder и на знак Field; `Escape` сворачивает её своей областью клавиш Obsidian (`В-196`). Дочернее поле под `Alt` (`321у`, `З-36`, `В-195`): четвёртое положение `Child Field`, и без значения у родителя тоже; исключение к З3 № 148 — вопрос «оставить ли» стоит тестом 3. Панель по бренд-буку (`322у`, `В-198`): цвета типов Field, знак в начале `General`, тон текстов со сторожем `panel_voice_tests.ts`.
>
> В заметке четыре теста, все с пустыми чекбоксами.
>
> **ЧТО ДЕЛАТЬ ПЕРВЫМ**
>
> **Прочитать его пометки** и начать с дефектов. Нет дефектов — спросить интервью, что брать следующим: очередь его заказов пуста, а короткий список `Ideas.md` он сам называл неоформленным — выбирает он.
>
> ЧТО НАДО ЗНАТЬ
>
> **Он меняет свой файл настроек по ходу** (правило 110), и числа стендов скачут без правки кода. Расхождения группировать по полю до разбора (правило 189). **У него сейчас ни у одного Field нет дочерних значений** — тест 3 начинается с того, что он заводит одно.
>
> **`Ф-8` перемерена:** 4 из 162 при `--split`, все на поле `test (right)`, и столько же на коммите до цикла 82. В его основном случае (разделители равны) — ноль.
>
> ЧТО МОЖНО И ЧТО НЕЛЬЗЯ
>
> Читать, править документы, писать код, запускать проверки, делать локальные коммиты — можно. `git push` и выпуск — только по его слову, названному тем же заходом. Вопрос о выпуске задаётся лишь когда в заметке нет пустых чекбоксов (правило 160); сейчас их три.
>
> Файлы под З3 — три случая: **В-120** (уборка при измеренном нулевом расхождении), **В-162** (работа, которую он назначил сам) и всё остальное — спрашивается до правки.
>
> РИТМ ПРОГОНОВ
>
> На итерации один файл: `npm test -- <часть имени>`. Перед каждым коммитом — полный набор, линтер, типы. Браузерный шаг — когда правка трогает слой редактора или панель, и один раз последним шагом; **два браузерных прогона подряд в одной команде не влезают в десять минут** — гонять по одному.
>
> **Стенды в семь шагов не входят и падают молча.** `node tools/line_matrix.js` (и с `--split`), `node tools/line_bench.js`, `node tools/undo_bench.js`, `node tools/renumber_bench.js`, `node tools/move_scroll_bench.js`, `node tools/tag_size_probe.js`, `node tools/theme_color_probe.js`.
>
> **Его проверка идёт установленной сборкой, и Obsidian её сама не перечитывает.** Починили — `npm run install:test`, и **скажите ему перезапустить плагин**.
>
> ФОРМА ЗАМЕТКИ ТЕСТОВ
>
> `node tools/testing_shape.js`. Заголовок `## N. Короткое название`, сразу под ним `- [ ] принято` и `- 💬` без пустой строки, четыре свёрнутых коллаута и три раздела `🎯А`, `🛠️Б`, `💥В`, где В — посимвольная копия Б.
>
> ЧТО СДЕЛАНО 82-М ЦИКЛОМ
>
> Отчёты `320у`…`322у`, PRD 10.13.250…10.13.252, исключение к З3 № 148, разбор `docs/dev/BRAND_PANEL_ANALYSIS.md`. Цена места знака была названа неверно (без поля бренда) и спрошена заново до правки — правило 92 сработало вовремя. Уроков новых нет; дважды сработали старые правила. Правило 101 — до выкладки: комментарий «событие дальше не идёт» оказался ложным, Obsidian ловит клавиши на окне с перехватом, и обработчик `Escape` у выбиралки снят. Правило 122 — в шестой раз: heredoc оболочки съел обратный слэш в файле пробы; патчи и пробы писать через Write.
>
> СОСТОЯНИЕ
>
> Семь шагов зелёные: `npm test` 94 из 94, линтер, типы, гейты фазы 5, девять проверок прототипа, сторож формы заметки и браузерный шаг (68 подмен). Обход строки — 0 из 162 при одном известном расхождении. Плагин в `test-vault` пересобран.
> ```
