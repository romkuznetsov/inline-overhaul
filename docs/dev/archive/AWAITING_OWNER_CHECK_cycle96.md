# Приняты его словом в цикле 96 (тесты 1–3 цикла 96)

Тесты 1 и 3 — галочки, тест 2 — «работает» в его `💬`.

### 376у. Ссылка `Inline to note` уходит в ту заметку, которую открывает щелчок — **сделано 2026-09-26, ночь** (его `💬` к тесту 2 цикла 95)

Три заметки `123` в разных папках; щелчок по `[[123]]` у него открывает `222/123.md`, а плагин писал в корневую: цель спрашивалась у `getFirstLinkpathDest` с пустым путём источника. Теперь — от заметки со строкой, как `openLinkText`. В настоящем Obsidian на копии его vault (`tools/obsidian_bench.js link-click`) щелчок открывает корневую `123.md`, и ссылка уходит туда же; его `222` не воспроизвелось — тест спрашивает, откуда он щёлкал. `transform_runtime_tests.js`, подделка резолвера берёт папку источника по `app.js`. PRD 10.13.276.

### 375у. `Jump over neighbor trees` при `Highlight moved lines` — **сделано 2026-09-26, ночь** (его `💬` к тесту 7 цикла 95)

Второе нажатие шло по строке: выделение, которое сам перенос ставит дереву, читалось выделением человека, а с ним перескок выключен. Теперь выделение от начала строки до конца её дерева перескок не выключает (`isWholeTreeSelection`, У-238); выделение человека — выключает. Подделка редактора в `move_line_renumber_tests.js` теперь запоминает выделение. Исключение к З3 № 156, PRD 10.13.276.

### 374у. Дочерний Field рядом с родителем с предусловием — **сделано 2026-09-26, ночь** (его `💬` к тесту 1 цикла 95)

У `test` есть `Prerequisite Field`, и `buildPanelGroupsFromTechOrder` ставил его вторым проходом без детей — `sub` вставал за `People`. Теперь дочерний Field идёт сразу за родителем. Строка не менялась: команда и раньше писала `#test1 #test1-1` рядом. Закреплено `parent_navigator_tests.js` с мутацией; обход `line_matrix` до и после совпал. Исключение к З3 № 156, PRD 10.13.276.

### 373у. `Jump over neighbor trees` в `Move lines` — **сделано 2026-09-25, ночь** (его заказ из сессии записи GIF, тест 7)

Переключатель под `Moving behavior`, путь `navigation.moveLine.jumpNeighborTrees`, умолчание `Off`, виден только при `Whole tree` (его ответ `В-231`). `On` — `Move up` ставит дерево перед всем соседним деревом того же уровня, `Move down` — после него; подпункты соседа остаются у соседа. Выделение, заголовки и выход из родителя — как прежде. Исключение к З3 № 155. **После его приёмки сказать ему: сессия записи GIF ждёт этот контрол.**
