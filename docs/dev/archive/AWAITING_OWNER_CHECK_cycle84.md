# Принято в цикле 84 (2026-09-23, вечер)

Галочками в `test-vault/testing.md`: тест 4 (панель по бренд-буку, знак оставлен над коллаутом) и тест 5 (красная рамка). Отчёты перенесены из `docs/dev/AWAITING_OWNER_CHECK.md` без правки.

### 322у. Панель по бренд-буку: цвета типов, знак, тон — **сделано 2026-09-23, ночь**

**Ваш выбор** (`В-198`): три бесспорных пункта разбора `docs/dev/BRAND_PANEL_ANALYSIS.md`; З6 остаётся. Знак — в начале вкладки General (ваш повторный ответ после пересчёта цены: над вкладками он занял бы около 107 точек на каждой вкладке).

**Цвета типов Field.** Тег — янтарь бренда `#ffb547` с тёмным текстом `#3a2400`, ссылка — сталь `#4a7b9b`; у `Emoji` цвета в бренде нет, он свой. Видно на чипах типа в списке Fields, в заголовке Field и в предпросмотре строки. Контраст посчитан: белый на янтаре 1,8:1 — поэтому у тега свой текст; тёмный на янтаре около 8,5:1, белый на стали около 4,6:1. Проверка сравнивает пары из самого `styles.css` с порогом панели и держит положительный контроль «белый на янтаре проваливает порог».

**Знак.** Готовый файл `docs/brand/io-wordmark-{light,dark}.svg`, как есть, строкой `data:` в `styles.css`; тёмный — по теме Obsidian, на телефоне знак прячется (бренд запрещает сжимать его уже 360 точек). Ширина 360, поле сверху и снизу — высота пузыря `#in`. Стоит **над** вводным коллаутом — это пересекается с вашим правилом К1 «коллаут — первый блок вкладки»; вопрос в тесте.

**Тон текстов.** 22 строки переписаны по `voice.md`: одно восклицание, 12 пассивов и 9 «you will». Смысл и имена контролов прежние. Сторож `panel_voice_tests.ts` держит ноль по всему каталогу панели, у него положительный и отрицательный контроли.

**Чем закреплено.** Подмены: убрать текст у чипа — краснеет; у пузыря предпросмотра — краснеет; белый вместо тёмного у тега — краснеет контраст; испорченная строка знака — браузер говорит «картинка не раскодировалась»; возврат «you will» — сторож называет ключ.

**Как проверить:** тест 4 в заметке.

### 325у. Панель: знак, вкладки, красная рамка — **сделано 2026-09-23, день**

**Ваши замечания к тесту 4 и одно новое.** Поле вокруг знака 31 → 8 точек; мигающая каретка — анимированная версия знака из `docs/brand/`, гаснет при выключенной анимации. `Transform` перед `Visual`: порядок стоял вторым экземпляром в генераторе схемы, теперь берётся из прототипа; тумблеры модулей на `General` — так же (ваш ответ «да, как вкладки»). Красная рамка обязательного поля проигрывала рамке фокуса Obsidian (`input[type='text']:focus`, 0-2-1) — класс утроен. **Чем закреплено:** пины порядка вкладок в `settings_layer_tests.ts`, браузерная проверка знака (раскодировалась, 360×47). **Как проверить:** тесты 4 и 5. Разбор — PRD 10.13.255.

