# Приняты его галочками в цикле 93 (тесты 2–6 цикла 92)

### 358у. PRD разделён — **сделано 2026-09-25, ночь** (его пункт из «Новое пишите сюда»)

Было 19 810 строк и 1,59 млн знаков одним файлом; в ядре `docs/dev/PRD_Settings_Overhaul_v1.md` осталось 1 858 строк и 0,25 млн знаков (−85 %): разделы 0–10 без записей 10.13.N, 12–14, Приложение A. В `docs/dev/prd/` без правок текста уехали записи 10.13.N тремя файлами по номерам (новая сверху), история исключений к З3, разделы 11 и 15 и сгенерированное Приложение B. Каждая строка прежнего файла стоит ровно в одном из новых — сверено мультимножеством строк скриптом разделения. Генератор, сверка прототипа (контроль: порча описи краснеет), хук-сторож и сторож З3 смотрят в новые файлы. Попутно найдено: исключения 136…151 стояли внутри незакрытого блока кода записи 10.13.121. Откат — коммит `0b29091`.

### 357у. `Parent is Navigator` у тега и ссылки — **сделано 2026-09-25, ночь** (его заказ к тесту 3)

Ряд под `Child Field`, недоступен при `Hide`; при нём ряд `Parent Value` не показывается. Навигатор сужает детей в tagWheel и на строку не пишется ни одной дорогой, на строке с ребёнком поле навигатора пустое, а выбор навигатора очищает ребёнка (его ответ `В-223` тем же циклом), команда ребёнка листает всех детей (`В-220`), команда родителя навигаторы пропускает и говорит вслух, если листать нечего; при `Nested` пишется только ребёнок (`В-219`). Родитель, стоявший на строке раньше, остаётся (правило 107). Навигатор ссылки — ссылка, его поправка к нюансу 2. Не сделано: навигатор в custom block и нюанс 10 (`Prerequisite Field`, `Smart Rules`, YAML). Закреплено `parent_navigator_tests.js` и половиной «навигатор» обхода `line_matrix`; исключение к З3 № 151, откат — коммит `0199cc5`.

### 356у. Дочерний Field у ссылки — **сделано 2026-09-25, ночь** (его заказ к тесту 3)

Панель заводила дочерний Field ссылки и прежде, а движки его не видели: не было ни команд `-sub`, ни места в tagWheel. Теперь разрешения переносятся на поле одинаково у тега и ссылки, ребёнок ссылки пишется в Block родителя обеими дорогами. На его конфиге у `Project_sub` положение встанет `Hide`, пока он не выберет другое: новый дочерний Field выключен, как у тега. Обход его конфига: расхождений 0 до и после, половина команд дочерних полей 3 → 4. Откат — коммит `0199cc5`.

### 355у. Добавление Value больше не перебрасывает к первому Field — **сделано 2026-09-25, ночь** (его дефект из «Новое пишите сюда»)

С цикла 90 списки `Left/Right Block active Field` стоят на вкладке редактора Fields, и любая запись в `pkm.fields` пересобирала вкладку — редактор рисовался заново с пустым выбором. Пересборка теперь — только когда список и правда сменился, выбор живёт у платформы. Заодно перестала пересобираться вкладка на каждой букве в полях редактора. Закреплено `fields_editor_selection_tests.ts`, обе половины мутацией; откат — коммит `8f7ff8f`.

### 354у. Постановка «Parent is Navigator» — **постановка 2026-09-24, ночь, исполнена в цикле 92** (`357у`) (его пункт из «Новое пишите сюда»)

PRD 10.13.269: что уже есть (дочерний Field тега, `Always` + `Child only`, `On Alt` без родителя), десять нюансов его формулировки с моими умолчаниями, адреса правки и мера. Его ответы `В-218`…`В-220`: сперва Tag, при `Nested` только ребёнок, команды листают всех детей подряд. Проверяется чтением постановки; кода нет.

### 353у. Короткие подписи во всех выпадающих списках — **сделано 2026-09-24, ночь** (его пункт из «Новое пишите сюда»)

Около 75 подписей в 45 контролах схемы и своих блоках: служебные слова сняты, пары выровнены, термины устоявшиеся (PRD 10.13.270). Значения в конфиге прежние (З1). Заменены и его собственные имена — `On Alt`, `Always`, `Default` / `Custom` / `Custom + default`. Чем откатывается: `git revert af2af85`.
