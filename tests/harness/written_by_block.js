"use strict";

/**
 * Пути конфига, которые спрашивает предикат видимости и не пишет ни одна
 * строка панели. Каждый пишет свой блок, и он назван рядом.
 *
 * Список один на гейт прототипа (`tests/prototype/gates.js`) и на проверку
 * слоя настроек (`settings_layer_tests.ts`): два списка разошлись бы молча
 * (У-32).
 */
module.exports = [
  /* Список custom block пишет редактор Fields (PRD 10.13.260); строке
     `Switch custom blocks on Tab` без блока переключать нечего. */
  "pkm.fields.order.custom",
];
