# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Что здесь лежит.** Отчёты о работе, которую вы ещё не подтвердили. Нажимать
в большинстве нечего — это уборка кода и починки, у которых нет предмета на
экране; подтверждаются они **словом**, а не галочкой. Сколько их сегодня,
скажет прогон, а не эта строка (У-145):
`grep -c "^### [0-9]*у." docs/dev/AWAITING_OWNER_CHECK.md`.

**Свежее — сессия 65 (2026-09-19): пройдена вся очередь ревизии.** Восемь работ,
отчёты `233у`…`240у`. Нажать есть что у трёх: настройки, изменённые снаружи
(`C1`), память открытой вкладки (`C2`) и вопрос к вашему глазу про отрисовку
(`C3`). Остальные пять — измерения, уборка и объяснения: у них нет предмета на
экране, и подтверждаются они словом.

**Живое, что ждёт вас:** `C1`, `C2` и `C3` — то, что можно нажать; `215у`
(подсветка прыжка), `F3` и `F4` — документы, по которым проверять нечем, кроме
чтения, и `П4` — работа плагина в вашем рабочем vault после релиза `0.3.1`.

**Шапка не пересказывает сессии по абзацу** (У-31, У-145): кто когда что закрыл
— в самих отчётах и в архиве `docs/dev/archive/AWAITING_OWNER_CHECK_*.md`.

### 273у. Значение поля-ссылки можно показать своим текстом — **сделано 2026-09-20**

**Ваш заказ (пункт 14):** «добавить для field=link в таблицы io-values values колонку io-values-col-show. У него две опции — default и custom. Custom работает как для fields=tag io-values-col-show (т.е. подмена названия на значение пользователя (эмодзи и т.д.)). При нажатии на этот замененный элемент должна открываться соответствующая wikilink».

**Что сделано.** У таблицы `Values` поля типа `link` появилась колонка `Show` с двумя положениями. При `custom` рядом открывается поле текста, и значение `[[Project A]]` рисуется в строке этим текстом; нажатие на него открывает ту заметку, на которую ссылка ведёт, — с `Ctrl` или средней кнопкой в новой вкладке. При `default` всё как было: ссылку рисует Obsidian.

**Почему этого не было раньше и что это стоило.** В слое оформления стояло правило: ссылку своим узлом не заменяем, потому что вместе с `[[имя]]` у человека пропадут клик, наведение и перетаскивание, — а вернуть их нечем. Вы заказали замену и назвали то, что должно вернуться, — клик. Он и возвращён, тем же вызовом платформы, каким открывает ссылку сама Obsidian. **Цену называю вслух:** у заменённого значения **нет предпросмотра по наведению и его нельзя перетащить** — это повадки ссылки Obsidian, и плагин их не воспроизводит. У значения при `default` они все на месте.

**Чем закреплено.** Ключ вида у ссылки один на панель и на заметку, и шов проверяется с обеих сторон (`link_shown_tests.ts`, девять утверждений): ключ, которым панель пишет, находит слой оформления; вид одной ссылки не достаётся другой; у тега скобочного ключа не появляется. Клик проверен тем же вызовом, каким его делает Obsidian, и с модификатором. В браузерном шаге на странице теперь есть строка со ссылкой при `custom`: узел проверяется в настоящем CodeMirror — там, где на один и тот же отрезок строки претендуют и Obsidian, и мы. Две новые подмены (`link-shown-never`, `link-shown-always`) краснеют каждая на своём утверждении.

**Как проверить, за минуту:** `Tags & PKM → Fields`, выберите поле типа `link`, в таблице `Values` поставьте `Show = custom` и впишите эмодзи. Откройте заметку со строкой, где это значение стоит, и нажмите на эмодзи.

### 272у. Вид репозитория: рабочие документы уехали в `docs/dev` — **сделано 2026-09-20**

**Ваше замечание (тест 4):** «много лишнего в `docs` — там файлы, которые не нужны для пользователя».

**Что сделано.** Шестнадцать документов и весь архив переехали в `docs/dev/`. В `docs/` осталось четыре записи, и у каждой есть читатель снаружи: `showcase.md` и `media/` (на них ведёт `README.md`), `command_ids_v1_v2.md` (на него ведут `README.md`, руководство и уведомление самого плагина о переименовании команд) и `prototype/` — он нормативен, и на него ссылается половина кода.

**Чем закреплено.** Ссылки правлены сплошным обходом: 81 файл, 384 адреса. После переезда битых адресов **ноль** — проверено обходом по всем markdown и коду; три оставшихся упоминания относятся к файлам, которых не было и до переезда (они лежат в архиве, текст исторический). Правило держит сторож в `entry_doc_tests.js`: в `docs/` разрешён закрытый список имён, у каждого названа причина, и лишний файл роняет прогон.

**Как проверить, за минуту:** откройте `docs` на GitHub.

### 271у. Перенос строк возвращает нумерацию списка — **сделано 2026-09-20**

**Ваше замечание (пункт 11):** «при move-lines up\down нумерованного списка — при перемещении вверх и вниз нумерация не восстанавливается — пример: я двигал sub-lines, и у меня нумерация под п.10 у дочерней строки начинается с .3 (а должна с 1)».

**Причина оказалась не там, где ожидалась.** Нумерацию списков Obsidian правит сама — фильтром транзакций, который смотрит затронутые строки после каждого изменения документа. У фильтра есть выход в первом же условии: транзакции, помеченные `userEvent: "set"`, он пропускает. А перенос строк записывался через `setValue`, и **именно эту пометку** `setValue` и отправляет. То есть плагин говорил платформе «это не правка человека», и нумерация оставалась прежней. Прочитано в `app.js` вашей версии Obsidian, а не выведено рассуждением.

**Что сделано.** Перенос пишется правкой окна изменившихся строк вместо замены всего документа. Окно минимальное: фильтр нумерует от затронутых строк, и замена документа целиком перенумеровала бы все списки заметки разом. Тем же вызовом и тем же окном перенос строк **таблицы** писался с самого начала — так что две половины одной команды сошлись.

**Чем закреплено.** Стенд сверяет старую и новую версию на восьми формах заметки, на каждой строке, в обе стороны, при трёх наборах настроек, с выделением и без: **468 пар, расхождений 0** — ни в тексте, ни в курсоре, ни в выделении. У стенда свой контроль: окно на строку короче даёт 273 расхождения из тех же 468. Проверка `move_line_renumber_tests.js` на коде до правки краснеет на шести случаях из шести.

**Это правка файла под запретом З3** (исключение № 135, `navigation_runtime.js`), и сделана она без вопроса: починку назначили вы. **Откатывается одной строкой** — возвратом `editor.setValue(newDoc)`.

**Как проверить, за минуту:** нумерованный список с дочерними строками, курсор на дочерней, `Move line up`/`Move line down` через соседнего родителя.

### 270у. Выпадающие списки говорят, какое значение стандартное — **сделано 2026-09-20**

**Ваш заказ (пункт 13):** «в настройках плагина все дефолтные значения контролов должны в названиях иметь приписку `_default` (или лаконичнее) — чтобы пользователь понимал, какой вариант контрола является стандартным».

**Что сделано.** У того значения, которое стоит умолчанием, подпись теперь кончается на `(default)` — во всех выпадающих списках строк настроек. Форма выбрана лаконичнее вашей: приписка в скобках читается как пояснение, а не как часть значения. **Приписка — строка каталога**, то есть переводится вместе с панелью.

**Где её нет и почему.** Список из одной строки не помечается: вариантов там нет, а этой одной строкой бывает сообщение о пустоте — у `Default template` без назначенной папки там стоит `Set a Templates folder first`, и «(default)» на нём читался бы насмешкой. Нашла этот случай проверка, а не чтение.

**Чего это пока не покрывает.** Выпадающие списки **внутри своих блоков** — редактор Fields (`Child Field`, `Behavior`, `Parent Value`), Smart Rules, `Color your Tags`. У них умолчание не объявлено отдельно: оно живёт запасным ответом внутри модели, и пометить его значило бы завести второе объявление того же правила. Вопрос об этом — в конце сессии.

**Чем закреплено.** Обход **всех** выпадающих списков панели в `settings_layer_tests.ts`: помечено ровно то значение, которое стоит умолчанием в схеме, и ровно одно. Список, заведённый завтра, попадёт под него сам.

**Как проверить, за минуту:** `Visual → Inline appearance → Stripe direction` — три значения, и `Both` подписано `Both (default)`.

### 269у. `to hotkeys` в каждом заголовке справочника и серый `not set` — **сделано 2026-09-20**

**Ваш заказ (пункты 12.3 и 12.4):** «добавить кнопку в хедеры всех уровней, чтобы при нажатии на неё открывались хоткеи Obsidian с фильтром на все команды, входящие в этот хедер… назови `to hotkeys` (визуально должна отличаться от контролов обычных команд — например, цветом, для каждого хедера должен отличаться)» и «`not set` — сделать серым шрифтом, чтобы сразу было видно, что хоткей не назначен».

**Что сделано.** Кнопка `to hotkeys` стоит в каждом заголовке справочника — у области, у части области и у каждого Field, — и уводит на экран `Hotkeys` Obsidian с уже вписанным запросом. Цвет у неё акцентный (кнопка хоткея в строке серая), и насыщенность своя у каждого уровня, как у заливки самих заголовков.

**Чего язык поиска Obsidian не умеет, и что с этим сделано.** Правило отбора прочитано в `app.js`: строка запроса делится по пробелам, и команда остаётся, если **каждое** слово есть в её имени (или в идентификаторе). Ни «или», ни списка команд этот язык не выражает — то есть произвольный набор команд запросом не задаётся в принципе. Поэтому запрос **считается** по вашему настоящему списку команд: берётся общее слово всех команд заголовка вместе с именем плагина, и из годных выбирается тот, под который попадает меньше чужих команд. Гарантируется охват: команды заголовка видны все. Там, где общего слова нет (например, у области `Navigation`), остаётся имя плагина — это всё ещё сужение (ваши команды против всех в Obsidian), и то, чем отобрано, видно в самом поле поиска.

**`not set` был бледным и раньше** — цвет темы `--text-faint` стоял у него с самого начала. Значит дело было не в цвете: пустая клетка выглядела такой же кнопкой, как назначенный хоткей. Теперь у неё **нет заливки**, рамка пунктиром и текст курсивом — пустое место под клавишу, а не кнопка со значением.

**Чем закреплено.** В `command_reference_tests.ts` четыре новые проверки: кнопка есть у каждого заголовка каждого уровня; её запрос показывает **все** команды своего заголовка (считается тем же правилом, каким считает Obsidian); запрос сужен там, где сужать есть чем; без приватного API кнопки неактивны. Вид сверяется в обеих отрисовках — панель и прототип, — и у уровней три ступени насыщенности, как у заголовков.

**Как проверить, за минуту:** `Keyboard → Commands & Hotkeys`, нажмите `to hotkeys` у своего Field.

### 268у. Список изменений: сводка и знак рода у каждого пункта — **сделано 2026-09-20**

**Ваше замечание (тест 3):** «не могу сказать, что changelog мне нравится… в начале не хватает краткого саммари (коллаута) с кратким описанием (1-2 предложения, что глобально изменилось в этой версии относительно прошлого релиза)… не хватает эмодзи в тексте (в начале пунктов), чтобы дать яркое представление о том, что это (новая функция, исправление бага, улучшение визуала и т.д.)».

**Что сделано.** У каждого выпуска сверху стоит сводка коллаутом — одно-два предложения о том, что в нём изменилось в целом. Её видно и в окне «что изменилось», и на GitHub: маркер `[!NOTE]` рисуют оба. У каждого пункта в начале знак рода, и знаков ровно пять: ✨ новая возможность, 🐛 починка, 🎨 видимое изменение, 🚀 скорость, 🔧 внутреннее. Знаков нарочно мало: тот, что встречается один раз, ничего не сообщает.

**Чем закреплено.** Обход `CHANGELOG.md` в `release_notes_tests.js` требует у каждого выпуска сводку, а у каждого пункта — знак из этого списка (и по-прежнему одну строку и не длиннее четырёхсот знаков). У обхода положительный контроль на строке-примере: он краснеет на каждой из пяти бед, в том числе на пункте **прежней** формы — без знака.

**Как проверить, за минуту:** `General → Help → Changelog → Open`. Окно «что изменилось» вы увидите при следующем обновлении плагина.

### 266у. Заметка тестов: форма по вашим шести пометкам — **сделано 2026-09-20**

**Ваши слова, по которым это сделано.** Сначала — «я не понимаю что мне делать в файле приемки — улучши его», потом — «делай заметку для тестов… настройки плагина, что нужно сделать, исходная строка (эталон), ожидаемый результат (эталон), проверка (исходная строка, но на которой пользователь должен совершить действие)… строки не должны начинаться с кавычек». Первую мою версию вы разобрали шестью пометками прямо внутри заметки, и нынешняя форма собрана по ним.

**Что вы написали и что с этим стало.**

| Ваша пометка | Что теперь в заметке |
|---|---|
| «у задачи наверху должен быть чекбокс с приёмкой или написать комментарий, не выделяя чекбокс… чтобы я сразу идентифицировал статус проверки» | галочка и строка `💬` — **первые две строки** под заголовком теста, одним уровнем, между ними ничего |
| «добавь callout (свернутый) с описанием этой задачи (почему она возникла, или из какой задачи — со ссылкой)» | свёрнутый коллаут `Откуда эта задача`: почему тест появился и номер отчёта с подробным разбором |
| «таблицы используй только для настройки и действие… под субхедером, чтобы можно было свернуть… структурируй по вкладкам» | подзаголовок `Настройки и действие`, сворачивается целиком; внутри таблица `Вкладка / Контрол / Значение` по строке на контрол и действие нумерованными шагами |
| «плагин в таблице не работает» | в таблице остались только настройки и действие; всё, на чём вы нажимаете команду, стоит обычной строкой заметки |
| «ты сделал текст в кавычках — надо без них… строка без кавычек, чтобы я сразу видел» | эталон и ваша строка написаны без обратных кавычек вовсе: что видите, то и должно стоять в заметке |
| «тут должен быть коллаут с логом — он должен обновляться после каждой проходки» | свёрнутый коллаут `Журнал` у каждого теста, по строке на заход с датой |

**Чем это закреплено.** Форму держит стенд `node tools/testing_shape.js`, и каждое ваше требование — отдельная его проверка. У стенда положительный контроль внутри (заведомо плохой тест, девять бед) и девять отрицательных: я ломаю в настоящей заметке по одному вашему требованию за раз, и краснеет каждое. Так форма не сползёт обратно молча — а сползала она уже дважды.

**Ваши пометки сняты целиком** в `docs/dev/archive/testing_2026-09-20_his_comments.md`, вместе со скриншотом: адрес замечания — ваш текст, и переписывать его я не стал.

**Как проверить, за минуту:** откройте `testing.md` и посмотрите на любой тест сверху вниз.

### 265у. «Что изменилось» в автокопии: имена контролов и вложенность — **сделано 2026-09-20**

**Ваше замечание (`S8`):** «смотреть setting `pkm.fields.tags.fields` — это непонятно для пользователя, ты должен написать название контрола (как в настройках плагина)… Также группируй элементы, если настройки находятся в дочерних контролах».

**Взял вашу же пару заметок** — `Settings 2026-09-19 19-58-50_autosave` и `Settings 2026-09-19 20-13-12_autosave` — и прогнал их через новый разбор. Было двенадцать строк, из которых что-то говорили две. Стало так:

```
- Field «test2» moved from the Left Block to the Right Block
- Field «Cat_sub»
	- «Child Field»: After parent → Show always
	- «Parent Value»: Leave the line alone → Add the parent Value
	- Value «#med» added
	- Value «#old» added
- Field «Cat»
	- Value «#old»
		- «Text color»: not set → #d23232
	- Value «#med»
		- «Text color»: not set → #1dcd41
- «Left Block text size» (Visual → Inline appearance): 50 → 80
- «Right Block text size» (Visual → Inline appearance): 50 → 80
```

**Что изменилось в разборе.** Ветка определений Fields больше не описывается длиной списка: сказано, у какого Field появилось или ушло значение. Контролы строки Value названы так, как они подписаны в панели, и стоят **внутри** своего значения, а значение — внутри своего Field. Имена берутся из самой панели, а не из отдельного списка: переименуете контрол — изменится и текст в заметке.

**Заодно убрал шум.** Панель пишет строку значения целиком, со всеми её полями сразу — заведя один цвет, вы получали четыре строки, три из которых говорили `not set → empty`. Правка, у которой обе стороны — умолчание, в отчёт больше не идёт. И `Child Field` теперь сказан вашими словами (`After parent → Show always`), а не `off → on`: на экране вы видите выбор из трёх, а не тумблер.

**Как проверить, за две минуты:** строка листа `S8`.

**И заодно ваш второй вопрос под `S5`** — «проверь, что в `data.json` и в генерируемых файлах бэкапа нет других мёртвых веток». Померено, и не на глаз: `node tools/dead_keys.js` на вашем `data.json` даёт **0 из 356**, на автокопии `Settings 2026-09-19 20-13-12_autosave` — 0 из 356, на ручной копии `Settings 2026-09-19 12-32-29` — тоже 0 из 356.

**У меры есть положительный контроль, и первый вариант его не прошёл.** Я подставил в файл ту самую снятую ветку конфиг-заметки — и мера промолчала: нормализация снимает её раньше, чем доходит до счёта, то есть мера отвечает на вопрос «что останется в файле», а не «что в нём лежит сейчас». Второй контроль — два листа, которых нормализация не трогает, — она назвала оба поимённо. Значит ноль выше настоящий, а не от того, что мерить было нечем.

### 263у. Дочернее поле может работать без родителя — **сделано 2026-09-19**

**Ваш заказ:** контрол `Sub-field works without parent` у каждого Field, и вторым контролом — дописывать родителя или нет.

**Что стало в настройках.** Ряд `Child Field` у поля, у которого есть дочернее, теперь выпадающий список из трёх положений: `Show always`, `After parent` (умолчание — так поле работало всегда) и `Hide`. При `Show always` под ним появляется второй ряд, `Parent Value`: `Leave the line alone` (умолчание, как вы просили) или `Add the parent Value`. Оба — у каждого Field свои.

**Что стало в работе.** При `Show always` панель показывает дочернее поле и на строке, где у родителя значения нет, а команды `<Field>_sub next/previous` листают **все** его значения подряд, в порядке настроек. При `Add the parent Value` команда и панель заодно ставят то значение родителя, которому долистанное значение принадлежит.

**Одного контрола из вашего заказа я не делал, и вот почему.** Вы просили третий контрол — про края списка — и просили уточнить, что это и зачем. Уточнение вышло короче правки: **револьвер, который вы описали (`last-1 < last < 0 > first > second`), уже работает именно так** — у каждого поля и в обе стороны. Проверил командой на ваших настройках: `previous` с пустой строки даёт последнее значение, потом предыдущие, потом пустую, потом снова последнее. Пустое место — часть круга. Вы согласились: «про края списка контрол не нужен — он должен работать так всегда».

**Чем это измерено.** На копии ваших настроек: при умолчании `Cat_sub next` на строке `- текст` по-прежнему ничего не делает и говорит `Cat_sub waits for Category`; при `Show always` выходит `- #new :: текст`; с дописыванием родителя — `- #work #new :: текст`. Обход строки на ваших настройках остался нулевым.

**Как проверить, за минуту:** строка листа `S11`.

### 262у. Панель больше не дописывает значение дочернего Field — **сделано 2026-09-19**

**Ваше слово:** «нет, не надо дописывать и исключи это дописывание из tagwheel».

**Сделано, и вот чем это измерено.** Обход строки на ваших нынешних настройках показывал шесть расхождений из 234 — все одной формы, и все появились в тот день, когда вы переставили `Cat` из правого Block в левый. Стало **0 из 234**; при разведённых разделителях было десять, стало четыре — остаток другого рода, он записан находкой и ждёт своей очереди.

**Причин было две.** Первая: сторону дочернего поля спрашивали его собственным именем в Order, а дочернее поле в этих списках не стоит вовсе — и ответом было умолчание «правый Block». Вторая: список значений дочернего поля выходил пустым, когда родитель не выбран, и строка не чистилась от прежнего значения перед записью — отсюда второе такое же значение.

**Правку я сузил, и ширину назвала покрасневшая проверка.** Первая версия отдавала сторону родителю всегда — и сломала ваше прежнее решение о поле, которое панель не показывает. Теперь родитель отвечает только там, где иначе ответом было бы умолчание.

**Вторую половину вашего слова я не делал, а спросил.** «Скроллинг по всем sub-values, когда родитель не выбран» отменяет ваше же прежнее решение: сегодня команда дочернего поля говорит `Cat_sub waits for Category: set it on this line first`. Это ровно тот случай, про который я в этой же сессии записал правило: новое слово, переворачивающее прежнее без объяснения, спрашиваю до правки. Вопрос задан в конце сессии.

**Как проверить, за минуту:** строка листа `S10`.

### 261у. Мёртвые ветки в вашем файле — и настройка, не доезжавшая до Transform — **сделано 2026-09-19**

**Ваш заказ:** «я хочу, чтобы ты проверил, что в data.json и в генерируемых файлах бэкапа нет других мёртвых веток. В целом ты ранее выполнял аудит кода, я ожидал, что после его завершения и выполнения всех выявленных проблем подобных ситуаций не будет».

**Сначала честный ответ про ревизию.** Она смотрела на **код**: мёртвые экспорты, тихие отказы, копии правил, объявленное и незваное. «Ключ, который лежит в вашем файле и не читается ничем» — другая сторона, и её не мерил никто. То есть моё «таких ситуаций не будет» опиралось на меру, которой не было. Теперь мера есть, и она в репозитории: `node tools/dead_keys.js`.

**Что нашлось в вашем файле: 35 листьев без читателя.** Три ключа конфиг-заметки (снята вашим решением 2026-09-03), семь ключей состояния старой панели (подвкладки, которых у новой панели нет), четыре тумблера вида редактора Fields, снятые ещё раньше, тумблер «новая панель», которым больше нечего переключать, и три листа приставок формы версии 1. Все сняты вместе с их объявлениями: теперь плагин их не создаёт и не поддерживает. **Стало 0 из 346.**

**Двадцать один из тридцати пяти оказался ложной находкой** — их читают свойством, а не путём (карта чекбоксов по значению, цели приставки). Они названы в самом обходе поимённо, с адресом чтения: пока список виден, его можно оспорить.

**И то, ради чего стоило искать.** Оказалось, что **Transform читал старую форму** правил приставок: три настройки — `Decide by`, `Field order source` и `Parent or child wins` — панель пишет в одно место, а Transform читал другое, оставшееся с прошлой версии. То есть вы меняли их в панели, все движки видели новое значение, а Transform продолжал работать по старому. Измерено на вашем файле и починено; условие «нет ветки — нет разбора приставки» я нарочно не трогал, чтобы не включить разбор там, где его сегодня нет.

**Как проверить, за минуту:** строка листа `S9`.

### 260у. «Что изменилось» — словами, а не путями — **сделано 2026-09-19**

**Ваше замечание:** «в заметке autosave текст в `# What changed` очень технический и не понятен для пользователя (`pkm.fields.order.left: list of 4 → list of 5`). Должно быть понятно — указывай название настроек и что конкретно произошло. В этом примере я переместил field `Cat` из right block в left block».

**Ваш пример теперь читается так:** `Field «Cat» moved from the Right Block to the Left Block`. Тем же способом называются добавление поля, снятие и смена порядка внутри Block; подпись поля берётся та, которую вы видите в панели.

**Обычная настройка называется именем строки и местом:** `«Opacity of the Left Block» (Visual → Inline appearance): 100 → 40`. Имена берутся из самой панели — второго списка имён нет, иначе он разошёлся бы с ней при первом же переименовании. Значения тоже словами: тумблер — `on`/`off`, пустое — `empty`, список — числом.

**Если имени у пути нет** (внутренний ключ), строка честно говорит `setting \`путь\``, а не выдаёт путь за название.

**Как проверить, за две минуты:** строка листа `S8`.

### 259у. Кегль и уровень написанного в Block — **сделано 2026-09-19**

**Ваше замечание со скриншотами:** «при 100 текст в left/right block должен быть таким же как в text block (находиться на том же уровне)».

**Вы увидели верно, и причин было две.** Первая: кегль пузыря считался от четырнадцати точек, записанных в коде, а текст у Obsidian шестнадцать — и задаётся он темой и вашей настройкой размера. То есть при 100 % пузырь рисовался **не тем** кеглем, которым написана строка рядом. Теперь кегль берётся у самой строки: у заголовка он свой, и текст в Block следует ему.

**Вторая: выравнивание.** Оно стояло «по середине», а «середина» у браузера считается от высоты буквы `x`, а не от написанного — промах 1,58 точки, обмерено браузером на 26 парах. Теперь подъём вычисляется: половина разницы кеглей строки и значения. При 100 % это ноль — значение стоит ровно на уровне текста; при уменьшенном кегле подъём ровно тот, которого не хватало, и ваше прежнее условие (значение по середине полосы) осталось выполненным.

**Мера врала трижды, и это стоило трёх заходов.** Она сравнивала разные рода ящиков, брала текст, который сам перенёсся на другой ряд, и сравнивала с той строкой, где стоит эмодзи — а его глиф выше буквенного. После трёх поправок промах **0 на 30 парах**.

**Цена названа вслух:** одна из прежних подмен перестала различать своё и чужое — значения теперь стоят там же, где текст строки, и два способа посчитать вертикаль сошлись. Подмена снята с объяснением, а вместо неё заведены два утверждения с порогами, проверенными возвратом прежнего кода.

**Как проверить, за минуту:** строка листа `S7`.

### 258у. Автокопия: три ваших замечания и одна находка — **сделано 2026-09-19**

**«Autosave слишком чувствительный».** Вы правы, и причина оказалась в одной строке отбора: сравнение брало «всё, кроме чужих вкладок», а ветка `ui` — та, в которой лежит открытая вкладка панели, — не принадлежит ни одной вкладке и проходила насквозь. Теперь сравниваются ровно те ветки, которые показаны вкладками. Переключение вкладки копию не снимает; правка любого контрола — снимает. Проверены обе половины: «не снимает» без второй половины было бы верно и у кода, который не снимает никогда.

**«Хочу уведомление».** Сделано: при снятой копии всплывает `Settings autosaved to …` с путём заметки. Если копия не понадобилась — молчит.

**«Текст „Write anything here…“ коллаутом».** Сделано, разметкой Obsidian. Разбор заметки это не задело: блок настроек лежит ниже, и обе стороны проверены.

**Про `data.json`, в котором вы не увидели своей правки — это не дефект, и вот чем это измерено.** Вы правили `pkm.fields.taxonomy.tagWheelConfig.wikilinks.Project.bySection.Project.defaults`. Плагин ваш файл **принял**: правка в нём осталась, её ничто не затирало. Но эту ветку не читает никто — во всём коде слово `taxonomy` встречается в двух файлах (карта переезда конфига и нормализация), а `bySection` — ни в одном. Значения Field `Project`, которые панель вам показала (`[[test1]]`, `[[test2]]`), лежат в другом месте: `pkm.fields.links.fields[].values[].token`. То есть панель показала правду, а правка ушла в ветку-призрак.

**Эту ветку я снял.** Это разобранный кеш конфиг-заметки TagWheel, которую вы сняли своим решением 2026-09-03: 589 знаков, ни читателя, ни контрола, и она успела ввести вас в заблуждение. Уходит она на первой же записи настроек; если что-то из неё понадобится, она лежит в любой копии настроек, включая автокопии в вашей папке. **И один раз после обновления вы увидите лишнюю автокопию** — это та самая запись, которой ветка уходит: настройки и правда изменились.

**Как проверить, за две минуты:** строка листа `S6`.

### 257у. Порядок в `Line view` — ваш прежний — **сделано 2026-09-19**

**Ваше замечание:** «я сказал, что нужно сделать в заданной мной очередности — `tags-text-size-left`, `tags-text-size-right` должны быть над `Color the Block with Stripe`».

**Вернул.** Порядок такой: прозрачность левого Block, прозрачность правого, кегль левого, кегль правого, полоса со всеми своими строками. Это ровно та последовательность, которую вы назвали пунктом 3.

**Я прочёл ваше прежнее слово наоборот.** «Контрол tags-block-fill должен быть над контролом tags-text-size-left» я понял как «выше в списке» — и переставил. Фраза читалась двояко, и надо было спросить: цена вопроса — одна строка интервью, цена ошибки — ваш вечер и это письмо. Записал правилом: когда ваше новое слово **переворачивает** прежнее, а причина переворота не названа, спрашиваю до правки.

**Как проверить, за минуту:** строка листа `S3` — она снова ждёт вашей галочки.

### 256у. `Autosave` — копия настроек при открытии плагина — **сделано 2026-09-19**

**Ваш заказ и ваш ответ.** Заказ: «для безопасности нужно добавить возможность откатa data.json на предыдущий вариант (до изменения)… контрол в settings-backup `Autosave` = on/off… бэкап должен быть максимальный… под хедером комментария (над json block) должны добавляться детали — что изменилось… в названии заметки должна быть приставка `_autosave`». Ответ на вопрос того же дня: снимать **при открытии плагина, если файл изменился** — то есть второй из вариантов, которые вы набросали сами.

**Сначала поправка к цене, которую я назвал в прошлый раз.** Я писал, что копия на каждую правку — это «сотни файлов при одной протяжке слайдера». Это неверно: запись отложена на четверть секунды, и таймер перезапускается на каждом движении ползунка, то есть протяжка доходит до диска **одной** записью. Настоящие числа на вашем файле: полная копия — 23 223 байта и 926 строк; двадцать правок за вечер дали бы двадцать заметок и 454 КБ; выбранный вами момент даёт **одну заметку на сессию**.

**Что сделано.** Тумблер `Autosave` в `Advanced → Backup`, выключен по умолчанию — заметки в ваш vault плагин без спроса не пишет. Включите: при каждом запуске Obsidian плагин сравнит ваш `data.json` с последней автокопией и, если они разошлись, напишет новую. Копия максимальная: все семь вкладок и хоткеи плагина. В имени файла — `_autosave`; это своя приставка, не `Autogenerated`, которой помечены копии перед восстановлением и сбросом.

**Раздел `What changed`** стоит там, где вы сказали: под разделом для ваших пометок и над блоком настроек. В нём — список изменившихся настроек **путями** (`visual.tags.blockFill.direction: both → left`), не больше двадцати строк, дальше «и ещё N». Пути, а не имена контролов: имя живёт в схеме панели, а копия снимается при загрузке, когда панель ещё не открыта; путь вы видите в `Advanced → Options IDs`.

**Копий держится десять новых**, старые уходят в корзину vault; копии, снятые вами руками, не трогаются вовсе. Числа вы не просили — если захотите другое, это одна строка.

**Чего эта копия не делает.** Она не заменяет отмену: откатить одну последнюю правку внутри панели — это команда `Undo last settings change`. Сливать два вопроса в один я не стал: получилась бы та самая простыня из заметок, про которую вы писали в соседнем замечании.

**Чем закреплено:** девять проверок на настоящих настройках и настоящей заметке — подделан только vault; три подмены (тумблер не спрашивается, сравнение идёт по полному конфигу, предел снят) краснеют каждая на своём утверждении. Плюс проверка на сам шов к vault: его зовёт только загрузка плагина, и без неё он остался бы непроверенным.

**Как проверить, за две минуты:** строка листа `S5`.

### 255у. Окно «что изменилось»: нумерация, одна строка, пропущенные выпуски и ссылка — **сделано 2026-09-19**

**Ваше замечание под `N4`:** «стало лучше, но всё равно плохо — текст с разрывами на предложениях; префикс исправлений должен быть нумерацией (чтобы было видно сколько пунктов изменено); текста много — нужны буллиты с понятным, но лаконичным описанием… этот changelog должен дублировать changelog.md в репозитории… changelog в obsidian показывает только изменения последней версии (или лучше N версий с последнего обновления плагина)… также в плагине должна быть ссылка на changelog.md в репозитории github… возможно я изобретаю велосипед — тогда подскажи более оптимальный и рациональный вариант».

**Про разрывы: причина не в окне, а в источнике, и она измерена.** Obsidian показывает одиночный перевод строки переносом — это прочитано в его `app.js`, а не выведено: настройка `Strict line breaks` по умолчанию выключена, и отрисовка при этом включает переносы. `CHANGELOG.md` был свёрстан по восемьдесят знаков, как любой файл репозитория, и потому приезжал к вам лестницей. Поэтому правка не в окне: **пункт теперь пишется одной строкой**, и это же ваше старое правило про документы, которые вы читаете.

**Нумерация и лаконичность сделаны в самом файле**, а не в окне: дом текста один, и второй рассказ о том же выпуске разошёлся бы с первым в первый же день. Файл переписан целиком — **106 пунктов**, у каждого свой номер, одна строка и предел длины. Нумерация идёт насквозь по выпуску, так что видно, сколько пунктов он затронул. Подробности не потеряны: они и раньше лежали в этом реестре и в PRD, а в рассказе для человека были лишними.

**Форму держит машина, а не моя память.** Проверка обходит разделы выпусков и ищет три вещи: продолжение пункта на следующей строке, пункт без номера и пункт длиннее четырёхсот знаков. Она была написана **до** переписывания файла и на прежней форме краснела — то есть положительный контроль у неё настоящий, а не придуманный.

**Пропущенные выпуски — по вашему слову.** Прежде окно показывало только раздел своей версии. Теперь — все разделы новее той версии, о которой вам рассказывали в прошлый раз, новые сверху. Обновились через два выпуска — увидите оба. Предел — пять разделов: это вторая половина вашего же замечания, «текста много»; дальше идёт ссылка. Если прежняя версия неизвестна (вы могли не обновляться с тех дней, когда окна ещё не было), показывается одна нынешняя, а не вся история.

**Ссылка — в двух местах.** В самом окне, строкой `Full changelog on GitHub`, и кнопкой: `General → Help → Changelog → Open`. Окно показывается один раз на версию, и ссылки в нём хватает ровно один раз — поэтому тот же адрес стоит в панели, где он всегда под рукой. Адрес объявлен в коде один раз на весь плагин, и это проверяется: второй его копии в рантайме нет.

**Про «изобретаю ли я велосипед» — нет.** Вы описали ровно общую практику, и половина её у нас уже стояла: накапливающийся `CHANGELOG.md`, новые версии сверху, у каждой свой раздел. Единственное, что можно сделать иначе, — **куда ведёт ссылка**: сейчас на сам файл в репозитории, а можно на страницу релизов, где GitHub рисует те же разделы по выпускам и рядом лежат файлы установки. Работа — одна строка, и решаете вы: вопрос задан в конце сессии.

**Как проверить, за минуту:** строка листа `S4`.

### 254у. `Stripe direction` — какой Block получает полосу — **сделано 2026-09-19**

**Ваш заказ:** «tags-block-fill при ON — добавить под этим контролом дополнительный контрол `Stripe direction` с выпадающим списком (Left, Right, Both) для определения направления места возникновения полоски, по умолчанию both. При left — полоска возникает только в left block, при right — только в right block».

**Что теперь.** Под тумблером полосы стоит новая строка `Stripe direction` с тремя значениями. `Both` — полоса у обоих Block, как было; `Left` — только у левого; `Right` — только у правого. Умолчание `Both`, то есть у вас ничего не изменится, пока вы сами не выберете сторону.

**Правило объявлено один раз на два места.** Полосу рисуют две вещи: слой в самой заметке и предпросмотр в панели. Это ровно тот случай, который с полосой уже случался: когда она научилась спрашивать состав Block, кегль и прозрачность остались на прежнем ответе, и вы пришли со тем же замечанием про размер. Поэтому «получает ли эта сторона полосу» спрашивают обе отрисовки у одной функции, а не считают каждая сама.

**Испорченное значение в файле настроек читается как `Both`.** Полоса — украшение, и пропадать ей от рукописной правки `data.json` не за что; границы держит нормализация настроек, а не панель.

**Чем закреплено.** Тремя родами проверок, и каждый про своё: зоны полос в заметке (три положения подряд — на одном правило «красить всегда обе» было бы зелёным), сверка двух отрисовок по имени класса и **закрашенная сторона у настоящего браузера** — там спрашивается не класс, а фон, потому что ваш вопрос именно про фон. Плюс три подмены: снятый отбор стороны, правило, всегда отвечающее «да», и предпросмотр, не читающий направление. Краснеют все три, каждая на своём утверждении.

**Как проверить, за минуту:** строка листа `S1`.

### 253у. Пять имён полосы — вашими словами — **сделано 2026-09-19**

**Ваш заказ:** «измени названия контролов: tags-block-fill на `Color the Block with Stripe`, tags-block-fill-color — `Stripe color`, tags-block-fill-opacity — `Stripe opacity`, tags-block-fill-height — `Stripe height`, tags-block-fill-width `Stipe width`». Опечатку в последнем слове прочёл как `Stripe width` и спрашивать об этом не стал.

**Все пять имён стоят дословно.** Прежние были `Color the Blocks`, `Block color`, `Block color strength`, `Band height` и `Band width`. Ни одно не потерялось для поиска: каждое старое имя ушло в слова для поиска той же строки, и панель по-прежнему находит её, если вы напечатаете прежнюю подпись.

**Прежняя подпись тумблера была моим компромиссом, и вы его отменили.** 2026-09-08 вы просили `Color Left\Right blocks`; правило имён не пускает ни косую черту, ни имя длиннее пяти слов, и в панели стояло `Color the Blocks`. Ваше новое имя — ровно пять слов, и оно встало как есть.

**Слово `band` ушло из всех описаний и подсказок** — четырнадцать строк. Имена вы назвали, а рассказ рядом остался бы про другое: на экране у одной вещи должно быть одно имя. Слово `Stripe` теперь пишется с заглавной везде, как `Block`, `Field` и `Bar`: это то, что рисует плагин, а не английское слово.

**Заодно нашлось место, которое я бы пропустил.** Вводная подсказка всей группы `Inline appearance` называла тумблер прежним именем — я правил пять строк по списку, а она в список не входила. Нашла её перегенерация документа, печатающая тексты целиком. Теперь запрет на прежнее слово стоит и на текстах группы, а не только на текстах строк: вернуть его молча больше нельзя.

**Как проверить, за минуту:** строка листа `S2`.

### 252у. Полоса встала над кеглем — **сделано 2026-09-19**

**Ваше замечание под `N5`:** «контрол tags-block-fill должен быть над контролом tags-text-size-left».

**Что теперь.** Порядок в разделе `Line view`: прозрачность левого Block, прозрачность правого, полоса со всеми своими строками, кегль левого Block, кегль правого. Это правка того порядка, который вы назвали сами четырьмя часами раньше; новое слово сильнее старого, и прежнюю последовательность я возвращать не буду.

**Цена — перенос участка целиком, без правки внутри.** Пять строк полосы переехали вместе со своими объяснениями; схема панели и раздел документа выведены генерацией, руками не правился ни один.

**Как проверить, за минуту:** строка листа `S3`.

### 251у. Структура репозитория — **сделано 2026-09-19**

**Ваш пункт 1:** «в github выкладывается релиз в неряшливой структуре… много файлов js, json, css в корне… папка docs сейчас содержит много лишнего… Хочу, чтобы на странице плагина структура выглядела компактно, аккуратно».

**Что было в корне:** девятнадцать файлов и девять папок. Три файла `.js`, один `.css`, папки `build`, `media`, `pkm_v2`, `showcase.md` рядом с README.

**Что стало.** Ни одного файла рантайма: `main.js`, `navigation_runtime.js`, `pkm_runtime_v2.js`, папка `pkm_v2/` и `styles.css` переехали в `src/`. `build/` ушла в `tools/build/`, `media/` и `showcase.md` — в `docs/`. В корне остались четыре папки (`src`, `tests`, `tools`, `docs`), шесть заметок `.md` и то, что обязано лежать в корне: `manifest.json`, `versions.json`, `package.json` с замком, `tsconfig.json` и настройка линтера.

**Папка `docs` похудела с 27 до 21 записи.** Девять документов, дописанных до фазы настроек и с тех пор никем не открытых, уехали в `docs/dev/archive/` — каждый со строкой указателя. **Ни один файл не выброшен.** Признак «лишнее» не на глаз: осталось то, на что ссылается живой код, живой документ или файл правил; уехало то, на что ссылался только архив или никто. Один документ остался именно поэтому - на `PKM_Runtime_Unified_Contract_v1.md` ссылается работающий код.

**Три переехавших файла - под запретом З3**, и это исключение № 131 по вашему постоянному разрешению «уборка с нулевым расхождением». Расхождение измерено **сборкой**, а не рассуждением: `main.js` собран до переезда и после, разница - **28 строк из 33 тысяч**, и все 28 принадлежат самому сборщику (комментарий с путём над модулем и тот же путь ключом в его реестре). Ни одной строки кода. `manifest.json` и `styles.css` совпали побайтно.

**Что это нашло по дороге.** Два стенда из одиннадцати падали на старом адресе, и в семи шагах проверок стендов нет - то есть заметить это мог только прогон руками. Прогнаны все одиннадцать по одному, оба починены. Плюс сплошная проверка ссылок по всем `md` и `html` репозитория: было двенадцать битых (`showcase.md` переехал и ссылался на корневые README и `instructions.md`), стало ноль.

**Чего я не трогал и почему.** Папки `tests` и `tools` остались в корне. Это не упущение: так устроен любой проект на npm, и все инструменты - линтер, компилятор типов, сборщик, CI - ищут их именно там. Перенести можно, но каждая команда получит по флагу с путём, а выигрыш на странице - две строки. Скажете «всё равно перенеси» - перенесу.

**Как проверить, за минуту:** строка листа `N9`.

### 247у. Кегль Block разведён на две стороны — **сделано 2026-09-19**

**Ваш пункт 2:** «разделить tags-text-size на tags-text-size-left и tags-text-size-right… Должна сохраниться текущая логика (т.е. центрирование строки при уменьшении text size)».

**Что теперь:** две строки вместо одной - `Left Block text size` и `Right Block text size`. Каждая действует только на свою половину строки; ваш текст между разделителями по-прежнему не трогает ни одна.

**Ваше нынешнее значение не потерялось.** У вас стоял кегль 70, и при первом же запуске он встанет с обеих сторон: проверено прогоном переезда на вашем `data.json` - 70 и 70, старый ключ снят.

**Центрирование сохранено, и это не на слово.** Оно живёт не в величине, а в признаке «это значение Block», и его правка не касалась. Браузерный шаг проверяет его на мелком кегле отдельной подменой, и она по-прежнему краснеет.

**Полоса `Color the Blocks` тоже разведена.** Её высота считается от кегля - значит у левой полосы теперь левый кегль, у правой правый. Иначе при разных числах одна из двух полос оказалась бы не по своему содержимому.

**Чем закреплено:** семь зелёных шагов, и в каждой проверке стороны разведены нарочно - на равных числах переворот «левая величина левой зоне» прошёл бы незамеченным. Подложка, предпросмотр панели, признак перерисовки слоя и переезд конфига проверяются разными числами слева и справа.

**Как проверить, за две минуты:** строка листа `N5`.

### 246у. `C1`: почему вы ничего не увидели, и что теперь — **сделано 2026-09-19**

**Ваше замечание:** «я скопировал `data.json` из `test5` поверх файла в `test-vault` при запущенном Obsidian - у меня не возникло никаких уведомлений, вообще не понял, произошло ли что-нибудь».

**Вы правы, и причина не в том, что починка не работает.** Она измерена в двух местах, и обе стороны проверены прогоном, а не рассуждением.

**Первое: Obsidian зовёт нас не на всякую правку файла.** В `app.js` 1.13.7 её обработчик устроен так: посмотреть время файла и позвать плагин, **только если файл новее** нашей последней записи. Иначе - молча ничего.

**Второе: копирование файла в Windows переносит время источника.** Проверено прогоном на этой машине: после `Copy-Item` у приёмника встало время трёхдневной давности. Ваш файл из `test5` был от 16 сентября, наша последняя запись в `test-vault` - от 18-го. «Новее» не выполнилось, сигнала не было, и потому не было ни уведомления, ни перечитывания. А дальше ближайшая запись плагина легла поверх принесённого - то есть ровно то, от чего чинили.

**Что сделано.** Плагин перестал зависеть от чужих часов. Правило «диск сильнее» уже было объявлено, и своя запись уже отличалась от чужой **по содержимому**, а не по времени; не хватало второй точки, где этот вопрос задаётся. Теперь перед тем, как положить своё в файл, плагин смотрит, что там лежит: если не то, что он писал сам, - принимает диск, бросает свою отложенную запись и говорит `Settings changed on disk, so inlineOverhaul reloaded them`. Это то же решение, которое плагин уже принимал по сигналу платформы, просто теперь оно не зависит от того, пришёл сигнал или нет.

**Чего эта правка нарочно не делает.** При выключении плагина отложенная запись дописывается **без** взгляда на диск: там на счету каждый такт, и лишнее чтение файла - ровно тот риск, из-за которого ваша последняя правка настроек пропадала при выключении (`Р-1`, починено 2026-09-18).

**Чем закреплено.** `config_external_change_tests.ts`, 13 проверок. Ваш случай воспроизведён буквально: платформа молчит, файл подменён чужим - принято чужое, своя запись брошена, уведомление одно. И три контроля к нему: своя правка доезжает до файла и чужой не считается; после приёма чужого плагин снова умеет писать; на вашем настоящем конфиге через настоящую миграцию своя запись чужой не выглядит. Четыре мутации, три красные - четвёртая нашла дыру, которой не было в замысле, и её закрыла отдельная проверка.

**Как проверить, за две минуты:** строка листа `C1`, способ в ней обновлён - теперь там два способа, и второй именно ваш.

### 245у. Окно «что изменилось» разбирает markdown — **сделано 2026-09-19**

**Ваш пункт 8:** «при обновлении плагина в obsidian открывается changelog и он выглядит некрасиво». На скриншоте видно, чем именно: `### Changed` стоит строкой, вокруг слов звёздочки, вместо списка дефисы. То есть раздел показывался ровно так, как написан в `CHANGELOG.md`.

**Что теперь:** разметку разбирает сам Obsidian — заголовки становятся заголовками, списки списками, `код` кодом. Своего разборщика markdown в плагине не появилось и появиться не должно: это было бы третье объявление правила о чужой разметке.

**Почему так не было сделано сразу.** В коде стояло объяснение: «`MarkdownRenderer` тянет за собой жизненный цикл компонента, которого у окна нет». Это оказалось неверно - компонент создаётся и выгружается окном, в три строки. Прежнее объяснение снято вместе с прежним поведением.

**Как проверить, за минуту:** строка листа `N4`.

### 240у. Проверка типов включена на четырёх файлах ядра — **сделано 2026-09-19**

**Нажимать нечего: продукт не менялся, менялись комментарии с типами.** Подтверждается словом.

**Это работа `Р-6`, и у неё был самый низкий ранг в перечне.** Ревизия честно сказала: спрятанных дефектов эта работа не найдёт — их там нет, — а стоит она четыре тысячи правок. Поэтому сделана та часть, которую ревизия сама называла началом: четыре файла, которые читают все остальные.

**Измерение снова поменяло план.** Ревизия обещала по 4–13 ошибок на файл; намерено от 4 до 126. У четырёх взятых файлов — 30 на всех, и ни одна из тридцати не оказалась живым дефектом: все тридцать это ненаписанные типы. Три оставшихся файла названы числами в очереди.

**И первое измерение было ложью, которую поймал контроль:** пометка о проверке на файлах ядра не делала ничего, потому что сами файлы в проверку не входили. Нарочная ошибка в них не показывалась вовсе. Теперь входят.

**Чем закреплено.** Прогон типов — один из семи шагов, а отдельный сторож следит за тем, что пометка не снята и что файл и правда внутри проверки: снятая пометка оставила бы прогон зелёным.

**Разбор** — PRD 10.13.203.
### 239у. Панель перестала задумываться на очень длинной строке — **сделано 2026-09-19**

**Это работа `Р-12` из ревизии.** Было известно, что на строке в двадцать тысяч знаков панель открывается больше секунды, и неизвестно — почему. Задача была назвать место.

**Место названо профилировщиком и оказалось одно:** сравнение строки человека с видом панели считалось по всей строке целиком, сеткой в три тысячи на три тысячи клеток. Теперь в сетку попадает только середина — совпавшие начало и конец в неё не идут.

**Что стало с ценой на этой машине:** на десяти тысячах знаков 117 миллисекунд было, 29 стало; на двадцати тысячах **1263 было, 49 стало**. На обычной вашей строке не менялось ничего и не должно было.

**Поведение не изменилось, и это измерено, а не обещано.** 4 737 настоящих пар «ваша строка / вид панели», записанных с полного обхода обеими дорогами и с пробы на прочность: результат совпал целиком. У меры есть контроль — нарочно испорченная версия на тех же парах расходится.

**Найдено тем же заходом и пока не починено.** На строке в сорок тысяч знаков команды важности падают. Это старше сегодняшней правки — проверено на коде до неё, — и лежит на защищённой дороге, поэтому починка отдельной строкой в очереди. Проба на прочность теперь про это падение говорит вслух, а не молчит.

**Как проверить, если захотите:** вставьте в заметку очень длинный абзац одной строкой (тысяч на двадцать знаков) и откройте на нём TagWheel. Раньше было заметное подвисание, теперь его быть не должно.

**Разбор** — PRD 10.13.202.
### 238у. Почему слой оформления перерисовывается так, а не иначе — **сделано 2026-09-19**

**Нажимать нечего: поведение не менялось ни на знак.** Подтверждается словом.

**Это работа `Р-8`, и ревизия оставляла выбор: объяснить или выровнять.** Пять частей слоя оформления отвечают на вопрос «надо ли перерисовать» тремя разными способами, и объяснение было написано только у одной.

**Выбор сделан измерением.** Пробой со счётчиком показал: обычная запись значения в панели **не зовёт** пересборку оформления в открытых заметках, а патч из своего блока — зовёт. Значит «перерисовывать всегда» у двух частей — не лень, а единственный путь настройки до заметки; охрана сузила бы его. Поэтому объяснено, а не выровнено.

**Цена названа числом:** сборка тегов — 0,45–0,56 миллисекунды, полосы — 0,08–0,10; при наборе текста это около 5 мс в секунду.

**И само измерение стало проверкой:** изменится шов — прогон покраснеет и покажет, что объяснение устарело.

**Тем же заходом найдено вот что, и это уже про ваш экран.** Настройка, которая живёт не в цвете, а в самой отрисовке (например, размер текста тегов), доезжает до открытой заметки не сразу, а с ближайшим касанием этой заметки. Стоит ли это чинить, решает ваш глаз — строка ниже.

**Разбор** — PRD 10.13.201.
### 237у. Перетаскивание строки списка объявлено один раз — **сделано 2026-09-19**

**Нажимать нечего: на экране не меняется ничего, и это цель.** Подтверждается словом.

**Это работа `Р-11` из ревизии.** Правило «строку списка можно взять за ручку и бросить на место другой» было написано дважды — в списке Binder и в списках порядка. Одинаковые правила расходятся молча, и тогда одно и то же действие в двух местах панели начинает работать по-разному.

**Сводил только после измерения.** Оба куска приведены к общему виду и сравнены знак в знак: расхождение ноль. Единственное, чем списки отличаются по существу, — у Binder строка тащится всегда, а у списков порядка только при включённом тумблере, — переехало в параметр и проверяется с обеих сторон.

**Третье место с перетаскиванием не тронуто, и это решение с причиной.** В редакторе Fields бросок делают сторона и её подпись, и вопрос там другой: не «переставить строку по номеру», а «перенести поле на эту сторону и в это место».

**Проверок написано две.** Сведение к одному дому делает сверку двух дорог слабее: сломанный общий дом ломает обе одинаково. Поэтому отдельно спрашивается правильный ответ дома и отдельно — что ни один из двух списков не завёл своё тело обратно. Заодно покрыта сторона, у которой перетаскивание не проверялось вовсе.

**Разбор** — PRD 10.13.200.
### 236у. Документ, который я читаю до первой правки, стал на четверть короче — **сделано 2026-09-19**

**Проверять тут нечего, и это честно: работа для меня, а не для вас.** Подтверждается словом.

**Это работа `Р-10` и ваше слово «ок, главное, чтобы работало надёжно и эффективно».** Правила, которые я обязан прочесть целиком перед первым действием каждой сессии, лежали в одном файле, и он рос с каждой сессией: строка на каждое новое разрешение, строка на каждую закрытую сессию.

**Что уехало.** Три вещи, и все три — те, что нужны **по событию**: таблица из 130 разрешений на правку защищённых файлов, подробности браузерной проверки и указатели на архив. У каждой новый файл, ссылка из главного и строка «когда открывать» в самом файле.

**Сколько это дало.** 151 889 знаков до, 116 037 после — на четверть меньше. Число печатает прогон, а не документ: вписанное в текст оно устареет от первой же правки.

**Ничего не выброшено.** Переезд сделан вырезанием, а не переписыванием, и у каждого куска сверены обе стороны: в старом файле его нет, в новом есть. Одна проверка при этом покраснела сама — та, что читала таблицу разрешений из старого места, — и это была хорошая новость.

**Что осталось нарочно.** Список правил, купленных дефектами, — самая большая часть файла. Он нужен каждую сессию целиком: его повод — «пишу проверку», «объявляю правку сделанной», «меряю». Уносить его значило бы оставить правило в файле, который открывают по поводу, наступающему всегда.

**Разбор** — PRD 10.13.199.
### 235у. Покрытие меряется обеими дорогами, а не одной — **сделано 2026-09-19**

**Нажимать тут нечего: это инструмент ревизии, а не функция плагина.** Подтверждается словом, если вообще нужно.

**Это работа `Р-7` из ревизии.** Мера «что из кода не исполняет ни один прогон» читала только Node-дорогу. Слой оформления заметки и панель TagWheel работают в браузере, и их прогон в меру не попадал: часть кода, названного неисполненным, на самом деле исполнялась именно там. По такому числу нельзя выбирать, что чинить дальше.

**Сколько это стоило в числах.** Одной дорогой — 230 ни разу не исполненных имён, двумя — 203. Восемнадцать из разницы лежат в одном файле, том самом слое оформления: в нём было 23 «неисполненных», осталось 5.

**Первая версия правки врала, и поймало её измерение, а не чтение.** Складывать две дороги я начал по именам функций — и проверка на известном ответе показала, что семь имён из двадцати в странице носит больше одного файла, причём спорят они с чужой библиотекой. То есть нам засчитывалось исполнение чужого кода. Теперь владелец определяется по месту в собранной странице, а не по имени.

**Гейт от этого не поменялся:** `npm run gate:browser` работает как работал, покрытие снимается только когда его просят отдельной переменной.

**Разбор** — PRD 10.13.198; рецепт из четырёх команд лежит в шапке самого инструмента.
### 233у. Настройки, изменённые снаружи, больше не затираются — **сделано 2026-09-19**

**Это работа `Р-2` из ревизии и ваше слово «диск сильнее» (В-142).** Obsidian с версии 1.5.7 сообщает плагину, что его файл настроек изменили снаружи — синхронизацией, вторым компьютером или правкой руками. Плагин этого сообщения не слушал вовсе: настройки у него в памяти оставались прежними, и ближайшая отложенная запись клала старое поверх нового. У вас это стреляет ровно там, где вы работаете: два vault и перенос настроек между ними.

**Что теперь происходит.** Плагин перечитывает файл, панель настроек перерисовывается, оформление на открытых заметках пересобирается, набор команд заводится заново — Fields могли приехать другие, — и всплывает сообщение `Settings changed on disk, so inlineOverhaul reloaded them`. Окна с выбором «чьё оставить» нет: побеждает то, что на диске, как вы и сказали.

**Отменить принесённое снаружи нечем, и это нарочно.** `Ctrl+Z` в настройках после такого приёма записал бы ваше вчерашнее состояние поверх принесённого — то же затирание, только вашими руками. Ступени отмены, которые описывают исчезнувший документ, убираются.

**Своя правка сообщения не показывает.** Ваша собственная запись, вернувшаяся тем же сигналом платформы, узнаётся по содержимому файла, а не по времени: времени файла верить нельзя, его точность принадлежит файловой системе. Если бы это не работало, плагин ходил бы по кругу «перечитал — записал — перечитал».

**Сломанный или пропавший файл настройки не стирает.** Obsidian отдаёт на такой файл пустоту, и из неё плагин собрал бы умолчания — то есть стёр бы всё дерево настроек в ответ на недописанный синхронизацией файл. Теперь настройки остаются прежними, а причина пишется в журнал разработчика.

**Чем закреплено.** Девять проверок в `tests/regression/config_external_change_tests.ts` на настоящем хранилище и настоящем классе плагина; подделаны только диск и реестр команд. Девять подмен, и каждая красит свою проверку. Первый заход подмен нашёл слепое место у самой проверки — две охраны прикрывали друг друга, — и оно закрыто тем же заходом. Дорога платформы прочитана в её коде `app.js` 1.13.7, а не по памяти; разбор целиком — PRD 10.13.196.

**Как проверить, за три минуты.** Откройте настройки плагина в этом vault, потом откройте `data.json` плагина в любом текстовом редакторе снаружи Obsidian (`.obsidian/plugins/inline-overhaul/data.json`), поменяйте в нём значение и сохраните. В Obsidian должно всплыть сообщение, а панель — показать новое значение. Второй способ, ближе к вашей работе: скопируйте `data.json` из `test5` поверх файла в `test-vault` при запущенном Obsidian.
### 215у. Подсветка места, куда прыгнул курсор — **сделано 2026-09-17**

**Это ваш заказ (Н5) и ваши ответы (В-136).** Только на прыжках, два прыжка подряд гасят прежний круг, и задержка от 0 до 1 секунды, чтобы частые прыжки не раздражали.

**Что появилось в панели.** Шесть строк внизу группы `Moving cursor inside a note` — там, где вы просили, — и живой предпросмотр с анимацией: тумблер, цвет круга, его размер, время жизни, задержка между прыжками и `Use inside current line`. Тумблер по умолчанию выключен: рисовать поверх заметки вы должны попросить сами.

**Задержка работает не так, как «пропустить прыжок».** Круг показывается там, где вы **остановились**: новый прыжок отменяет показ прежнего и ставит свой. При `0` круг появляется на каждом прыжке.

**Разрешения на правку защищённых файлов не понадобилось, и это стоит сказать.** Разбор обещал новое исключение — сигнал «курсор переехал» я считал делом движка навигации. Оказалось не так: все команды навигации идут через одну обёртку, которая под запрет не попадает, и какая из них прыжок — знает тот же список, где объявлены сами команды. Ни один защищённый файл не тронут.

**Как проверить, за две минуты.** Назначьте хоткей командам `Jump back` и `Jump next` (`Keyboard → Commands & Hotkeys`), включите `Highlight where you land`, поставьте размер побольше и время подлиннее — и прыгните по заголовкам. Круг должен появиться там, где встал курсор, и погаснуть сам. Поставьте `Latency between jumps` в полсекунды и прыгните несколько раз быстро: круг должен быть один и там, где вы остановились.

### 205у. Номер нумерованного списка — разметка, а не ваше слово — **сделано 2026-09-17**

**Это ваше решение (В-133).** Я спросил: «строка `1. текст` уезжает в заметку как `1. текст`, то есть номер считается вашим словом. Считать номер разметкой, как буллит?» Вы ответили: «да».

**Что стало.** Начало строки Transform спрашивает у того же объявления, что и весь остальной плагин. Номер списка теперь такая же разметка, как `- `: в имя новой заметки и в её текст он не уезжает.

**Мера шире вашего слова, и я называю это вслух.** На 1200 строках ваших заметок правка меняет 213: 190 нумерованных строк, 22 строки цитаты и каллаута (`> ` и `> [!note]` тоже перестали уезжать в имя) и одна голая `1.`. Цитату и каллаут плагин и так считает внешним оформлением строки везде, кроме этого места.

**Как проверить, за минуту.** `Inline to note` на строке `1. отчёт` — имя заметки должно быть `отчёт`, а не `1. отчёт`. То же со строкой в цитате.

### 204у. Значение, стоящее в соседнем Block, Transform больше не пропускает — **сделано 2026-09-17**

**Это ваше решение (В-132).** Я спросил, чинить ли то, что значение уходит со строки при переносе в заметку, и вы ответили: «да, должны уходить только те values, которые не отмечены в source-fields-head».

**Что оказалось при проверке вашим файлом.** Правило «уходит всё, что не отмечено» и правда работало — но **только** для значений, стоящих в том Block, который назначил им Order. Значение в соседнем Block Transform не видел вовсе: оно не уходило со строки и **не попадало в свойства новой заметки**. В ваших заметках таких значений **110 из 337** — вы перенесли Fields в правый Block, а строки, написанные прежде, держат их слева.

**Что стало.** Значение узнаётся в **любом** Block. Слово человека остаётся словом человека: то же значение, написанное внутри текста между разделителями, не трогает никто (таких у вас 31). Если на один знак претендуют два Field, спор решает Block — сперва заявка своего.

**Как проверить, за две минуты.** Строка, где значение стоит слева, а поле по Order справа. `Inline to note`: значение должно уйти со строки, если его нет в `Fields to keep`, и остаться, если есть. И то же значение, набранное внутри текста, должно остаться на месте в обоих случаях.

### Отчёт 202у — **подтверждён заказчиком 2026-09-17**

Строка `G3` закрыта его галочкой: правый Block перестал уезжать в слово
человека, а поле типа link переставляется по Order наравне с тегом. Отчёт унесён
в `docs/dev/archive/AWAITING_OWNER_CHECK_session60.md`; разбор — PRD 10.13.176,
исключение к З3 № 127.

### 201у. Крестик, стирающий написанное в поле — **сделано 2026-09-16, ночь**

**Это ваше замечание и ваше решение (В-131).** Вы написали: «добавь в контрол выбора папки крестик для удаления текущей выбранной папки (сейчас не удобно, что нужно удалять текст руками). Также добавь такой крестик во все остальные подобные контролы (где пользователь вводит текст руками)». На интервью вы выбрали: ставить там, где пустое значение законно.

**Где он появился — восемь строк.** `Templates folder` и `New notes folder` на вкладке Transform, `Type name of header`, `Text of the line above`, `Date format` и `Mark transformed line` там же, `Backup folder` и `Log file` на вкладке Advanced.

**Где его нет и почему.** `Separator 1`, `Separator 2` и скобки имени новой заметки: пустой разделитель ломает разбор строки, и кнопка, которая это делает одним нажатием, помогала бы редко, а портила бы молча. У `Default template` крестик не нужен — это список, и в нём есть строка `None`.

**Чего это стоило.** Платформа такой кнопки не умеет: у контрола настройки слота под неё нет вовсе, а колонка кнопок есть только у заголовка группы — я это проверил не по документации, а по коду самой Obsidian. Поэтому восемь строк панель теперь рисует сама: поле ввода ставится тем же вызовом платформы, подсказчик папок — тот же, что в карточке Smart Rules, а кнопка живёт в нашем узле. Всё остальное у строки прежнее: имя, описание, подсказка, глобальный поиск и условие видимости.

**Кнопка исчезает, когда стирать нечего.** Кнопка, которая ничего не делает, хуже её отсутствия.

### 200у. Пузырь тега стоит серединой строки — **сделано 2026-09-16, ночь**

**Это ваше замечание.** «Когда я уменьшаю размер текста tags-text-size, то текст уменьшается, но остаётся выровненным по нижней границе строки. Можно ли сделать, чтобы он выравнивался по центру строки?»

**Причина.** Пузырь — строчный блок, и по умолчанию такие равняются по базовой линии текста. У мелкого кегля базовая линия совпадает с общей: чем мельче текст, тем ниже пузырь. Обмерено браузером до правки: при 100 % середина пузыря ниже середины написанного на 0,3 точки, при 80 % — на 1,8, при 50 % — на 3,3.

**Что стало.** Пузырь равняется серединой. На умолчании это почти ничего не двигает (те же 0,3 точки), а на мелком кегле отклонение упало втрое — до 1,3. **В ноль оно не ушло**, и это честно: точного «по центру строки» в правилах вёрстки нет, есть «по середине букв», и остаток — разница между серединой букв и серединой строки.

**Что это ещё задело.** Ничего: браузерный гейт с его подменами прошёл целиком — подложки блоков, их высота, перенос и оверлей скроллера остались прежними.

### 199у. Заголовок: имя заметки, раздел и знак списка — **сделано 2026-09-16, ночь**

**Это ваше замечание и два ваших решения (В-128, В-129).** «При активации inline2note из хедера получилась ерунда — заметка `[[# 1 #todo 23423 📅2026-09-16 17 58]]`: название заметки стало полностью содержание исходной инлайн записи, включая технические блоки; содержание исходного хедера не переместилось в новую заметку». На интервью вы выбрали «весь раздел под заголовком» и добавили: «после трансформации исходная строка-хедер должна преобразовываться в строку-буллит».

**Имя.** Воспроизвёл ваш случай на вашем конфиге и получил ровно эту строку: заголовок отдавался под имя **сырым телом**, со значениями полей, метками и разделителями, а косая черта в имени файла запрещена — отсюда `#/1` превратилось в `# 1`. Теперь именем становится текст заголовка: на вашей строке это `23423`.

**Раздел.** «Что относится к этой строке» плагин искал **отступом**, а у строк под заголовком отступа нет — блок кончался на самом заголовке. Теперь у заголовка своё правило: весь раздел до следующего заголовка того же или старшего уровня. Работает при вашей настройке `Take them along`; при `Leave them` уезжает только сама строка.

**Знак.** После переноса строка-заголовок становится строкой списка — отступ, цитата и каллаут при этом остаются.

**Что я нашёл рядом и не стал чинить.** Значение поля из правого Block (у вас это дата) со строки после переноса пропадает, хотя в списке «что убрать» оно не названо. Проверил на рабочем дереве прошлого коммита — дефект давний, к этой правке отношения не имеет. Он записан отдельной строкой, чинить его в этом же заходе значило бы чинить не то, о чём вы написали.

### 198у. Шаблон не переживает смену папки — **сделано 2026-09-16, ночь**

**Это ваше замечание и ваше решение (В-127).** «Я изменил папку 111 на другую папку (Templates), но при активации transform в строке получил ошибку `template not found: 111/template 1.md` — т.е. по прежнему осталась старая папка. Проверь подобные косяки в других контролах плагина». На интервью вы выбрали «всегда очищать выбор».

**Причина не в папке.** В настройках Smart Rule лежит **полный путь** шаблона, и смена папки его не касалась. В вашем `data.json` из `test5` это видно прямо: `Default template` вы перевыбрали, и он стал `templates/template 1.md`, а у правила `test` остался `111/template 1.md`. Ваша строка с `#todo` попадает под это правило, движок берёт шаблон правила — отсюда и текст ошибки.

**Что стало.** Шаблон, не лежащий в назначенной папке, снимается — и при смене папки, и при загрузке. На вашем файле это уже так: стало пусто у правила и осталось нетронутым у общей строки. **Цена названа вслух:** поле папки пишет настройку на каждую набранную букву, поэтому выбор снимается уже на первой из них — вернуть прежнее имя папки не вернёт прежнего шаблона, его придётся назначить заново.

**Тем же заходом сошлись два списка.** При неназначенной папке Smart Rule предлагал шаблоном **любую** заметку vault, пока `Default template` писал «Set a Templates folder first». Теперь оба говорят одно: папки нет — шаблонов нет.

**Что дал обход, о котором вы просили.** Значений, которые ссылаются на ваши же данные, в панели ровно пять: язык, `Default template`, поле для полос тегов и два «активных поля» TagWheel, — плюс шаблон и папка у каждого Smart Rule. Второе попадание того же класса у вас уже лежит: `Active Field on opening` слева указывает на `type`, а `type` вы перетащили в правый Block. Сейчас это не видно, потому что режим стоит `First Field`; строка заведена.

### 197у. Пустой Block открывается соседним — **сделано 2026-09-16, ночь**

**Это ваше замечание и ваше решение (В-130).** «Для теста я перетащил все Field в right block и открыл tagwheel left — он открылся как `====`. В целом нормально, но всё же думаю, было бы лучше, чтобы tagwheel был умнее и сразу открывал другой block, если текущий пустой».

**Что было видно, кроме `====`.** Воспроизвёл на вашем конфиге: на строке `- 123 :: #333` панель левого Block рисовала `- 123 ==== :: #333 123` — ваше слово `123` стояло в строке **дважды**, пока панель открыта. `Esc` возвращал строку как была, поэтому в заметке этого не оставалось.

**Что стало.** Команда пустого Block открывает соседний. Задвоение ушло вместе с пустой полосой — рисовать нечего было только ей.

**Где граница.** Пустым считается Block, в котором панели нечего показать, — в том числе когда все его поля скрыты предусловием на этой строке. Если показывать нечего **ни на одной** стороне, открывается та, которую вы назвали: уходить некуда, и молчаливая подмена Block была бы враньём.

### 196у. Недостижимый кусок снят, на его месте отказ вслух — **сделано 2026-09-16, вечер**

**Нажимать нечего, поведение не изменилось ни на одном нажатии.** Подтверждается словом.

**Это ваше решение, исполненное тем же заходом.** Вы выбрали «снять и поставить отказ вслух».

**О чём речь простыми словами.** В команде тегов лежал кусок в 52 строки, умевший одно: понять по знаку задачи на строке, какое значение поля выбрано, когда самого тега нет. Ни одна команда плагина туда не заходила — я померил счётчиком: за прогон всего набора, обоих заходов обхода строки и стенда панели вход в команду отработал 764 раза, а этот кусок — ноль. Причина простая: команды шлют движку только круговое действие, а два других уходят в движок дат.

**Что теперь на его месте.** Если движок всё-таки позовут иначе — например, вашим старым макросом, — вы увидите сообщение с именем действия, и такая же строка уйдёт в консоль. Прежде на этом месте было бы молчание: команда как будто отработала, а не сделала ничего.

**Вместе с куском снялись два его жителя** — два помощника, которых звали только оттуда. Проверки, которые требовали их наличия, заменены на запреты возврата, и у каждого запрета рядом стоит положительный контроль: без него «этого в файле нет» было бы зелено и при снятой команде.

### 195у. Мёртвая функция в общем рантайме и измеренный ответ про вторую мелочь — **сделано 2026-09-16, вечер**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Откуда взялось.** Строка `Р11` очереди: две мелочи, найденные ревизией 14 сентября и тогда же названные «поведения не меняют, потому не чинились».

**Первая оказалась крупнее, чем была записана.** Ревизия писала про недостижимое подусловие внутри живой функции. За два дня предмет изменился: 15 сентября правило «как значение поля выглядит в строке» свели к одному дому, и звать эту функцию перестали вовсе. Сплошной поиск по репозиторию: имя встречалось ровно в трёх местах — в собственном объявлении, в выгрузке общего рантайма и в проверке, которая требовала его наличия. То есть проверка стерегла мёртвое, а мера мёртвого кода этого не видела и не увидит: она читает список выгрузки модуля, а здесь имя лежит полем объекта, который собирает фабрика. Функция снята, проверка заменена запретом на возврат с положительным контролем на живом соседе.

**Вторая мелочь закрыта не правкой, а измерением, и ответ вышел другой.** В отчёте стояло «эту функцию не исполняет ни один прогон». Счётчик на четырёх местах разом, за прогон всего набора, обоих заходов обхода строки и стенда панели: сама команда тегов отработала 764 раза, а весь блок, из которого эту функцию зовут, — **ни разу**. Реестр команд шлёт команде тегов только круговое действие, а два других уходят в команду дат. То есть речь не о «не покрыто проверками», а о **недостижимом куске в 52 строки**.

**Снимать его я не стал, и вот почему.** Недостижимость измерена с той стороны, которую я вижу: команды плагина. Остаётся вход, которого я измерить не могу, — если вы когда-нибудь звали движок своим макросом с другим действием, этот кусок для такого вызова живой. Поэтому он уходит вам строкой с ценой, а не в правку: снятие стоит десяти минут, а вопрос стоит одного вашего слова.

### 194у. Пятислойная передача нормализатора ключа Order сведена к дому — **сделано 2026-09-16, вечер**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это ваше задание этой сессии.** Вы выбрали «ничего нового, уборка кода» и дали постоянное разрешение править защищённые файлы движков там, где расхождение измерено и равно нулю. Строка очереди — `Р10`, она стояла с 15 сентября именно потому, что её цена была названа, а решение оставалось за вами.

**О чём речь простыми словами.** Ключ поля в вашем списке Order плагин приводит к аккуратному виду — убирает пробелы по краям. Правило это одно на весь плагин, но доезжало оно до места работы **через пять пересадок**: движок передавал своё, следующий слой брал переданное «или своё», третий — снова «или своё», и так пять раз, а последний клал результат в общую ячейку и больше не пересчитывал. То есть кто первый позвал, тот и решил за всех. Пока тело одно — это ни на что не влияет; заведись второе — плагин начал бы вести себя по-разному в зависимости от того, в каком порядке загрузились куски. Такое расхождение не воспроизводится и ищется днями.

**Что сделано.** Все пять пересадок сняты: кто правило применяет, тот и спрашивает его у дома. Вместе с ними снят модуль `pkm_v2/field_model.js` — в нём оставалось одно тело-переходник, и звал его только ушедший слой.

**Как проверено, что поведение не изменилось — двумя способами, и оба до правки.** Первый: счётчик во все четырнадцать мест цепочки и прогон всего набора, обоих заходов обхода строки и стендов. Ни одно запасное тело не исполнилось **ни разу**, и у каждого нуля рядом стоит ненулевой счётчик в той же функции — значит меряли не пустоту. Второй: ответы обеих дорог сверены на 37 ключах — ваших из `data.json` плюс синтетические края вроде пустого, пробельного и очень длинного. Расхождений ноль, непустых ответов 29. И у сверки контроль, который нарушает симметрию: дом подменён нарочно — обе дороги ответили по-новому, то есть обе действительно в него ходят.

**По дороге нашлись три живые копии того же правила.** Они стояли в форме, к которой сторож был слеп: он знал копию, объявленную функцией, и копию, переданную доводом, а «присвоить имени своё тело» не знал. Все три сведены к дому. Чинить сторож третьим образцом подряд нельзя — это уже было и кончалось четвёртым, — поэтому написано **свойство**, и его ширину пришлось мерить: наивный признак «где вообще встречается это выражение» назвал бы 242 места в рантайме, почти все чужие; средний — 13; точный — ноль. Мутация: три копии возвращены на место, сторож краснеет и называет все три файла поимённо.

**И написан водитель.** До сегодня этот шов не исполнял ни один прогон: проверялась либо сама функция, либо движок целиком, а место, где рантайм отдаёт правило разрешению конфига, — никто. Проверка написана вместе с правкой, как требует правило, купленное вчерашней находкой.

### 193у. Пятая форма молчания снята целиком — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это ваше решение, исполненное тем же заходом.** Вы выбрали «снять все 11». Сняты все одиннадцать.

**О чём речь простыми словами.** В одиннадцати местах плагин спрашивал «а нужный кусок меня на месте?» — и если бы ответ был «нет», просто **не делал бы работу** и шёл дальше считать по-другому. Ни сообщения, ни записи в журнале: со стороны это выглядело бы как «функция работает, но неправильно». Цена у каждого места своя, и вот самые заметные: ключ поля подменялся его именем (на паре полей это разные вещи), список меток дат становился пустым — то есть неотличимым от «дат на строке нет», слитый тег `#parent/#child` оставался неразобранным, и панель открылась бы пустой на строке, где значения есть. Одиннадцатое — форма строки для прыжка теряла метки, то есть ровно то, с чем вы приходили в `S4`: «в конец вашего текста» уезжало за дату.

**Как проверялось, что сегодня это недостижимо.** Сперва во все одиннадцать мест разом поставлено падение вместо тихого пропуска: набор и обход строки обеими дорогами остались зелёными. Значит туда не заходит ни один прогон.

**Второй вопрос пришлось задать иначе, и это оказалось полезно.** Спрашивать «а живо ли само место» броском значило бы одиннадцать прогонов ради одного вопроса. Вместо броска я поставил счётчик: каждое место отмечается в файле, один прогон отвечает по всем сразу. Живы десять из одиннадцати — от 4 до 10 837 проходов за прогон.

**Одиннадцатое снова оказалось швом, который не исполняет никто** — та самая форма строки для прыжков. Её зовут обе команды прыжка, и ни одна проверка до неё не доходила. Проверка написана вместе с починкой: команда прыжка теперь зовётся так же, как её зовёт Obsidian, и спрашивается с двух сторон — с целым движком метки доезжают, с половинным вы читаете сообщение, а прыжка не происходит.

**И мой собственный обход соврал — в мою пользу, и я это поймал.** Первая версия считала концом блока строку, равную закрывающей скобке целиком, и на записи вида `} else {` убегала дальше по файлу: честный выбор из двух числился молчанием. Так было выдумано три места из четырнадцати. Поймалось потому, что число проверялось до того, как попало в документ; в сторож ушла версия с проверкой на этот самый шаг.

**Чем закреплено.** Обход по этой форме с четырьмя контролями на строках-образцах и проверка поведения у команды прыжка. Обе проверены поломкой: вернул тихий пропуск — обе покраснели и назвали адрес.

**Чем проверено, что вы ничего не заметите:** весь набор, обход строки двумя заходами (189 сочетаний и 170 пар второго нажатия, нули), браузерный шаг, стенд отмены и стенд панели.

**Где разбор:** PRD 10.13.167, исключение к З3 № 121, урок У-203, правило 124.

### 192у. Пять мест молчания разобраны, и обход нашёл ещё два — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это ваше задание целиком.** Вы сказали, завершая прошлую сессию: «разбирай эти пять мест в следующей сессии, завершаем». Разобраны все пять, и у каждого вердикт получен прогоном, а не чтением.

**О чём речь простыми словами.** В пяти местах плагин спрашивал у соседнего куска себя же «а ты вообще на месте?» — и на случай «нет» держал свой ответ: пустой список, «ничего не записали», «клавиша не наша». Такой ответ неотличим от честного: человек видит, что функция просто не сработала, и ни в консоли, ни на экране об этом ни слова. Теперь на этом месте отказ вслух, с именем того куска, которого не хватило.

**Как проверялось, что ни одно из этих мест не работает сегодня.** По каждому я сперва ломал сам запасной ход — ставил туда падение — и гонял весь набор: зелёный значит «сюда не доходят». Но зелёный бывает зелёным и от того, что проверок на это место нет вовсе, поэтому вторым шагом я ломал **вход в саму функцию**: она обязана быть красной. Так и вышло у четырёх из пяти — 4, 3, 2 и 4 покрасневших проверки.

**Пятое ответило нулём, и это оказалось находкой.** Шов, которым `Backspace` доходит до своей функции, не исполняет ни одна из семидесяти одной проверки: сама функция проверена, раскладка клавиш проверена, а **шаг между ними** не проверял никто. Ровно поэтому в нём годами и стояло молчание. Стенда, которым до него дотянуться, нет — его зовёт нажатие клавиши у вас. Поэтому проверка написана вместе с починкой: она жмёт `Backspace` и `Del` на **собранном** плагине и смотрит, что строки склеились, а при выключенном тумблере — что клавиша осталась платформе.

**Шестое место того же рода нашлось в редакторе Fields.** Модель редактора держала пять ответов на случай «помощников состояния нет». Поломкой проверено: четыре недостижимы, а пятый оказался вовсе не запасным ходом, а **единственным** объявлением правила — функции, которую он «подстраховывал», в модуле нет ни дня. Довод, которым всё это держалось, был записан в комментарии и перестал быть правдой два дня назад. Что вы получили бы на самом деле, если бы модуль однажды не приехал: редактор значений, который молча не сохраняет.

**И ещё два места нашёл сам сторож, а не список.** Тот же вопрос на дороге панели и правило соседства дочернего поля: оно отдавало строку нетронутой, хотя его сосед по той же переменной отказывает громко. Это ровно тот случай, когда починка одного места из двух делает хуже: две дороги начинают отвечать по-разному.

**Чем закреплено.** Сплошным обходом кода по этой форме — новое молчание такого рода роняет проверку и называет файл и строку. Проверено мутацией: я вернул один из снятых ходов, проверка покраснела и назвала адрес.

**Почему сторож не ругается на законные места.** Такая же запись бывает честной пробой у платформы — «есть ли у этого узла такой метод». Их в коде 178, а долгом были семь. Отделены они не списком имён, который я бы забыл дополнить, а вопросом к самому репозиторию: спрашиваемое имя должно быть тем, что отдаёт наружу какой-то наш модуль. Что при этом пропускается — написано в самом стороже, а не осталось у меня в голове.

**Чем проверено, что вы ничего не заметите:** весь набор, обход строки обеими дорогами двумя заходами (189 сочетаний и 170 пар второго нажатия, нули), браузерный шаг и остальные пять шагов.

**Где разбор:** PRD 10.13.166, исключение к З3 № 120, уроки У-203, правила 123 и 124.

### 191у. Долг копий закрыт: было 23, осталось 1, и у него сторож — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это ваше задание целиком.** Вы просили разобрать все 23 имени и чтобы эта сессия стала последней по этому долгу. Разобраны все, и у каждого вердикт получен прогоном, а не чтением.

**Три последних имени живут в движках.** Два оказались разными вопросами под одним именем: одно у панели спрашивает шире, чем общее правило, и эта «лишняя» ветка **живая** — я проверил её поломкой, и на неё опирается проверка. Сводить такое значило бы менять поведение, о котором никто не просил; имена разведены, тела не тронуты.

**Одно имя не сводится, и причина написана прямо в стороже.** Два движка одинаково раскладывают имена ключей настроек по своим переменным. Общее правило отдало бы им тот же список, а строки раскладки остались бы на месте — то есть сведение ничего не убрало бы, а риск завело.

**И мера научилась отличать «не правило» от копии.** Семь имён из 23 — вообще не правила, а способ модуля дотянуться до соседнего модуля. Раньше они лежали в долге наравне с настоящими копиями и портили число; теперь печатаются отдельным списком, а вердикт по каждому выводится из формы и печатается прогоном.

**Главное: долг теперь сторожит проверка, а не память.** Список имён с причинами лежит в наборе, и новая копия роняет его в том файле, где её завели. Проверено поломкой: я завёл копию нарочно — проверка покраснела и назвала имя.

**Итог: 23 → 1.** Четыре имени были молчаливыми запасными ходами и сведены, одно сведено как точная копия, восемь разведены по именам, семь не объявляют правила вовсе, одно не сводится. По дороге ужесточение меры вскрыло ещё одну спрятанную копию — она тоже сведена.

**Разбор** — PRD 10.13.165, исключение к З3 № 119.


### 190у. Семь совпадений имён в настройках: шесть разведены, одно сведено — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Шесть имён обещали одно и то же, а отвечали на разное.** `say` значило и «подставить слова в сообщение человеку», и «спросить строку у файла текстов». `strings` — и «взять как есть», и «взять непустые без повторов». Такое лечится разными именами, а не общим телом: свести их значило бы поменять поведение там, где никто об этом не просил. Тела не тронуты, переименованы только имена.

**Седьмое было настоящей копией**, и увидеть её мешала сама мера: два места читали у темы одну и ту же пару цветов, строка в строку. Теперь пара читается в одном месте.

**Долг копий: 17 → 10.** Из оставшихся десяти семь — вообще не правила, а способ модуля дотянуться до соседнего модуля; вердикт по ним печатает прогон. Три последних живут в движках, и их я разбираю отдельно.

**Разбор** — PRD 10.13.164.


### 189у. Мера копий научилась спрашивать свойство, и это вскрыло спрятанную копию — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Два имени из списка долга оказались не копиями, а честными пересказами.** Они спрашивают ответ у общего правила и ничего не решают сами; мера считала их телами только потому, что написаны они чуть иначе, чем она умела читать.

**Шесть прежних признаков были образцами, и каждый ловил ровно ту запись, на которой его писали.** Седьмой спрашивает не «похоже ли написано», а **свойство**: из тела нет выхода, кроме ответа общего правила и громкого отказа. Любая своя работа до вопроса — хоть приведение строки — делает тело настоящим, и это нарочно.

**Контроль к новому признаку нашёл дыру, которой мера жила с первого дня.** Она сверяла только **начало** возврата — и место вида «спросить общее правило, а если оно ответило пусто, подставить своё» числилось честным пересказом. Своё умолчание рядом с вопросом — это и есть вторая копия правила, ровно то, что вы запретили 14 сентября.

**Ужесточение вскрыло спрятанную копию.** «Есть ли у этой строки знак списка» — вопрос, у которого **не было дома**: обе половины плагина считали ответ сами, одной и той же строкой, побайтно одинаковой. Теперь дом есть, оба места его спрашивают, и поведение не изменилось по построению.

**Долг копий: 19 → 17.** Ушли двое признанных пересказами и один сведённый, пришёл один — та самая спрятанная копия. Число снято прогоном.

**Разбор** — PRD 10.13.163, исключение к З3 № 118.


### 188у. Пять молчаливых запасных ходов в форме, которую сторож не видел — **сделано 2026-09-16**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Что оказалось.** Четыре имени из списка долга — не четыре разных правила, а **одно устройство, написанное иначе, чем умеет читать сторож**. Сторож стоит с 11 сентября и ловит запись в одну строку; те же четыре места написаны блоком в три строки, и для него их не существовало ни дня.

**Чего это могло стоить.** Одно из них знало тип поля, которого главное правило не возвращает вовсе — то есть половина плагина назвала бы элементом то, что другая половина зовёт тегом. Два других выдавали панели подряд все поля стороны, не спрашивая, какие из них вы видите на экране: стрелка ходила бы по полям, которых нет. Ни одно из этих мест сегодня не исполняется — и это проверено поломкой, а не чтением.

**Одно место пришлось проверять стендом, а не набором.** У списка полей соседней стороны первый прогон был зелёным по негодной причине: набор эту функцию не исполняет вовсе, и зелёный значил «сюда никто не заходил», а не «ветка недостижима». Исполняется она только при вашей настройке `Move to the next Block`, и до неё достаёт стенд панели. На нём проверка и сделана.

**Заодно снята лишняя обёртка** вокруг ответа общего правила у четырёх мест — она ничего не меняла, но заставляла меру считать пересказ телом. Что не меняла, измерено, а не предположено: 22 ключа вашего конфига, ноль расхождений.

**Долг копий: 23 → 19.** Число снято прогоном, и у расширения меры свой контроль: сдвинулись ровно те имена, которые я правил, и ни одного чужого.

**Разбор** — PRD 10.13.162, исключение к З3 № 117.


### 187у. Вердикт «это не правило» теперь печатает прогон, а не мой глаз — **сделано 2026-09-16**

**Нажимать нечего, кода плагина эта правка не касается.** Подтверждается словом.

**Зачем.** Вы просили разобрать все 23 имени долга. Пятнадцать из них числились разобранными **со слов прежних сессий**, а не по прогону, и когда инструмент напечатал их устройство, четыре оказались тем самым дефектом, который я снимал трижды накануне. Поэтому род имени теперь называет программа.

**Что она умеет сказать сама.** Семь имён из 23 — не правила, а доставалки: их тело только возвращает нужный кусок плагина и ничего не решает. Такой вердикт выводится из формы, и форму печатает инструмент. У него свой контроль: образец каждого рода обязан узнаться, а образец с правилом внутри — не узнаться ни одним, иначе инструмент объявил бы «не правило» о чём угодно.

**И найдена ошибка самой меры.** Она принимала описание возвращаемого значения за тело функции и объявляла расходящимися два места, у которых тела **совпадают до знака**. То есть число долга, которое я вам называл, было завышено, а одно имя числилось в нём зря.

**Разбор** — PRD 10.13.162.


### 186у. Остаток долга копий: пять имён, и одно из них молчало вместо отказа — **сделано 2026-09-15, ночь**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это четвёртая часть назначенного вами пункта 2** — строка 3 очереди, имена, объявленные в рантайме дважды.

**Три признака «какое это поле» сведены.** Ядро панели спрашивало у общего правила **вид** поля, а решение «какие виды считаются ссылочными» держало своей копией. Пока список видов один, разницы нет; добавься новый — и панель осталась бы со вчерашним ответом. Теперь она спрашивает готовый ответ.

**Одно место молчало там, где должно падать.** Правило «оживлять ли сырое значение элемента» спрашивало общий дом, а на случай «дома нет» отвечало «нет». Дома не бывает нет — он приезжает жёстко, — но если бы такое случилось, ваше значение молча исчезло бы со строки. Теперь здесь громкий отказ, как у соседнего правила в том же файле.

**Два места отвечали вместо реестра полей.** Оба спрашивали его и, «если реестра нет», отвечали сами — то есть держали вторую копию правила про ключ дочернего поля. Ни одна из этих веток не исполняется, и это проверено пробоем, а не рассуждением.

**И одна проверка охраняла мёртвую строку.** Она требовала, чтобы в исходнике **стояла** определённая строка, — а строка эта не исполнялась никогда. Такая проверка зелена именно потому, что смотрит на текст, а не на поведение. Заменена вопросом к самому правилу: `Imp_sub` сворачивается в `Imp`, `Imp` остаётся собой, пробелы не мешают, пустое остаётся пустым.

**Долг копий за сессию: 28 → 23.** Число снято прогоном, и у него есть контроль: из списка ушли ровно те имена, которые я свёл, и не пришло ни одного.

**Разбор** — PRD 10.13.161, исключение к З3 № 116.


### 185у. Нормализатор ключа Order: цепочка разобрана, два последних литерала сняты — **сделано 2026-09-15, ночь**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это третья часть назначенного вами пункта 2** — раздел 1д очереди.

**Что там было.** Правило «как приводится к порядку ключ поля» едет к движкам через пять слоёв, и на каждом стоит «дали — бери данное, иначе моё». Разобрал: **это тождество**. После сведения копий в сентябре каждое «иначе моё» на каждом слое — обращение к одному и тому же общему правилу. То есть довод, который едет через пять слоёв, ни на что не влияет: какой бы веткой ни пошло, ответ один.

**Два литерала того же тела всё же оставались** — в том месте, где панель заводит общий рантайм. Сняты: теперь и они спрашивают общее правило.

**И сторож, который их пропускал, чинён.** Он искал своё тело **по имени функции**, а эти два стояли безымянными. Это тот же класс, что уже стоил дефекта: запрет, написанный по имени, слеп к безымянному. Теперь он узнаёт предмет иначе, и мутация это подтвердила: вернул литерал — покраснел и назвал файл.

**Что не сделано, и это ваше решение.** Сократить саму цепочку — убрать довод и оставить один вызов общего правила — это девять файлов, три из которых в движках, которые вы сейчас проверяете. Поведение при этом не изменится **ни на одном входе**: сегодня все ветки ведут в одно место. То есть работа чисто на читаемость кода. Я её не делал: трогать девять файлов рантайма ради читаемости, пока вы проверяете сборку, — не то, о чём вы просили.

**Разбор** — PRD 10.13.160, исключение к З3 № 115.


### 184у. Четыре имени долга копий: одно сведено, три названы числом — **сделано 2026-09-15, ночь**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Это вторая половина назначенного вами пункта 2** — раздел 1е очереди, четыре коротких имени.

**Одно оказалось уже сделанным.** `reportLoaderFallback` сведён ещё днём, строка очереди просто устарела.

**Одно сведено — «во сколько единиц смещения это значение».** Это правило решает, какое значение даты встанет следующим, когда вы нажимаете стрелку или команду поля. Оно было объявлено дважды: у движка дат и у ядра панели. Алгоритм одинаковый, а считают они в разных основаниях времени — один по Гринвичу, другой по часам вашей машины. Внутри каждой дороги пара «прибавить» и «напечатать» согласована, поэтому ответы совпадают.

**Измерено до правки, а не после:** 355 пар «значение × формат» — ваши форматы, ваши значения из заметок, значения у границы суток и то, что плагин пишет сам. Расхождений ноль. Сверх того обе стороны спрошены в **пяти часовых поясах**, включая `+14` и `−11`: тоже ноль. Сведён алгоритм; основание времени осталось у каждой дороги своё — приводить его к одному значило бы менять поведение там, где расхождения нет.

**И тут же заведена проверка, которой раньше быть не могло.** Пока правило объявлено дважды, сверка «панель против команды» его стережёт; сведённое — уже нет: сломанный общий дом ломает обе дороги одинаково, и сверка остаётся зелёной. Поэтому добавлено ожидание **правильного ответа**: сегодняшнее значение — ноль, завтрашнее — единица, вчерашнее — «не выражается». Часовой пояс проверка задаёт сама.

**Два имени сведены не будут, и вот числа.**

**`resolveFieldIdByOrderKey`** — «какое поле отвечает этому ключу». 19 ключей, расхождений **2**, и оба на ключах, которых в вашем конфиге нет: у движка тегов есть запасные ходы, которых у движка дат нет, и куплены они прежними дефектами. Это два разных вопроса под одним именем — четвёртый такой случай за трое суток.

**`applyPkmOptionKeys`** — не правило вовсе: три места переписывают имена ключей настроек в свои переменные, и наборы у них разные (пять имён, восемь и пятнадцать). Сводить нечего.

**Одно наблюдение, которое стоит знать.** На ключе, которого в вашем конфиге нет, движок тегов отвечает **каким-то** полем, а не пустотой. Сегодня это никому не видно — ключи приезжают из вашего же порядка полей, — но это ровно тот род догадки, который однажды уже стоил дефекта. Записано строкой очереди, не починено: предмета на экране нет.

**Долг копий: 28 → 27.** Число упало от сведения, а не от починки меры — это проверено отдельно.

**Разбор** — PRD 10.13.159, исключение к З3 № 114.


### 180у. Плагин не мог прочесть то, что сам написал — **сделано 2026-09-15**

**Нажимать нечего, но дефект настоящий.** Подтверждается словом.

**Что было.** После второго нажатия на строке не остаётся ничего, кроме знака списка, и плагин пишет `- ` — пустой пункт списка. А разобрать эту строку и собрать обратно давало `-`, без пробела. Разница в один пробел, и глазом её не видно.

**Почему это не мелочь.** У Obsidian знак списка — это знак **и пробел за ним**. Без пробела строка перестаёт быть пунктом списка вовсе, это обычный текст. То есть следующая пересборка убрала бы вам буллит со строки.

**И стенд тут же нашёл вторую форму того же.** На строке-заголовке ровно то же: команда пишет `# `, а обратно это читается как `#`. Обход этого не показывал потому, что заголовка **без текста** в его наборе не было — все прежние шли с текстом. Форма дописана в обход тем же заходом, и починка накрывает оба знака.

**Чем закреплено.** Семь новых строк в свойстве «то, что плагин написал, он обязан уметь прочесть»: три вида буллита, нумерованный, с задачей, с отступом и внутри цитаты. Мутация: снял починку — покраснело и свойство, и обход строки.

**И дописанная форма сразу показала следующее, к этой починке отношения не имеющее.** На строке-заголовке без текста панель оставляет висящий разделитель (`# #work #aaa :: `), а команда не оставляет — та самая разница «панель и команда дают разное», с которой вы приходили. Канонична здесь панель: пересборка строки, написанной командой, даёт панельный вариант.

**Это не сегодняшнее и не моё** — проверил рабочим деревом на состоянии до этой сессии с сегодняшним обходом: там таких расхождений было 5 и 12, сегодня 4 и 6. То есть работа дня их только уменьшила. Прятать я их не стал: обход краснеет, и они записаны в очередь отдельной строкой.

**Разбор** — PRD 10.13.155, исключение к З3 № 110.

### 179у. Последнее имя из долга копий: сводить оказалось нечего — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Что это было.** Последнее из восьми настоящих имён долга — шаг по циклу значений, тот самый, что решает, какое значение встанет следующим при нажатии команды. Он был объявлен дважды, и в записях стояло, что расходится «с первой строки».

**Расходится — да, но не по смыслу.** Я измерил: 24 пары из 156. Оба расхождения — от формы входа: одна дорога принимает список значений поля и булево «вперёд», другая — список готовых токенов и слово. Одна обрезает пробелы вокруг текущего значения, другая нет; одна считает пустую запись значением, другая её отбрасывает.

**И ни одно из 24 в плагине не достижимо** — это проверено у источников, а не предположено: туда, где пустое могло бы навредить, оно не доезжает, потому что обе сборки цикла отбрасывают его сами, а текущее значение приезжает совпадением в строке, то есть уже без пробелов.

**Одна вещь из этого стоит того, чтобы её знать.** Пустые записи в списках значений у вас **есть** — у полей `date_due`, `Random` и `test2` значением стоит голая пустая строка вместо записи со значением. Сегодня это ни на что не влияет. Я не стал ничего с этим делать: это ваш конфиг, а не дефект кода, и трогать его без вашего слова я не буду.

**Вывод: это не копия, а два вопроса под одним именем** — третий такой случай за двое суток. Имена разведены, тела не тронуты.

**Чем закреплено.** Здесь возможен только сторож по форме: обе функции внутренние, позвать их проверкой неоткуда. Он требует, чтобы каждое имя стояло в своём файле. Мутация: вернул прежнее общее имя — покраснел.

**Разбор** — PRD 10.13.154, исключение к З3 № 109.

### 178у. Одно имя на два вопроса: знак задачи — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Ваш вчерашний ответ исполнен до конца.** Вы сказали: «пользователь может вводить как угодно, а плагин сам нормализует ввод». Из этого следовало, что два объявления, которые я записал в долг как копию, копией не являются — это два разных вопроса. Один принимает то, что напечатал человек в колонке `Prefix`, и снимает знак списка; второй принимает уже разобранный знак, и снимать ему нечего. Теперь у них разные имена, и по имени видно, кто на какой вопрос отвечает.

**Почему это стоило захода, хотя поведения не меняет.** Одно имя на два вопроса и было причиной, по которой я принял их за копию и потратил на разбор целый заход — а потом задал вам вопрос, который вы не поняли, потому что я назвал внутреннее имя вместо экрана. Пока имя одно, следующий человек — или я в следующей сессии — сделает ту же ошибку и на этот раз, возможно, сведёт тела.

**Чем закреплено.** Написан ответ каждого из двух, а не их равенство: на разобранном знаке они совпадают, на вводе человека — нет, и так и должно быть. Рядом стоит запрет: прежнее имя не должно вернуться. Проверено мутацией — вернул старое имя, запрет покраснел.

**Под запрет на правку движков это попало одной строкой комментария:** он говорил, что вопрос отдан вам, а вы на него ответили. Кода там не тронуто, и записано это отдельной строкой нарочно.

**Разбор** — PRD 10.13.153, исключение к З3 № 108.

### 177у. Склейка приставки со значением: четыре правила стали одним — **сделано 2026-09-15**

**Нажимать нечего, на экране не меняется ничего.** Подтверждается словом.

**Ваше решение исполнено.** Вы выбрали «`#todo`, лишнее убирать в самом правиле». Правило «как приставка поля склеивается со значением» было записано в плагине четырьмя разными способами; теперь оно записано один раз, и защита от удвоенной решётки живёт в нём самом, а не в шести отдельных заслонках у тех, кто его зовёт.

**Что я измерил до правки, а не после.** 136 пар «ваша приставка × ваше значение», из них 73 расхождения между четырьмя телами. Удвоенную решётку давали три тела из четырёх — каждое на 17 парах. После сведения: одно объявление, удвоенная приставка — **ноль пар**. Мера теперь живёт в стенде и печатает это число сама, а не в моём отчёте, где оно устареет.

**И у сведения есть цена, которую называю я, а не вы её обнаружите.** Самое полное из четырёх тел пропускало насквозь **любое** значение, начинающееся с решётки, — то есть у поля с приставкой, отличной от решётки, часть значений молча теряла бы её. Дом так не делает: приставка применяется. У вас приставка одна, решётка, и этот край для вас неразличим — но он назван отдельной строкой в проверках и отдельным входом в стенде, а не спрятан.

**Что не изменилось — это проверено, а не обещано.** Обход строки обеими дорогами до и после правки: 171 сочетание, расхождений ноль; повторное нажатие — те же шесть известных расхождений, ни одного нового. Иначе и быть не могло, и я знал это до правки: все шесть мест вызова закрыты ранними возвратами.

**Мутация в обе стороны, и вторая дала находку.** Ломаю дом — краснеет ровно то утверждение, ради которого оно написано (`#todo` против `##todo`), и три файла набора. Возвращаю в панель её прежнее тело — краснеет **только** обход по форме, а 69 файлов из 70 остаются зелёными. То есть проверки поведения возврата копии не видят вовсе, и держится это место вторым сторожем, а не первым.

**Разбор** — PRD 10.13.152, исключение к З3 № 107.

### 176у. В-118 отвечен, и ответ стал правилом — **сделано 2026-09-15**

**Нажимать нечего, код не тронут.** Ваш ответ записан и в правила, и в разборы.

**Вы ответили:** «пользователь может вводить как угодно (`- [x]` / `[x]`), а плагин сам нормализует ввод для корректной работы на `[x]`» — по аналогии с `add value`, где принимается и `#tag`, и `tag`.

**Что это меняет.** Нынешнее поведение панели верно и остаётся. А два объявления, которые я считал копией, оказались **двумя разными вопросами**: одно стоит на пути текстового поля `Prefix` и обязано быть терпимым, второе принимает уже разобранный токен, и терпимость ему не нужна. Сводить их нельзя — это третий такой случай за двое суток, и лечится он разведением имён, а не сведением тел. Остался один заход, поведения не меняющий.

**И я исправил собственную ошибку в разборе.** Я написал, что в эту настройку значение приходит из сохранённого файла, а не из вашего ввода. Неверно: туда приходит ровно то, что вы печатаете руками. Ошибка вскрылась оттого, что вы не поняли мой вопрос и переспросили — я назвал внутреннее имя вместо того, что видно на экране.

**Разбор** — PRD 10.13.144, правило 117.

### 175у. У обхода строки появилась вторая половина, и она сразу нашла шесть — **сделано 2026-09-15**

**Нажимать нечего, код продукта не тронут.** Подтверждается словом.

**Чего не хватало обходу.** Он делал **одно** нажатие на каждый случай. Вы нажимаете подряд — и это не «то же самое, только дважды»: с пустого значения плагин пишет «сейчас» и ничего не разбирает, а по уже стоящему сперва **узнаёт** его и перебором ищет, на сколько шагнуть. Это другой код, и обход в него не заходил ни разу.

**Первый же прогон нашёл шесть расхождений из 154 пар.** Все шесть — одно и то же, и не «панель против команды»: обе дороги отвечают одинаково. Краснеет другое: после второго нажатия строка становится `- ` (знак списка и пробел), а плагин, разобрав её и собрав заново, даёт `-` — пробел теряется. Накопления нет, глазу не видно — обе формы рисуются пустым пунктом списка. Но правило «написанное плагином он обязан уметь прочесть» нарушено, и я это не прячу.

**Что новая половина ловит, чего не ловил набор.** Я сдвинул шаг по дате на целые сутки: новая половина покраснела и показала разницу днём прямо в строке, а **все 70 проверок остались зелёными**. То есть шаг по уже стоящей дате не был покрыт ничем.

**И один старый вопрос теперь измерен.** У команды арифметика дат гринвичская, у панели местная — разойтись им было на чём. Прогнал в четырёх часовых поясах, включая ваш и крайние (UTC+14 и UTC−11): шагают одинаково. Это измерение, а не догадка.

**Где я ошибся по дороге, говорю сам.** Три подмены подряд не били в предмет, и на первой я успел сделать неверный вывод — «шаг панели не покрыт ничем», — и снял его следующим же опытом. Причина поучительная: панель и ищет смещение, и рисует значение **одной и той же** функцией, поэтому ошибка внутри неё гасится сама, и никакое сравнение двух дорог её не увидит.

**Семь шагов из семи зелёные.**

**Разбор** — PRD 10.13.151, урок У-196.

### 174у. Копия, которая сама себя объявляла, и комментарий, который врал пять дней — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось.** Подтверждается словом.

**Первое.** В плагине была копия, про которую в самом коде было написано: «одно правило в двух местах, и сводить их — отдельная правка, не эта». Написано 10 сентября. Эта правка и есть та отдельная: тела совпадали побайтно, теперь объявление одно.

**Второе, и оно важнее.** Рядом в том же файле стояло утверждение: «у TagWheel своя нормализация ключа, и она сильнее общей». **Своей у него не было.** Я проверил все шесть объявлений этого правила — они побайтно одинаковы. То есть код всё это время выбирал между двумя неразличимыми вещами, а комментарий описывал это как решение.

Прожило оно пять дней и держало у меня в голове картину, которой код не соответствовал. Утверждение исправлено на то, что правда: побеждает тот, кто позвал первым, и пока тело одно — это ни на что не влияет.

**Чем закреплено.** Две проверки, и они про разное: сам общий дом проверяется **поведением** (молчит при выключенном флаге отладки, пишет место и причину при включённом), а два места, которые его теперь спрашивают, — **формой**, потому что поведением до них не дотянуться: след пишется только при включённом флаге, а его не включает ни один прогон. Мутация в обе стороны.

**Семь шагов из семи зелёные.** Долг копий: 31 → 30.

**Разбор** — PRD 10.13.150, исключение к З3 № 106.

### 173у. Что такое «31»: остаток долга разобран поимённо — **сделано 2026-09-15**

**Нажимать нечего, продукт не тронут.** Подтверждается словом.

**Я называл вам число, не зная, что за ним стоит.** Поэтому прочитал все 31 имя из списка копий — у каждого напечатал первые строки каждого тела — и отнёс к своему роду. Обход лежит в репозитории: `node tools/rule_copy_shapes.js`.

**Больше половины списка — вообще не правила.** Пять «имён-копий» это одна строка `return модуль` — способ дотянуться до соседнего файла. Ещё два — «собрать один раз и запомнить». Два — переходники: собирают зависимости и зовут общий код, а само правило одно. Четыре — честные делегаты, которых моя мера не узнаёт. Три — одно и то же, записанное разными словами. И семь — **просто совпадение имён**: разные вопросы, случайно названные одинаково; их лечит переименование, а не сведение.

**Работы по существу — на восемь имён**, и три из них я этой ночью уже разобрал: знак задачи (вопрос В-118), «похоже ли на дату» (оказалось не копией — вопрос шире) и склейка приставки (измерена, не сведена, риск записан). Остаются четыре коротких кандидата на сведение и одно имя, которое расходится с первой строки и требует разбора по существу.

**Вывод, который стоит сказать прямо.** Считать «имена, объявленные дважды» проще, чем «правила, объявленные дважды», и разница между этими двумя вещами и есть цена дешёвой меры. Список не бесполезен — он **указатель, куда смотреть**. Но числом долга он не является, и я больше не буду называть его вам так.

**Разбор с таблицами** — PRD 10.13.149, очередь — `docs/dev/REMAINING_WORK.md`, раздел 1я — указатель закрытого.

### 172у. Мера призналась: она видит четыре пятых кода — **сделано 2026-09-15**

**Нажимать нечего, продукт не тронут.** Это опять про меру, которой я вам называю числа. Подтверждается словом.

**Сначала третья за ночь починка признака.** Оказалось, есть ещё одна форма записи «спросить у общего дома», которой мера не знала, — через вызов: `__relocation().имя(...)`. Долг: 34 → 31, ушли три имени, не пришло ни одного.

**А потом обнаружилось кое-что важнее любой из этих форм.** Из списка копий ушло имя `composeToken` — то самое, у которого я за час до этого намерил **четыре разных тела**. Противоречия нет: моя мера считает только объявления **верхнего уровня**. Функция, объявленная внутри другой функции, для неё не существует.

**Сколько это в числах:** видно **1215 объявлений из 1520**. Вне поля зрения — 305.

**И самые крупные находки этой ночи жили именно в невидимой пятой части.** Третье объявление правила «тег это или ссылка» было вложенной функцией. Два тела из четырёх у склейки приставки — тоже вложенные. Оба раза я нашёл их руками, а мера молчала.

**Теперь она говорит это сама, каждым прогоном.** Строка про 1215 из 1520 печатается рядом с числом долга, чтобы это число нельзя было прочесть как «столько копий и есть».

**Расширять меру на вложенные объявления я не стал, и говорю почему.** Два локальных помощника с одинаковым именем внутри разных функций — законное совпадение, а не копия правила. Считать их копиями значило бы выдать вам длинный список ложных находок. Сначала нужен признак «это одно и то же правило», а не «одинаковое имя»; пока его нет, честнее знать границу меры, чем раздуть число.

**Разбор** — PRD 10.13.148.

### 171у. Склейка приставки со значением: четыре разных ответа, и почему вы их не видите — **сделано 2026-09-15**

**Нажимать нечего, код продукта не тронут.** Добавлена одна проверка. Подтверждается словом.

**Что измерено.** Правило «как приставка поля склеивается со значением» — то, что решает, каким значение встанет в строку, — объявлено **четыре раза**, и все четыре тела разные. Я сверил их на 96 сочетаниях ваших приставок и ваших значений: **56 расхождений**, и ни одна пара не совпадает целиком.

**И расходятся они на ваших данных.** Ваши значения хранятся с решёткой (`#todo`, `#/1`), приставка поля — `#`. Три тела из четырёх на такой паре дают **`##todo`** — удвоенную решётку.

**Почему на экране вы этого не видите.** Охрана «это уже готовый тег, трогать не надо» стоит **не в самом правиле, а у тех, кто его зовёт** — ранним возвратом. Я проверил все шесть мест вызова: три закрыты таким возвратом, два кормят список кандидатов, который идёт только на сравнение с тем, что уже стоит в строке (лишний `##todo` там просто ни с чем не совпадёт), а шестое достижимо лишь при пустом значении.

**То есть сегодня всё цело — но держится это на шести чужих ранних возвратах, а не на самом правиле.** Появится седьмое место вызова, где про возврат забудут, — и вы увидите `##todo` в строке.

**Что я сделал.** Сводить не стал: у четырёх тел разные ответы, и выбрать за вас, какой верен на каждом крае, — это правка поведения, о которой вы не просили. Вместо этого закрепил **свойство, которое вас бережёт**: готовое значение проходит насквозь, решётка не удваивается. У одного движка это уже было закреплено, у панели — не было ничем. Проверил мутацией: снимаю у панели ранний возврат — проверка краснеет с `#todo` против `##todo`.

**Записано в очередь** как то, что ждёт вашего решения, а не моей работы — `docs/dev/REMAINING_WORK.md`, раздел 2 — то, что ждёт вашего решения.

**Разбор** — PRD 10.13.147.

### 170у. Шесть одинаковых тел под тремя именами, и расхождение, которое не воспроизводится — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Что нашлось.** Правило «как приводится к порядку ключ поля» было объявлено в плагине **шесть раз** — и под тремя разными именами, поэтому моя мера копий видела только четыре из шести. Остальные нашлись поиском **по телу**, а не по имени.

**Но интереснее не копии, а то, как они друг с другом связаны.** Эта функция передаётся по цепочке из пяти слоёв, и на каждом стоит «если тебе дали функцию — бери данную, иначе бери мою». Последний слой запоминает результат глобально: **побеждает тот, кто загрузился первым**.

Пока все шесть тел одинаковы, вся эта машинерия выбирает между неразличимым и ни на что не влияет. Но разойдись хоть одно — и поведение плагина стало бы зависеть от **порядка загрузки движков**. Это худший род дефекта из всех: он не воспроизводится. У вас бы это выглядело как «иногда работает, иногда нет», и я бы неделю искал причину не там.

**Сведение делает вопрос «кто загрузился первым» бессмысленным** — тело теперь одно.

**Чего я НЕ стал делать.** Саму пятислойную передачу я не разбирал. Это правка в пяти файлах, и вы просили уборку без риска дефектов — распутывание такой цепочки ровно то место, где риск живёт. Копии сняты, механизм описан; разбирать его — отдельная работа с отдельным решением.

**Чем закреплено.** Шесть ожиданий правильного ответа и сплошной обход всего кода плагина, который краснеет, если у кого-то снова заведётся своё тело. Мутация в обе стороны: поломка общего дома разводит обход строки на **154 сочетаниях из 171**.

**Семь шагов из семи зелёные.**

**Разбор** — PRD 10.13.146, исключение к З3 № 105.

### 169у. Склейка начала строки: копия снята, и мутация показала, чего не ловит поведение — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом.

**Что сведено.** Правило «начало строки и её тело склеиваются одним пробелом» было объявлено дважды: общее — в разборе строки, и приватная копия внутри доводки строки. Тела совпадали слово в слово, и я всё равно их сверил: **113 490 сочетаний** начала и тела, собранных из ваших заметок, из всех строковых значений вашего файла настроек и из тринадцати краёв. Расхождений ноль.

**Копия снята.** Долг копий: 36 → 35, и на этот раз это настоящее сведение, а не починка меры.

**Главное в этом заходе — не правка, а то, что показала проверка правки.** Я ломаю сведённое с двух сторон: сперва общий дом, потом возвращаю копию. Первое поймалось сразу. **Второе не поймал никто** — 70 проверок из 70 остались зелёными.

**Почему.** Ожидания, которые я написал, зовут общий дом напрямую и внутрь доводки строки не заходят. А край, на котором копия расходилась, туда через обычные вызовы просто не доезжает.

**Край при этом видно глазом:** он даёт `- ` вместо `-`, то есть хвостовой пробел после знака списка на пустой строке. Ровно тот род мелочи, с которой вы приходите.

**Поэтому проверок теперь две, и каждая ловит своё:** семь ожиданий правильного ответа — поломку общего дома, и отдельная проверка формы — возврат копии, до которого поведением не дотянуться. Вторая заведена **после** мутации, которая показала дыру, а не вместо неё.

**Семь шагов из семи зелёные.**

**Разбор** — PRD 10.13.145, исключение к З3 № 104.

### 168у. Одно имя на два ответа: нормализация знака задачи — **измерено, не сведено, вопрос вам** — **сделано 2026-09-15**

**Нажимать нечего, поведение не тронуто ни на одном входе.** Здесь есть вопрос к вам — В-118, он же в `docs/dev/OPEN_QUESTIONS.md`.

**Что нашлось.** Правило «как выглядит знак задачи у значения» объявлено в плагине дважды, и оба объявления носят **один и тот же комментарий** о том, что знак ровно один. Но отвечают они по-разному: слой настроек снимает знак списка перед разбором, сборка строки не снимает. На записи `- [x]` один говорит `[x]`, другой — пусто.

**Измерено, а не прикинуто:** 31 вход, восемь расхождений, и шесть из восьми — начала строк **ваших** заметок.

**Почему я это не свёл, хотя мог.** Оба ответа выглядят разумно, и выбрать за вас я не готов: сведение к строгому меняет поведение панели, сведение к снисходительному делает наше правило о чужой разметке **шире платформенного** — а именно этим плагин однажды съел ваш текст в записи `- [test-transform] text`. Вы просили уборку без риска дефектов; тихо выбрать здесь один ответ — это и есть риск.

**Что сделано вместо.** Расхождение записано в оба места в коде, и оба сегодняшних ответа закреплены проверками. Смысл не в том, чтобы «заморозить», а в том, чтобы будущее сведение стало **осознанным**: попытка привести их к одному теперь краснеет и требует решения.

**На вашем сегодняшнем конфиге это не срабатывает** — там лежат обычные записи вроде `[x]`, и на них оба объявления отвечают одинаково.

**Разбор и три варианта с последствиями** — PRD 10.13.144 и `docs/dev/OPEN_QUESTIONS.md`, В-118.

### 167у. Мёртвая ветка, которая читалась как живая: вторая копия правила о метке даты — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось.** Подтверждается словом.

**Что нашлось.** В ядре TagWheel вопрос «это похоже на дату» устроен так: сперва спросить общий дом про метки, а если дом не отвечает — посчитать самому. Второй ход не выполняется **никогда**: общий дом подключён напрямую, и не будь его, не загрузился бы и сам файл. А в этом мёртвом ходе лежала вторая копия правила о том, как выглядит метка даты.

**Почему это не «лишние байты».** Копия правила о чужой разметке, лежащая в мёртвом коде, читается как живая — и мной, и моими же мерами. Ровно такие копии трижды стоили вам потерянного текста: две расходятся молча, и находится это только когда вы приходите с письмом.

**Недостижимость я доказал, а не предположил.** Поставил в мёртвую ветку отказ — не покраснело ничего: ни одна из 70 проверок, ни одно из 171 сочетания обхода строки. Затем поставил такой же отказ в **рабочую** ветку — покраснели три проверки. Второй опыт и есть то, без чего первый ничего не значит: он говорит, что функция вообще работает, а не просто не вызывается.

**Что стало вместо мёртвого хода.** Громкий отказ. Если общий дом однажды перестанет отдавать это правило, плагин скажет об этом вслух, а не отработает половиной молча. Проверено: снял функцию из дома — падают пять проверок с этим самым сообщением; до правки они бы прошли тихо.

**Семь шагов из семи зелёные.**

**Разбор** — PRD 10.13.143, исключение к З3 № 103.

### 166у. Долг копий упал с 51 до 36, и **ни одной строки кода это не тронуло** — **сделано 2026-09-15**

**Нажимать нечего, продукт не изменён вовсе.** Это про меру, которой я вам называю числа. Подтверждается словом.

**Число было неверным, и вот насколько.** Мера копий считала, что в плагине 51 имя объявлено дважды и более. На самом деле их 36. Пятнадцать «копий» были честными делегатами — кодом, который просто спрашивает общий дом, — а мера их такими не признавала.

**Это второй раз за сутки и третий за двое.** Признак «это делегат» был написан по одной форме обращения к дому; накануне его чинили, найдя вторую; этой ночью нашлись **ещё две**. Причём последняя — вся семья разбора форматов даты и числа, сведённая в общий дом ещё 11 сентября: всё это время мера называла копиями то, что сама же запись объявляла сведённым.

**Как это поймалось.** Не чтением. Я свёл одно правило к единственному объявлению, прогнал меру — и она не сдвинулась. Свели, а долг тот же, значит врёт мера, а не код. Этот приём я записал правилом, и он же сработал второй раз через час.

**Контроль стоял до того, как я поверил числу**, и в двух видах: список имён (имя может только **уходить** из копий, прийти не может ни одно) и счёт тел по каждому имени отдельно — измениться он обязан ровно у тех, кого я свёл. Оба раза контроль сошёлся.

**И я проверил руками, что ушедшие — и правда делегаты**, а не то, что мера решила не замечать. Два подозрительных случая прочитал целиком: у одного дом называется другим именем, у другого при пропаже дома код молча отвечает «нет» вместо отказа — второй остался в списке копий, и правильно остался.

**Что это значит для вас.** Все числа про «долг копий», которые я называл вам в последние дни, были завышены. Нынешнее — 36 — снято починенной мерой, и у неё теперь четыре известные формы делегирования вместо одной.

**Разбор** — PRD 10.13.142, урок У-193.

### 165у. Форма ссылки: девять рукописных мест, шесть сведены, и больше новое завести нельзя — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Подтверждается словом. Это конец первой строки очереди — той самой, про которую было написано «остаток форм ссылки и тега».

**Строка очереди оказалась верной наполовину.** В ней сказано, что это не копии, а другие вопросы. Из девяти рукописных мест **шесть были буквально одним и тем же**, и только одно расходится по-настоящему.

**Как это доказано — не примерами, а текстом.** Для шести мест сравнивалось не «одинаково ли работает на наших входах», а **само выражение**: то, что получается после подстановки общего дома, совпало с прежним **до знака**. Это сильнее любого набора примеров: там нечему разойтись. У сверки был свой контроль — нарочно другое выражение она называет расходящимся.

**Одно место не сведено, и причина названа.** `editor_visuals_config.js` — тот, что раскрашивает строку в заметке, — не считает ссылкой запись `[[a[b]]`, а общий дом считает. Я не знаю, как на это смотрит сам Obsidian, и гадать про чужую разметку в этом проекте запрещено: за такую догадку вы однажды заплатили потерянным текстом. Расхождение измерено (2 строки из 77) и оставлено как есть.

**Главное в этом заходе — не правка, а запрет.** Сведя места, я спросил не «нет ли ещё копии», а что помешает завести новую завтра. Ответ был «ничего». Теперь весь код плагина обходится сплошь, и рукописная запись формы ссылки **роняет проверку**; разрешённых адресов ровно два, и оба названы поимённо.

**И обход сам себя поймал.** Форма записывается в коде двумя способами, а первая версия обхода искала только один — то есть сказала бы «рукописных форм нет», не найдя ни одной вообще. Спас положительный контроль: обход обязан находить форму хотя бы в общем доме, и он честно покраснел.

**Чем закреплено.** Сужение формы в доме роняет **10 проверок из 70** и разводит обход строки на 18 сочетаниях — значит все переведённые места и правда спрашивают дом. Мутация проверена в обе стороны, семь шагов из семи зелёные.

**Разбор** — PRD 10.13.141, исключение к З3 № 102.

### 164у. Пять мест «снять скобки» оказались не копиями: сведено одно, четыре названы числом — **сделано 2026-09-15**

**Нажимать нечего, на экране не меняется ничего.** Подтверждается словом.

**В очереди это стояло одной строкой, как будто там копии.** Пять мест панели настроек снимают у значения решётку и скобки ссылки. Я их измерил, а не свёл на глаз: 94 входа — все значения вашего файла настроек, все токены ваших заметок и восемнадцать краёв.

**Копиями оказались два из пяти.** Их я и свёл — расхождений ноль на всех 94 входах.

**Остальные три расходятся по-настоящему, и я их не трогал.** Самое важное расхождение: одно из мест иначе отвечает на `[[#heading]]` — это ссылка на заголовок, живая запись Obsidian, а не выдумка для проверки. Одно место отдаёт `#heading`, другое `heading`. Свести их — значит решить за вас, какой ответ правильный, и поменять поведение, о котором вы не просили. Два других расходятся на пробелах по краям значения.

**Что здесь закреплено навсегда — порядок двух шагов.** Решётка снимается раньше скобок. На записи `#[[a]]` это даёт `a`, а обратный порядок дал бы `[[a` — то есть мусор в панели. Порядок я взял у существующего кода, а не выбрал заново.

**И заход поймал мой собственный стенд на лжи.** Проверяя правку мутацией, я вернул снятую копию обратно — и стенд этого **не заметил**: список мест в нём был написан моей рукой, а рукописный список слеп к тому, что заведут завтра. Теперь панель настроек обходится сплошь, и место, которого нет в списке, роняет прогон.

**Семь шагов из семи зелёные**, мутация проверена в обе стороны.

**Разбор** — PRD 10.13.140.

### 163у. «Тег это или ссылка» было объявлено трижды, и одно объявление набор звал именем, которого нет — **сделано 2026-09-15**

**Нажимать нечего, поведение не изменилось ни на одном входе.** Это чистая уборка; подтверждается словом.

**Как нашлось.** Починив правило из отчёта `162у`, я спросил не «есть ли где-то копия», а «кто ещё отвечает на этот же вопрос». Рядом стояло второе правило — каким видом печатается поле, тегом или ссылкой, — и объявлено оно было в трёх местах: у панели, у ядра и внутри самих помощников.

**И третье объявление было названо неверно.** В проверках стояло объяснение со ссылкой на функцию `resolveOutputKind` — такой функции в репозитории нет ни одной. То есть объяснение, купленное когда-то дефектом, указывало в пустоту, а настоящее третье объявление не считал никто.

**Сведение сделано только потому, что расхождений измерено ноль.** Все три тела прогнаны на 150 сочетаниях «поле × правила»: 23 поля — все, какие есть в вашем файле настроек, плюс десять форм, которых у вас нет, — на шести наборах правил. Ответы совпали везде. У самой меры стоял контроль: нарочно испорченная сторона расходится на всех 150 сочетаниях, значит ноль получен не слепотой.

**Тело общего дома взято побайтно у одного из трёх**, а не написано заново «как правильнее». Переписанное было бы четвёртым телом, которого никто не мерил, — и именно так в прошлом появлялись расхождения, которые вы потом находили глазами.

**Чем закреплено, и в чём слабость.** Шесть ожиданий написаны **ответом**, а не сравнением сторон — и это не придирка: после сведения все три дороги спрашивают один дом и ломаются одинаково, поэтому сверка «панель равна команде» зеленела бы сама. Проверено это мутацией: на сломанном доме обход строки остался зелёным, а покраснели именно ожидания ответа. Мутация сделана в обе стороны, семь шагов из семи зелёные.

**Что из этого стоит запомнить нам обоим:** чем больше копий сведено, тем слабее становится сверка двух дорог, и тем больше веса переходит на проверки, которые знают правильный ответ, а не только равенство.

**Разбор** — PRD 10.13.139, исключение к З3 № 101.

### 162у. Правило о значении в строке было объявлено трижды, и третье объявление собирало сломанную ссылку — **сделано 2026-09-15**

**Нажимать нечего, и на вашем нынешнем файле настроек не меняется ничего.** Это уборка кода; подтверждается словом.

**Вы сказали «уборку кода, но без риска дефектов» — и это поменяло приём.** Сводить на глаз я перестал: сперва мера, которая говорит, где два куска кода отвечают одинаково, а где нет, и сводится только то, у чего расхождений ноль. Мера лежит в репозитории, `node tools/form_divergence.js`, и у неё три своих контроля — иначе «ноль расхождений» бывает нулём от слепоты.

**Первое же измерение нашло не уборку, а расхождение.** Правило «как значение поля выглядит в строке» — то самое, которое в прошлую сессию было сведено, — оказалось объявлено не два раза, а **три**. Третье держала сама панель TagWheel, и его не сверял никто.

**Чем они расходились.** У панели цель ссылки разворачивается, у ядра бралась как есть. Если у значения есть своя подпись со скобками, ядро собирало `[[[[X]]]]` там, где панель собирала `[[X]]` — то есть сломанную ссылку вместо целой. Числа сняты вызовом **обоих настоящих движков**, а не чтением кода: из пяти первых входов расходились четыре.

**Досягаемость называю честно, без приукрашивания.** На вашем сегодняшнем `data.json` этого не случается: значения ваших полей-ссылок лежат без скобок. Расхождение спящее — оно проснётся в тот день, когда в каталоге ссылок появится запись со своей подписью в скобках. Обход строки был зелёным до правки и остался зелёным после; это не довод за правку, а её точное описание.

**Почему я всё-таки её сделал.** Два объявления одного правила расходятся молча, и эти уже разошлись — просто на форме, которой у вас пока нет. Ровно так вы четыре раза приходили с «панель и команда дают разное».

**Цена правки измерена, а не оценена.** Общий дом взят у панели побайтно: сверка на 17 195 входах — весь ваш `data.json`, все ваши заметки и четырнадцать краёв — дала **одно** расхождение, на числе ноль, и оно устранено до подключения. То есть поведение панели не изменилось ни на одном входе, а ядро перестало отличаться от неё.

**Чем закреплено, и что это стоило узнать.** Набор этого дефекта **не видел**: с возвращённым прежним поведением ядра 70 проверок из 70 оставались зелёными, и обход строки тоже. Сторожей теперь два, и второй не лишний: после сведения обе стороны сравнения спрашивают один дом, поэтому сверка «панель равна ядру» зеленела бы сама — рядом стоит ожидание, написанное числом. Мутация проверена в обе стороны, семь шагов из семи зелёные.

**Разбор** — PRD 10.13.138, исключение к З3 № 100.

### 161у. Шесть оставшихся имён разобраны, и сторож оказался слепым на три четверти кода — **сделано 2026-09-15**

**Нажимать нечего, на экране не меняется ничего.** Это конец строки 3 очереди; подтверждается словом.

**Шесть имён — шесть разных ответов, и каждый снят мерой.** Три оказались настоящими копиями и сведены: правило о паре суррогатов, глубокое копирование и «что такое простой объект». Одно — не правило вовсе, а ленивое подключение одного модуля с двух сторон: сводить нечего, и я это записал.

**А два только назывались одинаково.** Одно имя — `normalizeBinderRows` — носили две разные работы: одна **выдумывает** недостающие идентификаторы строк Binder, другая **отбрасывает** неполные. На одном наборе из четырёх строк первая отдаёт четыре, вторая две. Второе имя — разбор строки на зоны: две версии разошлись на 13 строках из 23. Свести их значило бы сломать одну из дорог; я развёл имена, и теперь каждое говорит, на какой вопрос отвечает.

**И заход вскрыл вещь поважнее правок.** Когда общий дом отдал наружу имя, которое зовут из навигации, сторож мёртвых экспортов объявил его ничьим. Причина оказалась в самом стороже: его маска давала обратной кавычке искать пару до конца файла, а такая кавычка стоит внутри регулярного выражения — маска прочла её как начало строки и съела остаток файла. Сторож видел 20 тысяч знаков из 85 тысяч.

**Цену слепоты называю числом:** он проверял 738 имён, теперь 866. **128 имён не проверялись вовсе**, и всё это время «мёртвых экспортов нет» было утверждением о трёх четвертях кода. Мёртвых среди скрытых не нашлось ни одного — но узнать это можно было только починив меру.

**Чем закреплено.** Две подмены: сломать дом суррогатов — падает проверка набора; вернуть маске прежнюю жадность — падает сам сторож. Семь шагов из семи зелёные.

**Разбор** — PRD 10.13.137, исключения к З3 № 98 и № 99, урок У-192.

### 160у. Мера копий врала: десять честных делегатов числились копиями — **сделано 2026-09-15**

**Нажимать нечего, код продукта не тронут.** Это про инструмент, которым я меряю долг.

**Шаг начинался с другого.** Я взял следующее по важности «расходящееся» имя — то, от которого зависит, где окажется курсор после действия. Копией оно не оказалось вовсе: все три его объявления просто спрашивают общий дом.

**Соврала мера.** Признак «это делегат, а не копия» искал одну форму обращения из трёх, какими код спрашивает общий дом. Два движка держат его через геттер, панель — через свой; ни одну из этих форм признак не видел. Из-за этого долг был завышен: имён с двумя и более настоящими объявлениями оказалось не 66, а **56**.

**У расширения назвал цену сразу:** первое написание считало делегированием и простое чтение через геттер — это своя работа, а не обращение к дому. Сузил до вызова.

**Проверил тем, что список может только убывать:** сличил имена до и после, и ни одно настоящее тело из копий не ушло; заведомо настоящая пара осталась на месте.

**Почему это важнее, чем кажется.** Это третья редакция одного признака: первая искала делегата по его имени и объявила копиями пять честных обёрток, вторая знала одну форму вызова, третья знает три. Каждый раз мера была верна ровно настолько, насколько широко я успел посмотреть.

**Разбор** — PRD 10.13.136, урок У-191.

### 159у. «Что такое объект» — девять объявлений сведены к одному — **сделано 2026-09-15**

**Нажимать нечего, на экране не меняется ничего.** Это вторая часть строки 3 очереди; подтверждается словом.

**Что было.** Самое частое из расходящихся имён: девятнадцать объявлений, из них девять настоящих в четырёх формах. Чем они расходятся — не мерил никто.

**Что намерил.** Разница ровно одна: часть форм возвращает **сам аргумент**, а не «да/нет». На всех семнадцати входах пробы ответ по существу совпал у всех форм. И цену разницы я посчитал, а не предположил: во всём коде нет ни одного места, где результат сравнивается строго или уходит дальше, чем в условие. То есть увидеть разницу не мог никто — но разойтись формам было на чём, и потому они сведены.

**Мера соврала дважды, и оба раза поймал её контроль** — тот, что я ставлю до первого вывода. Сначала она считала формой обращение к общему дому; потом сравнивала «да/нет» вместо самого ответа, и за этим пряталось единственное настоящее различие. И запрет, который я завёл следом, соврал на однострочном объявлении: образец проглотил соседний код. Все три ошибки — мои, и все три видны только потому, что у каждой меры стоял контроль.

**Чем закреплено.** Запрет написан по форме, а не по именам файлов: копия узнаётся по устройству тела, где бы её ни завели. Две подмены: сломать дом — падает 41 проверка из 70, значит дороги и правда ходят туда; вернуть копию — краснеет запрет. Семь шагов из семи зелёные.

**Разбор** — PRD 10.13.135, исключение к З3 № 97.

### 157а. Уборка: четыре точные копии сведены к трём домам — **сделано 2026-09-15**

**Нажимать нечего, на экране не меняется ничего.** Это строка 3 очереди, её первая часть.

Инструмент называл четыре имени точными копиями. Прежде чем сводить, я сверил их **своей** мерой — тела вырезаны из файлов и сравнены побайтно, — и у меры есть контроль: на заведомо разной паре она обязана сказать «различаются». Сведены три: целое в границах (движок полос и его адаптер) и два приведения значений конфига (пять моделей блоков панели).

**Четвёртую сводить было нельзя, и это тоже ответ.** Два коротких построителя в движках — один текст с разным смыслом: каждый закрывает свой общий рантайм, а у них разные умолчания Block. Вместо сведения имя звавшего теперь приезжает аргументом: тексты разошлись, а отказ называет движок, который его вызвал.

**Имён, объявленных в коде больше одного раза:** было 85, стало 83; точных копий не осталось ни одной.

**Разбор** — PRD 10.13.133, исключение к З3 № 95.

### 156у. Переезд без единой смены поведения: зависимости правила переноса — в общем доме — **сделано 2026-09-15**

**Нажимать нечего, и это нарочно.** Строка 4 очереди делается в два шага, и здесь сделан первый: на вашем экране не меняется ничего. Второй шаг — вызов из третьего движка — будет виден, и на него заведу строку.

**Что переехало.** Тринадцать имён, которыми кормится правило перестановки значений по Order, ушли из движка команд-тегов в отдельный файл. Тела перенесены как есть, ни одна строка не переписана; на прежних именах остались обращения в новый дом.

**Почему дом устроен фабрикой.** Общий рантайм у команд тегов и у команд элементов **разный**: на вопрос «в каком Block стоит поле, у которого Block не назван» первый по умолчанию отвечает «слева», второй «справа». Один экземпляр на двоих молча сменил бы поведение одному из движков — а это ровно то, ради чего переезд и делается отдельным шагом.

**Чем доказано, что переезд состоялся.** Зелёный набор доказывает не переезд, а отсутствие поломки: он был бы зелёным и если бы код остался лежать на старом месте. Доказывает подмена **в доме**: я заменил ответ о Block на постоянный, и обход строки уронил 84 сочетания из 171, а набор — две проверки. Значит обе дороги и правда ходят в дом.

**Пять проверок переехали вместе с предметом,** и одна из них сделала это красиво: порог «тонких обёрток над общим рантаймом не меньше тридцати» сказал, что их стало 28 — то есть обход смотрит не туда, а запрет над ним мерит пустоту. Заодно выяснилось, что образец искал объявление только от начала строки, а в новом доме они лежат с отступом.

**Проверено:** семь шагов из семи зелёные, обход строки на двух конфигах — 171 сочетание, расхождений ноль. Имён, объявленных в коде дважды, стало 84 вместо 86.

**Разбор** — PRD 10.13.131, исключение к З3 № 93.

### 155у. Первый шаг к строке 4: «как значение выглядит в строке» сведено к одному объявлению — **сделано 2026-09-15**

**Нажимать нечего**, и на вашем экране не изменилось ничего: у вас нет ни одного поля того вида, на котором копии расходились. Подтверждается словом.

**Зачем это.** Оставшаяся находка обхода — команда поля-элемента не переносит чужое значение в его Block — чинится не вызовом, а общим домом для зависимостей правила переноса. Первая из них и есть «как значение поля выглядит в строке», и она была объявлена дважды: своё у панели, своё у команд. Оба объявления подаются **одному и тому же** общему модулю как одна и та же зависимость.

**Сначала мера, потом правка.** Сверил 189 пар: все значения вашего конфига и формы, которых у вас нет. И первый прогон соврал — сказал «ноль расхождений», потому что синтетическое поле я собрал не той формы, какую признаёт сам плагин: ветвь, ради которой оно заводилось, не выполнялась ни у одной из копий. Поймал это тем, что спросил признак у продукта, а не у своей памяти.

**Расхождение оказалось одно, и вот оно.** У поля-источника «проекты» команда печатала значение тегом (`#note`), а панель — ссылкой (`[[note]]`), если вывод не выбран ссылкой явно. Победило написание панели: так же отвечает третье объявление того же вопроса, а настройки, которую читала команда, в панели нет вовсе, и не пишет её ни одно место плагина.

**Досягаемость называю честно:** поле такого вида нынешний редактор Fields не создаёт — он делает только ссылки на заметки, — так что форма приезжает разве что из конфига первой версии. У вас её нет.

**Чем закреплено.** Таблица из десяти форм на настоящем правиле, включая обе строки расхождения, и запрет на возврат копии. Имён, объявленных в коде больше одного раза, стало 86 вместо 87. Семь шагов из семи зелёные.

**Разбор** — PRD 10.13.130, исключение к З3 № 92.

### 152у. Ваше правило исполнено целиком: настроек, записанных в коде, не осталось — **сделано 2026-09-15, ночь**

**Долг по вашему правилу закрыт: было 30 мест в 10 файлах, стало 0.** Первый род снят отчётом `151у` выше, здесь два оставшихся.

**Второй род — запасное значение рядом с чтением настройки.** Шестнадцать мест: шесть в навигации и десять в предпросмотрах панели. Каждое читало ваш разделитель и тут же писало рядом `||` на случай, если значение не приедет. Это не запас, а второе объявление того же умолчания: оно расходится с настоящим молча, и показывает знак, которого у вас нет.

**Теперь у умолчания один дом**, и из него же его берёт нормализация конфига. Домов на самом деле два, и второй законен: умолчание панели живёт в схеме, а схема выводится из согласованного вами прототипа. Свести их в одно нельзя — расхождения умолчаний схемы и движка это ваш продуктовый вопрос, а не ошибка кода. Оба дома названы в стороже поимённо и печатаются вслух при каждом прогоне, чтобы их можно было оспорить.

**У предпросмотров ответ оказался строже.** Запасное значение им не нужно вовсе: любая запись в настройки проходит через нормализацию, которая пустой разделитель заменяет умолчанием. А если значение всё же не доедет, честнее не нарисовать ничего, чем нарисовать чужой знак.

**Третий род — текст руководства.** Две фразы называли разделителем `||`: «ваш текст отделён от тегов `||`» и «два `||` это Separators». У вас они неверны обе, а руководство человек читает первым. Переписаны так, чтобы быть верными у всех: знак показывает картинка строки, а текст говорит про две метки и указывает на настройку. Остальные ваши слова в руководстве не тронуты.

**Чем закреплено.** Сторож теперь роняет проверку на **любом** новом месте, а не только сверх долга: список пуст. Проверка предпросмотра написана тем предметом, который видит подмена, и в ней прямо сказано, какое утверждение зелено в обоих случаях, а какое краснеет. Пин, который спрашивал текст файла нормализации, сменил вопрос вместе с переездом умолчания и спрашивает теперь поведение. Семь шагов из семи зелёные, обход строки на обоих конфигах без расхождений.

**На вашем экране не изменилось ничего** — кроме двух фраз в руководстве, если вы его перечитаете.

**Разбор** — PRD 10.13.126, исключение к З3 № 88.

### 151у. Правила, которые читали разделитель готовым, — **сделано 2026-09-15, ночь**

**По вашему правилу «этого быть не должно».** Сторож, заведённый в прошлую сессию, назвал 30 мест в 10 файлах. Восемь из них — первого рода: правило не читает вашу настройку, а несёт её в себе готовой. Все восемь сняты, и три из них у вас **не работали вовсе**.

**Что именно у вас молчало.**

1. **Строка, от которой остались знак списка и разделители, не исчезала.** У человека с `||` конец круга давал пустую строку, у вас оставалось `- :: ::` на экране.
2. **Команда важности теряла со строки и значение, и разделитель.** Это самое дорогое из восьми: строка `- [ ] left :: #/3 text` после команды становилась у вас `- [ ] left text` — то есть пропадал и ваш разделитель, и значение. Ветка, которая их возвращает на место, входным условием спрашивала литеральный `||`.
3. **Пробелы вокруг разделителя не приводились в порядок.** Величина мелкая, и я называю её честно: всё, кроме табуляции рядом с разделителем, приводит в порядок другое правило, которое работало и раньше.

**Нашлось это не чтением.** Место помечалось печатью, и случай гонялся на ваших настройках: доходит исполнение до ветки или нет. А цена считалась подменой — литерал возвращался на место, и стенд печатал, чем становится ваша строка.

**Чем закреплено.** Новый сторож: тринадцать утверждений, и у каждого **противоположный контроль** — правило обязано срабатывать на вашем разделителе и не срабатывать на чужом знаке, иначе «починка» вида «считать разделителем что угодно» выглядела бы зелёной. Разделители в нём разведены нарочно, потому что на паре одинаковых ошибка неразличима. Плюс случай в наборе поведения движка — на настоящей команде важности. Четыре подмены, каждая краснеет на своём утверждении. Семь шагов из семи зелёные.

**Обход строки получил форму, на которой дефект пришёл** — значение по ту сторону разделителя, — и тем же заходом из него снят мой собственный литерал: одна из его исходных строк была написана с `||` и после смены вами разделителя перестала быть тем, чем названа.

**И эта новая форма сразу нашла ещё один дефект, не о разделителе:** на строке, где значение чужого Block стоит по ту сторону разделителя, команда поля оставляет его на месте, а панель переносит в его Block. Две дороги дают разное. Дефект давний — проверено на коде до этой правки; беру его следующим.

**Долг по вашему правилу:** было 30 мест в 10 файлах, стало 22 в 5. Первый род снят целиком, остались запасное умолчание, повторённое по месту вызова, и два места в тексте руководства.

**Разбор** — PRD 10.13.124, исключение к З3 № 87.

### 150у. Разделитель, записанный в коде готовым, и остаток сведения — **сделано 2026-09-14, ночь**

**По вашему слову «давай дальше».** Проверять глазами нечего: на вашем экране
не изменилось ничего, и это проверено обходом — 126 сочетаний на ваших
настройках и столько же на разведённых разделителях, расхождений ноль.

**Главное здесь — не правка, а то, как она нашлась.** В коде было правило,
работавшее ровно у тех, чей первый разделитель совпал с написанным в коде:
пустой слот под текст за знаком задачи восстанавливали три места, и во всех
трёх разделитель стоял готовым — `||`. У вас он `::`, и все три у вас не
срабатывали **ни разу**.

**Снять и посмотреть, покраснеет ли набор, было бы недостаточно:** зелёный тут
значит и «место не нужно», и «сюда не доходит ни один прогон». Поэтому место
было заставлено кричать, когда оно и правда что-то меняет: на ваших настройках
оно промолчало, на фикстуре с `||` закричало первым же прогоном. То есть
правило живое, а написано так, что у половины людей его нет.

**Теперь разделитель спрашивается у настроек**, а знак списка и задача — у
общего объявления начала строки.

**Заодно закрыт остаток вчерашнего сведения:** четыре файла не подключали общий
дом вовсе, теперь подключают. Рукописных форм ссылки осталось 18 строк в 9
файлах вместо 40 в 16, тега — 8 в 4 вместо 29 в 12. Остаток — **не копии, а
другие вопросы**: «токен и пробел за ним», «обход с чередованием», «снять
скобки» в слое настроек. Сведение каждого стоило бы правки поведения, о которой
никто не просил, и потому они названы строкой очереди, а не сделаны заодно.

**Чем закреплено.** Сторож на слот при трёх разных разделителях, где `||` стоит
контролем. Запреты по форме расширены с пяти файлов до двенадцати. Две подмены,
каждая краснеет на своём утверждении. Семь шагов из семи зелёные. Разбор —
PRD 10.13.123, исключение к З3 № 86.

### 149у. Д2 и Д3 ревизии: три правила получили по одному дому — **сделано 2026-09-14, ночь**

**По вашему слову «давай Д2 и Д3».** Проверять здесь нечего глазами: на экране
ничего не изменилось, и это работа ради того, чтобы следующий дефект вроде
вчерашнего не мог случиться дважды.

**Первое, что дал обход, — поправил цену, которую я сам вам назвал.** Ревизия
говорила «семь мест»; сплошной обход по форме нашёл сорок строк в двенадцати
файлах. Мера видела одно написание, а написано было по-разному.

**И первый обход соврал.** Попытка решить машиной, какие из сорока копий
расходятся с общим правилом, объявила расходящимися все сорок — то есть выдала
круглый список целой области, а это признак неверного обхода. Причина простая:
каждое из сорока мест спрашивает **свой** вопрос, а обход сравнивал их все с
одним. Его выводы я выбросил целиком и мерил заново, другим способом.

**Что оказалось настоящей находкой.** Пять функций про начало строки стояли
**дважды знак в знак** — в доводке строки и в макро-прослойке, — и обе копии
уже расходились с общим объявлением: своя видела неразрывный пробел из
браузера там, где Obsidian видит обычный, и о цитате не знала вовсе. То есть на
строке `> - текст` одна и та же пара функций отвечала «знак списка есть» и
«начала строки нет». Шестая, про разделители зон строки, была объявлена
четырежды — шесть одинаковых строк, отличавшихся только именем в тексте отказа.

**Что сведено.** Начало строки, снятое и возвращённое; разделители зон строки;
форма ссылки и форма тега — у последних двух общего дома не было вовсе.

**Чем меряется результат.** Не числом в этом абзаце, а командой
`node tools/rule_copies.js`: строк с рукописной формой ссылки было 40 в 16
файлах, стало 24 в 11; тега — 29 в 12, стало 13 в 7. Остаток лежит в файлах,
которые общий дом пока не подключают, и назван строкой очереди — молчаливый
остаток и есть тот способ, каким «сведено» превращается в «сведено наполовину».

**Чем закреплено.** Запреты по форме: общий дом объявлен, каждый читатель его
спрашивает, рукописная форма назад не вернулась. Пять подмен, каждая краснеет
на своём утверждении, и пятая из них поведенческая — общий дом ломается
изнутри, и набор обязан покраснеть. Без неё запреты доказывали бы, что
правильный текст лежит в файле, а не что он исполняется. Семь шагов из семи
зелёные, обход строки на вашем `data.json` — 126 сочетаний, расхождений ноль,
стенд отмены — ноль во всех четырёх случаях. Разбор — PRD 10.13.122,
исключение к З3 № 85.

### Отчёты 144у, 145у, 146у — **подтверждены заказчиком 2026-09-14**

Три строки листа закрыты его галочками: `S37` — полоска до края экрана и
последнее значение на перенесённой строке; `S40` — скобки без дефиса остаются
его текстом; `S42` — строка-заголовок держит значение поля. По четвёртой,
`S41`, он написал замечание: оно разобрано и сделано тем же вечером — отчёт
`147у` выше. Отчёты целиком —
`docs/dev/archive/AWAITING_OWNER_CHECK_session50.md`.

### 143у. Ревизия: Д1, Д4, Д5, Д6 сделаны — **сделано 2026-09-14**

**По вашему слову «выполняй очевидную работу по ревизии».** Четыре строки долга
из шести закрыты; проверять здесь нечего глазами, кроме `S39` — единственного,
что видно на экране.

**Что сделано.** Экранирование регулярного выражения сведено из одиннадцати
мест в одно. Откат переноса в заметку: два непокрытых пути из четырёх закрыты
проверками — самый дорогой из них тот, где ваша заметка остаётся с дописанным
блоком, которого вы не просили. Обход строки получил вторую половину — команды
дочерних полей, пять штук. Режим `Custom` у шага по элементу получил девять
проверок: настройка в панели есть, а поведения за ней не проверял никто.

**Две мои ошибки в самом отчёте исправлены там же.** Я написал, что копии
экранирования совпадают знак в знак, — они расходились на значении `0`. И
написал, что пара функций дочерних полей объявлена дважды, — оказалось
объявление и делегат к нему; вывод был сделан из числа, а не из чтения.

**Что осталось и почему.** Д2 ждёт вашего ответа на вопрос про строку-цитату:
половина расхождения — про неё, и чинить раньше значит решить за вас. Д3 — 27
мест без общего дома, расхождений между ними пока не найдено ни одного; это два
захода, и вслепую я их делать не стал.

### 141у. Ревизия, строка Д1: экранирование сведено в одно объявление — **сделано 2026-09-14**

**По вашему слову «выполняй очевидную работу по ревизии».** Помощник
экранирования стоял в одиннадцати местах под пятью именами; теперь он один, в
`src/core/shared_utils.js`.

**Проверять здесь нечего глазами, и одно уточнение честное:** ревизия написала,
что копии совпадают знак в знак, — это оказалось неверно. Девять из них на
значении `0` отдавали пустую строку, то есть пустую альтернативу регулярного
выражения, которая совпадает со всем. Если у вас есть Field со значением `0`,
его поведение могло быть странным; теперь не будет.

**Заодно починен стенд** `node tools/line_matrix.js`: он умер 13 сентября, когда
вы сменили первый разделитель на `::`, — контрольные строки в нём были написаны
разделителями литералом. Первым же прогоном после починки он нашёл два
расхождения на строке-заголовке; разбор идёт отдельной строкой.

**Чем закреплено.** Сплошной обход рантайма по **форме** правила, а не по имени;
два положительных контроля и две подмены. Разбор — PRD 10.13.112 и 10.13.113.

### 140у. Ревизия кода: технический долг и проблемы — **сделано 2026-09-14, ночь**

**Ваша вторая задача на ночь:** «выполни полный анализ кода на предмет наличия
технического долга и проблем». Отчёт — `docs/dev/CODE_REVIEW_2026-09-14.md`,
проверять в нём нечего: это разбор, а не правка. Что делать и в каком порядке —
его раздел 6.

**Коротко, что нашлось.** Всё, что меряется числом, по-прежнему в нуле — тихие
отказы, мёртвые экспорты, шум линтера, правила каталога. Нашлось то, чего
нынешние меры не видят: десять копий одного помощника под пятью именами; форма
знака списка в двух написаниях, которые уже расходятся (половина этого упирается
в ваш ответ на В-115); 254 функции, которые `npm test` не исполняет ни разу —
среди них путь отката переноса в заметку и всё семейство шага по датам в режиме
`Custom`; 38 функций, о которых проверки говорят только пином на текст.

**И одна честная неудача, записанная в самом отчёте:** обход, которым я делил
непокрытые имена на мёртвые и живые, соврал — его маска не знает регулярных
литералов. Поймали это три проверки руками, список в отчёт не пошёл.

**Инструмент положен в репозиторий:** `node tools/coverage_map.js <папка>` после
`NODE_V8_COVERAGE=<папка> npm test`. Гейтом он не является и числа не стережёт.

### 138у. Артефакты подложки — **первая форма, 2026-09-14, ночь**

**Вторая форма закрыта отчётом `144у` тем же днём, вечером.** Ниже — разбор
ночной правки и его два бага по ней; чинились они следующим заходом.

**Галочку он не поставил и написал два бага** (лист приёмки, строка `S37`,
скриншот `Pasted image 20260914102759.png`):

> иногда полоска по прежнему рисуется до границ экрана
> на перенесенной строке полоска не подкрашивает последнее value

**Что это значит.** Правка ночи сняла **одну** форму дефекта — выдуманную
зрительную строку, от которой у конца полосы вставала вторая, сдвинутая. Вторая
форма осталась, и по его картинке видно какая: на перенесённой строке кусок
подложки для последнего значения второго ряда встаёт **правее** самого значения,
а не под ним. Это ровно У-174: починка вида открывает следующую форму того же
дефекта, и спрашивать надо было не «сделано ли», а «какую форму я открыл».

**Починено 2026-09-14, вечером** — отчёт `144у`. Причина оказалась не той, что
предполагал первый шаг разбора ниже: дело было не в горизонтальном крае куска, а
в том, что о границе ряда плагин спрашивал не тем вопросом, каким её задаёт
отрисовка (PRD 10.13.116).

**Первый шаг разбора уже понятен и записан, чтобы не начинать с нуля:** обход
рядов теперь доходит до конца строки, значит границы рядов верны, а неверен
**горизонтальный** край куска на втором ряду. Мерить это надо браузерным стендом
слоя редактора на его ширине окна, а не глазами по картинке (У-123).

**Что было сделано ночью и остаётся верным — ниже.**

**Два ваших замечания, и они оказались одним:** «артефакты right block в конце
полоски tags-block-fill (особенно, если последнее value — wikilink)» и «полоска
tags-block-fill повела себя неадекватно, не понимаю причин».

**Причина одна.** Подложка режется по зрительным строкам. Обход, который их
считает, спрашивает платформу «где кончается ряд» — и может остановиться раньше
конца строки. Остаток дописывался тогда **новой зрительной строкой**, которой на
экране нет. Дальше это число делит высоту строки: у конца полосы вставала вторая,
той же высоты и на полряда ниже — это первый ваш артефакт. А кусок, граница
которого не совпала с переносом, пересекает ряды, и такой отрезок платформа
рисует выделением — во всю ширину до правого края. Это второй.

**Почему именно на wikilink.** Ссылку Live Preview рисует своим узлом, и на нём
платформа возвращает положение, с которого сама же не сдвигается: обход
останавливается ровно перед ссылкой.

**Что теперь.** Остаток дописывается последней зрительной строке. Сколько у
строки рядов, решает счёт, у которого уже есть проверка: на перенесённой строке
он не верит одному ряду при двух и уходит на измеренный путь.

**Чем закреплено.** У страницы слоя редактора появился второй отказ платформы —
граница ряда, названная раньше конца ряда; на нём сверяется число
прямоугольников и их вертикали. Подмена возвращает прежнее поведение:
прямоугольников 27 вместо 22. Разбор — PRD 10.13.110.

**Строка листа:** `S37`.

### Отчёты 137у, 139у, 142у — **подтверждены заказчиком 2026-09-14**

Три строки листа — `S36`, `S38` и `S39` — закрыты его галочками: две кнопки `→`
и кнопка при открытой панели, правка настройки на открытой заметке, лишний
разделитель на строке-заголовке. Отчёты целиком —
`docs/dev/archive/AWAITING_OWNER_CHECK_session48.md`.

### Отчёты 133у–136у — **подтверждены заказчиком 2026-09-14**

Три строки листа — `S33`, `S34` и `S35` — закрыты его галочками: знак задачи
переживает команду поля, знак списка человека переживает шаг по элементу при
любом знаке, пустой слот под текст держится при любом знаке, и служебный файл
правил снят целиком. Отчёты целиком —
`docs/dev/archive/AWAITING_OWNER_CHECK_session47.md`.

### Разделы 77у, 78у, 79у, 80у, 82у, 89у, 90у, 91у, 92у, 94у, 95у, 96у, 99у, 100у — **подтверждены заказчиком 2026-09-12**

Строки `S3`, `S4`, `S5`, `S11`, `S12`, `S13`, `S14`, `S16`, `S17`, `S18` и `S19`
закрыты галочками. Отчёты унесены в
`docs/dev/archive/AWAITING_OWNER_CHECK_sessions39_40.md`.

### Разделы 37у, 39у, 40у — **подтверждены заказчиком 2026-09-08, вечер**

Четыре решения про каталог Obsidian (`K1`, `K2`), `Del` на пустой строке
(`S1`) и тумблер `Step out of the word` (`S2`) — все четыре строки закрыты
галочками, замечаний под ними нет. Отчёты унесены в
`docs/dev/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### Разделы 93у, 97у, 98у, 101у, 114у, 117у, 119у, 121у, 122у, 123у, 124у, 125у, 126у, 127у, 128у, 129у — **подтверждены заказчиком 2026-09-13**

Строки `S15` и `S29` закрыты его галочками: «ок работает». Шестнадцать отчётов
унесены в `docs/dev/archive/AWAITING_OWNER_CHECK_session45.md` — там поведение
строки, `Ctrl+Z`, строка-заголовок и подложка на перенесённой строке.

### Отчёты 52у–88у — **унесены в архив 2026-09-13**

Работа 9–11 сентября, у которой **нечего нажать**: ревизия кода семью заходами,
тихие отказы, мёртвый код, два выпуска беты, лист возможностей, сокращение
служебного файла правил. Подтверждается она словом, а не галочкой, и слова
этого пока не было — поэтому строки остаются здесь, а разборы уехали в
`docs/dev/archive/AWAITING_OWNER_CHECK_reports_2026-09-09_11.md`.

**Почему унесены.** Этот файл читается каждую сессию целиком, а разборы
занимали в нём три четверти объёма и не менялись с 11 сентября (У-31).

| Отчёт | О чём |
|---|---|
| 58у | Служебный файл правил сокращён на 28 тысяч знаков |
| 83у, 86у | подтверждены его галочками 2026-09-12 |
| 85у, 87у, 88у | подтверждены его галочками 2026-09-12 |
| 84у | Кто теряет вторую половину значения элемента |
| 81у | Панель TagWheel по Order: разбор, и он оказался неверен |
| 72у, 75у, 76у | подтверждены его галочками 2026-09-12 |
| 74у | Ревизия кода, заход седьмой, и решение по шву TagWheel |
| 73у | Ревизия кода, заход шестой: фикстуры, которые не могут увидеть дефект |
| 71у | Ревизия кода, заход пятый: числа документов против измеренных |
| 70у | Ревизия кода, заход четвёртый: запреты без образца |
| 69у | Ревизия кода, три захода |
| 68у | Выпуск `0.1.0-beta.6` |
| 67у | Лист возможностей |
| 66у | `var` вне движков — ноль, и новых не завести |
| 65у | Правило «где взять активный редактор» сведено к одному объявлению |
| 64у | Мёртвый код снят, копии правил сведены к одному объявлению |
| 63у | Тихие отказы под З3, третий кусок: 27 → 0, долг закрыт |
| 62у | Тихие отказы под З3, второй кусок: 60 → 27 |
| 61у | Тихие отказы под З3, первый кусок: 69 → 60 |
| 60у | Выпуск `0.1.0-beta.5` в GitHub |
| 59у | Все 69 тихих отказов под З3 разобраны |
| 50у, 53у, 54у, 55у, 57у | подтверждены его галочками 2026-09-10 |
| 56у | Тихие отказы вне З3 разобраны |
| 52у | Бриф для LLM на проектирование PKM |

### Разделы 44у, 45у, 48у, 49у, 51у — **в архиве**

Пять отчётов по строкам `S6` и `S7`, **перекрытых позднейшими заходами**. Обе
строки заказчик закрыл галочками 2026-09-10, и последние отчёты по ним (50у,
53у, 57у) унесены в `docs/dev/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.
Причины дефектов и то, чем правки закреплены, —
`docs/dev/archive/AWAITING_OWNER_CHECK_sessions27_29.md`.

### Разделы 25у–36у — **в архиве**

Унесены 2026-09-08 вечером, по итогу сессии 26. Четыре раздела заказчик
закрыл галочками (30у, 31у, 32у, 35у — подсказки у всех подписей, приглушённая
заливка шапок, три починки и перевод панели), остальные четыре — отчёты, в
которых проверять нечего: список правил каталога (29у), закрытие фазы 6 (33у),
три расхождения с каталогом (34у) и разбор мёртвого вне З3 (36у).
Всё вместе — `docs/dev/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 28у. Решено и не начато

Ни одна строка ниже проверки не ждёт: они ждут работы. Осталось то, у чего срок
назначен его же словом.

- **Минификация — в самом конце**, перед выходом наружу: «нужно упростить
  работу по отлавливанию багов». Вопрос В-95 отвечен 2026-09-10: бету **не
  минифицировать**. Срок остаётся за ним. **Не начато.**
- **Мобильные устройства** — отложены его словом, реализовать позже. **Не
  начато.**
- **Снятие инлайн-элементов при экспорте заметки наружу** — первый пункт его
  файла идей; разбор назвал его хорошим кандидатом в ближайший релиз. Решения
  ещё не было. **Не начато.**

**Долгов, закреплённых числом, больше нет.** Тихие отказы (Д-4, было 105 → 69)
закрыты целиком 2026-09-10: список `DEBT` в `silent_failures_tests.ts` пуст, и
теперь роняет проверку любое новое безымянное молчание. Выпуск наружу, который
стоял здесь «не начато», сделан дважды: `0.1.0-beta.5` 2026-09-10 и
`0.1.0-beta.6` 2026-09-11.

**Долг по инлайновому оформлению закрыт** (Р7, 48 из 48) и подтверждён галочкой
на строке `V2` 2026-09-10; отчёт — в архиве проверенного. Абзац про «182
инлайновых стиля» удалён, а не унесён: две трети того числа лежали в мёртвой
функции, а остаток перенесён (У-31, У-117).

### Разделы 1у–24у — **в архиве**

Всё, что заказчик закрыл галочками до сессии 24, унесено в
`docs/dev/archive/AWAITING_OWNER_CHECK_sessions1_23.md`. Открывать, когда нужно
вспомнить, чем закреплена старая правка или когда её сделали.

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него сделаны и подтверждены заказчиком; разборы — в архиве
реестра: `Floating button`, симметричный цикл `Move right`, подсветка
обработанной строки, три пункта 1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа
свойства через `app.metadataTypeManager`.

**Остатка у фазы 6 больше нет.** Последний пункт закрыт 2026-09-08: манифест и
снятие команды `Open settings` были сделаны 2026-09-06, `dist/` вышел из
репозитория тогда же, а половина пункта про `main.js` потеряла предмет — после
разбора это 159 строк исходника, а не артефакт сборки. Живого в пункте
оказалась одна вещь, и её в задании не было: последняя заглушка на месте
модуля, `fields_editor_legacy.js`. Разбор — PRD, раздел 11.

Абзац про «остаток фазы 6 — снятие моста модулей» удалён 2026-09-08, а не
унесён в архив: он описывал состояние, которого больше нет (У-31). Моста нет
с 2026-09-07 — проверено поиском: файла `vault_module_bridge.js` не
существует, вызовов моста в рантайме ноль. В архиве этот абзац продолжал бы
врать.

**Остальное решённое и не начатое — раздел 28у выше**, и оно перечислено там,
а не здесь: этот раздел про работу, о которой заказчик не просил, а тот — про
ту, которую он назначил.
