# Тестирование · inline Overhaul

> [!info] Где мы сейчас · 2026-09-22, вечер, выпуск `0.7.0` выложен
> **Голый адрес переехал на цвет адреса — как вы и передумали.** Теперь `Hyperlink address color` красит **оба** рода адреса: и тот, что внутри круглых скобок, и написанный сам по себе. `Hyperlink target color` остался тексту между квадратными скобками. Подписи обоих контролов переписаны: прежние обещали обратное.
> **Выпуск `0.7.0` выложен на GitHub по вашему слову.** Второй знак, потому что в разделе есть новые возможности. Все три вложения скачаны обратно и совпали с локальной сборкой **побайтно** — это и есть проверка выпуска, а не зелёный CI. Релиз обычный, не предрелиз: BRAT его увидит.
> **Что в выпуске:** 7 починок, 6 видимых изменений, 5 новых возможностей. Сводка в окне «что изменилось» считается по самим пунктам, а не пишется руками.
> **Скажу прямо одну вещь:** три теста в этой заметке вы ещё не отметили, а ваше же правило (В-126, 2026-09-19) говорит выпускать только при заполненных чекбоксах. Вы сказали выкладывать — я выложил; называю это, чтобы решение было при вас.

> [!tip]- Как устроен тест · разверните один раз
> Под заголовком — **галочка** и строка `💬`: принято, или что не так. Ниже четыре свёрнутых блока — описание, журнал, настройки, действие — и три раздела:
> **🎯А** — какой станет строка после действий, **🛠️Б** — эталонная исходная строка, **💥В** — её копия, на которой работаете вы.
> Трогайте только **В**. Расхождение между В и А разбираю я.
> При закрытии сессии я убираю принятое, чищу В, обновляю журнал.

## 1. Скобка — шаг своего права
- [x] принято
- 💬

> [!question]- Описание задачи
> Ваши слова: «при выделении одного слова перемещение работает нормально, но если выделить несколько слов, то перемещение сбоит — перепрыгивает через скобки».
> **Условие оказалось не в числе слов, и проверено это до правки.** На одном слове шаг перепрыгивал группу так же.
> **Причина — моё правило, а не ваше.** «Края парные — сосед целый» я завёл, чтобы шаг не рвал `[[test1]]`. Написанное по форме, оно накрыло `(слово)`, `«слово»` и `[слово]`.
> **Что теперь.** Целой остаётся **ссылка**; скобки вокруг обычного текста — обычный край, и шаг по ним идёт в обе стороны.
> **И побочно:** `[подпись](адрес)` под прежнее правило не попадала — шаг рвал её на `[текст подпись](адрес)`. Теперь она целая.

> [!example]- Журнал
> **Цикл 80 · 2026-09-22, ночь**
> - правило края спрашивает признак ссылки вместо формы скобок
> - ваши три нажатия выписаны утверждениями знак в знак, плюс зеркало влево
> - две подмены: снять признак ссылки — рвётся `[[test1]]`; вернуть прежнее правило — не входит в скобку

> [!settings]- Настройки
> `Navigation` → `Move lines (left\right)` → субхедер `Move text`
> Отдельного контрола у правила нет: оно в самом шаге

> [!action]- Действие
> 1. Перезапустить плагин: выключить и включить `inlineOverhaul` в `Community plugins`.
> 2. В первой строке `💥В` выделить `слово2 слово3` и нажать `Move right` три раза: обязано выйти `- (слово2 слово3 слово1)`, затем `- (слово1 слово2 слово3)`, затем `- (слово1) слово2 слово3`.
> 3. Там же выделить одно слово `слово3` и нажать `Move right`: шаг обязан быть таким же — в скобку, а не через неё.
> 4. Вторая строка — контроль: выделить `текст` и нажать `Move right` один раз. Ссылка обязана переставиться **целиком**.
> 5. Третья строка — тот же контроль для разметки: `[подпись](адрес)` обязана остаться целой.

### 🎯А. Ожидаемый результат
- (слово1) слово2 слово3
- [[test1]] текст хвост
- [подпись](https://example.com/a) текст хвост

### 🛠️Б. Эталонная исходная строка
- слово2 слово3 (слово1)
- текст [[test1]] хвост
- текст [подпись](https://example.com/a) хвост

### 💥В. Тест
- [I] слово2 слово3 (слово1)
- текст [[test1]] хвост
- хвост текст [подпись](https://example.com/a)

---

## 2. Цвета ссылок: пять контролов
- [x] принято
- 💬

> [!question]- Описание задачи
> Ваши слова, дважды. Сперва: «сделай отдельный контрол» на адрес в `[hyper](link)`. Потом: «я передумал, теперь я хочу, чтобы `[подпись](адрес)` (часть адреса) и просто ссылка в тексте управлялись одним контролом `hyperlink-address-color`».
> **Сделано по второму.** `Hyperlink address color` красит **оба** рода адреса. `Hyperlink target color` остался тексту между квадратными скобками — и только ему.
> **Ничего не изменится, пока вы сами не разведёте цвета:** строка адреса заводится от цвета скобок, которым адрес красился до неё.
> **Подписи обоих контролов переписаны:** прежние обещали ровно обратное, и оставить их значило бы соврать на экране.

> [!example]- Журнал
> **Цикл 81 · 2026-09-22, вечер**
> - у голого адреса больше нет подписи: весь отрезок — кусок адреса
> - проба гейта искала адрес по `://` и на `www.example.com` ослепла бы молча — теперь текст адреса приходит доводом
> - подмена «голый адрес снова на цвете подписи» краснеет
> **Цикл 80 · 2026-09-22, ночь**
> - знаков в разборе стало три, адрес — свой кусок
> - цвет темы у нового поля прочитан у Obsidian

> [!settings]- Настройки
> `Visual` → `Inline appearance` → `Link view` → `Link target color`
> `Visual` → `Inline appearance` → `Link view` → `Link brackets color`
> `Visual` → `Inline appearance` → `Link view` → `Hyperlink target color`
> `Visual` → `Inline appearance` → `Link view` → `Hyperlink brackets color`
> `Visual` → `Inline appearance` → `Link view` → `Hyperlink address color`

> [!action]- Действие
> 1. Перезапустить плагин.
> 2. Открыть `Visual` → `Inline appearance` → `Link view` и задать всем пяти строкам цвета **разные** заметные цвета.
> 3. Посмотреть на предпросмотр внизу раздела: во второй строке подпись, знаки и адрес — три разных цвета; в третьей голый адрес обязан быть цветом **адреса**.
> 4. Открыть строки `💥В` и поставить курсор на первую: адрес в круглых скобках — цветом адреса, скобки вокруг него — цветом скобок.
> 5. Вторая строка — голый адрес: обязан быть цветом адреса и вне строки с кареткой тоже.
> 6. Третья строка — контроль: картинка и адрес в обратных кавычках обязаны остаться такими, какими их рисует тема.
> 7. Текст строк не меняется ни на знак, поэтому 🎯А и 🛠️Б здесь совпадают.

### 🎯А. Ожидаемый результат
- ссылка [подпись](https://example.com/a) в тексте
- голый адрес www.example.com и https://example.com/a?b=1
- не ссылка: ![картинка](https://example.com/a.png) и `https://example.com`

### 🛠️Б. Эталонная исходная строка
- ссылка [подпись](https://example.com/a) в тексте
- голый адрес www.example.com и https://example.com/a?b=1
- не ссылка: ![картинка](https://example.com/a.png) и `https://example.com`

### 💥В. Тест
- ссылка [подпись](https://example.com/a) в тексте
- голый адрес www.example.com и https://example.com/a?b=1
- не ссылка: ![картинка](https://example.com/a.png) и `https://example.com`

---

## 3. Выпуск 0.7.0 у вас
- [x] принято
- 💬

> [!question]- Описание задачи
> **Выпуск выложен на GitHub по вашему слову.** Второй знак — `0.6.0` → `0.7.0`, потому что в разделе есть новые возможности.
> **Проверен обратной загрузкой, а не зелёным CI:** все три вложения — `main.js`, `manifest.json`, `styles.css` — скачаны с релиза и совпали с локальной сборкой побайтно.
> **Релиз обычный, не предрелиз** — BRAT берёт последний непредрелиз с тремя вложениями.
> **Что уехало:** 7 починок, 6 видимых изменений, 5 новых возможностей — сводка в окне «что изменилось» посчитана по самим пунктам.

> [!example]- Журнал
> **Цикл 81 · 2026-09-22, вечер**
> - `0.7.0`: тег выложен, сборку сделал `release.yml`, вложения сверены побайтно

> [!settings]- Настройки
> Настроек нет: это выпуск, а не поведение плагина

> [!action]- Действие
> 1. Открыть `Settings` → `Community plugins` → `BRAT` и обновить `inlineOverhaul`.
> 2. Посмотреть окно «что изменилось»: сверху обязан стоять счёт и список нового буллетами.
> 3. Убедиться, что плагин работает в вашем рабочем vault, а не только в тестовом.
> 4. Сказать, если в окне «что изменилось» что-то читается не так.

### 🎯А. Ожидаемый результат
—

### 🛠️Б. Эталонная исходная строка
—

### 💥В. Тест
—

---

## 4. Ctrl+Y после Ctrl+Z
- [ ] принято
- 💬убери этот таск, я не могу его воспроизвести

> [!question]- Описание задачи
> Ваш пункт 13: «иногда `Ctrl+Y` не работает после нажатия `Ctrl+Z`». Вы ответили «пока убираем в бэклог» — тест живёт напоминанием, а не работой.
> **Измерено, и вашими шагами не воспроизводится.** Стенд отмены гоняет настоящую историю CodeMirror: четыре сценария в двух режимах.
> **Ловить осталось условие.** Поймаете случай — назовите, что делали **до** `Ctrl+Z`.

> [!example]- Журнал
> **Цикл 81 · 2026-09-22, вечер**
> - оставлен живым по вашему слову «пока убираем в бэклог»
> **Цикл 78 · 2026-09-22, день**
> - тест оставлен живым: ваш случай ждёт условия, а не правки

> [!settings]- Настройки
> Настройки не нужны: `Ctrl+Z` и `Ctrl+Y` принадлежат Obsidian

> [!action]- Действие
> 1. Работать как обычно и поймать случай, когда `Ctrl+Y` не вернул отменённое.
> 2. Записать, что было **до** `Ctrl+Z`: какая команда, панель или набор руками, и сколько раз.
> 3. Написать это в `💬` — шагами, по одному действию в строке.

### 🎯А. Ожидаемый результат
—

### 🛠️Б. Эталонная исходная строка
—

### 💥В. Тест
—

---

## Новое пишите сюда
Всё, что заметите по ходу, — сюда, своими словами и без порядка. Я разберу и заведу отдельными тестами.

## Мои вопросы к вам

**Открытых вопросов нет.**

**Следующая работа — из вашей очереди:** выбиралка эмодзи и символов, либо четвёртое положение контрола `Child Field` под `Alt`. Скажите, что первым.
💬оба последовательно. Это будет ночная сессия, хочу чтобы ты работал автономно. В процессе работы используй ponytail для эффективного написания кода

> [!note]- Служебное · промпт для новой сессии Claude
> Это не вам. Наведите курсор на блок ниже, нажмите кнопку копирования в его углу и вставьте в новую сессию.
>
> ```
> Новая сессия по inline Overhaul. Прошлый цикл, 81-й, закрыт вечером 2026-09-22 выпуском `0.7.0`.
>
> ЧТО ПРОЧИТАТЬ ДО ПЕРВОГО ДЕЙСТВИЯ
>
> `CLAUDE.md` целиком. Затем `test-vault/testing.md`: его галочки, строки `💬` и **весь текст**, он правит заметку прямо в ней и вписывает задачи между блоками. Плюс `test-vault/Test.md` и `test-vault/Test 1.md`. Правите что-либо из того, что читает человек снаружи, — сперва `.claude/skills/repo-docs/SKILL.md`.
>
> КАК УСТРОЕН ЦИКЛ — ЕГО РЕШЕНИЕ `В-175`, 2026-09-21
>
> Вход — ссылка на эту заметку. Порядок: **сперва то, что он пометил дефектом, потом новые задачи**. Вопросы — интерактивным интервью и **после** починок. Затем, не спрашивая, идёт процедура завершения (`docs/dev/SESSION_END.md`). **`git push` в неё не входит.** Ответил на интервью — работа по ответу идёт тем же заходом.
>
> ГДЕ МЫ СТОИМ
>
> **Последний выпуск — `0.7.0` от 2026-09-22**, выложен его словом. Вложения сверены обратной загрузкой побайтно. `## Unreleased` пуст.
>
> Цикл 81 сделал одно: голый адрес переехал на `Hyperlink address color` (он передумал по своему же `В-191`), и выложил выпуск. Цикл 80 до него: скобка — шаг своего права (исключение к З3 № 147) и своя пара цветов у гиперссылки; обход строки на его конфиге сошёлся в 0 из 162.
>
> В заметке четыре теста, все с пустыми чекбоксами.
>
> **ЧТО ДЕЛАТЬ ПЕРВЫМ**
>
> **Прочитать его пометки.** Если дефектов нет — брать из его очереди: выбиралка эмодзи и символов (`В-182`, его пункты 9 и 10) или `З-36` — четвёртое положение контрола `Child Field`, `Show when press Alt`. В заметке он выбирает сам; не выбрал — спросить интервью одним вопросом.
>
> `З-36`: предмет — `tagwheel.js` и `tagwheel_core.js` (файлы под З3, но это В-162), `altKey` в исходниках не встречается ни разу, и новая настройка поля едет через четыре перечня свойств (правило 159).
>
> **Отложено его словом:** режим просмотра (`З-30`) и `Ctrl+Y` после `Ctrl+Z`.
>
> ЧТО НАДО ЗНАТЬ ПРО ЕГО ФАЙЛ НАСТРОЕК
>
> **Он меняет его по ходу**, и числа стендов от этого скачут без единой правки кода (правило 110). За два дня дважды: знак у `Due` (109 из 135 → 10 из 162) и значение у `test` (10 → 0). **Расхождения группировать по полю до разбора** (правило 189).
>
> ЧТО МОЖНО И ЧТО НЕЛЬЗЯ
>
> Читать, править документы, писать код, запускать проверки, делать локальные коммиты — можно. `git push` и выпуск — только по его слову, названному тем же заходом; слово одноразовое, и на `0.7.0` оно израсходовано. Вопрос о выпуске задаётся лишь когда в заметке нет пустых чекбоксов (правило 160); сейчас их четыре.
>
> Файлы под З3 — три случая: **В-120** (уборка при измеренном нулевом расхождении), **В-162** (работа, которую он назначил сам) и всё остальное — спрашивается до правки.
>
> РИТМ ПРОГОНОВ
>
> На итерации один файл: `npm test -- <часть имени>`. Перед каждым коммитом — полный набор, линтер, типы. Браузерный шаг — когда правка трогает слой редактора или панель, и один раз последним шагом.
>
> **Стенды в семь шагов не входят и падают молча.** `node tools/line_matrix.js`, `node tools/line_bench.js`, `node tools/undo_bench.js`, `node tools/renumber_bench.js`, `node tools/move_scroll_bench.js`, `node tools/tag_size_probe.js`, `node tools/theme_color_probe.js`.
>
> **Его проверка идёт установленной сборкой, и Obsidian её сама не перечитывает.** Починили — `npm run install:test`, и **скажите ему перезапустить плагин**.
>
> ФОРМА ЗАМЕТКИ ТЕСТОВ
>
> `node tools/testing_shape.js`. Заголовок `## N. Короткое название`, сразу под ним `- [ ] принято` и `- 💬` без пустой строки, четыре свёрнутых коллаута и три раздела `🎯А`, `🛠️Б`, `💥В`, где В — посимвольная копия Б.
>
> ЧТО СДЕЛАНО 81-М ЦИКЛОМ
>
> Отчёт `319у`, PRD 10.13.249, выпуск `0.7.0`. Уроков новых нет; один старый повторился четвёртый раз за сессию — **heredoc оболочки съедает уровень экранирования**, и патч с `\\n` внутри надо писать файлом, а не heredoc'ом (У-12, правило 122).
>
> СОСТОЯНИЕ
>
> Семь шагов зелёные: `npm test` 92 из 92, линтер, типы, гейты фазы 5, девять проверок прототипа, сторож формы заметки и браузерный шаг (66 подмен). Обход строки — 0 из 162 при одном известном расхождении. Плагин в `test-vault` пересобран.
> ```
