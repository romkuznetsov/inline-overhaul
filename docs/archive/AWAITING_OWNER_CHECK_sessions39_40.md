# Реестр проверенного: сессии 39 и 40

> Архив. Все разделы ниже заказчик **закрыл галочками** 2026-09-12;
> живой реестр — `docs/AWAITING_OWNER_CHECK.md`. Здесь они лежат затем,
> чтобы было чем ответить на «а чем это было закреплено» и «когда это
> сделали», не поднимая историю коммитов.

Унесено 2026-09-12 по итогу сессии 40: 14 разделов.

### 77у. Служебный файл правил, шаг второй: шаг по Values и по дате — **сделано 2026-09-11, ночь**

**Строка в листе есть — `S3`.** Разбор целиком — PRD 10.13.52, П-11; исключение
к З3 — сорок седьмое.

**Что сделано.** Шаг по значениям тега и шаг по элементу-дате получают правила
**из настроек**, а не из заметки `generated_rules.md`. Заметка ещё пишется и
читается: её разбирает TagWheel — это шаг третий.

**Что вы можете заметить.** То же, что и на первом шаге: правка настройки
действует сразу, а не после отложенной перезаписи заметки. Всё остальное
поведение обязано остаться прежним — это и есть главное, что нужно проверить.

**Чем закреплено.** Сверка двух ходов на одном конфиге: формы правил, поведение
движка на настоящей строке обоими ходами и — важнее всего — **разведённые
источники**: правила в настройках со своим разделителем против заметки на диске
со своим. Без этого равенство «оба хода дают одну строку» выполнялось бы и
тогда, когда ключ не читают вовсе.

**Первый прогон мутаций нашёл слепое место:** движок тегов не был покрыт вовсе —
подмена «игнорировать правила из настроек» не роняла ни новую сверку, ни
проверку поведения. Движка два, спрашивать надо оба. После починки краснеют все
пять подмен.

**Семь шагов зелёные**, 69 проверок; сборка в `test-vault` обновлена.

### 78у. Пустая строка: пустой слот под текст и второй разделитель — **сделано 2026-09-11, ночь**

**Строка в листе — та же `S3`**, она дополнена. Разбор — PRD 10.13.68, Р-7;
исключение к З3 — сорок пятое, второй заход того же дня.

**Два ваших замечания, оба воспроизведены прогоном:**

- на пустой строке значение слева и дата справа давали `- [ ] #todo || :: 📅…`,
  а должны `- [ ] #todo ||  :: 📅…` — с пустым слотом под текст;
- на пустой строке только дата справа давала `- [ ]  || 📅…`, а должна
  `- [ ]  :: 📅…` — со вторым разделителем.

**Оба места я на первом заходе не тронул нарочно**, и это была моя ошибка
границы: в разборе стояло «спец-ветки про пустую строку не трогаю, его случай
про строку с текстом». Правило оказалось одно и то же, а я разделил его по виду
строки.

**Пять ваших замечаний за день — один и тот же класс:** правило написано так,
что при одинаковых разделителях оно верно, а при разных нет. Отсюда новый
признак в правилах: найдя первое такое расхождение, не чинить его в одиночку, а
выписать все места, где участвует та же пара.

**Контроль, которого не было в первом заходе:** к каждому утверждению добавлено
парное — «у кого разделитель один, строка не изменилась». Правка не имеет права
менять поведение тем, у кого разделитель один, и теперь это сказано проверкой, а
не обещанием.

**Лист приёмки переписан без разрывов предложений** — по вашему слову. То же
правило записано в `CLAUDE.md`, чтобы разрывы не вернулись.

**Семь шагов зелёные**, сборка в `test-vault` обновлена.

### 79у. Правило разделителей сведено к одному объявлению — **сделано 2026-09-11, ночь**

**Строка в листе — та же `S3`**, она переписана. Разбор — PRD 10.13.68, Р-8;
урок У-150, правило 72.

**Вы сказали «результат становится всё хуже», и были правы.** Правило «чем
разделены зоны строки» объявлено **трижды**: в сборке строки, в её доводке и
своим способом в TagWheel. Я починил одно из трёх и объявил работу сделанной —
вы получили строку, собранную другим путём. Границы слота под текст, от которых
зависит курсор, оказались **пятым** местом того же правила.

**Что починено из ваших замечаний:**

- один пробел между разделителями вместо пустого слота — теперь два при любых
  разделителях;
- правый Block на пустой строке за первым разделителем — теперь за вторым, и
  это одинаково на всех путях;
- курсор прыгал в конец строки — теперь встаёт в слот под текст.

**Что не починено, и я не буду делать вид, что починено:** ваше замечание про
Order. Field, переставленный в левый Block, на **пустой** строке всё равно
встаёт справа, а TagWheel открывается не на первом поле порядка. Это другая
область, и чинить её по образцу этой правки, не разобрав, значило бы повторить
ту же ошибку в третий раз. Разбор — следующим заходом.

**Чем закреплено.** Правило вынесено в одну функцию, и проверка спрашивает
**оба** пути на одних входах: строка, собранная сборкой, и она же после доводки
обязаны совпасть. Четыре мутации краснеют. Пин, который эти объявления должен
был сводить, содержал утверждение «разные разделители держат один пробел, как и
было» — он честно охранял дефект, и покраснел ровно тогда, когда правило стало
верным.

**Семь шагов зелёные**, сборка в `test-vault` обновлена.

### 80у. Order доезжает до доводки строки — **СНЯТО 2026-09-12, см. 83у**

**Строка в листе — `S4`**, она дополнена. Разбор — PRD 10.13.68, Р-9.

**Ваше замечание:** «я перенёс due в left block самым первым значением в
order… при активации в строке он появился в right block». Воспроизведено и
починено.

**Причина найдена трассировкой, а не чтением.** Панель по Order читалась верно,
и до сборки метка уже стояла слева. Уносила её вправо **доводка** строки: она
решала «этот токен правый» по тому, в каком списке правил объявлена метка, — а
списки правил Order не отражают. В момент доводки левых меток было ноль, а все
четыре лежали справа.

**Пробросить Order пришлось в два места**, и второе нашлось только той же
трассировкой: доводка зовётся дважды, и первый вызов получал Order, а второй
нет — метка возвращалась вправо. На экране это выглядело бы как «правка не
подействовала».

**Чего не сделано:** второе замечание того же письма — при открытии TagWheel на
левом Block активным оказывается второе поле порядка, а не первое. Это слой
настроек, а не сборка строки: активное поле выбирается по отдельному ключу
порядка, и вопрос в том, кто его пишет. Разбор отдельный, берусь следующим.

**Три мутации краснеют**, семь шагов зелёные.

### 82у. Активное поле TagWheel — по тому, что видно — **сделано 2026-09-12**

**Строка в листе — `S5`.** Разбор — PRD 10.13.69, Т-8; исключение к З3 — сорок
восьмое.

**Взял ваш `data.json`, как вы и сказали, и случай воспроизвёлся сразу:**

- панель слева шла вашим порядком — элемент-дата, важность, тип;
- активным вставало **первое поле списка по типу**, то есть важность. Вы так и
  написали: «активное поле было imp, а не Due»;
- в правом Block индекс активного поля уходил **за край** отрисованного списка:
  2 при двух видимых.

**Причина.** Панель строится по вашему порядку — и она права. Активное поле
выбиралось индексом в другом списке, разложенном по типу. Два разных множества,
и индекс одного применялся к другому.

**Починено:** там, где поле никто не назвал, оно берётся из самой панели —
первое из видимых, — и запоминается именем, а не местом. Явный выбор настройки
остаётся за ней.

**Первая версия правки была слишком строгой** и отвергала даже поле, названное
настройкой; проверка это уронила, и граница проведена заново.

**Две мутации краснеют**, семь шагов зелёные.

### 89у. Контрол «на каком Field открывается TagWheel» — **сделано 2026-09-12**

**Строка в листе — `S11`.** Разбор — PRD 10.13.76; исключение к З3 пятьдесят
второе, сделано по вашему слову «работай автономно».

**Ваше решение от 2026-09-11** — три значения, и все три сделаны: первое поле
блока, среднее со смещением влево и поле, которое вы назовёте сами, отдельно
для левого и правого блока. Формулу середины взял вашу: два поля дают первое,
три — второе, четыре — второе, пять — третье. Проверено числами.

**Где искать:** вкладка `Visual`, группа `TagWheel opening`, сразу под
`TagWheel`.

**Место контрола — моё решение, и я его не скрываю.** В группе `TagWheel` было
одиннадцать настроек, а больше двенадцати проверка не пускает: три новые туда
не помещались. Двигать ваши согласованные строки ради места я не стал и выбрал
то, что легче всего переиграть. Скажете «в TagWheel» — перенесу, но тогда
придётся что-то оттуда убрать.

**Середина считается по тем полям, которые вы ВИДИТЕ**, а не по внутреннему
списку: они разложены по типу, и середина у них другая. Это та же разница, из-за
которой в сентябре панель открывалась не на том поле.

**Сторож нашёл дыру в этой же правке:** названное поле искалось в списке по
типу, и на элементе выбор молча не срабатывал. Починено тем же приёмом, что и в
сентябре: решает имя, а не место в списке.

**Чем закреплено:** шесть утверждений, семь подмен краснеют. **Семь шагов
зелёные**, 69 проверок; сборка в `test-vault` обновлена.

### 90у. Пересверка его файлов замечаний — **сделано 2026-09-12**

**Строка в листе — `S12`.** Документ — `docs/OWNER_REMARKS_RECHECK_2026-09-12.md`.

**Повод нашёлся сам.** Разбирая `Т9`, я наткнулся в `Test 1.md` на пункт,
починенный 1 сентября: он провисел у вас открытым одиннадцать дней. Значит вы
носите в голове работу, которой нет, и я прошёл ваши файлы целиком.

**Двадцать семь пунктов похожи на сделанные**, и у каждого сказано, чем это
подтверждено: спрошено у схемы панели, найдено в коде или измерено прогоном.
**Два не сделаны**, и это видно по схеме. **Восемь** остаются открытыми по вашим
же более поздним словам. Остальное решает ваш глаз, и машине там сказать нечего.

**Один из двух несделанных стоит обсудить.** Вы просили хедер `Inline to Note` с
заглавной `N`, а принятое правило текстов пишет с заглавной только сущности
плагина — `note` обычное слово. Сделать можно, но это исключение из правила, и
решать вам.

**Ваши файлы я не правил.** Вычеркнуть пункт может только ваше слово.

**И про метод, потому что он чуть не соврал.** Первый обход раздавал вердикты
сам и ошибся трижды из двадцати: `Commands` совпало с заголовком `Binder`,
сверка регистра пропустила `note` против `Note`, а выбор папки посчитала
текстбоксом. Поэтому машина отдаёт только факты, а вердикты я вынес чтением.

### 91у. Панель стирала соседние значения — **сделано 2026-09-12, вечер**

**Строка в листе — `S13`.** Ваши слова: «в строке `- #work || ` через tagwheel
изменил Imp, получил `- #/1 || `, ожидал `- #work #/1 || `» и «в строке
`- #/1 || ` изменил value у field Cat, но ничего не произошло». Оба случая
воспроизведены на вашем `data.json` до буквы.

**Это один дефект, и он объясняет, почему панель и команды расходились.**
Значения вашего Field важности записаны `#/1`, `#/2` — решётка, а сразу за ней
косая черта. Перенос значения по Block считал такой токен парой «родитель и
ребёнок» по одному признаку: в токене есть косая черта. Родителем получалась
одна решётка, а следом шла уборка «снять из строки всё, что начинается с
родителя» — и она выносила **каждый** тег строки.

**Почему командой то же самое работало.** Шаг по значению (`Imp next`) эту
уборку не зовёт вовсе: она объявлена только на пути панели. Отсюда и ваше
«tagwheel и command field next ведут себя по-разному».

**Правило было объявлено трижды**, и это третье объявление оказалось наивнее
двух первых: разбор панели и гидратация строки требуют, чтобы родитель был
токеном, а не одним знаком. Теперь правило одно и живёт в
`src/core/shared_utils.js`; все три места спрашивают его.

**И отдельно — почему этого не увидел набор.** Проверок с именем «применение
TagWheel» в нём двенадцать, и **ни одна не открывала панель**: у элементов
фикстуры не задан значок, панель отказывалась открываться со словами «these
Fields need an emoji», строка оставалась прежней — а утверждения спрашивали
ровно её. Двенадцать зелёных утверждений не проверяли ничего. Значки фикстуре
заданы, и рядом поставлен положительный контроль: не открылась панель — проверка
падает, а не зеленеет.

**Чем закреплено:** новая проверка на ваш случай (значение с косой чертой и
соседний Field, стоящий в Order **раньше**; в порядке по умолчанию поломка не
видна), подмена «вернуть прежний признак» краснеет. **Семь шагов зелёные**,
69 проверок из 69; сборка в `test-vault` обновлена.

### 92у. Панель открывалась не на первом поле вашего порядка — **сделано 2026-09-12, вечер**

**Строка в листе — `S14`.** Ваши слова: «при `wheel-active-field = First field`
в моём текущем order при открытии tagwheel в left block первое активное поле
было `Cat`, хотя должно было быть `Due`». Воспроизведено на вашем `data.json`:
при выборе «первое поле» активным вставал `Cat`.

**Причин оказалось две, и обе — второе объявление одного правила.**

**Первая.** Поле выбиралось по имени верно — `Due`, — а следом имя бралось
заново, **номером** в списке по типу. Элемента-даты в том списке нет вовсе, и
номер попадал в первый тег. Вторая такая же строка стояла на переходе в
соседний Block по `Tab`. Обе сняты: имя кладёт тот, кто его выбрал, а
согласование с номером делает одно место.

**Вторая.** Значение `First Field of the Block` **не отвечало ничего** — оно
означало «решай по-старому», и решал старый ключ правил. Теперь «первое поле»
отвечает первым полем вашего порядка.

**Порядок выбора стал таким, и он проговорён вслух:** названное вами поле
сильнее посчитанного. Сначала поле, названное контролом (`A Field you choose`),
затем поле, названное в правилах ключом `lead`, и только потом посчитанное —
первое или среднее. Иначе правило «взять первое» отменяло бы ваше прямое
указание.

**Чем закреплено:** две новые проверки — «панель открылась на первом поле
Order, даже когда первым стоит элемент» и «первое поле сильнее старого ключа
правил», — обе краснеют на возвращённом коде. **Семь шагов зелёные**, 69
проверок из 69.

**И заведена вторая фикстура правил** — `tests/fixtures/owner_shape_rules.md`, —
потому что на первой этот дефект **неразличим**: у неё совпадают разделители, а
имя поля элемента не равно ключу Order, то есть формы, которой плагин не пишет.
Новая повторяет формы вашего vault: разделители `||` и `::`, имя элемента равно
ключу, значение важности с косой чертой сразу за приставкой и формат из двух
слов.

### 94у. Панель ставила значение элемента в правый Block — **сделано 2026-09-12, вечер**

**Строка в листе — `S16`.** Ваши слова: «на пустой строке активировал tagwheel
и выбрал `due` — получил `-  :: 📅…`, а должен был `-  📅… || `, поскольку
`Due` находится в left block». Воспроизведено на вашем `data.json`, и это же
объясняет вторую половину замечания — «появляется в right block, после этого не
могу его убрать через tagwheel».

**Причина названа измерением, а не догадкой.** Панель ставила значение **верно**
— строка была `- 📅… || ` за шесть шагов до конца, — а последняя доводка
уносила его обратно. Она решает, что хвост строки принадлежит правому Block, и
спрашивала об этом **список правил**: метки берутся у полей правого списка. Ваш
`Due` в этом списке и объявлен, хотя Order говорит «слева». Теперь доводка
спрашивает Block по Order — тот же ход, что 10.13.73.

**Второе, и без него правка была бы половинчатой.** Та же доводка разбирала
строку **словами**, а ваше значение занимает два слова: `📅2026-01-02 03:04`.
Первая версия правки оставляла слева метку с датой и уносила вправо одно
`03:04`. Теперь тело разбирается значениями, а где кончается значение —
спрашивается у того единственного объявления, которое знает формат поля
(10.13.71).

**Про старые строки скажу прямо.** Те, что уже лежат в заметках с `due` в правом
Block, лечатся первым же открытием панели и применением: значение встаёт на своё
место по Order. Убрать его панелью получится **следующим** открытием — на такой
строке панель ещё не показывает значение своим.

**Чем закреплено:** проверка «панель не уводит значение элемента в правый Block,
когда по Order его место слева», подмена «вернуть метки по стороне списка»
краснеет. **Семь шагов зелёные**, 69 проверок из 69.

### 95у. Курсор при прыжках вставал не туда — **сделано 2026-09-12, вечер**

**Строка в листе — `S17`.** Шесть ваших примеров из `S4`, снятых с экрана.
Воспроизведены все шесть — до символа, на вашем `data.json` и вашими
настройками (`Line by line`, `End of your text`, шаг `sentence`).

**Причина одна на все шесть, и она знакомая.** «Где на строке кончается ваш
текст» было объявлено **четырежды**: один раз в общем модуле, которым ставят
курсор все движки PKM, и трижды в навигации — у прыжка, у шага внутри строки и
у догадки «единственный знак на строке — это второй разделитель». Три
навигационных объявления не знали двух вещей: что значение элемента текстом
человека не является и что пустой слот под текст — это место **между**
разделителями, а не вплотную к первому.

**Теперь объявление одно.** Прыжок и шаг спрашивают общее правило; свои ходы
остались запасными — они отвечают там, где разделителей в строке нет вовсе.
Заодно до прыжка доехали **метки элементов**: без них правило не отличало хвост
с датой от вашего текста.

**Догадка сужена до случая, ради которого она и заведена:** когда оба
разделителя записаны одинаково, отличить их нельзя ничем. У вас они разные —
`||` и `::`, — и там, где знак сам всё решает, догадка больше не участвует.
Именно она читала ваш `📅…` перед `||` как текст и объявляла зоной текста левую
часть строки.

**Четыре случая из шести теперь дают ровно то, что вы написали.** Два — шаг
вправо из зоны значений — дают **начало** вашего текста, а вы ждали конец:
вторым нажатием курсор доходит до конца. Это не недоделка, а вопрос к вам, и он
задан строкой листа: должен ли шаг из зоны значений прыгать сразу в конец
текста.

**Чем закреплено:** шесть проверок на ваши случаи с **разными** разделителями —
прежние проверки все до одной гоняли одинаковые, и на них дефект неразличим
(У-147). Плюс поправлены два прежних утверждения: одно требовало, чтобы концом
текста считался зазор перед разделителем, второе сверяло старый шов команды.
**Семь шагов зелёные**, 69 проверок из 69.

### 96у. `Templates folder` встал выше `New notes folder` — **сделано 2026-09-12, вечер**

**Строка в листе — `S18`.** Ваш пункт из `Test.md` (1.6.2.2): «поменяй местами
очередность `Templates folder` и `New notes folder`». Пересверка того же дня
показала, что он не сделан, — теперь сделан.

**Порядок стал такой:** `Templates folder`, `Default template`, `New notes
folder`. Дочерняя строка поехала **вместе со своей папкой**: между ними не
должно стоять чужих строк — это ваше же замечание C43 от 2 сентября, и я не стал
менять одно вашим словом, ломая другое. Скажете «хочу `New notes folder` сразу
за `Templates folder`» — переставлю, и `Default template` уедет ниже.

**Правка сделана в прототипе**, а схема и PRD выведены из него: руками их не
правят. Семь шагов зелёные.

### 99у. Перетаскивание Field на самый верх Block — **сделано 2026-09-12, ночь**

**Строка в листе — `S19`.** Ваши слова: «в `io-fields-list` как-то неудобно
работает drag-n-drop — при переносе fields в left block не могу их перетащить
на верхнее значение: при наведении выбранного поля наверх выделяется сразу вся
таблица field block, и при отжатии выбранное поле перемещается в конец этого
block».

**Причина простая, и она объясняет обе половины.** Выше первой строки лежит
только подпись стороны — `LEFT BLOCK`, — а своего броска у неё не было. Событие
доставалось стороне целиком: отсюда и подсветка всей таблицы, и «уехало в
конец» — бросок на сторону значит именно «в конец». Места, которое значило бы
«в начало», не существовало вовсе: стрелка `↑` у первой строки уводит Field на
другую сторону, а не поднимает его.

**Что теперь.** Подпись стороны — это место «перед первой строкой». При
наведении под ней загорается черта — ровно там, куда Field встанет, — и вся
таблица больше не подсвечивается. Бросок на пустое место стороны по-прежнему
значит «в конец»: это две разные цели, и теперь у каждой своя.

**Правый Block работал не лучше** — у него та же подпись и было то же
поведение. Просто туда вы переносили Field, которому конец списка и так
подходил.

**Как проверить, за минуту.** Возьмите любой Field за ручку `⠿` и наведите на
подпись `LEFT BLOCK`: должна загореться черта под подписью. Отпустите — Field
обязан встать **первым**. То же на `RIGHT BLOCK`. И проверьте, что бросок на
пустое место под списком по-прежнему ставит Field в конец.

**Чем закреплено:** проверка, которая сперва уводит Field в конец стороны — это
её положительный контроль, — и только потом бросает на подпись; подмена «бросок
без указания, перед кем» краснеет. Правка сделана **в обоих** местах, которые
это рисуют: прототип и панель (У-116). **Семь шагов зелёные**, 69 проверок из
69.

### 100у. `Start of your text`: одна строка решает оба хода курсора — **сделано 2026-09-12, ночь**

**Строка в листе — `S17`.** Ваши слова: «добавь в `heading-jumps-cursor` опцию
`Start of your text` — два режима будут определять, в начало или конец моего
текста курсор должен попадать». И ваш ответ на уточняющий вопрос: действовать
выбор должен **на оба места одной строкой**.

**Что появилось в панели.** У строки `Navigation → Jumping between headings →
Cursor position after jumping` теперь четыре положения вместо трёх: `Line
start`, `Line end`, **`Start of your text`** и `End of your text`. Новой строки
не добавилось — предел в двенадцать строк группы не тронут, и это была
единственная цена, из-за которой я рекомендовал не заводить настройку вовсе.

**Что она теперь решает.** Два хода, а не один. Первый — прыжок по заголовкам,
как и раньше. Второй — шаг вправо, которым вы входите в текст **из зоны
значений**: раньше он всегда вставал в начало вашего текста, а вторым нажатием
доходил до конца, и в вашем примере вы ждали конец сразу. Теперь говорит эта
строка. Ответ «в начало или в конец» объявлен один раз и спрашивается обоими
ходами: два объявления разошлись бы молча, и это ровно то, чем оказались пять из
шести ваших замечаний накануне.

**Одно сужение я сделал сам, и говорю о нём прямо.** Правило действует при
`Step size = Sentence` — это то, что стоит у вас. В режиме `Word` остановки —
сами слова, и первое слово текста и есть место, куда вы шагаете: увести курсор
сразу в конец значило бы отменить ход по словам. Скажете «хочу так во всех
режимах» — снимается одним условием.

**Как проверить, за две минуты.** Поставьте `End of your text` и на строке вида
`- [ ] 📅… #/1 || ывааы` встаньте среди значений, нажмите шаг вправо: курсор
обязан оказаться **в конце** `ывааы`, одним нажатием. Переключите на `Start of
your text` и повторите: тем же нажатием он обязан встать **перед** `ывааы`.

**И второе, за минуту:** при `Start of your text` прыжок по заголовкам на такую
строку обязан ставить курсор туда же — в начало вашего текста, сразу за
значениями.

**Чем закреплено:** четыре утверждения, и каждое спрашивает **обе** стороны
настройки — иначе «курсор в конце» было бы зелёным и у плагина, который
настройку не читает вовсе: конец и есть умолчание. Две подмены краснеют.
**Семь шагов зелёные**, 69 проверок из 69.

