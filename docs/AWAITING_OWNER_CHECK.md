# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Реестр на 2026-09-07, вторая автономная сессия.** Заказчик прочёл лист,
отметил галочками четыре пункта, оставил три замечания по одному и ответил на
все три вопроса. Дальше опять ушёл, сказав «учти комментарии и продолжай фазу 6,
работай автономно».

**Пять строк ушли из реестра: их подтвердили ваши галочки** — снятие команды
`Open settings`, сообщения плагина в редакторе, механизм перевода руководства и
оба замечания к листу (один файл в папке текстов, пункт без чекбокса). Из листа
они тоже вычищены.

**Осталось живого пять строк:** починка загрузки и проверка сборки (раздел 16у,
новый), живое обновление колонки `Preview`, поведение редактора Fields в
реальной заметке и документы `README.md` с `instructions.md`.

**Открытых вопросов нет.** Судьбу служебного файла правил заказчик поручил решить
мне — решение и его порядок в PRD 10.13.52, П-8.

Проверок в наборе **52**, пропусков ноль. Ни одного файла под З3 не тронуто.

Ниже — прежняя шапка и разделы прошлых заходов.

**Реестр на 2026-09-05, поздний вечер, после девятого заполненного листа.**
Заказчик подтвердил галочкой уведомление о хоткеях (E2), прислал вывод консоли
по переезду конфига (E1), дописал два замечания к свежему vault (E3) и свой
текст руководства (F2). Всё это разобрано и сделано — раздел 7с ниже. Тем же
вечером он заказал **две новые функции** и ушёл: `Smart Delete` и цвет
каретки — раздел 7т.

Проверок в наборе **49**, пропусков ноль. Ни одного файла под З3 не тронуто,
исключений по-прежнему восемнадцать.

## Что изменилось 2026-09-03, третий заход

Вы заполнили лист третий раз: **12 галочек, 8 строк с замечаниями (за ними
пять разных), 19 не проверено**. Галочка — это ваше слово, поэтому:

- **Строки, закрытые вашими галочками, ушли из реестра.** За три захода это
  83 пункта листа: 51, 20 и 12. Двенадцатая — **B7, «?» у заголовка**: вы
  отметили её 2026-09-03, уже после того как правка была сделана. Строка ниже
  осталась: та же правка стоит в листе ещё тремя адресами (C18, C41, C42), и
  снять их может только ваше слово.
- Раздел 7г закрылся целиком и заменён разделом 7д: одиннадцать его строк
  подтвердили вы, остальные три ушли в новые правки вместе со своими
  доделками.
- **Ни одна правка этого захода не потребовала файла под З3.**

Разбор всех ваших замечаний — `docs/OWNER_REMARKS.md`, **сессия 6**: по
каждому файл, функция, метка З3, цена и пин.

**Две вещи, которые прошлый заход объявил «только глазами», оказались
проверяемыми машиной.** Причина обеих была не в нашем коде, а в поведении
платформы, которого нет в её типах: `app.js` Obsidian 1.13.7 распакован и
прочитан. Из трёх «непроверяемых глазами» вещей осталась одна — как читается
панель TagWheel в живом редакторе.

## Как ведётся этот файл

1. **Адрес замечания — ваш текст, а не мой номер.** Сопоставлять ваше
   замечание со строкой реестра — моя работа, не ваша.
2. **Замечания приходят листом приёмки и блоками в `test-vault/Test.md` и
   `test-vault/Test 1.md`, и все три перечитываются каждую сессию** (У-30).

## Правила

- **Строка уходит из реестра только с вашим словом.** Не «проверки зелёные»,
  не «выглядит правильно» — ваша галочка в листе или ваше «принято».
- **Статус ставите вы**, одним из трёх: `не смотрели` (по умолчанию),
  `замечания` (со ссылкой на них), `принято`. Статус `замечания` я ставлю сам,
  когда ваше замечание попало ровно в эту строку, — это не приёмка, а адрес.
- Если что-то из перечисленного вы уже смотрели раньше — скажите, и строка
  уйдёт. Я не знаю, что именно вы открывали в Obsidian, и записал всё, что
  сделано, а не подтверждено.

Как проверять — `docs/MANUAL_TEST_PLAN.md`. Открытые вопросы, ждущие вашего
решения, а не проверки — `docs/OPEN_QUESTIONS.md`.

## 1. Панель целиком

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| Панель переехала на декларативное API Obsidian 1.13, старая удалена | вся панель | гейты Г1–Г22, дым по 40 блокам | не смотрели |

*Ушли по вашим галочкам 2026-09-02:* семь вкладок и 35 групп (C1), читаемость
текстов (C2), узкая панель (C3), светлая и тёмная тема (C4), общий поиск
Obsidian (C5), одна подсказка на узел (C6).

## 2. Свои блоки

Пять блоков нарисованы своим кодом. Машина видит здесь меньше всего.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| Binder: строки, свои команды, колонка хоткея | `Keyboard → Binder` | `binder_tests.ts` | **C13 доделан 2026-09-02**, см. 7г: повтор виден в окне |

*Ушли по вашим галочкам:* редактор Fields — один список, перетаскивание через
линию, правая колонка (C22, C24, C25, C26, C27, C28, C30); возврат скролла и
каретки (C29); строки Binder, свои команды и колонка хоткея (C12, C15, C16);
Smart Rules — правила, условия, предупреждение о конфликте (C49, C50, C51);
свои цвета тегов (C37, C38); справочник команд (C14, C17); колонка хоткея
через приватное API (C16).

## 3. Конфиг и миграция — фаза 2, пункт 4

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| Движки читают пути версии 2, мост путей снят | поведение любой настройки | карта чтений, 38 файлов проверок | не смотрели |
| Переезд `data.json` с версии 1 на версию 2 без потерь | ваш рабочий vault, первый запуск | ваш настоящий файл: 266 листьев, потерь нет, `_unmigrated` пуст | не смотрели |
| Копия `data.backup.v1.json` рядом с конфигом (МГ4) | папка плагина после переезда | пин `loadConfig` | не смотрели |
| Порядок трёх ступеней миграции держится пином | — | 10 проверок, 10 мутаций | не смотрели |
| Пять переименований старой формы ожили (В-10) | настройки после обновления с очень старой версии | пин на конфиге из одних старых имён | не смотрели |

## 4. Команды — фаза 2, пункты 8–9

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| Идентификаторы в kebab-case без приставки плагина | палитра, экран Hotkeys | `command_ids_tests.ts`, 10 мутаций | не смотрели |
| Имена без приставок модулей, из списка прототипа | палитра команд | тот же пин | не смотрели |
| Одноразовое уведомление о сменившихся хоткеях + карта в консоли | первый запуск после обновления | пин на однократность | не смотрели |
| Карта старых и новых ID | `docs/command_ids_v1_v2.md` | выводится генератором | не смотрели |

## 5. Умолчания — исполнение вашего ответа по В-7

| Что | Где смотреть | Статус |
|---|---|---|
| Transform выключен из коробки, тумблер на месте | `Transform → Inline to note` | не смотрели |
| Заметка после Transform не открывается, но есть включаемая настройка | `Transform → Note content` | не смотрели |
| Заголовок вставки `## Captured` | `Transform → Note content` | не смотрели |

*Ушла по вашей галочке:* Tag Bars выключены из коробки и рисуют полосы
(C40, D7) — вместе с ней снято замечание 1.5.3.2 «включаются, но ничего не
рисуют».

## 6. Новые функции слоя настроек (10.13)

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Копии настроек: сохранение и восстановление.** Из шести пунктов листа вы подтвердили четыре 2026-09-04 (C55, C57, C58, C60); остались C56 — снятие копии перед записью сделать необязательным — и C61 — что показывает окно при повторном восстановлении | `Advanced → Settings backup` | 17 проверок, 22 мутации | не смотрели |
| ~~Перенос настроек на второе устройство заметкой~~ | — | — | **ушла 2026-09-05: подтверждено вашими галочками на E4 и E5** |

## 7. Документы

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| Заметка-руководство, создаётся один раз и дальше открывается | `General → Help → Read` | 4 проверки, 7 мутаций | **C9 доделан 2026-09-02**, см. 7г: у строки теперь настоящая кнопка `Read` |
| `README.md` приведён к новой панели, предупреждение о хоткеях | репозиторий | пин документов, 13 мутаций | не смотрели |
| `instructions.md` + глоссарий терминов 7.3 | репозиторий | тот же пин | не смотрели |
| Отчёт о разошедшихся гифках, сами гифки не тронуты | `showcase.md` | 30 файлов байт в байт | отложено 2026-09-01 — гифки к финальной версии для GitHub (В-11) |

## 7а и 7б. Правки по замечаниям сессий 1, 2 и 3 — что от них осталось

**Ничего не осталось.** Все строки этих двух разделов закрылись 2026-09-02:
часть — вашими галочками (двадцать пять строк ушли из реестра целиком), часть —
правками по вашим же замечаниям, и эти правки перечислены ниже, в разделе 7в.

Формулировки строк и то, чем каждая была закреплена, — в архиве:
`docs/archive/OWNER_REMARKS_session1.md` и
`docs/archive/OWNER_REMARKS_sessions2_3.md`.

Одно из этих двух разделов стоит помнить и после закрытия: **тумблер
`Cycle in both directions` стоял в панели с самого начала и не читался никем**, а
**четыре «пропавшие функции» подряд оказались функциями без контрола**. Пятая
нашлась 2026-09-02 и была обратной: контрол на месте, а движок читал имя ключа
версии 1 (У-39).

## 7в. Правки по первому заполненному листу — 2026-09-02

**Раздел закрыт.** Двадцать его строк подтвердили ваши галочки второго захода
(A3, B9, B16, B18, B19, C7, C8, C11, C20, C31, C35, C39, C43, C44, C48, C51,
C52, D2, D3, D4), остальные вы вернули с замечаниями — и они переехали в
раздел 7г вместе со своими доделками.

Формулировки строк и то, чем каждая была закреплена, — в архиве:
`docs/archive/OWNER_REMARKS_session4.md`.

Одну вещь из этого раздела стоит помнить и после закрытия: **гейт, доказавший
наличие «?» у заголовков, ничего не сказал про то, что «?» делает** — в
подделке компонента кнопки обработчик нажатия выбрасывался (У-43). Четыре
захода правка считалась сделанной.

## 7г и 7д. Правки по третьему заполненному листу — 2026-09-03

**Пять строк раздела ушли 2026-09-04 по вашим галочкам:** подсказка у заголовка
(C18, C41, C42), приклеенная полоса вкладок, Field в условиях правила (B14) и
две строки про полосы (B22).

**Одиннадцать строк раздела 7г ушли из реестра: вы подтвердили их галочкой
2026-09-02** — панель TagWheel, цвет активного Field, коробка скроллера,
предпросмотр `Source line`, предпросмотр свойства, повтор в Binder, строка с
кнопками, тексты подсветки, перемещение заголовка, перемещение текста,
эмодзи-элемент и свойство `tags`. Разбор каждой остаётся в
`docs/archive/OWNER_REMARKS_session5.md`.

Разбор пяти замечаний третьего листа — **`docs/OWNER_REMARKS.md`, сессия 6**:
файл, функция, метка З3, цена и пин. Здесь только то, что сделано и закреплено
машиной.

**Главное этого захода: две вещи, которые прошлый заход объявил
«только глазами», оказались проверяемыми.** Причину обеих нашли не в нашем
коде, а в коде платформы: `app.js` Obsidian 1.13.7 распакован из
`obsidian-1.13.7.asar` и прочитан. **Ни одна правка не потребовала файла под
З3** — тринадцатое исключение не понадобилось.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Вводные фразы групп стали видны.** Их не показывали **ни в одном режиме** и ни разу: они ехали той же выброшенной строкой. Прототип рисует их абзацем под каждым заголовком | под заголовком любой группы, 28 мест | тот же файл: строка рисует фразу своим абзацем, и текст сверяется со схемой | не смотрели |
| **Пиктограмма сброса группы вернулась к правому краю**, «?» остался у текста заголовка. Обе кнопки платформа кладёт в одну колонку, поэтому развести их может только своя пометка. Заодно снят мёртвый отбор: `.setting-item-info` в строке заголовка группы у платформы **нет** | любой заголовок группы, у которого есть что сбрасывать | тот же файл: пометка стоит ровно на кнопке сброса и не на «?» | не смотрели |

## 7е. Снятие конфиг-заметки — 2026-09-03, четвёртый лист

**Три строки раздела ушли 2026-09-04: вы подтвердили C34 галочкой.** Остались
две, и обе ждут не C34: строка про группы в панели держится за C53, где у вас
новый вопрос про служебный файл правил, а строка про руководство и README — за
F2 и F3.

Решение заказчика по замечанию C34: заметку настроек убрать целиком (В-28),
блок `Generated files` тоже (В-29), два сообщения движка переписать
(В-30, тринадцатое исключение к З3). Разбор, из которого выросли варианты, —
`docs/OWNER_REMARKS.md`, сессия 7.

**Что исчезло из панели.** На `Tags & PKM` больше нет группы `Config note`
(путь к заметке, `How much detail`, кнопки `Generate` и `Apply`); на `Advanced`
— группы `Generated files` (`Template note` и `Regenerate`). Из палитры ушли
команды `Apply config note` и `Open config template`.

**Что осталось.** Служебный файл `InlineOverhaul_Generated_RULES_TagWheel.md`
плагин пишет сам — при загрузке и после каждой правки настроек. По нему движок
читает Fields и значения, и уйдёт он вместе с мостом в фазе 6.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Группы `Config note` и `Generated files` в панели нет**, и ни одной кнопки без действия не осталось | `Tags & PKM` до конца, `Advanced` до конца | `settings_layer_tests.ts`: групп в схеме 33, привязанных настроек 99, и каждое действие реестра стоит на живой кнопке; `actions_tests.ts` сверяет реестр с генератором в обе стороны | не смотрели |
| **Руководство и README** больше не обещают заметку: перенос настроек описан через `Advanced → Settings backup` | `instructions.md`, раздел `Carrying your setup to another vault`; `General → Help → Read` | `docs_terms_tests.ts`: список снятых контролов пополнен шестью именами, и документ, который их назовёт, краснеет | не смотрели |

**Что стало меньше.** Пять модулей (4358 строк) удалены целиком, из `main.js`
ушло 522 строки, сборка `dist/main.js` — 1,7 МБ вместо 2,03 МБ. Один из трёх
швов старой формы (`readFieldsWorkshop`, У-18) снят: осталось два. Проверок в
наборе 45, пропусков ноль.

**Уборка за заказчиком проверена — 2026-09-04.** Просьба удалить лишний пустой
Field `Type` (след того `Apply`) безопасна: на копии заметки правил без этого
Field все пять оставшихся команд полей попадают в своё поле. Проверялось не из
осторожности — рядом лежит A17, где ключ Order сопоставляется с Field по номеру
в списке, и удаление ключа вместе с полем могло бы его открыть. Не открывает:
корзина снимает и ключ, и поле, а ломается A17 на расхождении между ними (У-50).

**Что осталось лежать в vault.** Заметки `InlineOverhaul_Config.md` и
`InlineOverhaul_Config_template.md` — теперь обычные заметки: плагин их не
читает и не пишет. Удалять или нет — ваше решение. В `data.json` у тех, кто
успел нажать `Apply`, может остаться ветка `pkm.fields.taxonomy.tagWheelConfig`:
её никто не читает, и стирать её при миграции я не стал — данные ваши.

## 7ж. Две поломки движка — 2026-09-04

**Две строки раздела ушли: вы подтвердили галочкой 2026-09-04** — что хоткей
поля правит своё поле (D11) и что проверка A16 включена. Осталось две, обе про
текст строки.

Разрешение В-31: «обе поломки сразу». Четырнадцатое исключение к З3, оба файла —
`pkm_v2/status_tags.js` и `src/core/line_pipeline.js`. Разбор с ценой каждой
правки — PRD, строки A16, A17, A18; уроки У-48…У-52.

**Что было видно вам.** Строка, в которой тег плагина стоит **после** текста,
при нажатии хоткея поля получала копию текста. На ваших настройках:
`- задача #todo` и хоткей Field `Type` давали `- #source задача || задача #todo`
— «задача» дважды, и старый `#todo` оставался лежать. Теперь та же строка даёт
`- #source || задача`.

**Что было не видно вам и не сработало бы сегодня.** Движок искал поле для
команды **по номеру** ключа в списке порядка. Ваш конфиг это переживал: все шесть
команд попадали в своё поле. Спасал случай — дубль от переименования `type` →
`Type`, из-за которого старое поле осталось лежать в заметке правил. На копии
заметки, где переименование действительно переименовало, хоткей Field `Type`
уходил в `Category` и молча писал в строку `#work`. Теперь связь «ключ → поле»
читается оттуда, где её и пишет переименование, и на той же копии хоткей даёт
`#todo` — значение своего поля.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Текст не удваивается**, когда тег плагина стоит в строке после текста | поставьте курсор на строку вида `- задача #todo` и нажмите хоткей Field `Type`; должно выйти `- #source \|\| задача`, а не «задача» дважды | четыре проверки в `status_runtime_behavior_tests.js`; две мутации — снятие правки и её наивная версия — краснеют обе | не смотрели |
| **Ваш собственный тег остаётся вашим**: не исчезает из текста и не переезжает в панель поля | строка вида `- заметка #мойтег`, хоткей любого Field: `#мойтег` должен остаться там же, где стоял, за разделителем | две проверки: чужой тег и чужая ссылка выживают ровно по разу и остаются за разделителем. Первая версия правки уносила тег в панель, и все 45 проверок при этом были зелёными — второй пин появился из-за мутации | не смотрели |

**Уборка за вами не изменилась и остаётся безопасной.** Лишний пустой Field
`Type` можно удалять корзиной: проверено на копии заметки правил до правки и
после — все оставшиеся команды попадают в своё поле.

**Что стало меньше.** Снята мёртвая обёртка `isCompatibleFieldForKey` и снята
позиционная догадка целиком. Проверок в наборе 45 файлов, пропусков ноль; в
файле поведения движка их стало на пять больше.

## 7з. Пять доработок по вашему письму — 2026-09-04

**Пять строк раздела ушли: вы подтвердили их галочкой 2026-09-04** — красную
кнопку сброса (C64), окно выбора копии (C65), снятый пункт переезда (C66),
раздел `# Your notes` (C67) и возврат хоткеев (D13). Осталась одна — короткое
имя дочернего Field.

Ваши пять пунктов, разобранные и сделанные тем же заходом. Два упирались в ваши
же запреты, и вы их сняли: В-32 (короткое имя дочернего Field — пятнадцатое
исключение к З3) и В-33 (хоткеи — второе исключение к запрету на служебное API).
Разбор и цена каждой правки — PRD, требования Б17–Б19 и раздел 10.13.2.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| ~~**Дочерний Field в TagWheel носит короткое имя родителя**~~ — **правка не работала, доделана 2026-09-04 вечером, см. 7и.** Вы написали «по прежнему вижу cat и category_sub»; так и было. Шесть утверждений держали недостижимую ветку: конфиг они отдавали движку напрямую, а тот получает его через `parseOrderConfig`, который досыпает в `labels` сам ключ (У-55). Запись «прогнано на вашей настоящей заметке правил: `Cat_sub`» была неверна — прогон шёл не тем путём, каким идёт плагин | — | — | закрыто строкой в 7и |

**Ответ на ваш вопрос про остальные настройки.** Они в копии уже были все:
General, Navigation, Tags & PKM, Visual, Transform, Advanced — весь `data.json`,
кроме того, что принадлежит конкретному компьютеру (открытая вкладка,
развёрнутые группы, показанные один раз уведомления). Не было в ней только
хоткеев, и это потому, что Obsidian держит их не в настройках плагина, а в своём
`hotkeys.json`. Теперь они там отдельным блоком.

**Чего в этих правках не делалось.** Обратная функция `resolveOrderKeyForField`
в `pkm_v2/status_tags.js` по-прежнему делает позиционную догадку в другую
сторону (см. 7ж): в наборе её ветка не отрабатывает ни разу, и вслепую я её не
трогал.

## 7и. Восьмой заход — 2026-09-04, вечер, без вас

Вы ушли со словами «автономно исправляй замечания, оставь вопросы на конец».
Сделано семь правок: одна доделка вашего замечания D12, две по замечанию C56 и
четыре по вашим свободным замечаниям. Плюс письменный ответ на вопрос из C53 —
он в `docs/OWNER_REMARKS.md`, сессия 8, раздел «C53».

**Пять пунктов из семи вы подтвердили галочкой тем же вечером** — C53, C56,
D12, G1 и G2, — и они ушли из реестра. Оба исключения к З3, сделанные без
разрешения, приняты вашим словом (В-34 и В-35): исключений стало 17. Умолчания
трёх новых тумблеров закрыты теми же галочками (В-36…В-38).

**Осталось две строки:** субхедеры и переименованная группа.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Группа `Move left and move right` разделена субхедерами** `Move text` и `Move line`. Две таблицы порядка остались выше обеих подписей: они про всю группу. Смена отступа ушла под `Move line` — это тоже работа со строкой | вкладка `Navigation`, вторая группа | проверка в `settings_layer_tests.ts` ищет подпись **по тому, над чем она стоит**, а не по месту в массиве, и читает то, что подпись рисует. Две мутации краснеют | **посмотрели, есть замечание:** у субхедеров нет подсказок, и под каждым две горизонтальные линии вместо одной. Разбор — новой сессией |
| **Группа переходов по заголовкам называется `Moving cursor inside a note`** — парно `Moving cursor inside a line`. Прежнее имя было `Jumping between headings` | вкладка `Navigation`, последняя группа | проверка в `settings_layer_tests.ts` в обе стороны: новое имя есть, прежнего нет. `docs/MANUAL_TEST_PLAN.md` приведён к новому имени | галочки нет и замечания нет — жду вашего слова. Заодно приедет переименование субхедера `Move line` → `Moving lines (left and right)` |

**Чего в этих правках не делалось.**

- **Служебный файл `InlineOverhaul_Generated_RULES_TagWheel.md` не снимался, и
  вы решили, что с ним делать.** Разбор был письменный (C53), правки не было.
  Ваш выбор от 2026-09-04 вечером: **унести файл из vault в папку плагина
  сейчас, а к фазе 6 вернуться отдельно** (В-39). **Исполнено тем же вечером** —
  см. раздел 7к ниже.
- **Копия перед сбросом (`Delete all my settings`) осталась обязательной.**
  Тумблер её не касается: у сброса эта копия — единственный путь назад, и текст
  кнопки так и говорит. Если хотели и её сделать необязательной — скажите.
- **Обратная функция `resolveOrderKeyForField`** в `pkm_v2/status_tags.js`
  по-прежнему делает позиционную догадку в другую сторону (см. 7ж). В наборе её
  ветка не отрабатывает ни разу, и вслепую я её не трогал.

## 7к–7п. Четыре захода по листу — **закрыты вашими галочками 2026-09-05**

Девятый и десятый заходы (задачи H1–H8), второй заход по листу (шесть замечаний
по сделанному), третий (четыре, из них три «не работает») и четвёртый (вид
панели TagWheel). **В реестре не осталось ни одной их строки.**

Разбор каждого пункта — с местом, причиной, ценой и пином — в архиве:
`docs/archive/OWNER_REMARKS_session9.md` и
`docs/archive/OWNER_REMARKS_session10.md`. Сводка сессии 10 осталась в живом
`docs/OWNER_REMARKS.md`.

**Что из этих заходов стоит помнить, а не искать в архиве.** Четыре раза правка
была написана верно по смыслу и не работала по механике, и каждый раз объявлена
сделанной при зелёном наборе: правило стилей проигрывало каскад соседу по тому
же узлу (дважды), пересборка настроек не доходила до строки заголовка, а
сплошной слой заливки убрала собственная предыдущая правка. Уроки — У-67…У-70.

**Чего в этих правках не делалось.** Цвета тегов Obsidian внутри панели TagWheel
не видны: пузыри Value слой внутрь панели не рисует, поэтому все теги там были
одного чужого тона и ничего не различали. Различать ячейки — работа
`Non-active Field text color` и `Active Field text color`. И свёрнутое состояние
групп по-прежнему не переживает перезапуск Obsidian (5.4, ваше «как минимум в
рамках текущей сессии»).

## 7р. Пятый заход по листу — 2026-09-05, вечер

Вы дописали в лист пять пунктов. Три потребовали работы, два ждут вашего
взгляда на живом Obsidian. **Ни одного файла под З3 не тронуто**, исключений
по-прежнему восемнадцать. Проверок в наборе стало 46.

**Что оказалось главным.** Ваше замечание к E3 нашло дефект старше самого
замечания: требование ПЗ1 лежало в PRD с самого начала и не было исполнено
нигде. Из-за этого четвёртые сутки врала заметка-руководство, обещая набор,
которого не существовало, — тот же класс, что «not implemented» в README
(У-64). И там же нашлась вторая половина того же класса: пин руководства
**запрещал** называть плавающую кнопку и справочник команд, хотя кнопка
работает с 2026-09-01, а справочник стоит на вкладке Keyboard. Пин четверо
суток охранял устаревшее утверждение о состоянии.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Свежая установка получает четыре Field**: `Status` и `Priority` в `Left Block`, `Due` и `Project` в `Right Block`, шесть значений покрашены. `Start over` их стирает и не возвращает | `test3` рядом с `test-vault`: плагин стоит, `data.json` нет | `starter_config_tests.ts`, 13 проверок, 4 мутации. Проверки читают модель редактора Fields и `previewFields`, а не ветки конфига | не смотрели |
| **Заметка-руководство переписана вашим текстом** и вычитана: опечатки, длинные тире, три устаревших пути к настройкам и две фразы, расходившиеся с механикой | `General → Help → Read`. Ваш файл в `test-vault` не тронут, плагин заметку не перезаписывает | `actions_tests.ts`: разделы, отсутствие длинных тире и правдивость обещания про стартовый набор; 3 мутации | не смотрели |
| **`README.md` переписан целиком**, `instructions.md` правлен точечно | репозиторий | `docs_terms_tests.ts`, 9 проверок. Список снятых контролов теперь один на три документа, руководство сверяется им наравне с README | не смотрели |

**Чего в этих правках не делалось.**

- **`instructions.md` целиком не переписан.** 817 строк, и переписывать их в
  вашей манере без вашего слова я не стал. Сделано: сняты все длинные тире из
  текста, поправлен первый запуск (он просил завести Field руками) и приведён к
  умолчанию пример для копирования — он шёл с разделителем `::` вместо `||`.
- **Служебный файл правил в корне `test2` не разбирался.** Он приехал туда
  восстановлением старой копии: она несёт прежний путь. Перезапуск Obsidian это
  чинит сам, но до перезапуска плагин пишет служебный файл в vault как заметку.
  Записано в лист как попутная находка, разрешения на разбор не спрашивал.

## 7с. Девятый заход по листу — 2026-09-05, поздний вечер

**Ушла по вашей галочке одна строка:** уведомление о сменившихся хоткеях (E2) —
вы подтвердили, что на чистом vault его быть и не должно.

**Главное этого захода: я сказал вам неправду про переезд конфига, и вы её
поймали.** В листе стояло «271 лист на входе, 147 переехало, в `_unmigrated`
ноль». Ваш вывод из консоли показал в `_unmigrated` двадцать два листа. Данные
при этом не потеряны ни разу — врал отчёт, а не миграция (У-72).

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Переезд конфига разобран по вашему выводу.** Двадцать два старых значения уступили тому, что стоит в новой панели, и лежат в `_unmigrated`. Из них девять действительно отличались, тринадцать совпали | ваш `test4`; полный разбор — в листе приёмки, пункт E1 | `config_migration_v1_v2_tests.ts` держал это правило и раньше; починен **отчёт**: `tools/migration_report.js` печатает число листьев из самой ветки и сам говорит о расхождении | не смотрели |
| **`<your rows>` из справочника команд убрана.** Семья без единой команды больше не даёт строки вовсе: справочник называет то, что зарегистрировано, и ничего сверх того (З8) | `Keyboard → Commands & Hotkeys`, раздел `Binder`, в свежем vault | `command_reference_tests.ts`: шаблонных строк нет ни у Binder, ни у Fields, и строки без кнопки хоткея не бывает вовсе. Мутация краснеет | не смотрели |
| **Отступ плавающей кнопки уходит вместе с кнопкой.** Строка `Distance from the text` больше не гаснет, а исчезает, пока `Floating button` выключен | `Transform → Inline to note` | `settings_layer_tests.ts`: строка невидима при выключенном тумблере и при выключенном модуле. Мутация краснеет | не смотрели |
| **Заметка-руководство взяла ваши дописки.** Группа настроек у `Move line up`, закрытие TagWheel тем же хоткеем, порядок Fields и колонки цвета, как Value делается дочерним и когда дочерний доезжает до TagWheel, и абзац про команды, которые Field заводит себе сам | `General → Help → Read` | `actions_tests.ts`: **каждый путь вида `Вкладка → Группа → Строка` сверяется со схемой**. Три устаревших пути в прошлый раз нашлись глазами; теперь это машина. Мутация краснеет | не смотрели |

**Что в этих правках поправлено против вашего текста.** Имена приведены к
панели: тумблер Field называется `Child Field`, а не `Child Fields`; команды
строки — `Move line up` и `Move line down`, а не `Move up` и `Move down`.

**Чего в этих правках не делалось.**

- **Служебный файл правил в корне `test2` не разбирался** — вы про него ничего
  не сказали, разрешения не спрашивал. Строка остаётся в листе.
- **Девять разошедшихся значений не возвращены.** Победила форма новой панели —
  это записанное решение МГ7, и менять его без вашего слова я не стал. Список
  всех девяти — в листе, пункт E1.

## 7т. Две новые функции — 2026-09-05, поздний вечер, без вас

Вы ушли со словами «я собираюсь уйти — я хочу, чтобы ты добавил две новых фичи…
работай автономно, если есть вопросы — задай их в интерактивном опросе в конце».
Обе сделаны, обе описаны в PRD до кода (Р13): 10.13.32 и 10.13.33. **Ни одного
файла под З3 не тронуто.**

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **`Smart Delete`.** `Del` в конце строки приносит слова следующей строки без её отступа и без Prefix. Строка, в которой один Prefix, исчезает целиком. Три тумблера: снимать ли Prefix, ставить ли пробел на стыке и делать ли то же на `Backspace`. **Все умолчания выключены** | `Keyboard → Smart Delete`, над `Binder` | `smart_delete_tests.js`, 10 проверок. Семь мутаций краснеют: снятие Prefix, условие конца строки, исчезновение пустой строки, пробел на стыке, граница курсора у `Backspace`, его отдельный тумблер и пробел на стыке вверх | не смотрели |
| **Цвет текстового курсора.** Каретка красится своим цветом; пусто означает «взять у темы». Только в заметках: каретка в окне настроек и в поиске остаётся своей. **Умолчание выключено** | `Visual → Text cursor`, последняя группа вкладки | `caret_color_tests.ts`, 4 проверки. Пять мутаций краснеют, в том числе снятие любого из трёх объявлений цвета | не смотрели |

**Что стоит знать про обе.** Клавишу `Del` плагин перехватывает тем же
способом, каким перехватывает `Ctrl+A`, и **командой это не становится**: в
справочнике команд её нет и быть не должно. Цвет каретки переписывается на
каждой записи в конфиг своей подпиской на хранилище, а не перерисовкой панели:
та откладывается, пока фокус стоит в поле ввода, и цвет менялся бы после ухода
фокуса, а не под рукой.

**Вопросы по обеим заданы интервью, и заказчик ответил тем же вечером.** Три
ответа подтвердили сделанное: `Smart Delete` выключен из коробки, снимается всё
вместе с решётками заголовка, девять разошедшихся значений переезда остаются как
есть. Четвёртый ответ дал работу: **`Backspace` — да, но отдельным тумблером**.
Сделано тем же заходом (10.13.32 Д9), умолчание выключено: общий тумблер он
отверг, а отдельный, включённый вместе с `Smart Delete`, ничем бы от общего не
отличался.

## 7у. Замечания к двум новым функциям и три новые задачи — 2026-09-05, ночь

**Ушли по вашим словам две строки:** `Smart Delete` (G1) — «работает хорошо», и
цвет каретки (G2) — «работает». Пять замечаний к первой и три дописки ко второй
разобраны и сделаны; три новые задачи из вашего списка сделаны все.

**Три из них лежат в файлах под З3, и разрешение на каждую вы дали интервью**
после разбора — девятнадцатое, двадцатое и двадцать первое исключения.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **`Smart Delete\Backspace`: все пять ваших замечаний.** Заголовок называет обе клавиши; про `Backspace` сказано и в коллауте, и в подсказке; тумблер называется `Smart backspace`, стоит вторым и **включается сам по себе** — `Smart Delete` при этом может быть выключен | `Keyboard → Smart Delete\Backspace` | `smart_delete_tests.js`: каждая клавиша спрашивает только свой тумблер, в обе стороны. `settings_layer_tests.ts`: все четыре сочетания двух тумблеров — нижние строки живы, пока включена хоть одна клавиша. Две мутации краснеют | не смотрели |
| **Каретка: толщина и скорость мерцания.** Обе делаются в Obsidian — прочитано в его `app.js`, а не выведено из типов. Толщина двигает и парный сдвиг, иначе каретка съезжает вправо. `0` у скорости значит «не мигает вовсе» | `Visual → Text cursor` | `caret_color_tests.ts`, 7 проверок: целые строки объявлений, а не подстроки. Четыре мутации краснеют, среди них потеря `!important` и подмена нуля нулевой длительностью | не смотрели |
| **Живой предпросмотр каретки** последней строкой той же группы: показывает цвет, толщину и мерцание сразу и меняется, пока вы тянете ползунок | там же | `caret_color_tests.ts`: блок стоит в группе; формула скорости у панели и у заметки сверяется числом на всех десяти значениях (У-32) | не смотрели |
| **Экран при перемещении строки.** Причина найдена и она не в случайности: наш код прокруткой не управлял **никогда** — он спрашивал у редактора метод, которого у Obsidian нет. Прыгала платформа, двумя состязающимися «ближайшими». Теперь тумблер `Follow the moved line` и три положения | `Navigation → Moving lines`, две последние строки | `navigation_jumps_tests.js`: три положения уезжают в платформу разными режимами, выключенный тумблер держит экран и на следующем кадре тоже. Четыре мутации краснеют | не смотрели |
| **Два разделителя на строке без текста.** `- #/1 ::  :: 📅…` — как вы и просили, с местом под текст между ними. Работает и через TagWheel, и через команды `Field next` / `Field previous` | пустая строка, TagWheel, значение слева и дата справа | `runtime_unified_parity_tests.js`: обе половины правила и ваш случай слово в слово. Три мутации краснеют | не смотрели |
| **Переход активного Field между Block.** Все четыре ваши границы складываются в одно кольцо, и оно сделано одним переходом | `Visual → TagWheel → TagWheel navigation behavior` (в ту ночь строка называлась `At the last Field`) | `tests/TagWheel/tagwheel_tests.js`, `runEdgeModeSuite`: все четыре границы поимённо. Шов от настройки до движка — `bootstrap_loader_tests.js`. Три мутации краснеют | не смотрели |

**Что в этих правках сделано не буквально по вашему тексту.**

- **Опечатка в заголовке выправлена по смыслу.** Вы написали
  `Smart Delete\Backpace`; в панели стоит `Smart Delete\Backspace`.
- **Умолчание прокрутки — включено, положение «по центру».** Вы просили
  «чтобы при off экран не прыгал», и это работает; но выключенным по умолчанию
  оно вернуло бы вопрос с другой стороны — строка, уехавшая за край, двигалась
  бы вслепую. `heading-jumps-center`, который вы назвали образцом, тоже включён
  из коробки. Скажите слово — выключу по умолчанию, это одна строка.
- **Мерцание задаётся ползунком `0..10`, а не в миллисекундах** — по вашему
  ответу интервью. `5` — ровно то, чем Obsidian мерцает сейчас.

## 8. Служебное — вам смотреть не обязательно

| Что | Зачем | Статус |
|---|---|---|
| Заметка `Проверка панели.md` в тестовом vault переписана и теперь обновляется установщиком | врала про всё: «20 групп», «настройки не влияют на поведение», «нет своих блоков» | сделано |
| `node tools/panel_dry_run.mjs <файл>` — прогон панели на настоящем конфиге | нашёл, что дым Г16 проверял вдвое меньше, чем считалось | сделано |
| Г16 переведён на настоящий конфиг из фикстуры | подсказок открывается 25 вместо 12 | сделано |
| Мёртвый флаг `advanced.newSettingsPane` убран из установщика | панелей больше не две | сделано |
| Единственный пропущенный тест включён обратно; набор — **45 проверок, 0 пропусков** | пропуск стоял с причиной «включить после фазы 6»; фаза 6 ему была не нужна (A15) | сделано |
| Найдена написанная проверка, которую никогда не звали, и она падает | пока файл был в пропусках, её не видел никто; включать красную нельзя без разбора в файле под З3 (A16) | сделано, разбор за мной |
| Сплошной проход по карте переименований `ROUTES` против исходников движков | нашёл `clearOnLast` — ключ переехал, читатель остался. Из сорока переименованных такой один | сделано |
| Подделка компонента кнопки в гейте Г20 научена **сохранять** обработчик | без этого гейт доказывал размещение «?» и молчал про нажатие (У-43) | сделано |
| Заглушка DOM: `closest`, `ownerDocument` и работающий поиск по id | `getElementById` отдавал `null` всегда — любая проверка на поиск по id была зелёной ни о чём | сделано |
| Гейт Г16 и прогон панели различают свой блок и строку с кнопками | очистку возвращает тот, кто подписан на хранилище; строке с кнопками очищать нечего | сделано |
| Мёртвый CSS снят: правила для `io-strip-hidden-token` и `io-strip-hidden-space` убраны из `main.js` | классы не ставил никто — тег Field слой полос прячет заменой нулевой ширины, а не пометкой. Снято той же правкой слоя полос, при которой это и обещали | сделано |
| `app.js` Obsidian 1.13.7 распакован и прочитан | два замечания, объявленных «только глазами», объяснились поведением платформы, которого нет в её типах: отбор строк до отрисовки (`Z2`) и `setChildrenInPlace` в конце каждой группы и страницы | сделано |
| Заглушка DOM: `insertBefore` и `insertAdjacentElement` переносят узел, а не копируют его; добавлен `setChildrenInPlace` | заглушка была добрее браузера: узел оставался в дереве дважды. Из-за этого полоса вкладок рисовалась в двух экземплярах, а карта записей редактора Fields три захода считала три несуществующих контрола | сделано |
| Карта записей редактора Fields снята заново | после правки заглушки в ней стало 44 контрола вместо 47: три были призраками заглушки, а не панели | сделано |
| Мёртвый обработчик снят из описаний строк | платформа описание **клонирует** (`cloneNode`), поэтому `addEventListener` внутри `desc` в живом окне не работал никогда. Правило записано: внутри описания обработчиков не бывает | сделано |

## 8у. Каретка без выделения, экран при переходе и имя опции — 2026-09-06

**Ушли по вашим словам две строки:** два разделителя на пустой строке (H3) — разобрано и подтверждено, и `Smart Delete\Backspace` (G3).

**2026-09-06, второй заход: раздел закрылся целиком.** Каретку на строке без выделения (G4) и имя опции TagWheel (H4) вы подтвердили галочками, место на экране после перехода по заголовкам (H2) — словом «ок, работает» тем же вечером, после двух замечаний к нему; оба замечания разобраны в разделе 9у. Таблицы у раздела больше нет: в ней не осталось ни одной непроверенной строки.

**Три ваших замечания сделаны.** Одно из них — про каретку — оказалось не нашей ошибкой в стилях, а различием двух копий CodeMirror внутри самого Obsidian; разбор в PRD 10.13.33 Ц9 и в уроке У-75.

**Правка навигации лежит в файле под З3** (`navigation_runtime.js`) и сделана **без спроса** — вы ушли со словами «сначала выполняй доработку понятных замечаний, вопросы оставь на конец». Двадцать второе исключение, **принято вашим словом в тот же день** (В-61).

**Что сделано не буквально по вашему тексту.**

- **Подпись тумблера у перехода по заголовкам сменилась.** Он назывался `Center the screen on target`, а теперь `Follow the jump target`: положений стало три, и «по центру» перестало быть единственным. Ключ в конфиге прежний — переключатель у вас останется в том же положении, в каком стоял.
- **Умолчание нового списка — `Center of the screen`.** Это ровно то, что тумблер делал раньше: у того, кто ничего не трогал, поведение не меняется ни на шаг.

## 9у. Каретка у плавающей кнопки, порядок строк и счёт по каталогу текстов — 2026-09-06, второй заход

**Ушли по вашим галочкам две строки:** каретка на строке без выделения (G4) и имя опции TagWheel (H4).

**И ещё две — тем же вечером, вашим «ок, работает»:** каретка у плавающей кнопки (G5) и место на экране после перехода по заголовкам вместе с новым порядком строк (H2). В разделе осталась одна строка, и смотреть её не обязательно — это счёт, а не работа.

**Оба ваших замечания к строке H2 сделаны.** Первое — критичный дефект каретки — оказалось не дефектом кнопки и не дефектом стилей, а стороной, с которой измеряется положение каретки; разбор в PRD 10.13.33 Ц10. Второе — порядок строк — правка целиком в прототипе.

**Ни одного файла под З3 не тронуто, исключений по-прежнему двадцать два.** Проверок в наборе 49, пропусков ноль, шесть шагов зелёные — и зелёные не только у меня: набор прогнан в свежем клоне, CI на GitHub зелёный.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Плагин ставится через BRAT по одной ссылке.** Не ставился он не потому, что релиза нет: релиз был, но от 8 августа, помечен предрелизом — таких BRAT по умолчанию не видит, — и нёс два файла из трёх, без `styles.css`. Выпущен `0.1.0-beta.2` из нынешнего кода, обычным релизом, три вложения. Впредь выпуск делается тегом: `release.yml` сверяет тег с манифестом, собирает из того же коммита, прогоняет весь набор и прикладывает файлы сам | новый пустой vault, BRAT → `Add beta plugin` → `romkuznetsov/inline-overhaul`, без галочек | вложения скачаны с GitHub обратно и сверены с локальной сборкой — совпадают байт в байт; `main.js` разбирается как JavaScript. Версия сверяется в четырёх местах, две мутации краснеют | не смотрели |
| **Счёт по каталогу текстов и языку — ответ на ваш вопрос «что нужно для начала».** Кода нет ни строки: это разбор объёма, тот самый первый шаг, который был записан в решении. 773 видимые строки уезжают из прототипа, второй пласт — свои блоки, руководство и 25 сообщений в редакторе. Три находки меняют форму работы: каталог обязан быть `.js`, а не `.json`; плагин читает его адаптером, а не через vault; ключ придумывать не надо — он уже есть и уже показывается в `Advanced → Setting ids` | `docs/PRD_Settings_Overhaul_v1.md`, 10.13.38 | считано по схеме и исходникам, а не прикинуто | смотреть не обязательно |

## 10у. Пустой слот текста, окно после восстановления и команды — 2026-09-06, третий заход

**Четыре вещи сделаны, два файла под З3 тронуты.** Исключений стало двадцать четыре. Оба исключения сделаны **без спроса** — вы сказали «сначала разбери, затем исправь очевидные баги и замечания, после этого проведи интервью», — и вопрос «оставить ли сделанное» задан интервью того же дня.

**Проверок в наборе 49, пропусков ноль, шесть шагов зелёные.** Семь мутаций краснеют.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Форма строки, когда Field заведён на пустой.** Было `- :: 📅…`, стало `-  :: 📅…` — слот под текст есть, он просто пуст. Это третье расхождение одного правила, объявленного в двух местах | пустая строка → Field из правого Block | три утверждения и **сверка двух ходов на восьми формах левой части** в `runtime_unified_parity_tests.js`. Мутация краснеет | не смотрели |
| **Каретка при прыжке внутри строки встаёт в пустой слот**, а не за разделителем. Главное здесь не каретка: ваша метка `📅` до навигации не доезжала вовсе, и единственный `::` читался как первый разделитель — то есть зоной текста считался сам хвост с датой | та же строка, команда `Move cursor left in line` при `in-line-cross` = off | восемь утверждений в `navigation_jumps_tests.js`: номер столбца и путь целиком от документа правил. Три мутации краснеют | не смотрели |
| **Окно после восстановления копии** с советом перезапустить Obsidian. Одна кнопка, список вернувшегося, живёт до нажатия | `Advanced → Settings backup → Restore a backup` | три утверждения в `settings_backup_tests.ts`, три мутации | не смотрели |
| **Команды заводятся заново сразу после восстановления** — ответ на «часть хоткеев появляется только при перезапуске». Дело было не в хоткеях, а в командах: они строятся из конфига и заводятся один раз, при загрузке | восстановить копию с другим набором Fields и сразу открыть `Settings → Hotkeys` | закреплён **порядок** шагов (команды → хоткеи → окно), а не факт вызова | не смотрели |
| **Запасной путь служебного файла в `main.js` указывал в корень vault** — переезд файла в папку плагина правил модуль и не тронул запаску. В собранном плагине запаска не срабатывает, поэтому на экране это никак не видно | смотреть не надо | пин в `bootstrap_loader_tests.js` сверяет запаску со значением модуля. Мутация краснеет | смотреть не обязательно |
| **Служебный файл правил больше не возвращается в корень vault после восстановления копии.** Переезд В-39 жил только в загрузке, а копия несёт путь файла в себе и возвращала прежнее место | восстановить старую копию и посмотреть корень vault | два пина: миграция патча путь не чинит (`config_migration_v1_v2_tests.ts`) и шов решает той же функцией миграции (`bootstrap_loader_tests.js`). Две мутации краснеют | не смотрели |
| **Окно состава у `Save a backup`: комментарий, галочки вкладок и объём хоткеев.** Неотмеченная вкладка при восстановлении остаётся такой, какая есть (В-62) | `Advanced → Settings backup` | шесть утверждений в `settings_backup_tests.ts`, и среди них сверка галочек со схемой. Восемь мутаций краснеют | не смотрели |
| **Конфликты хоткеев снимаются галочкой, по умолчанию выключенной** (В-64), со списком чужих команд поимённо | там же, окно `Restore a backup` | поведением — в `settings_backup_tests.ts`, сам шов по исходнику поимённо по пяти местам | не смотрели |
| **Плагин называется `inlineOverhaul`** (В-65). Идентификатор прежний, хоткеи целы. Папка копий меняется только у чистой установки; заметка-руководство под прежним именем открывается, а не дублируется | `Settings → Hotkeys`, поиск `inlineOverhaul` | гейт прототипа, пин документов и проверка старой заметки в `actions_tests.ts`. Две мутации краснеют | не смотрели |

**Чего в этом заходе нет.** Файл в корне vault, настраиваемое окно сохранения копии, чужие хоткеи в копии, принудительное разрешение конфликтов и переименование плагина — всё это ушло в интервью: там решение ваше, а не моё.

## 11у. Три замечания к копиям настроек и каталог текстов — 2026-09-06, четвёртый заход

**Ни одного файла под З3 не тронуто, исключений по-прежнему двадцать пять.** Проверок в наборе 50, пропусков ноль, шесть шагов зелёные. Двадцать четыре мутации краснеют.

**Прототип тронут одной группой** — `Language` на вкладке General; схема и Приложение B перегенерированы, гейты прототипа зелёные.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Текст окон крупнее, а строка про перезапуск полужирная.** Окна брали размер и цвет описания строки настройки — там это верно, а в окне читать нечего, кроме него | `Advanced → Settings backup → Restore a backup`, окно после восстановления | пин по исходнику и по стилям в `settings_layer_tests.ts`: класса описания строки в окнах нет, у текста окна свой размер, у строки «что дальше» — свой вес | не смотрели |
| **Окно `Save a backup`: три заголовка, у каждого своя подсказка.** Абзац под заголовком уехал в подсказку; между комментарием и галочками встал заголовок `Choose modules you want to backup`. Подсказки слушают `Show tips` | `Advanced → Settings backup → Save a backup`, потом то же с выключенным `General → Help → Show tips` | шесть утверждений в `settings_backup_tests.ts` (заголовки, подсказки, тумблер) плюс пин шва по исходнику. Мутация краснеет | не смотрели |
| **Папка копий не переезжает вместе с настройками.** Копия из другого vault приносила чужой путь, и следующее нажатие искало копии не там. Теперь путь остаётся ваш — и при восстановлении целиком, и по галочкам, и при сбросе | восстановить копию, снятую в другом vault, и посмотреть `Backup folder` | четыре утверждения в `settings_backup_tests.ts` на всех трёх путях. Две мутации краснеют | не смотрели |
| **Окно восстановления говорит про хоткеи всегда.** Конфликты есть — называет их поимённо и предлагает галочку; нет — так и пишет; копия объёма `all` — объясняет, что она перезапишет чужие клавиши сама; хоткеев в копии нет — говорит и это | `Advanced → Settings backup → Restore a backup` | четыре утверждения на четыре состояния. Три мутации краснеют | не смотрели |
| **Сравнение хоткеев научилось читать `Mod` и вторую форму записи клавиши.** `Mod` — это Ctrl на Windows и Cmd на macOS, а не буквы; клавиша бывает записана и через `code`. Копия такую привязку раньше выбрасывала на входе | смотреть не надо | шесть утверждений в `settings_backup_tests.ts`, среди них пример с ваших собственных данных. Три мутации краснеют | смотреть не обязательно |
| **Каталог видимых текстов и язык панели.** Плагин кладёт `texts/en.js` и `texts/ru.js` рядом с `data.json`; правите строку, перезагружаете плагин — панель говорит вашими словами. Язык переключается на лету, новый язык добавляется копией файла | `General → Language`, потом папка `.obsidian/plugins/inline-overhaul/texts` | восемнадцать проверок в `texts_catalog_tests.ts` и гейт Г24. Шестнадцать мутаций краснеют | не смотрели |

**Чего в каталоге пока нет.** Переключаются панель, её подсказки, коллауты вкладок, предпросмотры и справочник команд — 782 строки. Не переключаются: тексты внутри редактора Fields и соседних блоков (около 576 кандидатов), заметка-руководство и 25 сообщений в редакторе. Это второй и третий куски работы; первый полезен без них.

**Русский заведён плейсхолдером.** Переведено десять строк — имя языка, семь подписей вкладок и заголовок группы `Language`: ровно столько, чтобы переключение было видно с первого нажатия. Остальное лежит в файле английским и ждёт руки.

## 12у. Файл текстов: `default.js`, его полнота и порядок — 2026-09-06, пятый заход

**Три строки из семи подтверждены заказчиком 2026-09-06** и остаются здесь только ради разбора: тексты окон, порядок файла и `Language` над `Help`. Остальное ждёт его взгляда.

Ваши три замечания к каталогу текстов (K1, K2, K6). Разбор — PRD 10.13.46.

**Ни одного файла под З3 не тронуто, исключений по-прежнему двадцать пять.** Проверок в наборе 50, пропусков ноль, шесть шагов зелёные.

**Прототип тронут одной строкой** — порядок группы `Language`; схема и Приложение B перегенерированы.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **`texts/default.js` — файл плагина, и он всегда нынешний.** Новая настройка, новое окно и переформулированная строка приезжают в него сами. Его копируют, чтобы завести язык | папка `.obsidian/plugins/inline-overhaul/texts` | три проверки в `texts_catalog_tests.ts`: догоняет каталог, без расхождения не пишется, файл человека не трогает. Плюс дымовой прогон на вашей же папке | не смотрели |
| **Английского снимка плагин больше не кладёт.** Он замораживал каждую позднейшую переформулировку: в подстановке файл стоит раньше схемы, и вчерашнее слово выигрывало у сегодняшнего молча (У-83) | `General → Language`, английский по-прежнему на месте | те же проверки плюс порядок подстановки | не смотрели |
| **Ваш `en.js` подхвачен и обновлён**, потому что вы в нём ничего не правили: было 782 строки, стало 886. Ваш `ru.js` не тронут — в нём четыре ваши правки; ваш `de.js` назван сломанным и оставлен как есть | те же файлы | дымовой прогон на копии вашей папки: три случая из трёх разобраны верно | не смотрели |
| **Тексты модальных окон теперь в каталоге.** Их не было вовсе: панель говорила выбранным языком, а окна `Save a backup`, `Restore a backup` и `Delete all your settings` — по-английски. Заодно переводятся строки счёта и подписи вкладок в окне копии | `default.js`, раздел `dialog.*` | гейт Г25 и 50 проверок набора | не смотрели |
| **Порядок файла стал порядком панели.** Коллауты, предпросмотры и справочник команд лежали разделом в конце, через сотни строк от своей вкладки. Теперь: вкладка → её коллаут → группа → строка → окно, которое эта строка открывает | `default.js`, читать сверху вниз | проверка порядка: строки вкладки не разрываются, коллаут не дальше 12 строк от заголовка вкладки, окно не дальше 10 строк от своей кнопки | не смотрели |
| **Гейт Г25 отвечает на ваш вопрос машиной.** Читает исходники, собирает видимые строки и вычитает каталог. Что осталось — перечислено пофайлово, с числом и причиной; число выросло — гейт краснеет | `npm run gate` | сам гейт | смотреть не обязательно |
| **`Language` стоит над `Help`** | `General` | утверждение о порядке групп в `settings_layer_tests.ts` | не смотрели |

**Что гейт говорит сейчас.** В каталоге **886** строк (было 782). Видимых строк в исходниках 441, вне каталога **337**. Из них 17 — имена команд (переводить нельзя, Я2), 24 — формат заметки копии (плагин читает её обратно), остальные 296 — второй кусок: тексты внутри редактора Fields, Smart Rules, Binder, своих тегов и списков порядка. Это вопрос В-67.

**Чего вам придётся коснуться руками.** Ваш `ru.js` остался на 782 строках и новые уже не получит: файл ваш, и плагин к нему не подходит. Хотите полный — скопируйте `default.js` заново.

## 13у. Второй кусок каталога — 2026-09-06, по вашему ответу

**Подтверждено заказчиком 2026-09-06 галочкой.** Раздел остаётся ради разбора.

Вы ответили на В-67 «брать сейчас». Разбор — PRD 10.13.47.

**Ни одного файла под З3 не тронуто.** Проверок 50, шесть шагов зелёные, четыре мутации краснеют.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Редактор Fields говорит выбранным языком.** Имена строк, объяснения, подсказки, подписи кнопок, названия колонок таблицы Values, оба окна (удаление и переименование) и все проверки имён | `Tags & PKM → Fields` на любом языке | 103 проверки редактора зелёные, плюс пин «блок спрашивает каталог, и перевод доезжает до экрана» | не смотрели |
| **То же у соседей:** Smart Rules, Binder, свои теги, списки порядка Fields и Prefix, таблицы `Move left` / `Move right`, «что остаётся на строке», справочник команд, свойства YAML | те же вкладки | свои проверки каждого блока зелёные | не смотрели |
| **Слова при этом не менялись ни одного.** Английское взято из самих констант сверкой по именам: 50 совпали дословно, 10 взяты из исходника, объявления сняты | смотреть не надо | сверка машиной перед правкой; весь набор после | смотреть не обязательно |
| **В каталоге стало 1189 строк** (было 886). Двенадцать файлов ушли из списка непокрытых целиком | `default.js` | гейт Г25 | не смотрели |

**Что осталось за каталогом — 49 строк, и это решения.** 15 — имена команд (переводить нельзя, Я2); 24 — формат заметки копии, которую плагин читает обратно; 4 — имена строк прототипа, по которым узнаются семьи команд; 3 — суффикс `sub` и шаблон строки выбора; 2 — ошибки, которые бросаются исключением; 1 — строка про прежнее имя настройки, у которой шов есть, а панель его пока не кормит. Список с причинами — в `tests/gates/texts_coverage.ts`.

## 14у. Один файл в папке текстов — 2026-09-06, по вашему слову

Ваши замечания к L1 и L6: «файл `en.js` не нужен, я его удаляю, тк `default.js` = en» и «пока должен остаться только `default.js` = en. Других языков пока не будет». Разбор — PRD 10.13.48.

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Плагин не кладёт ни одного файла языка.** В папке остаётся ровно один — `default.js`, тот, который он ведёт сам. Русский плейсхолдер больше не кладётся: механизм проверен, а перевод вы решили делать в конце | снесите папку `texts` и перезагрузите плагин: в ней обязан появиться один файл | утверждение «первый запуск кладёт один файл»: список записанных путей равен одному | не смотрели |
| **Список языков всё так же собирается из папки**, и английский в нём есть всегда. Скопировали `default.js` под своим именем — язык появился | `General → Language` | утверждения про список языков и про подхваченный чужой файл | не смотрели |
| **Ваши старые `en.js` и `ru.js` плагин заново не создаст.** Если они у вас лежат и не тронуты — он держит их в строю; тронули хоть строку — не подходит к ним больше никогда | смотреть не надо | утверждение «нетронутый файл языка догоняет каталог, а тронутый плагин больше не трогает» | смотреть не обязательно |

**Подтверждено вами 2026-09-06** и потому здесь больше не лежит: тексты модальных окон в каталоге, порядок файла = порядок панели, `Language` над `Help`, второй кусок каталога целиком, релиз `0.1.0-beta.4`.

## 15у. Фаза 6, мелкое и безопасное — 2026-09-06, автономная сессия

Вы разрешили всю фазу 6 (В-71) и отдали сессию под автономную работу. Порядок
взят по риску, а не по важности: сначала то, что закрывается за час. Разбор
очереди — PRD 10.13.49.

**Смотреть вам здесь почти нечего, и это нормально:** гигиена репозитория и
манифест на экране не видны. Строки ниже помечены честно — где есть что
открыть, где нет.


**Из этого раздела ушло семь строк 2026-09-07.** Пять — ваши галочки: снятие
команды `Open settings`, сообщения плагина в редакторе (четыре строки о них) и
механизм перевода руководства (три строки). Две — ответы на вопросы: ссылки в
карточке отложены вашим словом, цена сообщений оставлена вашим словом. Остальные
строки раздела — отчёты о том, что на экране не видно, и они остаются.
| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Манифест приведён к требованиям подачи** (пункт 4 фазы 6). Пустое поле `authorUrl` убрано: Obsidian читает его на истинность, и пустая строка на экране неотличима от отсутствующего поля, а на review — замечание. `fundingUrl` не заведён: он рисует **кнопку** `Donate`, и плейсхолдер в нём означал бы нерабочую кнопку у каждого, кто откроет карточку | смотреть не надо — на экране ничего не изменилось | `manifest_publish_tests.js`, четыре мутации краснеют | смотреть не обязательно |
| **Два числа про скорость панели теперь мерятся, а не обсуждаются.** Они были объявлены в PRD с самого начала и не проверялись ничем. Сейчас: открытие вкладки стоит 0,1–2,5 мс нашей работы; одна настройка будит только тех, кто на неё подписан | смотреть не надо | обе первые версии этого гейта были зелёными и не мерили ничего — разбор в PRD, раздел 12; три мутации краснеют | смотреть не обязательно |
| **Плагин похудел на 121 КБ.** В сборке лежали 2284 строки старой доски Order — панель, которую вы больше не видите с фазы 3. В продукте её не рисовал никто, но в бандл она уезжала к каждому, кто ставит плагин. Было 1 958 988 байт, стало 1 835 035 | смотреть не надо; если заметите, что панель `Tags & PKM → Fields` стала вести себя иначе — это замечание, вести себя она обязана точно так же | 49 проверок набора зелёные; гейты Г1–Г5 заведены по коду впервые, шесть мутаций краснеют | не смотрели |
| **Собранный плагин ушёл из репозитория** (пункт 3 фазы 6). Лежал под контролем версий и расходился с исходником молча — как только кто-то правил исходник и не пересобирал | смотреть не надо | третье правило `repo_completeness_tests.js` спрашивает git, а не читает `.gitignore`; проверено клоном в чистую папку — все шесть шагов зелёные | смотреть не обязательно |

## 16у. Починка загрузки плагина — 2026-09-07, автономно

Ваше замечание по P2: «из command-reference пропали все команды, кроме Transform
inline to note. Также команды inlineOverhaul не вызываются из Command Palette
Obsidian». Это оказался дефект, и завела его прошлая сессия. Разбор целиком —
PRD, раздел 11, «Кусок второй: загрузчики свёрнуты».

| Что | Где смотреть | Чем проверено машиной | Статус |
|---|---|---|---|
| **Команды вернулись все.** В сборке не работал единственный путь загрузки модулей: путь к модулю стоял в переменной, а сборщик подставляет модуль только по литеральному пути. Реестр команд не доезжал, и на его месте вставала заглушка из четырёх пустых функций — она отвечала «я годна» на собственную проверку, поэтому вторая попытка загрузки не делалась никогда | `Ctrl+P` и `Keyboard → Command reference` | `bundle_onload_tests.ts`: включает **собранный** плагин и спрашивает список команд; 17 команд, все названные поимённо | не смотрели |
| **Вместе с командами не работало ещё пятеро.** Тем же путём загружались движок навигации, движок Tags & PKM, `Ctrl+A` по своим правилам, Smart Delete и полоса приоритета. Всё это было мертво в вашей сборке от 7 сентября 01:07, и на экране выглядело как «плагин включён, но в заметке ничего не делает» | заметка `Демо строки.md`: перемещение строки, переход по заголовкам, TagWheel, полоса | тот же файл проверок: движки обязаны доехать до полей плагина | не смотрели |
| **Заглушек модулей в плагине больше нет ни одной.** Заглушка на месте модуля означает не «плагин переживёт отказ», а «плагин работает наполовину и молчит». Заодно ушли пять копий логики, которые за них держались, — среди них вторая копия единственного пути записи настроек | на экране не видно | `bootstrap_loader_tests.js`: перепись всех 23 модулей сходится в обе стороны, каждый `require` обязан быть литералом | смотреть нечего |
| **Плагин похудел ещё на 31 КБ**, а `main.js` — на 926 строк (6134 → 5208) | на экране не видно | числа сняты с `dist/main.js` до и после | смотреть нечего |
| **Колонка `Preview` в редакторе Fields меняется на каждой букве** (ваше второе замечание по P2). Подпись перерисовывалась по уходу фокуса, а уход на другую вкладку фокус и забирал. Меняется одна подпись одного пузыря, а не таблица: перерисовка унесла бы каретку (У-20); в конфиг значение уезжает по-прежнему по уходу фокуса (A9) | `Tags & PKM → Fields`, колонка `Show` = `custom`, печатайте в поле рядом | `fields_editor_view_tests.ts`, Ф9б: подпись пузыря до набора, после набора и на пустом тексте; две мутации краснеют | не смотрели |
| **Служебный файл правил: доказано, что он ничего не добавляет.** Это первый пункт того порядка, которым я решил его снимать (вы поручили решить мне). Проверка гоняет два хода из одного конфига — через файл и напрямую — и сверяет их по каждому блоку. Из десяти блоков шесть проходят через диск без изменений, два — ровно через одну известную функцию, один записывается и этим разбором не читается вовсе. Кода в движках не тронуто ни строки | на экране не видно | `rules_document_roundtrip_tests.ts` на двух фикстурах конфига; мутация «разбор перестал читать блок цветов» краснеет | смотреть нечего |
| **Заведена проверка, которой не было вовсе:** она берёт собранный файл, включает плагин и спрашивает то, что видите вы, — список команд. До правки она красная, после зелёная. Дырка была ровно здесь: весь остальной набор гоняет исходники, где путь к модулю разрешается, а у вас стоит один плоский файл | на экране не видно | `npm test`, 52 файла проверок зелёные | смотреть нечего |

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него уехали в раздел 7б — они сделаны: `Floating button`,
симметричный цикл `Move right`, подсветка обработанной строки, три пункта
1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа свойства через
`app.metadataTypeManager`.

**Остаток фазы 6 — снятие моста модулей.** Числа сняты заново 2026-09-07:
**93 вызова моста в десяти файлах, и все десять под З3.** Разбор с таблицей —
PRD, раздел 11, «Кусок третий». В работу не взята и не берётся, пока заказчик
не подтвердит, что плагин у него работает: десять файлов рантайма поверх
непроверенной починки загрузки — это ровно то, что стоило дефекта A33.
