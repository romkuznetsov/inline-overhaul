# Что осталось до полного завершения технических работ

> **Что здесь есть и чего нет.** Только техническая работа по нынешнему
> плагину: новых функций в списке нет — они живут в `test-vault/Ideas.md` и
> разбираются отдельно. Список собран 2026-09-11 обходом документов, а не по
> памяти: у каждой строки назван источник, чтобы её можно было перепроверить.
>
> **Строка отсюда уходит двумя способами:** работа сделана и подтверждена, или
> заказчик её снял. Зелёная проверка приёмкой не является.

---

> **Его слово 2026-09-16, завершая сессию 56:** «разбирай эти пять мест в
> следующей сессии, завершаем». **Сделано 2026-09-16, сессия 57** — раздел
> **1к** ниже вычеркнут: пять названных мест и два, найденных тем же обходом,
> отвечают громким отказом с именем модуля. Исключение к З3 № 120, разбор —
> 10.13.166, отчёт `192у`.
>
> **Прежняя очередь закрыта целиком.** Раздел 1 — пять строк, которые он
> назначал с 15 сентября, — вычеркнут. Последней была строка про имена,
> объявленные дважды: долг копий был 23, стал 1, и остаток закреплён **списком
> имён** в наборе, а не числом в документе.
>
> **Что закрыто этой сессией.** У каждого из 23 имён вердикт, полученный
> **прогоном**: четыре сведены (копия на молчаливом запасном ходу), одно
> сведено как точная копия, восемь разведены по именам, семь правила не
> объявляют вовсе, одно не сводится и названо причиной. По дороге ужесточение
> меры вскрыло спрятанную копию `hasListPrefix` — сведена. Исключения к З3
> № 117…119, отчёты `187у`…`191у`, разборы — 10.13.162…10.13.165.
>
> **Сторож на долг — `tests/regression/rule_copies_debt_tests.js`.** Он держит
> список имён с причинами, а не число: новое второе объявление роняет прогон и
> называет имя. Проверено мутацией.
>
> **Его слово 2026-09-16 по строке `S47`: «проверил, ок»** — курсор, уезжавший
> в конец строки, закрыт; отчёт `183у` унесён в
> `docs/archive/AWAITING_OWNER_CHECK_session55.md`. Рабочий vault он на тот
> момент ещё не смотрел, и пункт П4 раздела 3 остаётся живым.
>
> **Рамка прежняя, его словом:** «уборка, но не хочу риска дефектов». Сводится
> только то, у чего расхождение **измерено** и равно нулю; остальное
> называется числом и остаётся его решением (У-194, У-195, правила 113…116).
>
> **Релиз не делается без его нового слова:** он проверяет `0.1.0-beta.7`.
> Уборка идёт локальными коммитами и выкладкой в `main` шагом завершения
> сессии.

## 1. Моя работа: очередь, которую он назначил — **закрыта целиком 2026-09-16**

**Очередь его**: то, что он назначил и не отменял. Все пять строк вычеркнуты;
таблица оставлена не для работы, а затем, чтобы по каждой строке было видно,
чем она закрыта и где разбор. **Числа в ней сняты прогоном, а не памятью** —
команда названа в самой строке (У-145).

| № | Что | Откуда | Чем меряется |
|---|---|---|---|
| ~~1~~ | **Остаток форм ссылки и тега — измерен и закрыт 2026-09-15, ночь** | Д3 и его остаток закрыты 2026-09-14 | Строка была верна **наполовину**: из девяти рукописных образцов шесть совпадали с домом **до знака** и сведены подстановкой (исключение № 102, отчёт `165у`), у трёх расхождение измерено и они **не сведены**. Остаток назван поимённо, а не словом «остальное»: `editor_visuals_config.js` не считает ссылкой `[[a[b]]` (2 строки из 77 — и что об этом думает Obsidian, не спрошено, У-91); в слое настроек три места «снять скобки» отвечают иначе, и одно из них расходится на `[[#heading]]`, живой форме Obsidian (отчёт `164у`). Класс закрыт **обходом**, а не списком: рукописная форма ссылки роняет `bootstrap_loader_tests.js`, разрешённых адресов два и оба названы. Мера — `node tools/form_divergence.js` |
| ~~2~~ | **Настройка человека, записанная в коде готовой — закрыто 2026-09-15** | его слово 2026-09-14 | Было 30 мест в 10 файлах, стало 0; список долга у сторожа пуст. Исключения к З3 № 87 и № 88, отчёты `151у` и `152у`, разборы — PRD 10.13.124 и 10.13.126 |
| ~~3~~ | **Имена, объявленные дважды — закрыто 2026-09-16** | его слово «разбери все 23» | **Было 23, стало 1.** Число живёт в прогоне — `node tools/rule_copies.js`, — а список имён с вердиктами теперь держит сторож `tests/regression/rule_copies_debt_tests.js`: новое имя роняет проверку в том файле, где его завели. Разобрано так: четыре имени были копией на молчаливом запасном ходу и сведены; одно (`themePair`) сведено как точная копия; восемь разведены по именам — у них разные вопросы; семь правила не объявляют вовсе (доставалка модуля и ленивая сборка своего экземпляра, вердикт печатает `node tools/rule_copy_shapes.js`); одно, `applyPkmOptionKeys`, **не сводится**, и причина написана в самом стороже: раскладка идёт в модульные имена каждого файла, наборы разные. По дороге ужесточение меры вскрыло спрятанную копию `hasListPrefix` — сведена. Исключения к З3 № 117…119, отчёты `187у`…`191у`, разборы — 10.13.162…10.13.165 |
| ~~4~~ | **Команда поля-элемента не переносит чужое значение в его Block — закрыто 2026-09-15** | найдено обходом строки | Сделано в два шага: переезд зависимостей в общий дом без смены поведения и вызов из третьего движка. Исключения № 93 и № 94, отчёты `156у` и `157у`, строка листа `S44`, разборы — PRD 10.13.131 и 10.13.132 |
| ~~5~~ | **Смена значения родителя уносит значение зависимого поля — закрыто 2026-09-15** | В-117, его слово «уносить всегда» | Панель спрашивает то же объявление, что и команды. Исключение № 91, отчёт `154у`, строка листа `S43`, разбор — PRD 10.13.128 |

**Д2 и Д3 закрыты 2026-09-14 ночью** его словом «давай Д2 и Д3». Три правила о
чужой разметке получили по одному дому: начало строки, разделители зон строки,
форма ссылки и тега. Разбор — PRD 10.13.122, исключение к З3 № 85, отчёт
`149у`.

**И тем же вечером, его словом «давай дальше», закрыты обе прежние строки этого
раздела:** разделитель, записанный в коде готовым (три места, у заказчика не
срабатывали ни разу), и остаток сведения — четыре файла, не подключавшие общий
дом. Разбор — PRD 10.13.123, исключение к З3 № 86, отчёт `150у`.

**`S42` он закрыл галочкой 2026-09-15**, отчёт `153у` унесён в
`docs/archive/AWAITING_OWNER_CHECK_session52.md`. **Его глаза ждёт `S43`** — его
же решение В-117, исполненное тем же днём (отчёт `154у`). Остальное в очереди
выше — уборка кода, и предмета на экране у неё нет.

**Сессия 52 (ночь на 2026-09-15) закрыла его правило целиком** — строка 2
вычеркнута — и нашла обходом строки четыре дефекта, о которых в письмах не
было. Три починены (`151у`, `153у`), один назван известным расхождением с
ценой (строка 4), ещё один стал вопросом (строка 5).

**Приём, которым нашлись все четыре** (У-167): не читать дороги, а обернуть
экспорты модулей и спросить у каждого вызова «вход нёс предмет, выход не
несёт». Чтением до этого трижды выходили неверные кандидаты.

**И приём, которым каждая находка проверена на «моя ли она»** (У-187): рабочее
дерево на коммите до сессии в коротком пути и тот же стенд там. Все четыре
оказались давними.

**Обход строки вырос с четырнадцати форм до девятнадцати**, и четыре новые
куплены этой ночью: значение по ту сторону разделителя, вложенная строка,
номер списка, ссылка в зоне значений и значение дочернего поля без родителя.

## 1к. Очередь, назначенная им 2026-09-16 — **закрыта 2026-09-16, сессия 57**

Пять мест молчания разобраны, и тем же обходом нашлись ещё два. У каждого
вердикт получен **прогоном**: пробой внутри запасной ветки и положительный
контроль броском на входе в ту же функцию. Все семь веток оказались
недостижимы; молчание заменено громким отказом с именем модуля.

Два ответа оказались не такими, как ожидалось, и оба стоили работы. У шва
`Backspace` в `main.js` контроль дал **ноль** покрасневших проверок: набор этот
шов не исполняет вовсе, и стенда, которым бы он исполнялся, нет — зовёт его
нажатие клавиши. Водитель написан вместе с починкой и жмёт обе клавиши на
сборке. В `fields_model.ts` из шести ходов вокруг помощников состояния красным
стал один — и тот оказался не запасным ходом, а **единственным объявлением
правила**: `denormToken` модуль не экспортировал ни дня.

Закреплено обоими родами проверок: сплошным обходом по этой форме в
`bootstrap_loader_tests.js` с пятью контролями на строках-образцах и ожиданиями
ответа у `createFieldRelocation` и `createFieldsModel`. Своё отделено от проб
платформы **свойством** — спрашиваемое имя обязано экспортировать какой-то наш
модуль, — а не списком имён; что этим пропускается, названо в самом стороже.

Разбор — PRD 10.13.166, исключение к З3 № 120, отчёт `192у`, уроки У-203 и
правила 123, 124.

## 1я. Закрытое: указатель, а не пересказ

Разборы живут в PRD 10.13.* и в `docs/archive/`; здесь только адрес, чтобы
строку можно было найти, не помня её (У-31: пересказ состояния, которого больше
нет, врёт и в архиве).

| Что было строкой очереди | Когда закрыто | Где разбор |
|---|---|---|
| Строка `"- "` не переживала собственный разбор (было 6 из 154) | 2026-09-15 | 10.13.151 и 10.13.155, З3 № 110, отчёт `180у` |
| Строка-заголовок: панель и команда давали разное (было 4 из 180 и 6 из 162) | 2026-09-15 | 10.13.156, З3 № 111, отчёт `181у` |
| «Что такое наша метка»: шесть объявлений, двое отвечали пустотой | 2026-09-15, ночь | 10.13.158, З3 № 113, отчёт `183у`, строка листа `S47` |
| Четыре кандидата долга копий: один сведён, три названы числом | 2026-09-15, ночь | 10.13.159, З3 № 114, отчёт `184у` |
| `composeToken` объявлен четырежды (73 расхождения из 136 пар) | 2026-09-15 | 10.13.152, З3 № 107, отчёт `177у` |
| `normalizeCheckboxToken`: два вопроса под одним именем (В-118) | 2026-09-15 | 10.13.144 и 10.13.153, З3 № 108, отчёт `178у`, правило 117 |
| Ревизия кода 2026-09-14: шесть строк долга, все закрыты | 2026-09-14…15 | `docs/CODE_REVIEW_2026-09-14.md`, раздел 6 |
| Снятие служебного файла правил, все четыре шага | 2026-09-11…14 | PRD 10.13.52, П-8…П-13 |
| Браузерный шаг в CI: падало у них, зелено у меня | 2026-09-13 | PRD 15.7, У-176 |
| Сессии 45…48: подложка, `Ctrl+Z`, знаки начала строки | 2026-09-13…14 | `docs/archive/AWAITING_OWNER_CHECK_session45.md` и `…session47.md` |
| Решённое им 2026-09-11 (подсветка строки, гифки, мобильные, шум линтера, шов в `tagwheel.js`) | 2026-09-11 | `docs/archive/ANSWERED_QUESTIONS.md`, PRD 15.7 |
| `Ctrl+Z` после сессии панели возвращал то, чего не было (Р5) | 2026-09-13 | закрыто его галочкой под `S15`; стенд `node tools/undo_bench.js` |

## 2. Ждёт решения заказчика — работа начнётся после ответа

**Здесь только то, у чего цена измерена, а решение продуктовое.** Ни одна
строка не начата: начинать без его слова значило бы менять поведение там, где
об этом никто не просил (его рамка — «уборка, но не хочу риска дефектов»).

| № | Что | Число | Почему решает он |
|---|---|---|---|
| Р4 | **Минификация сборки** | — | Бету решено **не** минифицировать (В-95, 2026-09-10): «нужно упростить работу по отлавливанию багов». `0.1.0-beta.7` выпущен без минификации по этому же слову. Срок прежний — подача в каталог |
| Р6 | **Метка шва навигации против сторон Order** | 7 входов корпуса из 8 разводят места; пар с нулём — ни одной, кроме двух пустых | Дороги разные: навигация не зовёт сборку, сборка не читает шов. Сведение меняло бы поведение там, где сегодня его никто не спрашивает. Мера — `node tools/form_divergence.js`, группа «что такое наша метка» |
| Р7 | **Дом движков у́же остальных на три признака** | 3 входа из 8 | `getDateMarkersFromRules` требует у поля `orderKey` и знакомый `kind` и смотрит только правую сторону. Расширение — смена поведения перемещений, а не уборка |
| Р8 | **Список меток элементов объявлен дважды** | на его конфиге расхождений 0; на фикстуре, где метка объявлена одним способом, ответы расходятся | Доводка берёт метки из `rules.dates.markers`, сборка — из сторон Order. Сведение вслепую пробовано и уронило обход на его данных: значение элемента занимает **два** токена, а признак сборки спрашивается о токене. Оба ответа закреплены в `runtime_unified_parity_tests.js`: попытка привести их к одному покраснеет. Разбор — 10.13.157 |
| Р9 | **Ключ Order, которого в конфиге нет вовсе** | 2 расхождения из 19 ключей, оба на синтетических краях | Движок тегов отвечает **каким-то** полем, а не пустотой. Сегодня недостижимо — ключи приезжают из самого Order, — но это тот же род догадки, что стоил дефекта A17. Предмета на экране нет. Мера — `node tools/form_divergence.js`, вопрос «какое поле отвечает этому ключу Order» |
| Р10 | **Пятислойная передача нормализатора ключа Order** | цепочка оказалась тождеством: каждое «иначе моё» — обращение к одному дому | Литералы из неё сняты (З3 № 115), сторож класса расширен. Сокращение самой цепочки — девять файлов, три под З3; поведение не меняется ни на одном входе, это работа на читаемость. Разбор — 10.13.160 |
| Р11 | **Две мелочи ревизии 2026-09-14** | — | В `buildOutputTokenForField` условие `/^#\//` недостижимо (строкой выше ранний возврат), `getFieldValueByToken` не исполняет ни один прогон. Поведения не меняют; снятие — уборка, и её цену он уже называл |
| Р12 | **Пятая форма того же молчания: положительная охрана без иначе** | 11 мест; запасной ход не исполняется ни одним прогоном, а само место живо у десяти из одиннадцати (счётчик: от 4 до 10 837 проходов) | Разбор сделан целиком, правка — нет: пять мест вы назначили поимённо, а эти нашёл тот же обход. Форма выглядит так: `if (mod && typeof mod.X === "function") { … }` и **никакого иначе** — не приехал модуль, и плагин молча считает ответ сам, другим правилом. Цена правки: семь файлов, пять из них под З3, то есть ещё одна строка исключений. Поведение не меняется ни на одном входе — меняется только то, что плагин скажет в день, когда модуль не приедет |

## 3. Ждёт его глаза — проверить и сказать слово

| № | Что | Как проверить |
|---|---|---|
| П1 | **`README.md` и `instructions.md`** (строка `F3` листа приёмки) | Открыть оба и посмотреть, не обещают ли они того, чего нет, и не молчат ли о том, что есть |
| П2 | **`FEATURES.md`** (строка `F4`) | То же: лист возможностей читают те, кто проверить написанное не может |
| П3 | **Вычитка заметки-руководства** (В-2) | Черновик написан 2026-08-29, девять разделов. Заметка принадлежит человеку — плагин её больше не переписывает |
| П4 | **Работа плагина на настоящем Obsidian после `0.1.0-beta.7`** | **Снова через BRAT:** запрет на выкладку снят 2026-09-15, релиз выпущен, и BRAT берёт последний обычный релиз. Поставить обновление и убедиться, что включается и работает как работал. Он сказал 2026-09-15, что сделает это отдельно, когда техническая работа кончится, — она кончилась. Здесь никакая ревизия глаз не заменит |
| П5 | **Отчёты 52у–88у реестра** | Работа 9–11 сентября, у которой нечего нажать: ревизия кода, тихие отказы, мёртвый код, две беты, лист возможностей. Строка на отчёт — в `docs/AWAITING_OWNER_CHECK.md`, разборы — в `docs/archive/AWAITING_OWNER_CHECK_reports_2026-09-09_11.md`. Подтверждаются словом, а не галочкой |

## 4. Отложено его словом — в списке, чтобы не потерялось

- **Мобильные устройства** — «реализовать позже». Флаг «только для компьютера» решено оставить (2026-09-11), и снимать его имеет смысл только вместе с проверкой на телефоне.
- **Переключатель `AND` / `OR` внутри одного типа Field в Smart Rules** (C51) —
  «в дальнейшем»: сегодняшнее поведение подтверждено галочкой. Формально это
  пункт фазы 5.3, по существу — новая функция.
- **Подача в каталог Obsidian.** Блокеры фазы 6 закрыты, правила каталога
  сходятся все; сама подача — его решение и его аккаунт.
- **Две ссылки в карточке плагина** — отложены 2026-09-11 до его адресов.
- **`TagWheel` в произвольном месте строки** (пункт 8 его файла идей). Его слово
  2026-09-13: «это из старой версии плагина функция по вызову tagwheel в
  произвольном месте строки… однажды я хочу вернуться к этой функции, но пока
  можешь снять». Старая реализация — `tests/TriggerWheel`, 464 строки; она
  **снята шагом 4 в ночь на 2026-09-14** и остаётся в истории git: коммит снятия
  назовёт, откуда её достать. Возвращаться к ней как к коду не стоит: расхождений с нынешним
  TagWheel накопилось больше, чем сходств, — это заказ на функцию, а не на
  восстановление файла.

## 5. Долгов, закреплённых числом, не осталось

Проверено прогоном 2026-09-15, а не памятью:

- настройка человека, записанная в коде готовой — **0 мест**
  (`config_literals_tests.js`, список долга пуст, домов умолчания два и оба
  названы поимённо);
- тихие отказы без объяснения — **0** (`silent_failures_tests.ts`, список
  `DEBT` пуст);
- мёртвые экспорты — **0 из 866 имён** (`dead_exports_tests.js`). **Число имён
  выросло с 738 не от работы, а от починки самого сторожа:** его маска давала
  обратной кавычке искать пару до конца файла, а такая кавычка стоит внутри
  регулярного выражения — сторож видел 20 227 знаков из 85 362 и проверял три
  четверти кода, не зная об этом (У-192, PRD 10.13.137);
- имена, объявленные в рантайме больше одного раза — **28** против 71.
  **Чем опущено с 30 (У-118):** одно сведением — склейка приставки со
  значением, четыре тела в один дом (10.13.152); два разведением имён, где
  копии не было вовсе — знак задачи (10.13.153) и шаг по циклу (10.13.154).
  **И это нижняя граница, а не число:** мера видит только объявления верхнего
  уровня — 1217 из 1522; вложенные функции и стрелочные замыкания в счёт не
  попадают, а самые крупные находки ночи на 2026-09-15 жили именно там
  (10.13.148). Прогон говорит это сам.
  **И мера видит не всё:** одно правило бывает объявлено под разными именами,
  и тогда она считает их разными предметами. Так было с ключом Order — шесть
  тел под тремя именами, из которых мера видела четыре (10.13.146). Такие
  находятся поиском **по телу**, а не по имени
  (`node tools/rule_copies.js`): точных копий не осталось ни одной, а признак
  делегата научился видеть **четыре** формы обращения к дому — четвёртую,
  «связать, охранить отказом, вернуть», он не видел до 2026-09-15 и держал
  два честных делегата настоящими телами (У-191, второй раз);
- `no-unused-vars`, `no-undef` — **0 ошибок**;
- инлайновое оформление — **0** (`catalog_rules_tests.ts`);
- `var` вне движков — **0**;
- правила каталога Obsidian: расходится **ни одного**, четыре по букве с
  названной причиной;
- открытых вопросов заказчику — **ни одного**: В-114, В-115 и В-117 отвечены и
  исполнены, разборы унесены в архив.

**Одно новое место, найденное 2026-09-13 и уже решённое им:** под `tests/`
лежит `TriggerWheel` — 464 строки, копия движка со своим разбором заметки
правил. В продукт не входит, но это ровно то дублирование, ради снятия которого
задан порядок 1а. Его ответ: снять вместе с шагом 4, саму функцию («TagWheel в
произвольном месте строки») записать в задачи — сделано, раздел 4.

**Числа, которые растут от обычной работы, живут в прогоне, а не здесь**
(У-145): перепись записей в консоль, `var` под З3 и размер `styles.css`
печатает `catalog_rules_tests.ts`, число подмен — `npm run gate:browser`.
