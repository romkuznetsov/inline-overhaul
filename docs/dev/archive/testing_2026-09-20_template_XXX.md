# Его шаблон карточки теста и пометки, 2026-09-20

Снято при перезаписи заметки: адрес задачи — его текст, и переписывать его я не стал.

---

# Тестирование · inline Overhaul

> [!info] Где мы сейчас · обновлено 2026-09-20
> Версия `0.3.2` собрана в этот vault — **перезапустите Obsidian перед проверкой**.
> Наружу ничего не выложено: ваше слово «пока не выкладывай на gh» в силе.
> Пока здесь остаётся хоть одна незакрытая галочка, про выпуск релиза я не спрашиваю — это ваше правило.
> Три теста про круг дочернего поля вы приняли, и я их отсюда убрал.
> **Новое: тест 2** — по дефекту, который вы нашли вчера вечером.

> [!tip]- Как устроен тест · разверните один раз
> Каждый тест — одна и та же форма, сверху вниз:
> 1. **Галочка и строка `💬`** сразу под заголовком. Поставили галочку — принято. Не приняли — галочку не ставьте, напишите в `💬` главное: что не так и чего вы ждали. Одной фразы хватит, адрес найду сам.
> 2. **Откуда эта задача** — свёрнутый коллаут: почему тест появился.
> 3. **Настройки и действие** — подзаголовок, сворачивается целиком: выставили настройку один раз и убрали с глаз.
> 4. **Эталон** — в рамке, и трогать его не нужно. **Ваша строка** — единственная обычная строка в тесте, работайте только на ней.
> 5. **Журнал** — свёрнутый коллаут: что с этим тестом было по заходам.
>
> **Почему эталон теперь в рамке.** Вчера вы тестировали прямо здесь, и плагин записал значения в строки эталона и в блок «Действие»: для него любая строка заметки — рабочая. Теперь голая строка в тесте ровно одна, ваша, а эталон в рамке трогать нечем.

## 1. Панель даёт тот же круг, что и команда

- [x] принято
- 💬 нет команда sub

> [!question]- Откуда эта задача
> Дефект круга дочернего поля жил в **обеих** дорогах сразу, и потому мой обход строки его не видел: он сверяет панель с командой, а сломаны они были одинаково. Поэтому панель проверяется отдельно от команды, а не заодно с ней.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| Tags & PKM | Fields → `Cat` → `Child Field` | `Show always` |
| Tags & PKM | Fields → `Cat` → `Parent Value` | `Add the parent Value` |

Действие:

1. Открыть TagWheel на вашей строке.
2. Дойти стрелкой вправо до поля `Cat_sub`.
3. Нажать стрелку вверх столько раз, сколько у поля значений, и ещё один.
4. Закрыть панель на `Esc`.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Исходная:
> - текст
>
> Панель должна показать тот же круг, что даёт команда: каждое значение поля по очереди, потом пустое место, потом снова первое. Обе дороги обязаны совпасть до знака.

Ваша строка, работайте на ней:

- #med :: текст

> [!example]- Журнал
> - 2026-09-20 · тест заведён вместе с правкой круга дочернего поля
> - 2026-09-20 · вы нашли на нём другой дефект — команда при открытой панели портила строку; он вынесен в тест 2, а этот остался про сам круг

---

## 2. Команда при открытой панели говорит, а не портит строку

- [x] принято
- #old :: 💬

> [!question]- Откуда эта задача
> Ваше замечание: «при активации field-sub previous в строке у меня не меняется sub на предыдущий, вместо этого курсор прыгает вниз и возникает странный артефакт — визуально у меня на странице несколько кареток курсора». Пока панель открыта, её вид лежит в самом тексте заметки, и команда правила не вашу строку, а эту картинку: работа пропадала целиком, а на экране оставался мусор. Дефект старше вчерашних правок — проверено на прежнем коде.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| Tags & PKM | Fields → `Cat` → `Child Field` | `Show always` |
| Tags & PKM | Fields → `Cat` → `Parent Value` | `Add the parent Value` |

Действие:

1. Открыть TagWheel на вашей строке.
2. Не закрывая панель, вызвать команду `Cat_sub previous` — хоткеем или из палитры команд.
3. Закрыть панель на `Esc`.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Исходная:
> - текст
>
> Команда не делает ничего, и всплывает сообщение `TagWheel is open on this line: finish it with Enter or close it with Escape first`.
> Строка остаётся такой, какой её рисует панель, а после `Esc` возвращается к исходной. Никаких лишних кареток и обрывков.

Ваша строка, работайте на ней:

- текст

> [!example]- Журнал
> - 2026-09-20 · дефект найден вами на тесте 1, причина измерена стендом, правка сделана тем же заходом

---

## 3. «Что изменилось» в копии настроек читается человеком

- [ ] принято
- 💬 

> [!question]- Откуда эта задача
> Ваше замечание 2026-09-19: «смотреть setting `pkm.fields.tags.fields` — это непонятно для пользователя, ты должен написать название контрола (как в настройках плагина)… группируй элементы, если настройки находятся в дочерних контролах». Подробный разбор — отчёт 265у.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| Advanced | Backup → `Autosave` | включено |

Действие:

1. Завести у любого Field новое значение и покрасить ему текст.
2. Перезапустить Obsidian.
3. Открыть новую заметку в папке копий, раздел `What changed`.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Значение стоит отступом под своим Field, а цвет — отступом под значением:
> - Field «Cat»
> 	- Value «#med» added
> 		- «Text color»: not set → #1dcd41
>
> Строк вида `pkm.fields.tags.fields` и `visual.tags.byTag…` не должно быть ни одной.

Живой строки здесь нет: проверка идёт в панели настроек и в заметке копии.

> [!example]- Журнал
> - 2026-09-19 · ваше замечание к первой версии раздела `What changed`
> - 2026-09-20 · разбор переписан, тест заведён заново

---

## 4. Копия снимается от настроек, а не от хождения по вкладкам

- [ ] принято
- 💬 

> [!question]- Откуда эта задача
> Ваше замечание 2026-09-19: «autosave слишком чувствительный — он делает автосохранение, даже если изменилась активная панель в настройках». Причина была в одной строке отбора: сравнение брало «всё, кроме чужих вкладок», а ветка с открытой вкладкой не принадлежит ни одной из них.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| Advanced | Backup → `Autosave` | включено |

Действие:

1. Открыть Obsidian, походить по вкладкам настроек ничего не меняя, закрыть и открыть снова.
2. Поменять любой контрол и снова перезапустить Obsidian.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> После первого захода заметок в папке копий столько же, сколько было.
> После второго — на одну больше, и всплыло уведомление с её путём.
> В самой заметке строка `Write anything here…` оформлена коллаутом, а не обычным текстом.

Живой строки здесь нет: проверка идёт в панели настроек и в папке копий.

> [!example]- Журнал
> - 2026-09-19 · тест заведён по трём вашим замечаниям к автокопии

---

## 5. Список изменений версии

- [ ] принято
- 💬 

> [!question]- Откуда эта задача
> Ваше замечание: «текст с разрывами на предложениях… префикс исправлений должен быть нумерацией… нужны буллиты с понятным, но лаконичным описанием». Причина была не в окне, а в самом файле: Obsidian показывает одиночный перенос строки переносом, а файл был свёрстан по восемьдесят знаков.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| General | Help → `Changelog` | нажать `Open` |

Действие:

1. Открыть `General` → `Help` → `Changelog` и нажать `Open`.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Откроется список на GitHub: каждый пункт одной строкой и со своим номером, новые версии сверху.
> Само окно показывается один раз на версию, поэтому сейчас его не вызвать — на GitHub лежит ровно тот же текст.

Живой строки здесь нет: проверка идёт в панели настроек.

> [!example]- Журнал
> - 2026-09-19 · тест заведён после переписи `CHANGELOG.md` в 106 нумерованных пунктов

---

## 6. Вид репозитория

- [ ] принято
- 💬 

> [!question]- Откуда эта задача
> Ваш пункт про структуру репозитория и следующий вопрос «в корне много файлов .json .mjs — можно их тоже спрятать?». Ответ вы дали сами: оставить как есть.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| — | настройки не нужны | — |

Действие:

1. Открыть страницу репозитория на GitHub.
2. Посмотреть список файлов в корне.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Ни одного файла самого плагина в корне нет.
> Служебные `.json` и `.mjs` остались там по вашему решению — их ищут в корне Obsidian, BRAT и npm.

Живой строки здесь нет: проверка идёт на GitHub.

> [!example]- Журнал
> - 2026-09-19 · тест заведён после переезда файлов плагина в `src/`

---

## 7. Два документа для людей

- [ ] принято
- 💬 

> [!question]- Откуда эта задача
> Документы пишутся под панель, а панель меняется каждую сессию. Пин ловит имена команд и контролов, но не ловит утверждений о состоянии — «не реализовано» про то, что уже работает, он не видит. Это может увидеть только ваш глаз.

### Настройки и действие

| Вкладка | Контрол | Значение |
|---|---|---|
| — | настройки не нужны | — |

Действие:

1. Открыть в репозитории `README.md`, `INSTRUCTIONS.md` и `FEATURES.md`.

### Эталон и проверка

> [!check] Эталон — трогать не нужно
> Документы не обещают того, чего в плагине нет, и не молчат о том, что для вас важно.
> Читать целиком не нужно — просмотрите заголовки и зацепитесь за то, что покажется неверным.

Живой строки здесь нет: проверка идёт на GitHub.

> [!example]- Журнал
> - 2026-09-19 · тест заведён после правки обоих документов и появления `FEATURES.md`

---

## Новое пишите сюда

Всё, что заметите по ходу, — сюда, своими словами и без порядка. Я разберу и заведу отдельными тестами.

💬 ХХХ. измени файл testing ниже пример типового  пункта и в дальнейшем придерживайся этой структуры
## №. Понятное название короткое 
`общие правила - должно быть максимально компактно, т.е. без лишних пустых строк между элементами и текстом. При завершении сессии ты должен очищаться этот файл - принятые убирать, не принятые - обрабатывать комментарии, очищать В.Тест, актуализировать Журнал и т.д.`
- [ ] принято
- 💬 

> [!question]- Описание задачи
> Емкое и понятное описание задачи\проблемы
> [[ссылка на prd или более детальное описание]]

> [!example]- Журнал
>лог событий по этой задаче, структурированный по сессиям 
>пример
>1 сессия - yyyy-mm-dd_hh:mm
> - выполнена доработка после первого тестирования, сделано Х

> [!settings]- Настройки
> структурированный список нужных настроек для тестирования
> пример структуры настроек
> **1. Panel X**
>  - control N: OFF -> ON

> [!action]- Действие
> Структурированное описание необходимых действий для тестирования плагина
> Пример
> 1. Открыть в репозитории `README.md` - [[ссылка]].
> 	1. прочитать, при необходимости отредактировать
> 	2. подтвердить 
### 🎯А. Ожидаемый результат
`то, что должно получиться после выполнении требуемых действий. если это строка, то должна быть в markdown нотации, например`
- [ ] #todo :: текст :: 📅2026-09-20 01:23
`пользователь не трогает этот раздел, он используется как эталон`
### 🛠️Б. Эталонная исходная строка
`Исходная строка на которой нужно применить действие для получения ожидаемого результата. Пользователь не трогает эту строку, она используется как эталон`
текст
### 💥В. Тест
`Идентичная копия Б на которой пользователь будет выполнять действия для получения ожидаемого результата выше. Ты должен анализировать расхождения между В и А для поиска проблем`
текст

---

> [!note]- Служебное · промпт для новой сессии Claude
> Это не вам. Наведите курсор на блок ниже, нажмите кнопку копирования в его углу и вставьте в новую сессию.
>
> ```
> Новая сессия по inline Overhaul. Прошлая, 68-я, прошла его очередь целиком, трижды переделала заметку тестов по его пометкам внутри неё и починила дефект, который он этой заметкой нашёл.
>
> ДВА ЕГО РЕШЕНИЯ, ДЕЙСТВУЮТ
>
> **Выкладка остановлена его словом** 2026-09-19: «пока не выкладывай на gh, хочу оттестировать перед релизом». Шаг 4 завершения сессии не выполняется, `git push` не делается ни по ходу, ни при закрытии; локальные коммиты нужны как обычно. Снимается только его новым словом.
>
> **Выпуск требует заметки тестов без пустых чекбоксов.** Условие считается командой до вопроса: `grep -c "^- \[ \]" "test-vault/testing.md"`. Больше нуля — вопрос о выпуске не задаётся вовсе, вместо него идёт доработка по его строкам `💬`. Правило 160 в `CLAUDE.md`.
>
> ЧТО ДЕЛАТЬ ПЕРВЫМ
>
> Открыть `test-vault/testing.md` и прочитать галочки и строки `💬`. **Он правит эту заметку прямо в ней** — вписывает пометки между блоками и тестирует на её же строках; читать надо весь файл целиком, а не только строки `💬`.
>
> **Форма теста задана его словами и держится сторожем** `node tools/testing_shape.js`: заголовок, под ним галочка и строка `💬` одним уровнем, свёрнутый коллаут «Откуда эта задача», подзаголовок «Настройки и действие» с таблицей по вкладкам и нумерованным действием, подзаголовок «Эталон и проверка», где эталон лежит в коллауте `[!check]`, а голая строка списка ровно одна — его рабочая, и свёрнутый коллаут «Журнал». Эталон и рабочая строка — без обратных кавычек. Мои объяснения живут в `docs/dev/AWAITING_OWNER_CHECK.md` и в заметку не дописываются; заметка пересобирается каждый заход.
>
> **Перечень тестов пополняется тем же заходом, что и правка** — его требование, а не пожелание.
>
> ЧТО СДЕЛАНО 68-Й СЕССИЕЙ
>
> Его `S11`: круг дочернего поля при `Add the parent Value` схлопывался в одно значение. Ответ на «наш родитель или его» объявлен один раз в `pkm_rules_runtime_helpers.js`. Тем же заходом сняты две следующие формы. Исключение к З3 № 134, разбор 10.13.216, отчёт 264у, урок У-238, правило 161. **Он принял это тремя галочками.**
>
> Его `S8`: «что изменилось» в автокопии называет контролы именами панели и вкладывает их друг в друга. Разбор 10.13.217, отчёт 265у.
>
> **Его новый дефект и починка:** команда поля, вызванная при открытой панели, правила вид панели, лежащий в документе, — работа пропадала, на экране оставался мусор. Правило «пока панель открыта, строкой распоряжается она» стоит в двери команд `runPkmRuntime` (вне З3), открытие самой панели из-под него выведено. Закреплено `tagwheel_session_guard_tests.ts`, три подмены краснеют. Разбор 10.13.218, отчёт 267у.
>
> СОСТОЯНИЕ
>
> Семь шагов зелёные: `npm test` 88 из 88, обход строки 0 расхождений из 234 (с `--split` — 7 из 234 и 4 из 211, это находка `Ф-8`), браузерный шаг зелёный при 50 подменах. Наружу не выложено ничего.
> ```
