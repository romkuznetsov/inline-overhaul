# Его записи «Новое пишите сюда», ночь на 2026-09-22

Четырнадцать пунктов дословно, как он их написал. Сделаны 1, 3, 4, 5, 6, 12, 14;
остальные — в интервью цикла 77.

## Новое пишите сюда
Всё, что заметите по ходу, — сюда, своими словами и без порядка. Я разберу и заведу отдельными тестами.

💬 новые задачи - сначала проанализируй, исправь очевидное, а по более сложным задачам сформулируй вопросы для уточнения в виде интерактивного опроса (вопросы задавай в конце). Не обязательно делать все за одну сессию - можешь отложить их на другой раз. Однако следующая сессия будет ночная, я хочу, чтобы ты сделал как можно больше автономной работы
1. В tag-appearance слишком много настроек и это мешает смотреть на live preview (т.е. требуется проскроллить экран настроек наверх, чтобы увидеть preview). Я хочу, чтобы ты сделал ЛИБО 
	1. субхедеры io-tip-sub-line-view, io-tip-sub-tag-view и io-tip-sub-link-view сворачиваемыми (треугольником). Если так сделать можно, хочу, чтобы все субхедеры были сворачиваемыми 
		1. под io-tip-wheel-preview сделай субхедер `Panel` (в него входят все настройки до субхедера `Scroller`). Также перенеси в него wheel-active-field - под panel-markers (субхедер io-tip-sub-tagwheel-opening не нужен, можешь его удалить). Субхедер `Panel` должен быть сворачиваемым
		2. io-tip-sub-scroller
		3. io-tip-sub-move-text
		4. io-tip-sub-moving-lines-left-and-right
		5. все субхедеры в global-hotkeys (io-tip-sub-expanded-ctrl-a-a и т.д.)
	2. (если прошлый вариант со сворачиваемым субхедером сделать сложно) сделать их отдельными хедерами (сворачиваемым треугольником), а под  хедером tag-appearance должен быть только live preview. 
2. баг - в io-tip-wheel-preview panel-highlight не пропадает при panel-highlight=off, а также ее цвет не меняется в зависимости от panel-background
3. сделай в command-reference (и везде где есть поля для присвоения хоткеев) цвет текста `not set` - серым (сейчас он курсивный, но я хочу еще больше подсветить то, что это не выделено цветом) 
4. переименуй 
	1. команды
		1. Move line up → Move up
		2. Move line down → Move down
		3. Move cursor left in line → Jump left
		4. Move cursor right in line → Jump right
	2. хедеры
		1. move-lines → Move lines (up\down)
		2. left-right → Move lines (left\right)
		3. in-line → Jump inside line (left\right)
		4. heading-jumps → Jump inside note (up\down)
		5. backlinks → Auto-MOC in your Links
5. show-setting-ids перенеси в diagnostics, а сам хедер setting-ids удали
6. Сейчас в колонке io-cmd-col-col-command все команды называются полным названием `Navigation: Move left`, но у команд есть хедеры, например `Navigation` и из-за этого выглядит гротескно. Я хочу, чтобы в этой колонке команды не содержали префикс хедера, т.е. команда должна называться `Move left`. Это только для отображения в этой колонке, пусть реальное название команды остается таким же полным. То же самое в io-field-commands и в других местах, где встречаются названия команд
7. баг при move left/right - если в предложении `(слово1 слово2 слово3)` выделить `слово2` и move right, то получается `(слово1 слово3) слово2`, а я бы хотел, чтобы перенос был до символа → `(слово1 слово3 слово2)`, и только после следующего move right хочу получить `(слово1 слово3) слово2`. Понятное дело, что это только один кейс, а проблема шире - изучи ее сам и сформулируй грамотное решение
	1. Если я в этом же примере переношу вторую скобку влево , то получаю `(слово1 слово2 )слово3`, а ожидал `(слово1 слово2) слово3` 
8. я ставил тебе задачу, чтобы inlineOverhaul работал в режиме просмотра - я хочу, чтобы был контрол в General "Where inlineOverhaul works" = Editor\Preview\Everywhere. При Editor - только в режиме редактирования (как сейчас), Preview - только в режиме просмотра, Everywhere - везде
9. в binder в модальном окне создания новой команды в inserts делать выпадающий список с вкладками - в которых есть элементы (смотри ниже), в которых будут кликабельные примеры для упрощения выбора пользователя (т.е. он по прежнему может вписать в text block что угодно, это лишь поможет ему выбрать элемент). Должно быть реализовано удобно, например, чтобы элементы выбирались на из тупого вертикального списка, а например, была возможность скроллинга или что-то такое, best practice
	1. эмодзи (`❓` и т.д.)
	2. символов unicode  (`→` и т.д.)
	3. текстовые спецсимволы (`(◕‿◕)` и т.д.)
	4. чтобы окно command name могла подобрать дефолтное название исходя из выбранного inserts (например при `→` должно быть `Arrow right`). Пользователь может скорректировать его при желании, но дефолтное название всегда должно быть предложено
10. по аналогии сделать для io-element-marker - чтобы при нажатии на панель ввода открывался выпадающий список с эмодзи - пользователь может ткнуть в понравившийся, а может сам вставить что хочет.
11. для задачи `З-36` уточню, что этот режим должен быть опцией в контроле  io-field-child = `Show when press Alt`. 
12. в io-field-child сделай дефолтным полем After parent
13. нужен smart redo (ctrl+y) - т.к. сейчас обычный ctrl+y не антагонистичен ctrl+z (возможно это с учетом доработок ctrl+z для отмены всех изменений tagwheel одним нажатия и т.д.). Иногда у меня что-то сбоит, и я могу отменить ввод с crlt+z, но потом ctrl+y не возвращает обратно (т.е. как будто очищает сохраненный буфер обмена)
14. Также сейчас если я при создании field=emoji не задал io-element-marker, то затем перейдя в заметку при активации tagwheel я получу ошибку `these fields need an Emoji`. Это не интуитивно. Я хочу, чтобы у всех обязательных настроек была красная рамка (в скриншоте ниже у textblock `one character or emoji` рамка должна быть не светло-серая, а ярко-красная, пока пользователь не вставит туда значение, затем она должна меняться на обычный цвет). Проверь, нет ли подобных обязательных полей  для ввода и сделай так же (например у Binder `inserts` и т.д.)
	1. ![[Pasted image 20260922005318.png]]

