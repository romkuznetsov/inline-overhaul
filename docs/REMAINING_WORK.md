# Что осталось до полного завершения технических работ

> **Что здесь есть и чего нет.** Только техническая работа по нынешнему
> плагину: новых функций в списке нет — они живут в `test-vault/Ideas.md` и
> разбираются отдельно. Список собран 2026-09-11 обходом документов, а не по
> памяти: у каждой строки назван источник, чтобы её можно было перепроверить.
>
> **Строка отсюда уходит двумя способами:** работа сделана и подтверждена, или
> заказчик её снял. Зелёная проверка приёмкой не является.

---

> **Очередь, которую он назначил, закрыта 2026-09-15.** Его слово, завершая
> сессию 52, было «в следующей доделываем оставшуюся техническую работу»;
> сессии 53 и 54 её доделали. Все строки раздела 1 вычеркнуты, найденное по
> дороге либо починено, либо названо числом и записано отдельной строкой.
>
> **И тем же днём снят запрет на выкладку** (В-106, держался с 12 сентября):
> «размещай релиз на gh и завершаем сессию». Выпущен `0.1.0-beta.7` — первый
> релиз с 11 сентября и первый, в который уехало всё, что делалось эти четыре
> дня.
>
> **Что осталось в этом файле:** уборка кода, у которой предмета на экране нет
> (раздел 1, строка 3, и разделы 1д, 1е, 1и), решения заказчика (раздел 2) и
> то, что ждёт его глаза (раздел 3).
>
> **И эта уборка назначена его словом 2026-09-15:** «пока я проверяю в рабочем
> vault, хочу чтобы ты сделал этот пункт 2 в следующей сессии». То есть задача
> следующей сессии — **строка 3 раздела 1 и разделы 1д, 1е, 1и**, и ничего
> кроме них, пока он не принесёт замечаний из рабочего vault.
>
> **Рамка, которую он поставил ещё 2026-09-15 к первой такой уборке:** «но не
> хочу риска дефектов». Приём остаётся прежним: сводится только то, у чего
> расхождение **измерено** и равно нулю; остальное называется числом и остаётся
> его решением (У-194, У-195, правила 113…116).
>
> **И одно обстоятельство этой сессии.** Он проверяет `0.1.0-beta.7`, который
> уже выпущен и заморожен: уборка идёт локальными коммитами и его сборки не
> трогает. Нового релиза без его слова не делать — иначе непонятно, что именно
> он проверял.

## 1. Моя работа: очередь следующей сессии

**Очередь его**: то, что он назначил и не отменял. Порядок здесь, а не в
голове; всё остальное в этом документе — справка. **Числа в ней сняты прогоном,
а не памятью** — команда названа в самой строке (У-145).

| № | Что | Откуда | Чем меряется |
|---|---|---|---|
| ~~1~~ | **Остаток форм ссылки и тега — измерен и закрыт 2026-09-15, ночь** | Д3 и его остаток закрыты 2026-09-14 | Строка была верна **наполовину**: из девяти рукописных образцов шесть совпадали с домом **до знака** и сведены подстановкой (исключение № 102, отчёт `165у`), у трёх расхождение измерено и они **не сведены**. Остаток назван поимённо, а не словом «остальное»: `editor_visuals_config.js` не считает ссылкой `[[a[b]]` (2 строки из 77 — и что об этом думает Obsidian, не спрошено, У-91); в слое настроек три места «снять скобки» отвечают иначе, и одно из них расходится на `[[#heading]]`, живой форме Obsidian (отчёт `164у`). Класс закрыт **обходом**, а не списком: рукописная форма ссылки роняет `bootstrap_loader_tests.js`, разрешённых адресов два и оба названы. Мера — `node tools/form_divergence.js` |
| ~~2~~ | **Настройка человека, записанная в коде готовой — закрыто 2026-09-15** | его слово 2026-09-14 | Было 30 мест в 10 файлах, стало 0; список долга у сторожа пуст. Исключения к З3 № 87 и № 88, отчёты `151у` и `152у`, разборы — PRD 10.13.124 и 10.13.126 |
| 3 | **Имена, объявленные дважды, вне семьи разметки** | мера заведена 2026-09-14 | `node tools/rule_copies.js`: **83 имени** (было 87), у **56** два и более настоящих объявления — и число это упало не от правок, а от починки самой меры: признак делегата видел одну форму обращения к дому из трёх (10.13.136, У-191). **Точных копий не осталось ни одной** — сведены 2026-09-15 (исключение № 95, отчёт `157а`), и у каждой сверка была своей мерой с контролем. **Шесть оставшихся настоящих копий разобраны 2026-09-15** (исключения № 98 и № 99, отчёт `161у`): три сведены, одна признана не правилом, а две — разными вопросами под одним именем, и у них разведены имена. Имён с двумя и более настоящими объявлениями: **51**, а после двух починок меры в ночь на 2026-09-15 — **36**. **И самое частое из расходящихся — `isObj` — сведено тем же днём** (девять объявлений, четыре формы, исключение № 97, отчёт `159у`): расхождение оказалось одно и невидимое ни одному вызову. **2026-09-15, ночь: 51 → 50**, и это сведение, а не починка меры — `resolveFieldOutputMode` был объявлен трижды, тела сверены на 150 парах и разошлись на нуле (исключение № 101, отчёт `163у`). **Но мера снова врала, и второй раз тем же способом** (У-191): признак делегата не знал третьей формы обращения к дому — «связать, охранить отказом, вернуть», какой движки под З3 берут помощников через шов. После починки признака контроль по У-191 пройден: из списка ушло ровно одно имя, не пришло ни одного, и счёт тел изменился ровно у него (3 → 1). Остаток — расходящиеся копии помельче, и у них расхождения **не измерены**: мерить тем же приёмом — стендом `node tools/form_divergence.js`, у которого три своих контроля, и контроль, что мера видит расхождение (У-189, У-142) |
| ~~4~~ | **Команда поля-элемента не переносит чужое значение в его Block — закрыто 2026-09-15** | найдено обходом строки | Сделано в два шага: переезд зависимостей в общий дом без смены поведения и вызов из третьего движка. Исключения № 93 и № 94, отчёты `156у` и `157у`, строка листа `S44`, разборы — PRD 10.13.131 и 10.13.132 |
| ~~5~~ | **Смена значения родителя уносит значение зависимого поля — закрыто 2026-09-15** | В-117, его слово «уносить всегда» | Панель спрашивает то же объявление, что и команды. Исключение № 91, отчёт `154у`, строка листа `S43`, разбор — PRD 10.13.128 |

**Д2 и Д3 закрыты 2026-09-14 ночью** его словом «давай Д2 и Д3». Три правила о
чужой разметке получили по одному дому: начало строки, разделители зон строки,
форма ссылки и тега. Разбор — PRD 10.13.122, исключение к З3 № 85, отчёт
`149у`.

**И тем же вечером, его словом «давай дальше», закрыты обе прежние строки этого
раздела:** разделитель, записанный в коде готовым (три места, у заказчика не
срабатывали ни разу), и остаток сведения — четыре файла, не подключавшие общий
дом. Разбор — PRD 10.13.123, исключение к З3 № 86, отчёт `150у`.

**`S42` он закрыл галочкой 2026-09-15**, отчёт `153у` унесён в
`docs/archive/AWAITING_OWNER_CHECK_session52.md`. **Его глаза ждёт `S43`** — его
же решение В-117, исполненное тем же днём (отчёт `154у`). Остальное в очереди
выше — уборка кода, и предмета на экране у неё нет.

**Сессия 52 (ночь на 2026-09-15) закрыла его правило целиком** — строка 2
вычеркнута — и нашла обходом строки четыре дефекта, о которых в письмах не
было. Три починены (`151у`, `153у`), один назван известным расхождением с
ценой (строка 4), ещё один стал вопросом (строка 5).

**Приём, которым нашлись все четыре** (У-167): не читать дороги, а обернуть
экспорты модулей и спросить у каждого вызова «вход нёс предмет, выход не
несёт». Чтением до этого трижды выходили неверные кандидаты.

**И приём, которым каждая находка проверена на «моя ли она»** (У-187): рабочее
дерево на коммите до сессии в коротком пути и тот же стенд там. Все четыре
оказались давними.

**Обход строки вырос с четырнадцати форм до девятнадцати**, и четыре новые
куплены этой ночью: значение по ту сторону разделителя, вложенная строка,
номер списка, ссылка в зоне значений и значение дочернего поля без родителя.

## 1ж. Найдено новой половиной обхода — 2026-09-15, ночь

**У обхода строки появилось второе нажатие** (`node tools/line_matrix.js`,
строка «второе нажатие»): каждая дорога нажимает по своей строке ещё раз, как
человек. Первый прогон дал **6 расхождений из 154 пар**.

| Что | Число | Статус |
|---|---|---|
| ~~**Строка `"- "` не переживает собственный разбор**~~ — **починено 2026-09-15** | было 6 из 154, стало 0 | У Obsidian знак списка — это знак **и пробел**, и без него строка перестаёт быть пунктом списка. Сборка обрезала пробел на строке, где кроме начала ничего нет. Та же форма нашлась у заголовка — её нашёл стенд, а не обход, и она дописана в обход тем же заходом. Исключение к З3 № 110, отчёт `180у`, разбор — 10.13.151 и 10.13.155 |

## 1з. Открыто дописанной формой обхода — 2026-09-15, день

**Заголовок без текста дописан в обход** (`node tools/line_matrix.js`, строка
«заголовок без текста»), и на нём сразу видно расхождение, к починке 1ж
отношения не имеющее.

| Что | Число | Статус |
|---|---|---|
| ~~**На строке-заголовке без текста панель оставляет висящий разделитель, а команда нет**~~ — **починено 2026-09-15** по его слову | было 4 из 180 и 6 из 162, стало 0 и 0 | Причин было две, и обе одного рода — правило написано по месту, а не по предмету: к знаку заголовка приклеивалось содержимое слота текста (а туда при совпадающих разделителях попадает правый Block), и пустой слот держался при литерале `left === "-"`. Прятало обе **совпадение его разделителей**: на разведённых ноль расхождений было и до починки (У-147). Исключение к З3 № 111, отчёт `181у`, разбор — 10.13.156 |

**Что эта половина ловит, а набор нет:** сдвиг шага по стоящей дате на сутки
разводит её 6 → 25, `npm test` при этом 70 из 70 зелёные.

## 1и. «Что такое наша метка» — закрыто 2026-09-15, ночь

**Мест оказалось шесть, а не два, и на его конфиге они расходятся.** Мера
заведена стендом (`node tools/form_divergence.js`, группа «что такое наша
метка») на его живых правилах и семи их мутациях. Четверо отвечают одинаково,
двое — пустотой; один из этих двоих ставит курсор, и человек видел это как
«курсор уезжает в конец строки, за дату» — его же замечание 2026-09-11.

**Что сведено:** признак «этот токен — наше значение» объявлен один раз
(`makeFieldValueTokenTest`). Два тела совпадали по построению, у третьего цена
измерена — 14 пар из 160, меняется только курсор. Исключение к З3 № 113, отчёт
`183у`, строка листа `S47`, разбор — 10.13.158.

**Что осталось числом и его решением:** метка шва навигации
(`rules.dates.markers`, три записи в `navigation_runtime.js`) сборке
неизвестна, а метка поля **левой** стороны неизвестна доводке и дому движков.
Ни одна из двух форм не встречается на дороге, где спрашивают обе, — сводить
их значит менять поведение без предмета на экране.

| Что | Число | Почему не сделано |
|---|---|---|
| Метка шва навигации против сторон Order | 7 входов корпуса из 8 разводят места; пар с нулём — ни одной, кроме двух пустых | Дороги разные: навигация не зовёт сборку, сборка не читает шов. Сведение меняло бы поведение там, где сегодня его никто не спрашивает |
| Дом движков у́же остальных на три признака | 3 входа из 8 | `getDateMarkersFromRules` требует у поля `orderKey` и знакомый `kind` и смотрит только правую сторону. Расширение — смена поведения перемещений, а не уборка |

## 1и-бис. Прежняя запись строки (закрыта, оставлена для истории) — 2026-09-15, день

**Найдено проверкой, которую я писал к правке 10.13.157**, а не обходом: на
конфиге заказчика оба списка совпадают, и обход этого показать не мог.

| Что | Число | Почему не сделано |
|---|---|---|
| **Список меток элементов объявлен дважды** | на его конфиге расхождений 0; на фикстуре, где метка объявлена одним способом, ответы расходятся | Доводка строки берёт метки из `rules.dates.markers`, сборка — из сторон Order (`fieldsShape`). Сведение вслепую уже пробовано в этом же заходе и уронило обход на его данных: значение элемента занимает **два** токена (`📅2026-09-15` и `13:16`), а признак сборки спрашивается о токене. То есть у сведения есть цена, и мерить её надо отдельно — стендом `node tools/form_divergence.js`, добавив туда эту пару. Оба сегодняшних ответа закреплены в `runtime_unified_parity_tests.js`: попытка привести их к одному покраснеет. Разбор — 10.13.157 |

## 1е. Остаток долга копий разобран поимённо — 2026-09-15, ночь

**Из 31 имени работы по существу — на восемь**, и три из них уже разобраны.
Остальные двадцать три — не правила: доставалки модуля (5), ленивая сборка (2),
переходники (2), охраняемые делегаты, которых не видит мера (4), тонкие
пересказы через общий дом (3) и **совпадения имён** (7) — разные вопросы,
случайно названные одинаково; лечатся переименованием, а не сведением.

**Восемь настоящих:** ~~`normalizeCheckboxToken`~~ (**имена разведены 2026-09-15**, 10.13.153),
`isDateLikeToken` (не копия — вопрос шире, разобрано), ~~`composeToken`~~
(**сведено 2026-09-15** по его слову, 10.13.152), и четыре коротких кандидата на сведение —
`resolveDateOffsetByFormatValue`, `resolveFieldIdByOrderKey`,
`applyPkmOptionKeys`, `reportLoaderFallback`. Восьмое,
~~`nextCycleTokenByDirection`~~, **разобрано 2026-09-15 и оказалось не копией**:
24 расхождения из 156 пар, и ни одно в продукте не достижимо — у двух дорог
разные формы входа. Имена разведены, тела не тронуты (исключение № 109,
отчёт `179у`, разбор — 10.13.154).

Разбор с таблицами — PRD 10.13.149. Мера — `node tools/rule_copies.js`, и она
печатает свою слепую зону сама.

## 1д. Найдено мерой 2026-09-15, ночь — ждёт его решения, а не работы

| Что | Число | Почему не сделано |
|---|---|---|
| ~~**`composeToken` объявлен четырежды, и ни одна пара не совпадает**~~ — **решено и исполнено 2026-09-15** | было 73 расхождения из 136 пар, стало 0; удвоенная приставка — 0 пар вместо 17 у каждого из трёх тел | Его слово: «`#todo`, лишнее убирать в самом правиле». Сведено к одному объявлению — `composeToken` в `shared_utils.js`; охрана переехала из ранних возвратов у звавших в само правило. Цена названа: дом у́же прежнего тела помощников на один край — приставка, не равная решётке, больше не теряется. Поведение на его данных не изменилось (обход 171/0). Исключение к З3 № 107, отчёт `177у`, разбор — 10.13.152 |
| ~~**`normalizeCheckboxToken` объявлен дважды**~~ — **решено и исполнено 2026-09-15, В-118** | 8 расхождений из 31 входа | Это не копия, а **два вопроса**, и так решил заказчик: «пользователь может вводить как угодно, а плагин сам нормализует ввод». Имена разведены тем же днём: `normalizeCheckboxInput` в слое настроек принимает ввод человека, `normalizeCheckboxToken` в доводке строки — готовый токен. Поведение не изменилось ни на одном входе. Исключение к З3 № 108 (правка только в комментарии), отчёт `178у`, разборы — 10.13.144 и 10.13.153, правило 117 |
| **Пятислойная передача нормализатора ключа Order** | пять слоёв, на каждом «дали — бери данное»; шов `globalThis`, побеждает загрузившийся первым | Копии сняты (исключение № 105), сама цепочка не разобрана: пять файлов, три под З3. Разбор — 10.13.146 |

## 1г. Прежняя строка раздела 1 — закрыта

**Сессия 48 (ночь на 2026-09-14) — автономная.** Три ваших замечания от
13 сентября разобраны и сделаны, и все три оказались не о том, о чём выглядели:
две кнопки `→` — о разорванном окне отрисовки, артефакты полосы — об
выдуманной зрительной строке. По следу первого найден и починен третий дефект,
о котором замечания не было. Строки листа — `S36`, `S37`, `S38`.

**И выполнена ваша вторая задача на ночь** — полный разбор технического долга:
`docs/CODE_REVIEW_2026-09-14.md`. Что из него делать и в каком порядке —
раздел 6 того документа; сюда перенесены только строки, ждущие работы.

**Строка `Т6` закрыта 2026-09-14 ночью** — знак задачи переживает команду поля и
на строке без текста. Условие оказалось шире замечания: знак пропадал у любой
команды поля без своих знаков, при любом знаке, и второй раз — на выходе из
круга, когда строка сворачивается к началу. Разбор — 10.13.105, исключение к З3
№ 76, отчёт 133у, строка листа `S33`.

**Ждёт его глаза, а не работы.**

## 1а-бис. Ревизия кода — закрыта

Ревизия кода закрыта семью заходами (PRD 15.7). Последняя строка — `Т5`,
падение браузерного шага в CI, — **закрыта 2026-09-13 прогоном на сервере**:
семь шагов из семи зелёные, шаг стоит в `ci.yml` и больше оттуда не убирается.

**Чем это оказалось.** Причину назвал журнал того самого прогона
(`gh run view <id> --log-failed`), а не перебор окружения. Признак дефекта был
написан через величину, принадлежащую машине: «подложка дошла до правого края
содержимого». Законная подложка правого Block растёт наружу за конец
написанного на 8.8 точки — значит на строке, где написанное кончается ближе
девяти точек к краю ящика, **правильная** подложка края достигает. На Linux
моноширинный шрифт шире (DejaVu против Consolas), написанное кончалось в десяти
точках от края: подложка `294.98…519.7` при крае `520` и написанном до `510.06`.

**Воспроизведено у себя** — `IO_GATE_WIDTH=472`. Окно, в котором это видно,
шириной семь точек (470…476), и семнадцать ширин перебора в него не попали ни
разу: перебор отвечает на «часто ли», а вопрос был «при каком условии» (У-176).

**Как признак выглядит теперь.** Подложка перенесённой строки выходит за
написанное не дальше, чем подложки строк **без** переноса: там выделением
рисовать нечего, и они — эталон с той же страницы, снятый той же машиной.
Величины машины в вопросе больше нет (У-155). Порог берётся из предмета
(У-143): 53.78 против выхода 155.77 у подмены `rows-never-cut`.

**Работа по этому разделу кончилась.** Всё, что осталось, — в разделах ниже:
шаг 4 снятия служебного файла (ждёт его проверки шага 3), решения заказчика и
то, что ждёт его глаза.

## 1а. Снятие служебного файла правил — порядок исполнен целиком

**Решение принято 2026-09-07** и делегировано мне его же словами: «максимально
эффективный плагин, в котором отсутствуют избыточные элементы и дублирования».
**Условие «после подачи в каталог» он снял 2026-09-11** и выбрал порядок:
**шагами, с остановками на его проверку**.

Что это за файл — словами: плагин сам пишет `generated_rules.md` в свою папку
при каждом запуске и после каждой правки настроек, а движки читают правила
**из него**, а не из настроек. То есть одно и то же описано дважды, между ними
стоит сборщик на 149 строк, и каждое открытие панели разбирает файл обратно.
Разбор целиком — PRD 10.13.52.

**Порядок обязателен, и он такой:**

| Шаг | Что | Чем кончается |
|---|---|---|
| 0 | сверка двух ходов на одних входах — **сделана 2026-09-07** | пин, который краснеет, если прямое чтение разойдётся с чтением через файл |
| 1 | `navigation_runtime.js` — **сделан 2026-09-11, вечер** | он проверил и принёс три замечания; все три разобраны (PRD 10.13.68) и оказались давними, а не следствием шага. Подтверждено галочкой 2026-09-12 |
| 2 | `status_tags.js` и `status_date.js` — **сделан 2026-09-11, ночь** | `S6` подтверждён галочкой 2026-09-12; ждёт `S4` |
| 3 | `tagwheel.js` и `tagwheel_core.js` вместе с разбором файла — **сделан 2026-09-13 и подтверждён его галочкой `S32`** | у файла не осталось ни одного читателя |
| 4 | сборщик, оркестратор, ключ `Rules path`, путь в конфиге, сам файл — **и `tests/TriggerWheel`** — **сделан 2026-09-14, ночь** | файла в папке плагина больше нет, и плагин убирает оставшийся от прошлых версий; ушли второй шов старой формы, разборщик JSON-блоков и 464 строки `TriggerWheel`. Отчёт — 10.13.52, П-13 |

**Все четыре шага сделаны.** Отчёты: шаг 1 — PRD 10.13.52, П-10; шаг 2 — П-11;
шаг 3 — П-12 (подтверждён его галочкой `S32`); шаг 4 — П-13.

**Что снял шаг 4:** сборщик, оркестратор сборки, модуль-шов, ключ `Rules path`,
путь в конфиге, разборщик JSON-блоков и 464 строки `TriggerWheel`. Файла, что
остался у человека от прошлых версий, плагин убирает сам — с трёх адресов и
только если внутри блок его сборщика.

**Чего это не меняет:** `applyOrderToRules` остаётся — она не про файл, а про
доработку прочитанного (короткие имена Fields, дочерние, раскладка токенов).

## 1б. Последние три сессии — что в них сделано

**Сессия 45 (2026-09-13, вечер и ночь) — закрыта его галочками `S15` и `S29`:**
полоса на строке-заголовке, путь измерения к панели, `Ctrl+Z` после панели,
текст из двух цифр, подложка на перенесённой строке четырьмя заходами. Отчёты —
`docs/archive/AWAITING_OWNER_CHECK_session45.md`, разборы — PRD 10.13.97…104.

**Сессия 46 (2026-09-13, ночь) — шаг 3 снятия служебного файла**, подтверждён
галочкой `S32` (10.13.52, П-12), и закрыта строка `Т5`: браузерный шаг вернулся
в CI. Отчёты — `docs/archive/AWAITING_OWNER_CHECK_session46.md`.

**Сессия 47 (ночь на 2026-09-14) — автономная**, четыре куска и два вопроса:

| № | Что | Строка листа | Разбор |
|---|---|---|---|
| Т6 | знак задачи переживает команду поля и на строке без текста | `S33` | 10.13.105, З3 № 76 |
| — | знак списка человека переживает шаг по элементу при любом знаке | `S34` | 10.13.106, З3 № 77 |
| — | пустой слот под текст держится при любом знаке списка | `S34` | 10.13.107, З3 № 78 |
| — | снятие служебного файла правил, шаг 4 — порядок 1а закрыт целиком | `S35` | 10.13.52, П-13 |

**Что стоит помнить.** Два дефекта из трёх нашёл **обход**, а не его глаз: идя
по следу его замечания, я прогнал все знаки начала строки, а не один. И третий
раз за ночь одно и то же правило оказалось объявлено в нескольких местах, каждое
со своим представлением о том, чем бывает начало строки (У-150, У-177).

**Два вопроса ждут интервью:** В-114 (скобки без знака списка) и В-115
(строка-цитата теряет цитатность) — `docs/OPEN_QUESTIONS.md`.

## 1в. Долг, найденный ревизией 2026-09-14 — четыре строки из шести закрыты

Разбор целиком — `docs/CODE_REVIEW_2026-09-14.md`, здесь адрес и цена. Ни одна
строка не начата: ревизия была задачей на разбор, а не на правку.

**Д1, Д4, Д5 и Д6 сделаны 2026-09-14** по его слову «выполняй очевидную работу
по ревизии»; разбор — раздел 6 самого отчёта. Осталось две строки:

| № | Что | Цена | Почему не сделано |
|---|---|---|---|
| Д2 | знак списка: семь копий во втором написании | заход | **разблокировано 2026-09-14**: ответ на В-115 есть, делается одним заходом с ним |
| Д3 | ссылка и тег: 27 мест без общего дома | два захода | **решено 2026-09-14** (В-117): свести оба, но после его замечаний |

**И две мелочи, найденные по дороге:** в `buildOutputTokenForField` условие
`/^#\//` недостижимо (строкой выше ранний возврат), и `getFieldValueByToken` не
исполняет ни один прогон. Поведения не меняют.

## 2. Ждёт решения заказчика — работа начнётся после ответа

| № | Вопрос | Что стоит за ним | Где разбор |
|---|---|---|---|
| Р5 | **`Ctrl+Z` после сессии панели возвращает то, чего не было** — **сделано и подтверждено им 2026-09-13** | Панель держала свою полосу в тексте заметки и временно удаляла то, что на строке стояло. Теперь она встаёт **рядом**: документ получает только вставку, а закрытое полосой прячется оформлением | закрыто его галочкой под `S15` и словом «ок работает». Стенд `node tools/undo_bench.js`: ноль во всех четырёх случаях, прежняя форма записи стоит в нём положительным контролем |
| Р4 | **Минификация сборки** | Бету решено **не** минифицировать (В-95, 2026-09-10): «нужно упростить работу по отлавливанию багов». `0.1.0-beta.7` выпущен без минификации по этому же слову: выпуск беты через BRAT — не тот «выход наружу», ради которого она заводится. Срок прежний — подача в каталог, и решение за ним | реестр 28у |


## 2а. Решено 2026-09-11 — строки ушли из списка

- **Подсветка строки под TagWheel** — включена по умолчанию. Тумблер был
  и раньше; вопрос был только про умолчание. Проверка — строка `G1` листа приёмки.
- **Гифки `showcase.md`** — 17 записей сняты целиком, файлы гифок на месте.
- **Мобильные и флаг «только для компьютера»** — оставлено как есть: мобильных
  проверок не было ни одной, и обещать работу на телефоне не на чем.
- **Два «открытых вопроса» PRD** — закрыты как устаревшие: оба спрашивали про
  то, что уже сделано по ходу фазы 2.
- **Браузерный шаг проверок — решено поставить в CI** (2026-09-11, прежнее «не
  в CI» снято им же). **Исполнено 2026-09-13:** шаг стоит в `ci.yml`, семь
  шагов из семи зелёные на сервере. Разбор падения, которое это откладывало, —
  раздел 1 выше.
- **Две ссылки в карточке плагина** — **отложены его словом 2026-09-11**.
  Ждут его адресов; пункт «добавить донаты» из файла идей остаётся открытым.
- **Ревизия, заходы 6 и 7** — закрыты. Слепых фикстур было четыре из пяти,
  стало одна и названа; правил, которых набор не держал, было три из пяти,
  стало ноль.
- **Шум линтера** — снят: предупреждений было 37, стало 0. Все одного рода —
  лишний обратный слэш в регулярном выражении; выключено само правило, а не
  переписаны выражения, и причина стоит в `eslint.config.mjs`.
- **`tagwheel.js`: шов внутри главного входа** — решено **не трогать**. Шов
  оказался не остатком, а несущей конструкцией набора: через него проверки
  заводят движок и нарочно снимают модуль, чтобы прогнать путь «не приехал».
  Разбор — PRD 15.7.

## 3. Ждёт его глаза — проверить и сказать слово

| № | Что | Как проверить |
|---|---|---|
| П1 | **`README.md` и `instructions.md`** (строка `F3` листа приёмки) | Открыть оба и посмотреть, не обещают ли они того, чего нет, и не молчат ли о том, что есть |
| П2 | **`FEATURES.md`** (строка `F4`) | То же: лист возможностей читают те, кто проверить написанное не может |
| П3 | **Вычитка заметки-руководства** (В-2) | Черновик написан 2026-08-29, девять разделов. Заметка принадлежит человеку — плагин её больше не переписывает |
| П4 | **Работа плагина на настоящем Obsidian после `0.1.0-beta.7`** | **Снова через BRAT:** запрет на выкладку снят 2026-09-15, релиз выпущен, и BRAT берёт последний обычный релиз. Поставить обновление и убедиться, что включается и работает как работал. Он сказал 2026-09-15, что сделает это отдельно, когда техническая работа кончится, — она кончилась. Здесь никакая ревизия глаз не заменит |
| П5 | **Отчёты 52у–88у реестра** | Работа 9–11 сентября, у которой нечего нажать: ревизия кода, тихие отказы, мёртвый код, две беты, лист возможностей. Строка на отчёт — в `docs/AWAITING_OWNER_CHECK.md`, разборы — в `docs/archive/AWAITING_OWNER_CHECK_reports_2026-09-09_11.md`. Подтверждаются словом, а не галочкой |

## 4. Отложено его словом — в списке, чтобы не потерялось

- **Мобильные устройства** — «реализовать позже». Флаг «только для компьютера» решено оставить (2026-09-11), и снимать его имеет смысл только вместе с проверкой на телефоне.
- **Переключатель `AND` / `OR` внутри одного типа Field в Smart Rules** (C51) —
  «в дальнейшем»: сегодняшнее поведение подтверждено галочкой. Формально это
  пункт фазы 5.3, по существу — новая функция.
- **Подача в каталог Obsidian.** Блокеры фазы 6 закрыты, правила каталога
  сходятся все; сама подача — его решение и его аккаунт.
- **Две ссылки в карточке плагина** — отложены 2026-09-11 до его адресов.
- **`TagWheel` в произвольном месте строки** (пункт 8 его файла идей). Его слово
  2026-09-13: «это из старой версии плагина функция по вызову tagwheel в
  произвольном месте строки… однажды я хочу вернуться к этой функции, но пока
  можешь снять». Старая реализация — `tests/TriggerWheel`, 464 строки; она
  **снята шагом 4 в ночь на 2026-09-14** и остаётся в истории git: коммит снятия
  назовёт, откуда её достать. Возвращаться к ней как к коду не стоит: расхождений с нынешним
  TagWheel накопилось больше, чем сходств, — это заказ на функцию, а не на
  восстановление файла.

## 5. Долгов, закреплённых числом, не осталось

Проверено прогоном 2026-09-15, а не памятью:

- настройка человека, записанная в коде готовой — **0 мест**
  (`config_literals_tests.js`, список долга пуст, домов умолчания два и оба
  названы поимённо);
- тихие отказы без объяснения — **0** (`silent_failures_tests.ts`, список
  `DEBT` пуст);
- мёртвые экспорты — **0 из 866 имён** (`dead_exports_tests.js`). **Число имён
  выросло с 738 не от работы, а от починки самого сторожа:** его маска давала
  обратной кавычке искать пару до конца файла, а такая кавычка стоит внутри
  регулярного выражения — сторож видел 20 227 знаков из 85 362 и проверял три
  четверти кода, не зная об этом (У-192, PRD 10.13.137);
- имена, объявленные в рантайме больше одного раза — **28** против 71.
  **Чем опущено с 30 (У-118):** одно сведением — склейка приставки со
  значением, четыре тела в один дом (10.13.152); два разведением имён, где
  копии не было вовсе — знак задачи (10.13.153) и шаг по циклу (10.13.154).
  **И это нижняя граница, а не число:** мера видит только объявления верхнего
  уровня — 1217 из 1522; вложенные функции и стрелочные замыкания в счёт не
  попадают, а самые крупные находки ночи на 2026-09-15 жили именно там
  (10.13.148). Прогон говорит это сам.
  **И мера видит не всё:** одно правило бывает объявлено под разными именами,
  и тогда она считает их разными предметами. Так было с ключом Order — шесть
  тел под тремя именами, из которых мера видела четыре (10.13.146). Такие
  находятся поиском **по телу**, а не по имени
  (`node tools/rule_copies.js`): точных копий не осталось ни одной, а признак
  делегата научился видеть **четыре** формы обращения к дому — четвёртую,
  «связать, охранить отказом, вернуть», он не видел до 2026-09-15 и держал
  два честных делегата настоящими телами (У-191, второй раз);
- `no-unused-vars`, `no-undef` — **0 ошибок**;
- инлайновое оформление — **0** (`catalog_rules_tests.ts`);
- `var` вне движков — **0**;
- правила каталога Obsidian: расходится **ни одного**, четыре по букве с
  названной причиной;
- открытых вопросов заказчику — **ни одного**: В-114, В-115 и В-117 отвечены и
  исполнены, разборы унесены в архив.

**Одно новое место, найденное 2026-09-13 и уже решённое им:** под `tests/`
лежит `TriggerWheel` — 464 строки, копия движка со своим разбором заметки
правил. В продукт не входит, но это ровно то дублирование, ради снятия которого
задан порядок 1а. Его ответ: снять вместе с шагом 4, саму функцию («TagWheel в
произвольном месте строки») записать в задачи — сделано, раздел 4.

**Числа, которые растут от обычной работы, живут в прогоне, а не здесь**
(У-145): перепись записей в консоль, `var` под З3 и размер `styles.css`
печатает `catalog_rules_tests.ts`, число подмен — `npm run gate:browser`.
