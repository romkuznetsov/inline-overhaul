# Ревизия кода 2026-09-14: технический долг и проблемы

> **Что это.** Ответ на вашу задачу «выполни полный анализ кода на предмет
> наличия технического долга и проблем», поставленную в ночь на 14 сентября.
>
> **Чем он получен.** Прогонами, а не чтением. У каждого обхода — контроль до
> первого вывода (У-119), и там, где контроль покраснел, это здесь написано:
> один из обходов соврал, и его находки в отчёт не пошли.
>
> **Числа живут в прогоне** (У-145). Команда, которой снимается каждое, названа
> рядом; в документе они стареют быстрее всего.

---

## 0. Что уже держится числом — и держится

Прогон, не память:

| Долг | Сейчас | Чем снято |
|---|---|---|
| тихие отказы без объяснения | **0** | `node tests/regression/silent_failures_tests.ts` |
| мёртвые экспорты | **0** из 723 имён | `node tests/regression/dead_exports_tests.js` |
| `no-unused-vars`, `no-undef` | **0 ошибок** | `npm run lint` |
| инлайновое оформление | **0** | `node tests/regression/catalog_rules_tests.ts` |
| правила каталога Obsidian | расходится **ни одного** | там же |
| семь шагов | **зелёные**, подмен 52 | все семь |

Эта таблица — не предмет ревизии, а её отправная точка: всё, что меряется,
измерено и в нуле. Ниже — то, чего **нынешние меры не видят** (У-137: мера
видит одну форму дефекта).

---

## 1. Одно правило, объявленное в нескольких местах

**Чем снято:** обход регулярных литералов по маске кода (комментарии и строки
стёрты, У-138), контроль на четырёх шагах — разбор, комментарий, строковый
литерал, счёт. Литералов-правил в рантайме **170**, из них объявлены больше чем
в одном файле **40**.

Из сорока разобраны те, что записывают **правило о разметке человека** — то
есть класс У-91 и У-150, на котором за две недели вышло три дефекта.

### Д1. Один и тот же помощник под пятью именами — **сделано 2026-09-14**

`escapeRegExp`, `escapeRx`, `escapeRegex`, `escapeRe`, `escapeRegexLiteral` —
одно тело, пять имён, **девять объявлений плюс одно место, где тот же набор
знаков написан прямо в строке**:

```
src/core/editor_visuals_config.js:424     escapeRegExp
src/core/line_pipeline.js:631             escapeRx
src/core/pkm_line_finalize_unified.js:378 escapeRx
src/core/pkm_macro_shared.js:36           escapeRegex
src/core/pkm_rules_runtime_helpers.js:494 escapeRegex
src/core/shared_utils.js:12               escapeRe
src/core/status_line_runtime_unified.js:467 escapeRegex
src/core/status_runtime_common.js:296     escapeRx
src/features/transform_feature.js:520     escapeRegexLiteral
src/core/pkm_macro_shared.js:243          — без имени, прямо в выражении
```

**Это утверждение оказалось неверным, и исправлено при правке.** Здесь было написано
«дефекта сегодня нет, тела совпадают знак в знак». Девять копий пишут
`String(s || "")`, а общий дом — `String(nz(s, ""))`. На `0` и `false` копия
отдаёт пустую строку — пустую альтернативу регулярного выражения, а та
совпадает со всем. Сведено на верное тело.

**И мест оказалось одиннадцать, а не десять:** одиннадцатое — стрелочная
копия в `navigation_runtime.js`, и мой обход её не увидел — та же слепота на
регулярных литералах, о которой раздел 5. Нашлась она поиском после правки. **Цена — завтрашняя:** пять разных имён означают, что обход «найди
копию по имени» не находил их ни разу (У-126: запрет, написанный по имени, слеп
к безымянному). Следующая правка — добавить знак в набор экранируемых, учесть
суррогатную пару — сядет в одно место из десяти, и дороги разойдутся
**по-разному** в зависимости от того, какой кнопкой человек это сделал (У-150).

**Как чинить:** оставить `escapeRe` в `shared_utils.js`, остальные восемь
объявлений и одно безымянное место заменить на его вызов. Цена — девять правок в
восьми файлах, шесть из них под З3. Риск нулевой: тела совпадают, и это проверяется тем же обходом.

### Д2. Форма знака списка объявлена в двух написаниях, и они **уже расходятся** — сделано 2026-09-14, ночь

Общее объявление — `LINE_BULLET_RE` и `LINE_ORDERED_RE` в `shared_utils.js:763`
(исключение к З3 № 77, ночь на 14 сентября). Рядом живёт второе написание,
`/^([-*+]|\d+[\.)])\s+/`, в семи местах:

```
src/core/pkm_line_finalize_unified.js:317, 326, 466, 742, 1392, 1445
src/core/pkm_macro_shared.js:362
```

**Расхождение измерено, а не предположено.** Прогон обоих правил по пятнадцати
началам строки:

| строка | общее правило | копия |
|---|---|---|
| `- текст` | знак длиной 2 | `"- "` |
| `- текст` (неразрывный пробел) | **не список** | **`"- "` — список** |
| `> - текст` | знак длиной 4 | **не список** |

Первое расхождение — `\s` против `[ \t]`: `\s` ловит неразрывный пробел, который
приезжает со вставкой из браузера. Второе — цитата, и это **предмет открытого
вопроса В-115**: как поступать со строкой-цитатой, решаете вы. Поэтому чинить
эту копию до вашего ответа я не стал: правка предвосхитила бы решение.

**Как чинить, после ответа на В-115:** свести семь мест к общему объявлению
одним заходом (У-150: починка одного объявления из семи делает результат хуже).

**Сделано 2026-09-14 ночью, и цена оказалась другой.** Мест было не семь:
сплошной обход по форме нашёл сорок строк в двенадцати файлах — мера видела
одно написание (У-137). Предмет же нашёлся третьей мерой, по именам: пять
функций про начало строки стояли дважды знак в знак, и обе копии уже
расходились с общим объявлением. Разбор — PRD 10.13.122. Число копий живёт
теперь в прогоне: `node tools/rule_copies.js`.

### Д3. Те же копии, разобранные до числа, но не чинившиеся — сделано 2026-09-14, ночь

| Правило | Копий | Где живёт общее |
|---|---|---|
| чекбокс `^\[[^\]]\]\s+` | 8 в 3 файлах | `CHECKBOX_ONE_CHAR_SRC`, `shared_utils.js` |
| знак заголовка `^\s*#{1,6}\s+` | 16 в 3 файлах | `LINE_HEADING_RE`, `shared_utils.js` |
| ссылка `^\[\[[^\]]+\]\]$` | 19 в 8 файлах | общего нет |
| тег `^#\S+$` | 8 в 5 файлах | общего нет |
| дата `^\d{4}-\d{2}-\d{2}$` и время `^\d{2}:\d{2}$` | 5 и 5 | формат поля (исключение № 73) |
| цвет `^#[0-9a-f]{6}$` | 4 | общего нет |

Чекбокс и заголовок **уже имеют общий дом** — то есть копии рядом с ним это
ровно признак У-150 («у правила есть общий дом и ссылка на него из второго
места, а второе место считает само», У-159). Ссылка и тег общего дома не имеют
вовсе, и девятнадцать мест — самая большая куча в этом списке.

**Сделано 2026-09-14 ночью.** У ссылки и тега появился дом —
`WIKILINK_TOKEN_SRC` и `TAG_TOKEN_SRC` в `shared_utils.js`, и пять вопросов
поверх них: вопросы разные нарочно, `^#\S+` без якоря конца отвечает «да» и на
`#work=text`. Сведены места в файлах, которые общий дом подключают; остаток
назван строкой очереди в `docs/REMAINING_WORK.md` и меряется тем же прогоном.

---

## 2. Что набор не исполняет ни разу

**Чем снято, две команды:**

```
NODE_V8_COVERAGE=<папка> npm test
IO_COVERAGE_OUT=<папка> node tests/browser/check_editor.js
IO_COVERAGE_OUT=<папка> node tests/browser/check_tagwheel.js
node tools/coverage_map.js <папка>
```

Меру даёт V8, а не мой разборщик. Контролей четыре, и один из них **нашёл ошибку
в самой мере**: функции точки входа исполняются в `dist/main.js` (их зовёт
`bundle_onload_tests.ts`), а по исходнику выглядели непокрытыми. Теперь инструмент
их вычитает и называет отдельным числом.

**Числа прогона:** именованных функций рантайма в покрытии **2972**, исполнено
только в сборке **92**, не исполнено ни разу **254** (8,5 %).

**Оговорка, без которой число врёт:** «набор» здесь — `npm test`, шесть шагов из
семи. Седьмой, браузерный, поднимает слой редактора и сессию панели в Chromium, и
его покрытие сюда не попадает: часть из 254 исполняется **там**
(`buildSourceMarkDecorations` — точно, он и есть предмет сегодняшней правки).
Снять покрытие и с него — отдельная работа.

### Д4. Путь отката переноса в заметку — **сделано 2026-09-14**

`src/features/transform_feature.js`, четыре замыкания `rollback` (строки 2747,
2768, 2800, 2810). Зовёт их одно место — 2918: **правка исходной строки не
удалась после того, как заметка уже создана**. Тогда откат обязан убрать
созданное или вернуть дописанное.

**Здесь отчёт ошибся, и ошибка исправлена при правке.** Написано было
«проверок на него нет ни одной». Мера V8 говорила точнее, а прочитал я её
грубо: из четырёх замыканий исполнялись **два** — создание новой заметки и
перезапись существующей, у обоих есть свои проверки с 2026-09-08. Не
исполнялись другие два:

* **откат создания заметки, которой не было, в режиме `add_to_note`** — дописывать
  не во что, плагин создаёт, и при отказе обязан убрать созданное;
* **откат самого дописывания** — заметка человека остаётся с блоком, которого он
  не просил.

Второй и есть самый дорогой: цена отказа — чужая заметка с лишним куском, и
человек узнаёт об этом только глазами.

**Как сделано.** Две проверки в `transform_runtime_tests.js`, обе через
настоящий `runInline2Note` на фикстуре, где правка строки бросает после записи
цели. У отката дописывания мера не «файл цел», а **равенство тому, что в нём
лежало**, и рядом положительный контроль «дописывание и правда что-то добавило»:
без него «содержимое прежнее» было бы правдой и при половине отката. Подмены
две, каждая краснеет на своём утверждении.

**Мерой проверено, а не на слово:** после правки исполняются **все четыре**
замыкания отката (1, 1, 2 и 1 раз).

### Д5. Тридцать восемь функций, о которых проверки говорят только пином на текст — **разобрано частично**

Это У-141 числом: пин `assertTrue(/имя\(/.test(src))` зелен по построению — он
спрашивает, есть ли строка в файле, и не спрашивает, доходит ли до неё
исполнение.

Из 254 неисполненных разобрано 249 (пять имён не прочёл разборщик списка):
**119** проверки не упоминают вовсе, **38** упоминают только текстовым пином. Полный список — в прогоне; то, что стоит прочитать
первым:

```
pkm_v2/TagWheel/tagwheel_core.js  formatNowByMask, parseHhmm
pkm_v2/status_date.js             parseTokenlessProgress, parseHhmm, formatNowByMask
src/core/status_runtime_common.js getActiveValues, getFieldValueById,
                                  getFieldValueByToken, getAllowedSubValues,
                                  normalizeImportanceTokenShape
src/core/line_pipeline.js         detectMarkerPanel, getPanelSearchText,
                                  resolvePanelByMarkerPresence, clearMarkerFromLine
src/core/pkm_line_finalize_unified.js  relocateOffEntriesToRightPanel,
                                  applyFullNoSourceNormalization,
                                  applyMinimalSelectionNormalization
src/core/pkm_runtime_bootstrap.js reportLoaderFallback, loadOrderConfigFromPluginData
```

**Здесь отчёт ошибся во второй раз, и это тоже исправлено при правке.** Было
написано: `getAllowedSubValues` и `normalizeImportanceTokenShape` объявлены
**дважды**, и «либо одна копия мертва, либо расходятся молча». Открыл — и ни то
ни другое: в `pkm_v2/status_tags.js` стоит не копия, а **делегат** к общему
объявлению, ровно тот уклад, который в этом коде и принят. Вывод «две копии»
был сделан из числа — имя встречается в двух файлах, — а не из чтения (У-109).

**Настоящее здесь другое, и оно осталось:** правило «какие значения разрешены
дочернему Field при таком родителе» не исполнял ни один прогон, а у заказчика
пять команд дочерних полей. Причина найдена: обход строки ходит по
`order.left`/`order.right`, а дочерние Field отдельными ключами там не стоят —
их команды строит реестр от родителя.

**Сделано 2026-09-14:** у обхода строки появилась вторая половина — команды
дочерних полей, пять штук на девять исходных строк. Спрашивается неподвижность:
свойство «написанное плагином он умеет прочесть», то самое, которым нашёлся
заголовок. Расхождений ноль. Мерой проверено, что заход и правда доходит до
предмета: `getAllowedSubValues` исполняется теперь 4 раза, `getActiveValues` и
`getFieldValueById` — по разу.

**Что в этом списке осталось непокрытым** и почему — `getFieldValueByToken` и
`normalizeImportanceTokenShape`. У второго нашлась причина, и она сама по себе
находка: в `buildOutputTokenForField` до него доходят только значения, начатые
с `/` без решётки, — условие `/^#\//` **недостижимо**, потому что строкой выше
стоит ранний возврат по `/^#\S+/`. Мёртвое подусловие в живой функции; правка
поведения не меняет и потому не сделана.

### Д6. Семейство шага по датам в режиме `Custom` — **сделано 2026-09-14**

`stepByCustom`, `stepByCustomForward`, `buildCustomPlan`, `getDateProgressForStep`,
`formatNowByMask`, `parseHhmm` в `tagwheel_core.js`, и рядом `projectMatches`,
`matchSelectedProjectFilters`, `splitCombinedTagToken`.

Проверено поимённо поиском, а не разбором: в `tests/**` имена `stepByCustom`,
`buildCustomPlan`, `projectMatches`, `matchSelectedProjectFilters`,
`splitCombinedTagToken` не встречаются **ни разу**, а V8 говорит, что их не
исполняет ни один прогон.

**Причина названа и она простая:** у вас оба элемента настроены на `standard` и
`command`, а стенды гоняются на вашем конфиге. То есть настройка в панели есть,
человек её видит, а поведения за ней не проверял никто.

**Как сделано.** Девять проверок на сами правила режима — разбор списка шагов
(числа, `N (повтор)`, слово `END`, мусорная строка, старая форма), шаг вперёд и
шаг назад, пустой список и запрет нулевого шага. Проверяются **правила**, а не
движок: в режиме `custom` он делегирует сюда все три вопроса. Его собственная
ветка «круг кончился по `END`» осталась непокрытой, и это сказано вслух в самой
проверке: экспорта у неё нет, а заводить его ради набора значило бы менять
продукт под проверку. Подмены четыре, каждая краснеет на своём утверждении.

---

## 3. Утверждения о платформе: обход есть, второй находки нет

**Чем снято:** обход комментариев рантайма, ищущий утверждения вида «Obsidian
делает то-то», у которых не назван источник. Контроль из трёх шагов пройден.

**Находок обход дал 53, и это не 53 находки, а 53 места для чтения** (У-119:
настоящие находки этого класса приходят по одной, а большой круглый список целой
области — признак того, что врёт обход). Прочитаны все, у которых утверждение
**несущее** — то есть на нём держится ветка кода: их двенадцать.

**Настоящая находка одна, и она уже починена этой же ночью** — разбор в PRD
10.13.111, урок У-181: «Obsidian ставит расширения плагина только тем
редакторам, что откроются после загрузки» держало ветку досылки, было неверно и
стоило `RangeError` на каждый патч настроек. Остальные одиннадцать проверены
против `app.js` 1.13.7 и верны.

**Что с этим делать:** обход в его нынешнем виде в набор не годится — он даёт 53
строки шума. Годится более узкий вопрос: утверждение о платформе, на котором
держится `if`. Заводить его как гейт стоит только вместе с правилом «рядом с
таким утверждением называется команда, которой оно прочитано».

---

## 4. Числа в проверках, которые стареют

**Чем снято:** обход утверждений вида «счёт равен числу» в `tests/**`. Всего их
**358**, и почти все законны: они считают то, что положила фикстура (строк в
таблице, кнопок в окне).

Стареет от обычной работы другое — счёт по **тексту исходника продукта**. Таких
осталось **три**:

```
tests/regression/settings_backup_tests.ts:811   «ровно два места»
tests/regression/settings_layer_tests.ts:2979   «ровно три заголовка»
tests/regression/settings_layer_tests.ts:2981   «ровно три подсказки»
```

У каждой рядом стоит список, которым число объясняется, — риск низкий.
**Четвёртая такая проверка покраснела сегодня ночью** от правки, которой она не
касалась («компартмент отметок строки встречается ровно три раза»), и переписана
на равенство трём соседним компартментам: правило было верным, число описывало
форму.

---

## 5. Обход, который соврал, и почему он здесь назван

Разделить неисполненные имена на «мёртвое» и «живое, но непроверенное» я
пробовал своим разбором: маска кода плюс поиск вызовов. Контроль обхода прошёл,
список вышел на 44 имени — и **три проверки руками нашли у них живых зовущих**
(`indentMultipleLines`, `hasTextPartBeforeFirstSeparator`, `findAllowedLineUp`).

Причина названа: маска не знает регулярных литералов, и `/['"]/` в коде
открывает для неё строку — ровно У-139, и ровно на том же месте, что и в первый
раз. Список в отчёт не пошёл; вместо него в разделе 2 стоит то, что меряет V8, и
то, что проверено поиском по именам.

**Это тоже результат ревизии:** своя мера — такое же объявление правила, и врёт
она правдоподобно (У-119, У-96).

---

## 6. Что я предлагаю делать, и в каком порядке

**Сделано 2026-09-14 по его слову «выполняй очевидную работу по ревизии,
вопросы в этой сессии не задавай»:**

| № | Что | Чем кончилось |
|---|---|---|
| Д1 | одиннадцать мест экранирования под пятью именами | сведено в `shared_utils.escapeRe`; сплошной обход **по форме**, два контроля, две подмены |
| Д4 | откат переноса в заметку | два непокрытых замыкания из четырёх закрыты проверками; исполняются теперь все четыре |
| Д5 | правило дочерних Field | у обхода строки появилась вторая половина: пять команд × девять строк, расхождений ноль |
| Д6 | шаг по элементу в режиме `Custom` | девять проверок на сами правила, четыре подмены |

**И два дефекта, найденных по дороге, а не в списке:**

* **оба стенда, гоняющиеся на его конфиге, были мертвы** — обход строки с
  13 сентября (он сменил разделитель), стенд отмены с того же дня (он
  переименовал поле). Оба брали предмет литералом. Починены, разбор — 10.13.113
  и 10.13.115;
* **строка-заголовок получала лишний разделитель** — нашёл починенный обход
  первым же прогоном. Разбор — 10.13.114, строка листа `S39`.

**Осталось, и оба по своей причине:**

| № | Что | Почему не сделано |
|---|---|---|
| Д2 | знак списка, семь копий во втором написании | **ждёт вашего ответа на В-115**: половина расхождения — строка-цитата, и чинить раньше значит решить за вас |
| Д3 | ссылка и тег, 27 мест без общего дома | самая большая куча, но и самая ровная: расхождений между копиями не найдено ни одного. Два захода, и делать их вслепую в ночь без вас я не стал |

**Мелочи, найденные по дороге и не чинившиеся** (поведение не меняют, но код
читается как живой): в `buildOutputTokenForField` условие `/^#\//` недостижимо —
строкой выше стоит ранний возврат по `/^#\S+/`; и `getFieldValueByToken` не
исполняет по-прежнему ни один прогон.
