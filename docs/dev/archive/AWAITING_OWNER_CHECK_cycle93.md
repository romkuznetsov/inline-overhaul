# Приняты его галочками в цикле 94 (тесты 2, 4, 5 цикла 93)

### 362у. Постановка трёх контролов `Inline to note` — **постановка 2026-09-25, день, код ждёт вашего согласия** (`В-224`)

PRD 10.13.272: `Link to Navigator` в `Auto-MOC in your links`, `YAML of navigator values` в разделе свойства Field, `Keep sub-fields` под `Fields to keep` — у каждого место, путь в конфиге, умолчание `Off` (как сегодня) и нюансы: два навигатора у ребёнка, навигатор тега не заметка, навигатор уже на строке. Проверяется тестом 5 заметки: согласие или поправка.

### 361у. `Smart Rules`: ребёнок считается за навигатора — **сделано 2026-09-25, день** (`В-222`, тест 4)

Условие по значению навигатора и «любое значение Field» родителя ловят строку с ребёнком этого навигатора; ребёнок чужого навигатора не ловит. `addNavigatorsOfChildren` в `transform_feature.js`. Закреплено `parent_navigator_tests.js` с отрицательным контролем «без навигатора» и мутацией.

### 359у. Навигатор в custom block — **сделано 2026-09-25, день** (`В-221`, тест 2)

Запись у каретки писала навигатор как обычное Value (`#doing #review`); теперь только ребёнок. Исключение к З3 № 152. Обход строки на вашем конфиге — расхождений 0.
