# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Реестр на 2026-09-08, восьмой заход.** Заказчик проверил всё, что ждало
проверки, и **закрыл три строки из четырёх галочками**: вид панели (`M2`),
`Ctrl+Z` после TagWheel (`W6`) и сообщение об ошибке в движке PKM (`P1`).
Разбор `main.js` он подтвердил тем же заходом.

**Живого в реестре осталось две строки:** `F3` («не смотрел») и решённое, но
не начатое — приглушить заливку шапок таблиц (В-85).

**Он ответил интервью на четыре вопроса** и сказал: «в этой сессии ничего
больше не делаем». Решения записаны в PRD 15.0 и в `docs/OPEN_QUESTIONS.md`;
работа по ним — следующая сессия.

**Одно его уточнение стоит держать на виду:** маркер, который панель TagWheel
оставляет на строке, обязан соответствовать **префиксу этой строки**, а не
быть литеральным `- `. У него для тега `#todo` префикс `- [ ] `, и панель
обязана оставлять именно его. Так и работает; закреплено утверждением.

### 28у. Решено и не начато — на следующую сессию

Ни одна строка ниже проверки не ждёт: они ждут работы.

- **Приглушить заливку шапок таблиц** (В-85, решено 2026-09-08, **не
  начато**). Тот же акцентный цвет, но полупрозрачный, текст обычного цвета.
- **Четыре починки из разбора готовности** (PRD 15.2, решено 2026-09-08, **не
  начато**): не переписывать служебный файл правил без нужды; закрывать
  TagWheel при выгрузке плагина; убрать шум линтера; правило про тихие отказы.
- **Правила каталога Obsidian** — его решение сделало их требованием (PRD
  15.0). Первым делом их надо выписать списком против нынешнего состояния, а
  не править наугад. **Не начато.**
- **Минификация — в самом конце**, перед выходом наружу: «нужно упростить
  работу по отлавливанию багов». **Не начато и раньше срока не делается.**
- **Мобильные устройства** — отложены его словом, реализовать позже. **Не
  начато.**
- **Пункт 4 фазы 6** (гигиена репозитория, манифест, снятие команды
  `Open settings`) — он его не открывал. **Не начато.**

### 27у. Двенадцать замечаний четвёртого листа — **подтверждены заказчиком 2026-09-08**

Девять по виду панели (`M2`), `Ctrl+Z` целиком (`W6`) и сообщение об ошибке в
движке PKM (`P1`) — все три строки отмечены галочками, замечаний не написано.
Разборы каждого дефекта остаются в PRD, строки A42–A45.

Отдельно: `W6` закрыл и `A41`, который сутками раньше я объявил починенным
наполовину. Остаток оказался целым дефектом — урок У-104.

### 26у. Разбор `main.js`: 5208 → 159 строк — **подтверждён заказчиком 2026-09-08**

Строка `M1` прежнего листа закрыта галочкой на `M2`: вид панели он проверял уже
на сборке после разбора. Отчёт о кусках — PRD, раздел 11.


### 25у. Три замечания третьего листа — **подтверждены заказчиком 2026-09-07, ночь**

Полосы тегов (`B1`) и `Transform` на его строке (`T6`) он отметил галочками.
Третий пункт, `Ctrl+Z`, оказался починен наполовину — разбор и починка целиком
лежат разделом 27у выше (дефект A43).

### Разделы 1у–24у — **в архиве**

Всё, что заказчик закрыл галочками до сессии 24, унесено в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`. Открывать, когда нужно
вспомнить, чем закреплена старая правка или когда её сделали.

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него сделаны и подтверждены заказчиком; разборы — в архиве
реестра: `Floating button`, симметричный цикл `Move right`, подсветка
обработанной строки, три пункта 1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа
свойства через `app.metadataTypeManager`.

**Остаток фазы 6 — её пункт 4:** гигиена репозитория, манифест и снятие
команды `Open settings`. Заказчик этот пункт не открывал.

Абзац про «остаток фазы 6 — снятие моста модулей» удалён 2026-09-08, а не
унесён в архив: он описывал состояние, которого больше нет (У-31). Моста нет
с 2026-09-07 — проверено поиском: файла `vault_module_bridge.js` не
существует, вызовов моста в рантайме ноль. В архиве этот абзац продолжал бы
врать.

**Остальное решённое и не начатое — раздел 28у выше**, и оно перечислено там,
а не здесь: этот раздел про работу, о которой заказчик не просил, а тот — про
ту, которую он назначил.
