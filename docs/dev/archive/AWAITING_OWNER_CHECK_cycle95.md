# Приняты его словом в цикле 96 (тесты 3–6 цикла 95)

Тест 3 — его ответ интервью «было off, переключил — всё ок»; тесты 4, 5, 6 — галочки.

### 372у. Плавающая кнопка не рисуется на обработанной строке — **сделано 2026-09-25, ночь** (его пункт в «Новое пишите сюда»)

На строке с меткой `Mark the line as done` (`#processed`) кнопки `Inline to note` нет; метку узнаёт `lineHasProcessedToken`, то же, что у подсветки. Команда и хоткей остаются. Закреплено `source_marks_tests.ts` с отрицательным контролем `#processed-later` и мутацией.

### 371у. `В-229`: смена дочернего Value снимает Field, который его ждал — **сделано 2026-09-25, ночь**

Field, у которого в `Prerequisite Value` названо дочернее Value, снимается со строки, когда дочернее Value сменилось; у навигатора смена ребёнка снимает всех, кто ждёт родителя, как смена родителя. Панель и команда — одним домом `clearDependentSelections`; дочерняя ветка команды его не звала. Исключение к З3 № 154.

### 368у. `YAML of navigator values` только при `Parent is Navigator` — **было сделано в цикле 94** (его пункт в «Новое пишите сюда»)

Ряд уже рисуется только при `On`, панель перерисовывается целиком, сторож — `fields_editor_view_tests.ts`. Тест заведён, чтобы он сказал, где видел иначе.

### 366у. `YAML of navigator values` — **сделано 2026-09-25, вечер** (`В-224`, `В-227`, тест 5 цикла 93)

Ряд в разделе `YAML property` у Field, чьи Values — навигатор (при `Parent is Navigator` = `Off` ряда нет). Дом настройки — карта `order.yamlNavigator[<ключ>_sub]`, перенос на поле в `subPermissions` (исключение к З3 № 153). `On` — навигатор идёт в свойство родителя перед ребёнком (`type: [#doing, #review]`), у ссылки — ссылкой; `Off` — только ребёнок. В прототип внесён и ряд `Parent is Navigator`, которого там не было с цикла 92.
