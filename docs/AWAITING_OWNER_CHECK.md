# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Реестр на 2026-09-12, ночь, сессия 41.** Заказчик принёс два замечания под
`S15` и дописал в лист четыре новых пункта. Галочек этой сессией не было,
и в архив ничего не уехало.

**Сделано и ждёт его галочки — четыре строки:** `S20` (панель узнаёт
строку, которую плагин написал сам), `S21` (строка без знака списка),
`S22` (курсор при прыжке), `S23` (разделитель у полосы панели) — разделы
102у…106у. И по-прежнему ждут `S15`, `F3` и `F4`.

**Правки нет у одного его замечания — `Ctrl+Z` после панели** (раздел 104у).
Разбор сделан, три формы записи измерены, решение он принял — панель
переедет на накладку поверх строки, но **после его четырёх новых пунктов**.

**Открытых вопросов нет.**

**Шаг третий снятия служебного файла правил открыт, но сдвинут** его словом
2026-09-12: сначала его четыре новых пункта.

**Выкладка наружу остановлена его словом** 2026-09-12 (В-106): «не коммить в gh —
сначала нужно закончить доработку». Работа копится локальными коммитами; шаг 4
завершения сессии не выполняется.

**Лист приёмки держится в форме, которую он попросил** 2026-09-11: только промпт
новой сессии и строки с чекбоксами. Всё, что рассказывается словами, живёт
здесь.

**Одно его уточнение стоит держать на виду:** маркер, который панель TagWheel
оставляет на строке, обязан соответствовать **префиксу этой строки**, а не быть
литеральным `- `. У него для тега `#todo` префикс `- [ ] `, и панель обязана
оставлять именно его. Так и работает; закреплено утверждением.

### Разделы 77у, 78у, 79у, 80у, 82у, 89у, 90у, 91у, 92у, 94у, 95у, 96у, 99у, 100у — **подтверждены заказчиком 2026-09-12**

Строки `S3`, `S4`, `S5`, `S11`, `S12`, `S13`, `S14`, `S16`, `S17`, `S18` и `S19`
закрыты галочками. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions39_40.md`.

### Разделы 37у, 39у, 40у — **подтверждены заказчиком 2026-09-08, вечер**

Четыре решения про каталог Obsidian (`K1`, `K2`), `Del` на пустой строке
(`S1`) и тумблер `Step out of the word` (`S2`) — все четыре строки закрыты
галочками, замечаний под ними нет. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 108у. Тег без своего цвета: пузырь рисует плагин, цвет берётся у темы — **сделано 2026-09-12, ночь**

**Строка в листе — `S25`.** Ваша первая запись из блока «баги»: «теги, у которых стоит дефолтный fill и text, не подчиняются настройкам tag-appearance (высота и ширина пузырька не изменилась и т.д.)».

**Вы правы, и причина простая: пузырька у такого тега не было нашего вовсе.** Тегу, которому вы задали цвет, пузырёк рисует плагин — своим узлом, и все величины живут в нём. Тегу без цвета пузырёк рисует сама тема по своим правилам, а наши настройки в её узел не попадают ни при каком порядке. Отсюда ровно то, что вы видели: размер текста на такой тег действовал (он приезжает отрезком стиля), а высота, ширина и скругление — нет.

**Решение выбрали вы, и условие ваше же.** «Пусть его рисует плагин, цвет из темы» — с оговоркой «меня не устраивает, что по такому тегу нельзя кликнуть, это недопустимо». Поэтому сделано два дела сразу: тег в Block теперь рисует плагин, а щелчок по **любому** нашему пузырю тега открывает поиск по этому тегу. Раньше щелчок не работал ни у одного пузыря, включая цветные.

**Как открывается поиск — взято у платформы, а не придумано** (правило 10). По клику Obsidian берёт токен из синтаксического дерева и для тега зовёт `internalPlugins.getEnabledPluginById("global-search").openGlobalSearch("tag:" + текст)` — это его код, версия 1.13.7. Тот же вызов теперь делает и пузырь. Если поиск у вас выключен, щелчок просто ничего не делает — так же, как и у самого Obsidian.

**Что изменится на вид, и я говорю это прямо.** При умолчаниях наш пузырёк чуть плотнее того, что рисует тема: у темы поля 0.25em и 0.65em, у нас 3 и 6 точек. То есть теги без цвета станут выглядеть так же, как ваши цветные, — и тем и другим теперь правят одни ползунки. Цвет и фон при этом берутся у темы, так что цветовая гамма не меняется, а при наведении пузырёк подсвечивается, как подсвечивается тег.

**Чего правка не трогает.** Ссылка `[[Note]]` и эмодзи-элемент пузыря не получают: заменить их своим узлом значит забрать у ссылки клик, наведение и перетаскивание, а поиск по тегу им не замена. И в **вашем** тексте между разделителями не рисуется ничего, как и раньше.

**Чем закреплено.** В наборе: узел тега без цвета обязан нести класс темы и класс щелчка, а сам щелчок — открыть поиск запросом `tag:#todo` (подделан реестр плагинов Obsidian, вызов взят из его кода); ссылке ни то, ни другое не достаётся. В браузере: тег без цвета в Block обязан получить наш пузырь, расти от шкалы, иметь непрозрачный фон и указатель ссылки, а такой же тег между разделителями — не получить ничего. Три подмены краснеют: пузыря нет вовсе, пузырь везде, цвет темы потерян. Мутацией проверен и щелчок — снятый вызов роняет проверку. Семь шагов зелёные, 69 проверок из 69, подмен в браузерном гейте 28.

**Файл под З3 не тронут ни один:** правка в `src/ui/editor/decorations.js`, `src/core/editor_visuals_config.js` и `styles.css`.

### 107у. Размеры `Inline appearance` кончаются на границе Block — **сделано 2026-09-12, ночь**

**Строка в листе — `S24`.** Ваша вторая запись из блока «баги»: «tags-text-size меняет высоту не только left и right blocks, но и тегов между сепараторами (кроме тегов с дефолтным fill и text) — то, что внутри сепараторов, изменяться от этой опции не должно».

**Вы правы, и это воспроизвелось числами с первой пробы.** Стенд на настоящем CodeMirror с вашими разделителями (`||` и `::`): тег `#work`, стоящий в вашем тексте между разделителями, при `Text size` = 140 получал кегль 20 точек вместо 14 и вырастал с 22.8 до 30 точек по высоте — ровно как теги в блоках.

**Причина — правило, объявленное дважды, и знала его одна половина.** Про зону спрашивала только отрисовка отрезком (`buildBlockStyleCss`): для середины строки она не даёт ничего, потому и тег без своего цвета там не менялся — это ваше «кроме тегов с дефолтным fill и text». А пузырю (`TagVisualTokenWidget`) размеры передавались **безусловно**, какая бы зона ни была. Теперь правило объявлено один раз — `tagVisualSizingForZone`, — и обе отрисовки спрашивают его.

**Что попало под правило, и это шире одной вашей настройки.** Размеры — `Text size`, `Tags bubble width`, `Tag bubble height` и `Empty tags bubble width` — кончаются на границе Block. **Форма — нет:** `Tags bubble corners` приезжает к пузырю в любой зоне, как и цвет. Причина названа прямо: круглый пузырь посреди квадратных читался бы как дефект, а про форму вы ничего не писали. Скажете иначе — поменяю одной строкой.

**Чем закреплено.** Браузерный гейт, пункт 10: на странице теперь есть строка, где цветной тег стоит в тексте человека, и проверка меряет пузыри по зонам, спрашивая зону у того же объявления, каким её считает продукт. Две подмены краснеют: `middle-takes-sizing` возвращает прежний код, `block-loses-sizing` ломает правило в обратную сторону — без неё утверждение «в середине не выросло» было бы зелёным и у слоя, который не рисует размеров нигде. В наборе закреплено само объявление (`tag_visual_render_tests.ts`). Семь шагов зелёные, 69 проверок из 69, подмен в браузерном гейте 25.

**Файл под З3 не тронут ни один:** правка в `src/core/editor_visuals_config.js` и `src/ui/editor/decorations.js`.

### 58у. Служебный файл правил сокращён на 28 тысяч знаков — **сделано 2026-09-10**

**Строки в листе у этого пункта нет, и это правильно** (У-84): проверить его
нажатием нельзя, на экране не меняется ничего. Отчёт.

**Чего это стоило до правки.** `CLAUDE.md` — файл, который читается **целиком**
перед первым действием каждой сессии, — вырос до 139 720 байт, и четверть
объёма занимали истории тридцати пяти исключений к запрету З3: у каждого дата,
все места, цена и чем закреплено. Первые двадцать две истории при этом уже
лежали в PRD 3.3 — то есть у них было два дома, и расходиться им было на чём
(У-32).

**Что сделано.** Истории 23–35 перенесены в PRD 3.3, к первым двадцати двум и в
той же форме — абзац на исключение. В `CLAUDE.md` осталась таблица «номер, дата,
файл, одна строка» и ссылка на PRD. Пять историй из первых двадцати двух
дополнены тем, чего в PRD не было: `buildTags` и `relocateTagLikeByOrder` у
седьмого, уроки У-48…У-52 у четырнадцатого, форма ключа `<ключ родителя>_sub` у
пятнадцатого, `readRulesMarkdownWithFallback` у восемнадцатого и файл у
шестнадцатого.

**Было / стало.** `CLAUDE.md` 139 720 → **112 218** байт (−27 502, минус пятая
часть). Сам переезд снял 28 429; тысяча вернулась уборкой того же дня — правило
58, ссылка на новый урок и абзац про ваше слово на выпуск. PRD 1 168 198 →
1 189 413 (+21 215); `LESSONS.md` 299 727 → 304 710 (+4 983, два новых урока).
Разница между снятым и добавленным — то, что в снятом было пересказом разборов
из 10.13.*.

**Ни одного имени не потеряно, и это измерено, а не обещано.** У снятого куска
собраны 244 якоря — имена файлов, функций, пинов и ссылки вида `У-32`, — и
каждый найден либо в таблице, либо в PRD. Снят нарочно один: номер строки
`tagwheel.js:1769` из истории первого сентября — файл с тех пор менялся, и номер
стал утверждением о состоянии (У-64).

**Сверка объединением оказалась слепой, и это стоило урока** (У-134). Она
отвечает на «имя выжило хоть где-то» и молчит о том, встало ли оно в **свой**
пункт; её положительный контроль не краснел, пока имя снимали из одного файла из
двух. Вторая сверка — по пунктам, файлы каждого против его собственного абзаца —
нашла дыру, которой не видела первая: у шестнадцатого исключения в PRD **не был
назван файл**, он стоял только в снятом списке.

**Заодно снято устаревшее утверждение** (У-135). Абзац «Двадцать шестое
спрашивается у заказчика» простоял четверо суток после того, как двадцать шестое
исключение было разрешено и сделано; рядом с ним в том же файле стоял пункт 26 с
пометкой «разрешено». Теперь там правило, которое не стареет: **следующее**
исключение спрашивается у заказчика, и не раньше полного разбора.

**Чем закреплено.** `tests/regression/z3_exceptions_split_tests.js` — новый пин:
в таблице ровно тридцать пять строк с датой и файлом, у каждого номера свой
абзац в PRD 3.3, файлы строки стоят **в своём** абзаце, а формы снятого списка в
`CLAUDE.md` больше нет. Внутри стоит положительный контроль нарезки PRD (имя
первого исключения не должно находиться в абзаце тридцать пятого) — без него
последний абзац тянулся бы до конца файла и «содержал» бы любое имя. Четыре
мутации краснеют: снятая строка таблицы, подменённый в строке файл,
переименованный абзац PRD и вернувшаяся в `CLAUDE.md` история. Семь шагов
зелёные, 65 проверок из 65.

### 106у. Разделитель у полосы панели виден с обеих сторон — **сделано 2026-09-12, ночь**

**Строка в листе — `S23`.** Второй из ваших четырёх новых пунктов, первая его половина.

**Вы были правы, и причина одна строка.** У правой панели ветка «текста нет» ставила разделитель всегда, а у левой такая же ветка не ставила вовсе. Теперь правило одно: разделитель стоит с той стороны, где остальная строка.

**Вторая половина вашего пункта — режим «не прятать значения противоположного Block» — ещё не сделана.** Это новая строка в панели настроек, и она идёт через прототип: следующим заходом.

**Чем закреплено.** Проверка спрашивает обе стороны и считает разделители: их обязан быть один. Подмена краснеет. Семь шагов зелёные. Исключение 67.

### 105у. Курсор при прыжке: один пробел остаётся слева — **сделано 2026-09-12, ночь**

**Строка в листе — `S22`.** Первый из ваших четырёх новых пунктов.

**Правило написано вашими словами** — «при прыжках не создавать пробел, если его не было изначально», — и потому узкое: пробелов нет или он один — курсор не двигается вовсе; больше одного — курсор встаёт перед последним. Все три ваших примера этим объясняются.

**Правило одно на три хода:** прыжок по заголовкам, шаг вправо внутри строки и курсор после команды спрашивают одну функцию.

**Чем закреплено.** Седьмой случай в блоке проверки курсора: строка с двумя пробелами и рядом обе границы правила. Подмена краснеет. Семь шагов зелёные. Разбор — PRD 10.13.85, исключение 66.

### 104у. `Ctrl+Z` после панели: разбор без правки — **сделано 2026-09-12, ночь**

**Строки в листе у этого пункта нет, и это правильно** (У-84): нажать пока нечего, починка ждёт вашего решения. Вопрос — В-108 в `docs/OPEN_QUESTIONS.md`.

**Ваша запись:** «в процессе тестирования нажимал ctrl+z — за несколько нажатий в строке получил …».

**Воспроизвелось.** Завёл стенд `node tools/undo_bench.js`: настоящая история отмен редактора, ваши настройки, настоящие команды и настоящая панель. Последовательность «команда поля, ещё одна, потом панель» даёт со второго `Ctrl+Z` строку, которой никогда не было.

**Причина, словами.** Пока панель открыта, она держит свою полосу в самом тексте заметки, а значит на время убирает то, что на строке стояло: значения противоположного Block она прячет, своего — заменяет списком полей. Шаг отмены, который это убранное когда-то написал, теряет своё место — и при отмене текст встаёт не туда.

**Почему набор этого не видел.** Проверка на `Ctrl+Z` есть и мера у неё верная, но в её примере человек только печатает. Шага, написанного командой поля, там нет вовсе — а панель прячет ровно его текст.

**Почему не починил сам.** Все три формы починки меняют то, как панель выглядит на экране, а это ваше решение, не моё. Измерил три и назвал результат каждой, включая ту, которая починкой не является. Разбор — PRD 10.13.84.

### 103у. Строка без знака списка: те самые пять разделителей — **сделано 2026-09-12, ночь**

**Строка в листе — `S21`.** Этого вы не присылали отдельно — это всё тот же «что вообще за артефакты», только другой дорогой, и он оставался в плагине после прошлой починки.

**Скажу прямо, почему прошлый обход его не нашёл.** Все три исходные строки в нём либо пустые, либо начинаются с `- `. А вы встаёте на пустую строку, печатаете слово и только потом жмёте команду — знака списка на ней в этот момент ещё нет. Именно этого случая в обходе и не было. Теперь есть: было 27 сочетаний, стало 36.

**Что это давало.** На строке `1231` команда поля правого Block давала `- 1231 :: :: 👤111`, а панель — `- 1231 :: :: :: :: :: 🤣…`. Ваше слово считалось зоной значений, а не текстом, и к ней каждый круг дописывался ещё один разделитель.

**Знак списка решает настройка, а не разбор.** Условие стояло из опасения, что строка без списка получит список. Я проверил это прямо, на обеих сторонах тумблера `Strict: add a bullet`: при выключенном знак не появляется, при включённом появляется.

**И одна моя ошибка по дороге, которую стоит назвать.** Первая версия правки была из двух кусков, и поломка показала, что второй кусок не меняет ничего. Снял его: в плагин уехало ровно то, что работает.

**Чем закреплено.** Новая проверка: один разделитель обеими дорогами, ваш текст на месте, строки равны; второй половиной — выключенный тумблер. Подмена краснеет и на проверке, и на обходе. Семь шагов зелёные. Разбор — PRD 10.13.83, исключение 65.

### 102у. Панель узнаёт то, что плагин написал сам — **сделано 2026-09-12, ночь**

**Строка в листе — `S20`.** Ваши слова: «был баг, когда в right block были выбраны элементы, но при активации tagwheel right они не распознались как выбранные values, а отображались как текст».

**Воспроизвелось с первой пробы, и строка для пробы нашлась сама.** Команда поля правого Block на пустой строке пишет `-  :: 👤111` — первого разделителя в такой строке нет, и правый Block отделяется вторым. Открываем на ней TagWheel right — панель не узнаёт ни одного значения, а сверху дописывает второй разделитель.

**Причина старая и знакомая: правило было объявлено дважды.** Где в строке проходят границы зон, считал общий разбор — и своя копия внутри TagWheel. Ветку «первого разделителя нет, второй есть» общий разбор получил 11 и 12 сентября, копия — нет. Именно об этом урок У-150: починка одного объявления из двух делает результат хуже — команда значение узнавала, панель нет.

**Разбирал обходом, а не случаем.** Каждый ваш Field пишется своей командой, и на получившейся строке открывается панель того же Block. Вопрос один: узнала ли она значение, которое плагин только что написал. Из девяти ваших Fields не узнавались все три поля правого Block, на каждой исходной строке; шесть левых узнавались. После правки не узнанных ноль.

**И скажу прямо: набор этого не видел ни дня.** Строку панель не портит — `Esc` возвращает исходную, — а все проверки спрашивали строку. Я проверил это поломкой: с прежним разбором весь набор остаётся зелёным. Теперь спрашивается не строка, а состав выбранного.

**Заодно снято мёртвое.** Три копии правила «что в этом куске правый груз» звал только разбор; после правки их не звал никто, и в сборке их больше нет.

**Чем закреплено.** Новая проверка в `tests/regression/status_runtime_behavior_tests.js`: строку пишет команда поля правого Block, панель открывается на написанном, спрашивается состав выбранного. Контроль — «команда и правда написала». Мутация возврата прежнего разбора краснеет. Семь шагов зелёные, обход строки — 27 сочетаний, расходится 0. Разбор — PRD 10.13.82, исключение 64.

### 101у. Поведение строки разобрано целиком, три дефекта — **сделано 2026-09-12, ночь**

**Строка в листе — `S15`.** Ваши слова: «в пустой строке активировал Random,
получил `:: :: :: :: :: 🤣ZdK1x-`, ожидал `-  :: 🤣ZdK1x-` — это что вообще за
артефакты? Полностью проанализируй поведение строки, мне надоело, что постоянно
ломается».

**Сделал не разбор случая, а обход.** Разбирать по одному тут бесполезно: у
каждого вашего замечания свой Field, свой Block и своя исходная строка, а
расходятся дороги там, где сходятся правила. Завёл сплошной обход — каждый ваш
Field, обеими дорогами, на трёх исходных строках: **двадцать семь сочетаний**.
По каждому спрашивается, что написала команда, что написала панель и — главное —
**переживает ли получившаяся строка собственный разбор**.

**Это последнее и оказалось тем, чего не хватало.** Доводки строки устроены как
«разобрать — поправить — собрать» и зовутся по нескольку раз подряд. Строка,
которую плагин написал, но прочесть не может, растёт с каждым кругом — отсюда и
пять разделителей. Это не пять ошибок, а одна, повторённая пять раз.

**Нашлось три дефекта, и третий вы не называли.**

1. **Строка, которую плагин пишет, а прочесть не может.** У разбора не было ветки
   для строки, где заполнен только правый Block: он читал `:: 👤111` как левый
   сегмент вместе с разделителем, и следующая доводка дописывала ещё один.
2. **Пустой слот текста исчезал при склейке префикса.** Два пробела между знаком
   списка и разделителем — это место, куда встанет слово. Один шаг снимал знак
   списка вместе со **всеми** пробелами за ним, другой приклеивал его обратно
   одним. Отсюда `- :: 👤111` вместо `-  :: 👤111` — ровно то, что вы написали в
   ожидании.
3. **Предусловие Field спрашивала одна дорога из трёх.** Ваш `Project` ждёт
   `Category = #work`. Панель его на пустой строке не показывает — а команда
   `Project next` значение ставила. Правило записано у меня же в разборе, слово в
   слово: Field с предусловием не показывается «ни в TagWheel, ни в своих
   командах». То есть правило было записано и на живом пути команд не спрошено ни
   разу.

**Что теперь.** Двадцать семь сочетаний из двадцати семи: команда и панель дают
**одну и ту же строку**, и каждая строка переживает свой разбор. Строка, где
Field ждёт предусловия, — согласие, а не расхождение: панель его не показывает,
команда отказывается и **говорит почему**.

**Как проверить, за три минуты.** Перенесите любой Field в `Right Block`, встаньте
на пустую строку и поставьте его значение — сперва командой, потом панелью на
другой пустой строке. Обе строки обязаны получиться **одинаковыми**, и в них
обязан быть один разделитель и пустой слот: `-  :: 🤣…`. Раньше панель давала
пять разделителей, а команда съедала пробел.

**И второе, за минуту.** На пустой строке нажмите `Project next`: команда обязана
**отказаться** и сказать, что `Project` ждёт `Category`. Поставьте `#work` и
нажмите снова — теперь обязана сработать. Панель ведёт себя так же и вела всегда.

**Чем закреплено:** три проверки в наборе, и каждая с контролем внутри; три
подмены краснеют. Фикстура догнала ваши формы — у `Project` в ней появилось
предусловие, как у вас: без этого отличия третья проверка была бы зелёной и на
сломанном коде. Сам обход лежит в репозитории и запускается руками:
`node tools/line_matrix.js` — в набор он не входит, потому что читает ваш
`data.json`, а набор не имеет права зависеть от приватного снимка настроек.
**Семь шагов зелёные**, 69 проверок из 69.

### 98у. Команды писали гринвичское время вместо вашего — **сделано 2026-09-12, ночь**

**Строка в листе — `S15`.** Ваши слова: «при `command due previous` время не
исчезло, когда оно стало ниже текущего — в tagwheel, когда делаешь `previous`
относительно первого значения, field становится пустым».

**Найденное оказалось не про шаг вниз, и это важнее самого замечания.** Часы
машины в момент прогона показывали `17:05`. Команда `Due next` на пустой строке
писала `📅2026-09-12 **14:05**`, панель TagWheel в то же мгновение —
`📅2026-09-12 **17:05**`. Расходились не правила шага, а само время, и ровно на
три часа — на смещение вашего часового пояса. То есть всякий раз, когда вы
ставили дату **командой**, в заметку уезжало гринвичское время.

**Почему при этом не очищалось поле.** Шаг вниз очищает значение, когда оно
дошло до нуля отсчёта, а ноль — это «сейчас». Значение, поставленное панелью,
стояло на три часа выше того нуля, от которого считает команда: вы видели, как
время проходит мимо текущего и продолжает убывать. Теперь обе дороги считают от
одного и того же «сейчас», и шаг вниз очищает поле одинаково.

**Причина названа местом.** В общем модуле дат значение разбирается и печатается
в одном договоре: `Date` там несёт не момент, а настенные часы. Ветка «день и
крупнее» договор соблюдала, ветка **времени** отдавала настоящий момент, и
печать сдвигала его на пояс. У панели свой разбор форматов, целиком на местных
часах — то же правило, объявленное второй раз и иначе.

**Проверьте, пожалуйста, именно время.** На любой строке нажмите `Due next` и
сравните с часами в углу экрана: должно совпасть до минуты. Раньше не совпадало,
и вы могли этого не заметить — в вашем примере из письма стоит `13:40`.

**Чем закреплено:** проверка **сама задаёт часовой пояс** (`Asia/Tokyo`) и
спрашивает обе дороги; рядом контроль «пояс и правда сменился». Без этого она
была бы слепа: на машине, живущей по Гринвичу, обе стороны совпадают сами собой
— и CI как раз такая машина, поэтому дефект и дожил до сих пор. Подмена «вернуть
прежнее» краснеет на девять часов. **Семь шагов зелёные**, 69 проверок из 69.

### 97у. Буллит на пустой строке: панель и команда отвечали по-разному — **сделано 2026-09-12, ночь**

**Строка в листе — `S15`.** Ваши слова: «при активации в пустой строке `command
due next` я получил `📅2026-09-12 13:40 || `, а должен был `- 📅2026-09-12 13:40
|| `. При активации через tagwheel буллит появился. Мне не нравится, что
расходится поведение строки у tagwheel и `commands field next/previous` — оно
должно быть идентичным и определяться settings».

**Воспроизведено на вашем `data.json` до символа.** Стенд собирает настройки
движку той же дорогой, что и плагин, и зовёт те же определения команд. При
выключенном тумблере `Strict: add a bullet` команда давала `📅… || `, панель —
`- 📅… || `. При включённом обе давали строку с буллитом.

**Причина.** Настройку спрашивают обе дороги, и спрашивает её одна общая
функция. Но у панели рядом стояло **второе объявление того же правила**:
признак «префикс сохранить» был записан литеральным `true`, и вычисление уходило
в эту ветку раньше, чем настройка была спрошена. У шага по тегу тот же признак
**считается** — его просят только поля режима `minimal`. Теперь считается и у
панели. Это ровно тот класс, которым объяснились пять из шести ваших замечаний
днём раньше (У-150).

**Что вы увидите.** У вас тумблер `Tags & PKM → Placement modes → Strict: add a
bullet` **выключен**, и теперь обе дороги дают строку **без** буллита. Включите
— обе дадут с буллитом. Расхождения между панелью и командой больше нет ни при
одном положении тумблера; какое из положений вам нужно, решаете вы.

**Чем закреплено:** проверка спрашивает обе дороги на **обеих** сторонах
тумблера, и вторая сторона — положительный контроль: без неё «буллита нет ни у
кого» было бы зелено и у плагина, который буллит не ставит никогда. Подмена
«вернуть литерал» краснеет; вторая подмена, вернувшая рядом стоявшее запасное
`'-'`, **не краснеет** — и это сказано в самом коде: та строка была второй
записью того же ответа, а не причиной. **Семь шагов зелёные**, 69 проверок из
69.

### 93у. Шаг по дате уносил ваши значения и не спрашивал настройку — **сделано 2026-09-12, вечер**

**Строка в листе — `S15`.** Ваши слова: «первое применение `Due next` —
технические поля улетели за сепаратор; второе — улетели в right block». И
рядом: «на пустой строке без префикса получил строку без буллита, а с учётом
настроек должен был с буллитом». Оба случая воспроизведены на вашем `data.json`
до буквы.

**Первое: кто объявил ваши теги прозой.** Доводка строки спрашивает «что здесь
текст человека», и у этого вопроса было **два** ответа. Зрелый снимает
объявленные значения по всему телу строки и знает, где кончается значение с
пробелом в формате. Второй — свой обход в том же файле — снимал токены только
**с начала** и останавливался на первом незнакомом: всё, что стояло за датой,
объявлялось вашей прозой и уезжало в слот текста, а следующим нажатием — в
правый Block. Второе объявление снято, ответ один.

**Второе: настройка `Strict: add a bullet`.** Шаг по тегу спрашивает её всегда,
панель тоже, а шаг по элементу не спрашивал вовсе — и буллит появлялся от
одного способа и не появлялся от другого. Теперь правило спрашивают все трое.

**Про ваш случай скажу прямо.** У вас этот тумблер **выключен**, и при
выключенном строка без буллита — верное поведение: «плоская строка остаётся
как есть». Включите его, если хотите буллит; проверено обоими способами, они
теперь согласны.

**Чем закреплено:** две проверки — «шаг по дате не уносит значения за
разделитель и не заводит правый Block» и «шаг по элементу спрашивает ту же
настройку, что шаг по тегу» (у второй контроль стоит внутри: сначала
проверяется, что шаг по тегу буллит ставит). Обе краснеют на возвращённом коде.
**Семь шагов зелёные**, 69 проверок из 69.

### Разделы 83у и 86у — **подтверждены заказчиком 2026-09-12**

Закрыты галочками `S6` и `S8`; отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_session38.md`.

### Разделы 85у, 87у, 88у — **подтверждены заказчиком 2026-09-12**

Закрыты галочками `S7` (хвост прошлого значения), `S9` (повтор после разделителя
и значение, которое не убиралось) и `S10` (панель по Order). Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_session38.md`.

**Под `S7` он оставил замечание про артефакт** — блок текста после разделителя
при открытой панели. Оно снято на его же примере: строка воспроизведена символ в
символ, повтора нет. Замечание было написано по сборке, которая стояла у него до
правки того вечера.

### 84у. Кто теряет вторую половину значения элемента — **разобрано 2026-09-12**

**Строки в листе нет, и это правильно** (У-84): нажать тут нечего, правка не
сделана. Разбор целиком — PRD 10.13.71.

**Ваш ряд объяснён до конца, и объяснён измерением.** Я воспроизвёл его на
вашем `data.json` и провёл трассировку. Один шаг выглядит так:

- перенос и уборка работают **правильно**: новое значение встаёт слева, старое
  снимается целиком;
- следом идёт уборка «вашего текста». Слот текста пуст, поэтому она идёт искать
  текст в левый блок — и снимает оттуда только то, что узнаёт. Узнаёт она метку
  плюс **одно слово**, а ваш формат `YYYY-MM-DD hh:mm` занимает два. Хвост
  `21:32` она объявляет вашим текстом;
- доводка честно возвращает «ваш текст» на место и, увидев текст, уносит
  элемент в правый блок. Отсюда и прыжок между блоками.

**Виновник — рукописный образец значения.** Функция, которая говорит, чем
записано значение элемента, **формат поля не спрашивает вовсе**: у неё нет
аргументов, а внутри стоит написанная руками пара «дата» и «время».

**Прежде чем чинить, я посчитал, сколько раз объявлено это правило** — так
велит правило, купленное вашим же замечанием прошлой сессии. Объявлений
**девять**, и каждое я опросил на вашем значении: три выводят образец из
формата и отвечают верно, четыре отвечают «половина», одно угадывает. Починка
одного из них сделала бы результат хуже, а не лучше — ровно это случилось в
прошлый раз.

**Что даёт починка, я измерил подменой, а не обещаю.** С подменой ваш ряд
становится чистым: одно значение, никаких хвостов, элемент по блокам не прыгает.
**И сразу видно, чего она не даёт:** элемент остаётся справа, хотя по вашему
порядку он слева. Это отдельный дефект, и выводить его починку из этого разбора
нельзя.

**Заодно нашлось то, о чём вы не писали.** Если вы когда-нибудь меняли формат
поля, хвост старого значения не убирается **вообще** — ни слева, ни справа, ни
при вашем тексте в строке. Условия там шире вашего случая, и чинится это тем же
заходом.

**Вы ответили интервью «починить целиком», и правка сделана** — отчёт по ней
в разделе 85у.

### 81у. Панель TagWheel по Order: разбор, и он оказался неверен — **2026-09-12**

**Мой разбор не выдержал прогона, и я это записал, а не переписал задним
числом.** Подробности — PRD 10.13.69, подраздел Т-7.

Я утверждал: панель берёт список полей по типу, поэтому элемент, переставленный
в левый Block, «не появится в ней никогда». Прогон показал обратное: элемент в
панели **есть и стоит первым**, а `panel: "left"` проставляется полю ровно по
вашему порядку. Панель уже строится по Order — этим занимается функция, до
которой я не дочитал.

Правку, основанную на неверном разборе, я успел сделать и **откатил**: первая же
проверка набора покраснела на случае, который и без неё работал. В продукте её
нет.

Про ведущее поле тоже не подтвердилось: три прогона с разным значением дали либо
первое поле порядка, либо то, что записано. «Второе вместо первого» не
воспроизвелось ни разу.

**Значит ваш случай я не воспроизвёл, и называть причину сейчас не буду** — это
была бы третья догадка подряд. Нужны ваши данные.

**Что из разбора осталось верным:** списки полей в правилах действительно
разложены по типу, а не по Block; и контрола для ведущего поля в панели
действительно нет — функция, которая его пишет, не зовётся ниоткуда. Ваше
решение про `Active field on opening` остаётся в силе независимо от того, чем
окажется ваш случай.

### Разделы 72у, 75у, 76у — **подтверждены заказчиком 2026-09-12**

Закрыты галочками `G1` и `S2`; отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_session37.md`.

### 74у. Ревизия кода, заход седьмой, и решение по шву TagWheel — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего. Отчёт — PRD 15.7,
подразделы «Заход 7» и «`tagwheel.js`: шов внутри главного входа».

**Заход 7: три правила плагина держались ни на чём.** Я выворачивал правила
наизнанку прямо в плагине и смотрел, заметит ли это набор проверок. Из пяти
подмен **три прошли незамеченными**: можно поменять местами разделители, можно
перестать читать тип, который человек задал Field, и можно перестать читать тип
из порядка — и все 67 проверок остались бы зелёными.

**Код при этом был верным. Слеп был пример.** Во всех проверках первый
разделитель равнялся второму — менять местами нечего, — а тип, заданный Field,
совпадал с типом из порядка: когда две стороны согласны, не видно, какую из них
читают.

**Сделано:** одна проверка, где стороны спорят нарочно. Все пять подмен теперь
краснеют.

**Решение по шву в TagWheel — не трогать, и вот почему.** В списке оставшихся
работ стояла строка «четыре модуля берутся с глобального шва, хотя два из них
подключены прямо». Разбор показал обратное ожидаемому: шов там **держит набор
проверок** — через него проверки заводят движок и нарочно снимают модуль, чтобы
прогнать путь «модуль не приехал». Цена правки — исключение к запрету на правку
движков, переписанные проверки и **потеря** этого пути; выгода — только
читаемость. Строка снята с названной причиной.

**Ревизия кода закрыта семью заходами.** Что осталось из технической работы —
одна строка в `docs/REMAINING_WORK.md`, и та про ваше решение.

**На экране не меняется ничего.**

### 73у. Ревизия кода, заход шестой: фикстуры, которые не могут увидеть дефект — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего. Отчёт целиком —
PRD 15.7, подраздел «Заход 6».

**Что спрашивалось.** Проверка бывает зелёной оттого, что её пример физически
не может показать разницу. Такую слепоту чтением не видно: пример выглядит
полным.

**Как искалось.** Не чтением, а **порчей самого плагина**: я заменял в нём имя
Field на его внутренний ключ в пяти местах и смотрел, заметит ли это набор
проверок. **Четырёх из пяти он не заметил.**

**Что за этим стояло — три разные вещи, и это важнее числа:**

- **набор имён панели TagWheel проверки составляли руками**, а в плагине его
  собирает своя функция — и она не выполнялась **ни одним** прогоном. То есть
  сломайся она, панель перестала бы узнавать себя в заметке, а все проверки
  остались бы зелёными;
- **имя Field, уезжающее движку**, никто не сверял: функция выполнялась
  четырьмя файлами, а на её ответ не смотрел никто;
- **в предпросмотре Transform имя Field читалось и тут же выбрасывалось** —
  мёртвое вычисление, которое линтер показать не может по устройству. Снято.

**Что сделано.** Набор имён теперь строит сама функция плагина, из настроек;
заведена новая проверка на имя Field, уезжающее движку; мёртвые строки сняты.
Слепых мест осталось **одно из пяти**, и оно названо прямо в проверке: до ветки
Field типа ссылка фикстура не доходит.

**Мои две ошибки по ходу поймали проверки, а не чтение:** первая версия нового
примера обходила нормализацию настроек — то есть я написал ровно такую слепую
фикстуру, за какой охотился, — а одно утверждение оказалось догадкой о замысле
вместо чтения плагина.

**На экране не меняется ничего.**

### 71у. Ревизия кода, заход пятый: числа документов против измеренных — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего. Отчёт целиком —
PRD 15.7, подраздел «Заход 5».

**Что спрашивалось.** Число в документе — такое же утверждение, как строка
кода, и стареет оно тише всего: документ читается каждую сессию и выглядит
правдой.

**Что померено.** 22 живых документа, 5621 предложение с числом, из них 308 —
утверждения о **нынешнем** состоянии. Архив не проверялся: он описывает
прошлое, и числа там верны как история.

**Нашлось семь расхождений.** Самое поучительное: каталог Obsidian говорил
«`console.error` — 78», прогон дал 89. Выросло это число **от хорошей работы** —
третий заход ревизии заменил тихие отказы громкими. Значит вписывать его в
документ бессмысленно по устройству.

**Поэтому чинилось не подстановкой свежего числа.** Числа переехали в прогон:
`catalog_rules_tests.ts` печатает перепись записей в консоль, `var` пофайлово и
размер `styles.css`; браузерный гейт печатает число подмен. В документах
остались ссылки на команду.

**Сошлось и правки не потребовало** — 144 урока, 66 правил, 42 правила
каталога, 43 исключения к З3, 9 проверок прототипа, ноль `innerHTML`, ноль
`var` вне движков, 105 закрытых вопросов, диапазоны ползунков в `README.md` и
`instructions.md` против схемы.

**Мой замер соврал дважды**, и оба раза поймал контроль на известном ответе:
счёт по сырому тексту насчитал три `innerHTML` там, где их ноль (все три — в
объяснениях), а неверный список движков объявил 46 `var` вне З3 при зелёном
линтере.

**На экране не меняется ничего.**

### 70у. Ревизия кода, заход четвёртый: запреты без образца — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего, на экране ничего не
меняется. Отчёт целиком — PRD 15.7, подраздел «Заход 4».

**Что спрашивалось.** Из десяти находок первых трёх заходов три были в самих
проверках, и все три одного рода: проверка была зелёной, потому что искать
стало нечем. Заход спросил это у всего набора разом.

**Что померено.** В наборе 4476 мест утверждений, из них **642 запрета** — тех,
что истинны от отсутствия предмета. У 299 из них форма «список пуст», и у этой
формы есть механический признак годности: рядом должен стоять порог,
доказывающий, что обход вообще что-то нашёл.

**Что нашлось — тринадцать мест в девяти файлах.** Все одного рода: порог стоял
не на том предмете, из которого построен запрет. Самое дорогое — сторож листа
возможностей `FEATURES.md`, написанный накануне: он проверял, что документ
прочитан и не пуст, а спрашивали в нём реестр команд и список вкладок панели.
Опустей любой из них — и «лист называет каждую команду» стало бы правдой,
которую никто не проверял.

**Как отделено настоящее от правдоподобного.** Мутацией: знаменатель
обнуляется, проверка гоняется, файл возвращается копией. Зелёная после
обнуления — находка. Каждая правка проверена тем же способом в обратную
сторону.

**Мой обход соврал дважды, и оба раза поймал контроль, а не чтение** — маска
сдвигалась на эмодзи и съедала куски кода, а regex-литерал со скобкой рвал счёт
скобок. Подробности — в отчёте.

**На экране не меняется ничего.** Это работа над проверками, а не над плагином.

### 69у. Ревизия кода, три захода — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего, на экране ничего не
меняется. Отчёт целиком — PRD 15.7.

**Как она делалась.** Не вычиткой: читать 56 файлов глазами — ровно тот способ,
каким в этом проекте дважды пропускали находки. Заходами по **признаку**
дефекта: у каждого захода свой сплошной обход, у каждого обхода свой контроль, и
каждая находка проверена поимённо до того, как названа.

**Заход 1 — мёртвое, которого линтер не видит.** Экспортированное имя он
считает использованным. Найдено три: функция `runtimeEnglish` с объяснением
«нужен ровно одному месту» при нуле вызовов и два флага беты. Все три сняты, и
теперь их стережёт новая проверка.

**Заход 2 — одно правило, десять объявлений.** Ключ, по которому сообщение ищет
свой перевод, собирался в десяти местах, и над шестью копиями стояла строка
«Строит его одна функция, и её зовут оба конца» — то есть комментарий утверждал
ровно то, чего не было. Цена расхождения, если бы оно случилось: человек видит
английский там, где выбрал свой язык, и молча. Девять копий сняты.

**Заход 3 — что выполняется, когда общий код не приехал.** Из 81 такого места 13
отвечали сами за себя. Пять оказались живыми копиями правил — в том числе там,
где общий модуль был подключён строкой выше и просто не спрашивался, — а два
выдумывали ответ: `свободный ход выключен` и целый объект умолчаний, не
заглядывая в ваши настройки. У того, кто поставил Field режим `full`, он молча
становился `off`. Всё это снято.

**Три находки были в самих проверках**, и это стоит сказать отдельно: список
файлов в стороже начинался с файла, где искомого не осталось; пин требовал,
чтобы снятая копия **была**; а запрет, поставленный тем же утром, видел одну
форму записи из двух. Проверка, которая ничего не проверяет, хуже её
отсутствия — потому что считается сделанной.

**Чего ревизия не покрыла, и это честный список:**

- **сколько запретов в наборе не имеют образца**, на котором они обязаны
  краснеть. Это следующий заход, и он самый крупный;
- **числа в документах против измеренных** — отдельный проход;
- **фикстуры, которые не могут увидеть дефект** (У-47): обходом такое не
  ищется, только чтением по одной;
- **поведение на настоящем Obsidian.** Здесь по-прежнему решает ваш глаз, и
  никакая ревизия этого не заменит.

### 68у. Выпуск `0.1.0-beta.6` — **сделано 2026-09-11**

**Лежит в GitHub:**
`https://github.com/romkuznetsov/inline-overhaul/releases/tag/0.1.0-beta.6`

Не предрелиз, три вложения — BRAT возьмёт именно его. Собран не у меня, а на
сервере, из того коммита, на который указывает тег, и после полного набора
проверок. Все три прогона CI зелёные.

**Что в нём нового против `0.1.0-beta.5`** — всё, что сделано двумя сессиями:

- **31 февраля больше не читается как 3 марта**, пустой идентификатор Field не
  попадает не в тот Field, дочерний Field не теряет место в Order;
- **когда файла правил нет или правила не сходятся**, плагин говорит об этом и
  прекращает работу; если само сообщение показать не удалось, причина уезжает в
  консоль, а не пропадает;
- **долг «плагин молчит, а почему — нигде не сказано» закрыт целиком**: было 69
  мест, стало 0;
- **копии правил в движках сведены** — сначала подстановка текста сообщений,
  теперь разбор форматов даты и числа и поиск активного редактора;
- **мёртвого кода на 23 объявления меньше**, и правила каталога Obsidian
  сходятся все;
- **лист возможностей** `FEATURES.md` — новый документ, ссылка первой строкой
  README.

**На экране почти ничего из этого не видно, и так и задумано.** Проверять здесь
нечего нажатием — кроме одного: поставьте обновление через BRAT и убедитесь, что
плагин включается и работает так же, как работал. Это единственная строка, по
которой у вас может быть замечание.

### 67у. Лист возможностей — **сделано 2026-09-11**

**Это первая строка за сессию, которую вы и правда можете открыть и прочитать.**
Файл — `FEATURES.md` в корне репозитория, по-английски, и ссылка на него стоит
первой строкой README.

**Что в нём.** Что плагин умеет, словами и по делу: Fields и их Values, TagWheel,
навигация, оформление строки, Transform со Smart Rules, три вещи с клавиатуры
(`Ctrl+A`, Smart Delete, Binder), модули, язык, копии настроек. Полная таблица
всех тринадцати постоянных команд и отдельно — те, что заводятся из ваших Fields,
строк Binder и модулей. В конце раздел «чего он не делает»: только рабочий стол,
перевода в поставке нет, это бета, никакой синхронизации и никакой телеметрии.

**Как проверить, за пять минут:** открыть `FEATURES.md` и посмотреть, не обещает
ли он того, чего нет, и не молчит ли о том, что для вас важно. Это тот же вопрос,
что по `F3`, но документ новый и короткий.

**Почему он не устареет молча.** Собран он не по памяти, а обходом продукта:
команды взяты из реестра команд, области — из схемы панели. И у него стоит
сторож: сняли команду или добавили новую, а лист не поправили — проверка
краснеет. То же и в обратную сторону: назвали контрол, которого в панели больше
нет, — тоже краснеет. Обе стороны проверены нарочной поломкой.

**Зачем он нужен, кроме вас:** это половина описания для каталога Obsidian и
основа любого разговора о деньгах — без внятного списка умений не продать ни
лицензию, ни идею.

### 66у. `var` вне движков — ноль, и новых не завести — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): это правило каталога Obsidian о том,
как написан код, а не о том, что видно на экране.

**Что сделано.** Каталог требует `const` и `let` вместо `var`. Вне защищённых
движков таких объявлений было 75 — все переведены, и правка сверена машиной:
строк в файлах столько же, и каждая изменённая отличается **только** ключевым
словом. Правило поднято до ошибки линтера, то есть новых `var` в проекте больше
не завести.

**Чего сделано не было, и почему.** В движках под З3 осталось 1389 объявлений, и
переписывать их я не стал. Три причины, и ни одна не про лень: принятое правило
Р4 прямо говорит, что существующий код движков не переписывается; поведение этих
файлов снято проверками не целиком, а цена ошибки там — молчаливая потеря вашего
текста; и главное — замена не безобидна: `var` живёт в функции, `let` в блоке, и
у замыкания внутри цикла смысл при этом меняется, а таких циклов в движках сотни.
Покупается стилем, платится риском в самом дорогом месте.

**Решение записано так, чтобы не растаять:** шесть файлов движков выведены
из-под правила отдельной оговоркой в настройке линтера, и причина написана там
же. Проверено мутацией в обе стороны: `var` вне движков роняет проверку, `var` в
движке — нет.

**Каталог Obsidian после этого захода:** из сорока двух правил сходится 38,
расходится **ни одного**, четыре расходятся по букве с названной причиной. На
2026-09-09 было «сходится 36, расходится 3».

### 65у. Правило «где взять активный редактор» сведено к одному объявлению — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): на экране ничего не меняется, нажать
нечего. Отчёт целиком — `docs/OBSIDIAN_CATALOG_RULES.md`, Р6.

**Что было.** Плагин пять раз отвечал на вопрос «в каком редакторе человек
сейчас стоит», и все пять ответов были разные. Четыре из пяти спрашивали
активную **панель**, а не активный редактор, — а панель может показывать не
заметку вовсе, и тогда команда либо молчит, либо правит не то. Правила каталога
Obsidian называют это прямо: П1 и П8.

**Что стало.** Ответ один, и порядок в нём от точного к запасному. Прежний путь
не выброшен, а отодвинут в конец: случай «заметка открыта, а платформа не
пометила её активной» через него и работал.

**На экране не меняется ничего, и это сверено, а не обещано:** все пять
реализаций прогнаны на четырёх состояниях рабочего стола, которые у Obsidian
бывают, — ответ совпал у всех пяти.

**Два правила каталога закрыты этим заходом:** было «сходится 36, расходится 3»,
стало «сходится 38, расходится 1». Оставшееся расхождение — `var` в движках, и
это решение, а не долг.

### 64у. Мёртвый код снят, копии правил сведены к одному объявлению — **сделано 2026-09-11**

**Строки в листе с чекбоксом нет** (У-84): это разбор, нажать нечего. Отчёт
целиком — PRD 15.2, «Д-3, второй отчёт».

**Что разобрано.** Линтер показывает 23 объявления, которых никто не зовёт, все
в семи файлах под З3. По каждому названы файл, строка, что это за функция и кто
звал её последним — вопрос задан истории (`git log -G` и счёт вхождений имени на
первом коммите против нынешнего), а не памяти. Потерей при переносе (У-16) не
является ни одно: шесть осиротели тем же коммитом, которым их тела уехали в
общий модуль, пятнадцать приехали мёртвыми вместе с движками, одна заведена
нами сверх нужды. Цена снятия — 139 строк, четыре пина, остающихся без предмета,
и один общий помощник, теряющий единственного звавшего. **Заказчик разрешил
снять все 23 в тот же день** (В-102, тридцать восьмое исключение к З3), и это
сделано: `no-unused-vars` **23 → 0**. Снималось по одному, с прогоном линтера
после каждого шага, и он показал цепочку из четырёх имён, живших только для
снятого. Четыре пина, стерёгших текст мёртвых функций, сняты вместе с ним; на
месте каждого написано, каким живым пином то же правило держится.

**Хвост закрыт в тот же день:** общий помощник
`line_pipeline.clearMarkersFromLine` потерял единственного звавшего и снят по
вашему слову (В-104, сороковое исключение). Спрашивал отдельно по одной
причине: имя экспортировано, а про экспортированное линтер молчит — само по
себе оно не всплыло бы никогда.

**Поправка к листу приёмки.** Там стояло «копий больше нет — ни в движках PKM,
ни в TagWheel». Это неверно, и я исправил строку в самом листе. Сплошной обход
**по устройству** находит ещё 24 такие функции: двенадцать в `tagwheel_core.js`
и двенадцать в `status_date.js`, все про разбор форматов даты и числа. На экране
из-за них сейчас ничего не происходит — общий модуль на месте всегда, до копии
дело не доходит, — но прежнее утверждение было выведено из поимённого списка, а
не из обхода, и сторож, которым оно закреплено, написан по имени переменной и
этих 24 не видит (У-126, У-137).

**Заказчик выбрал свести их к одному объявлению** (В-103, тридцать девятое
исключение), и это сделано. Сведены **22**, а не 24: двух я не досчитал — мой
обход считал вложенные объявления неверно, и нашлись они только тогда, когда
запрет переписали по форме и он покраснел.

**Поведение сверено до правки:** каждая копия против общего модуля, **4512
входов**. Расхождение нашлось одно — на списке шагов из одного слова `END`, — и
на экран оно не доходит: величину, в которой они расходятся, все три читателя
спрашивают только при непустом списке шагов.

**Находка сверх правки, и она важнее самой правки.** Движок дат брал общие
помощники не своим `require`, а швом, который ставит точка входа. У человека
шов на месте всегда; а проверка поведения зовёт движок напрямую — и там шва не
было ни дня. То есть **набор проверял копию, а у вас работал общий модуль**: та
самая пара «продукт и проверки читают разные объявления», которая 2026-09-07
стоила плагина без единой команды. Теперь и там литеральный `require`.

**Что изменится у вас на экране: ничего.** Единственное отличие — если общий
модуль когда-нибудь не приедет, плагин скажет об этом громко, а не начнёт
молча отвечать по-другому.

### 63у. Тихие отказы под З3, третий кусок: 27 → 0, долг закрыт — **сделано 2026-09-10, вечер**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего, и на экране почти
ничего не меняется. Отчёт целиком — PRD 15.2, «Д-4, третий кусок В-97».

**Что изменилось в работе плагина — одно, и вот оно словами.** Когда файла
правил нет или правила не сходятся, плагин говорит об этом сообщением и
прекращает работу. Если **само сообщение** показать не удалось, прежде не
оставалось ничего: ни результата, ни причины — команда молча не делала ничего.
Теперь причина уезжает в консоль, с приставкой имени плагина. Проверено
поведением: `Notice` платформы заставляют бросить, и в консоли обязана оказаться
строка.

**Семь мест из двадцати семи исчезли как места, а не обзавелись комментарием:**
мёртвый отчётчик об отказе загрузки (объявлен, не позван ни разу с тех пор, как
ушёл мост модулей); четыре копии правила «показать сообщение», сведённые к
одному помощнику; мёртвая охрана вокруг сборки порядка Fields — сработать она
не могла, а сработав, тихо теряла порядок.

**Остальные семнадцать объяснены на месте**, и у каждого названа семья: проба,
уборка, украшение. **Одно моё объяснение было неверным** и переписано: про
отчётчик TagWheel я написал, что человеку про отказ уже сказано рядом, — так
делают два места вызова из четырёх, а два молчат нарочно.

**Находка сверх правки — дефект в самой мере долга.** `catch (_) {}`,
написанный в объяснении **прозой**, сторож считал местом молчания: число росло
от того, что кто-то объяснил соседнее место словами. Обход переписан на поиск
по маске, где комментарии и строки стёрты. **Первая версия этой маски была
сломана и молчала об этом**: она бежала за парной кавычкой до конца файла и
стёрла `settings_backup.js` целиком вместе с девятью настоящими местами.
Покраснел порог рядом — «а находит ли обход ещё хоть что-нибудь», — и это
единственное, что о промахе сказало. Уроки — У-138 и У-139.

**Из последних трёх мест вырос вопрос В-100, и вы его закрыли той же сессией:**
правило подстановки текста сообщений было объявлено четыре раза, три копии в
движках сняты. Поведение сверено на 234 входах, расхождений ноль, мутация в
каждую сторону краснеет. Это тридцать седьмое исключение к З3.

**Заодно спрошено у платформы**, откуда движки берут `Notice` без импорта:
у Obsidian это глобальное имя (`window.Notice` в `app.js` 1.13.7). Дефекта тут
нет, и теперь это записано, а не предполагается.

Долг Д-4: **69 → 0** за три куска одного дня. Всего пустых `catch` в рантайме
47, объяснены все 47. Семь шагов зелёные, 65 из 65.

### 62у. Тихие отказы под З3, второй кусок: 60 → 27 — **сделано 2026-09-10**

**Строки в листе с чекбоксом нет** (У-84): нажать нечего, и **на экране не
меняется ничего**. Это тот случай, когда правка нужна не ради нового поведения,
а ради того, чтобы у каждого правила осталась одна реализация.

**Что было.** В двух файлах движка PKM лежали 32 функции такого устройства:
спросили общий код, а сразу за вопросом — **своя копия того же правила**. Копия
доставалась ровно тогда, когда общий код дал сбой, и тогда плагин не падал и
ничего не писал, а тихо начинал отвечать по-другому. Из таких копий у нас уже
вырастали дефекты.

**Что стало.** Копий нет: общая реализация зовётся прямо. Ещё шесть функций
после этого остались вовсе без вызовов — сняты как мёртвые. Одна функция
переименована: в её имени стояло слово «Safe», а держалось оно на снятой ветке.

**Поведение не изменилось, и это измерено, а не обещано.** Снятые ветки и
раньше были недостижимы, поэтому испортить что-то можно было ровно одним
способом — переврать сам вызов. **Все 35 вызовов сверены посимвольно «до» и
«после»: расхождений ноль**, и у самой сверки есть контроль — подменённый
аргумент она видит.

**Тридцать пять проверок заменены одной.** Прежние держали список имён с полным
перечнем аргументов: такой список проверяет список, а не предмет, и новая
обёртка в него просто не попадает. Теперь стоит сплошной обход по признаку:
функция, зовущая общий код, не имеет права проглотить его отказ молча. **Первая
версия этого обхода была неверной** — она мерила длину функции и объявила
нарушением три законные, — и это записано в ней самой.

**Три мутации краснеют**, и третья из них важнее прочих: я подменил образец,
которым ищет сам обход, и он покраснел. Без этой проверки обход был бы зелёным
от собственной опечатки.

**Находка по ходу — В-98, и вы её закрыли той же сессией.** Те же четыре правила
были объявлены второй раз ещё и в коде TagWheel, по той же схеме, но **без
молчаливого `catch`** — копия пряталась за одним условием, и сторож долга не
видел её ни дня. Копии сняты, все четыре вызова сверены посимвольно.

**Запрет теперь написан по признаку самой копии, а не по её последствию:**
спрашивать у общего кода, есть ли у него такая функция, имеет смысл только
тогда, когда за ответом «нет» лежит свой. **И мутация нашла дефект в первой
версии этого запрета:** его положительный контроль держал собственную копию
образца и не краснел, когда я ломал рабочий, — то есть охранял себя, а не
запрет. Образец сведён к одному объявлению; теперь краснеют обе мутации.

Долг 60 → **27**, всего мест молчания в плагине 90 → **57**. Плюс четыре копии в TagWheel, которых сторож долга не считал вовсе. Семь шагов
зелёные, 65 из 65.

### 61у. Тихие отказы под З3, первый кусок: 69 → 60 — **сделано 2026-09-10**

Ваш ответ на В-97 (вариант 1) — это **тридцать шестое исключение к З3**. Разбор
был сделан до вопроса, правка — после, порядок тот же, каким закрыты остальные
тридцать пять.

**Строки в листе с чекбоксом нет** (У-84): нажать нечего. Но **три вещи в
поведении изменились**, и вот они, словами про то, что видно:

1. **Несуществующая дата больше не превращается в соседнюю.** Если в Field типа
   даты оказывалось `2026-02-31`, плагин молча читал это как 3 марта. Теперь
   такая дата отвергается. Существующие даты разбираются как прежде — это
   проверено отдельно, чтобы «отвергает» не превратилось в «отвергает всё».
2. **Пустой идентификатор Field больше не попадает не в тот Field.** Раньше
   поиск сравнивал напрямую, и пустое значение совпадало с Field, у которого
   идентификатор не задан вовсе.
3. **Дочерний Field не может потерять место в Order** — ветки «модуль не
   приехал» не осталось ни одной, а последняя такая отвечала «ключ равен
   самому себе».

**Девять мест исчезли как места**, а не обзавелись комментарием: три заглушки на
месте модуля (запрещённый класс), два `await` в пустых `catch` перед открытием
TagWheel и четыре копии, расходившиеся с оригиналом.

**Одно моё утверждение из вчерашнего разбора оказалось неверным, и я его
исправил.** Два места перед открытием TagWheel я назвал «дороже прочих по
последствию» — панель, мол, откроется на неполных данных. При правке
выяснилось, что одна из двух функций имеет **пустое тело**, а вторая делает
работу, которой нечем отказать. Я прочёл место вызова и не прочёл сами функции.
Обе сняты как мёртвые; урок записан — У-136.

**Находка сверх правки: одна проверка охраняла мёртвый код.** Правка уронила
пин «модель Fields относит ключи вида даты к типу element». Разбор: этот тип
возвращала только запасная копия внутри функции, которую **не звал никто ни в
одном коммите с первого релиза**, — спрошено у истории, а не у памяти, и
проверено, что это не потеря при переносе. Функция снята, пин переписан на то,
что верно.

**Чем закреплено.** Числа по файлам в `silent_failures_tests.ts` (69 → 60,
`field_model.js` ушёл из списка целиком); запрет `require` внутри `try` по
форме; четыре пина делегирования переехали за своим предметом. **Три мутации
краснеют** — возврат заглушки, возврат молчаливого `catch` вокруг разбора даты
и возврат своего правила о типе ключа, — а без подмены оба сторожа зелёные.
Поведение спрошено у самих функций одиннадцатью вопросами, с положительными
контролями. Семь шагов зелёные, 65 из 65.

**Осталось в очереди:** 32 копии, повторяющие общий код (второй кусок), и 28
верных молчаний без объяснения на месте (третий).

### 60у. Выпуск `0.1.0-beta.5` в GitHub — **сделано 2026-09-10**

Одноразовое разрешение, данное его словом «после чего разместишь в gh новый
релиз», исполнено. **Строки в листе с чекбоксом у этого пункта нет** (У-84):
проверить нажатием нечего — плагин у него в vault уже стоит той же сборкой.

**Что выложено.** Тег `0.1.0-beta.5` на коммит `00ecd70`, релиз собран
`release.yml` из того же коммита и после полного набора проверок. Не предрелиз,
три вложения: `main.js`, `manifest.json`, `styles.css`. Это ровно то, что берёт
BRAT: последний **не**предрелиз с тремя файлами — и в списке релизов он первый.
Прошлый `0.1.0-beta.4` был сломан, номер вырос.

**До тега — чистый клон** (У-78): `git clone` в пустую папку, `npm ci` и весь
набор там. 65 проверок из 65, линтер, типы, оба гейта и `test:release` зелёные.
«65 из 65 у меня» на этот вопрос не отвечает, и клон — единственное, что
отвечает.

**Сборка не минифицирована** — решение В-95. `main.js` 1,8 МБ, имена функций
читаются: его «перестало работать» разбирается по имени в консоли.

**`main` переведён на рабочую ветку** — решение В-96. Перемотка вперёд без
слияния; что она возможна, спрошено у git (`merge-base --is-ancestor`), а не
принято на веру. Отставание было 114 коммитов, и по ссылке на репозиторий
видно было 6 сентября.

**Чем закреплено.** `version_consistency_tests.js` — номер в четырёх местах и в
собранном манифесте; он же поймал, что `dist/` собран прежней версией, и не дал
выпустить манифест, называющий себя иначе. Тег и манифест сверяет сам workflow
до сборки. `release_bundle_tests.js` — 55 файлов рантайма, путей внутри vault
нет ни одного.

### 59у. Все 69 тихих отказов под З3 разобраны — **сделано 2026-09-10**

**Строки в листе у этого пункта нет, и это правильно** (У-84): проверить его
нажатием нельзя — **ни одна строка кода не изменена**. Это разбор, и он
назначен вашим словом: сначала все места, потом вопрос (У-9). Отчёт целиком —
PRD 15.2, «Д-4, третий отчёт».

**Числа взяты прогоном, а не из документа**: всего пустых `catch` в рантайме 99,
объяснено 30, в долге 69. Мой обход воспроизвёл то же распределение по девяти
файлам — это контроль до первого вывода, а не после (У-119).

**Главное, что нашлось.** 36 мест из 69 — это вовсе не молчание, а **второе
объявление правила** (У-32): рядом с вызовом общей функции лежит её локальная
копия, и достаётся она, только если общая бросила. Все 36 имён общий модуль
действительно отдаёт — спрошено у самого модуля, — а сам модуль берётся
литеральным `require` в шапке файла, то есть лежит в бандле. Значит копия — не
запасной путь, а второй ответ на тот же вопрос.

**Копии сверены с оригиналом, все 35 через общий модуль, и три расходятся по
существу.** Дороже прочих `parseDateByFormat`: общая после разбора сверяет дату
обратно и отвергает несуществующую, копия — нет. То есть `2026-02-31` копия
молча отдаёт как 3 марта. Второе — поиск Field по идентификатору: копия
сравнивает без приведения к строке и без охраны пустого значения, и на пустом
идентификаторе отдаёт не тот Field.

**Три места — запрещённый класс.** `pkm_rules_runtime_helpers.js` и
`field_model.js` держат свой `require` внутри `try` — заглушку на месте модуля,
которая вне З3 снята целиком (У-90, A33). У первого запасных ответов даже два, и
последний отвечает «ключ равен самому себе» — то есть при отказе модуля дочерний
Field молча теряет своё место в Order.

**Два места дороже прочих по последствию:** `tagwheel.js:1799` и `:1800` —
загрузка ключей и реестра доменов **перед открытием панели**. Отказ любой из
двух открывает TagWheel на неполных данных, и человек об этом не узнаёт. Класс
тот же, что у самой дорогой находки прошлого захода.

**Остальные 28 — молчание верное, объяснения нет.** Прокрутка, журналы,
подстановка текста, уведомления, уборка панели. Здесь правка — объяснение на
своём месте, и это не приписка в смысле У-118: за ней остаётся верное поведение.

**Чем закреплено сейчас:** ничем сверх того, что уже стоит, — правки не было.
`tests/regression/silent_failures_tests.ts` продолжает держать 69 по файлам и
краснеет на любом новом молчании. **Что закрепит правку** — таблица цены в конце
третьего отчёта PRD.

**Всё перечисленное лежит под З3 и ждёт вашего слова — вопрос В-97.**

### Разделы 50у, 53у, 54у, 55у, 57у — **подтверждены заказчиком 2026-09-10**

Галочки на строках `S6`, `S7`, `V1` и `V2`: имя шаблона в свёрнутом
правиле, подложка Left и Right Block (третий и четвёртый заходы),
нижние границы четырёх ползунков вида с переименованием
`Tag bubble height` и перенос инлайнового оформления в классы.
Замечаний под галочками нет. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.

### 56у. Тихие отказы вне З3 разобраны — **сделано 2026-09-09**

Долг Д-4, лежавший в списке «известного и не начатого»: **105 → 69**.

**Строки в листе у этого пункта нет, и это правильно** (У-84): проверить его
нажатием нельзя. На экране не меняется ничего — кроме одного: если теперь
что-то из перечисленного ниже сломается, в консоли Obsidian (`Ctrl+Shift+I`)
появится строка с приставкой `[inline-overhaul]`, а не тишина.

**Заговорили пять отказов**, и самый дорогой из них — постановка оформления в
редактор: отказ там значит, что **эта заметка не красится вовсе** — ни
пузырей, ни полос, ни подложки. Вы пришли бы со словами «перестало красить», а
узнать причину было бы не из чего. Остальные четыре: окно отладки полос, снимок
пакета полос, потеря предыдущего журнала при перевороте и оборванный конец
журнала.

**И одна вещь важнее чисел.** Три места из двадцати одного оказались
**заглушками на месте модулей** — то, что правила проекта запрещают прямо, — и
каждая переписывала рукой то, что уже объявлено в модуле: список имён настроек,
правило подстановки в текст и ответ «всегда тег» на вопрос о типе Field. Копия
подстановки жила в **четырёх файлах сразу**. Сняты целиком: модуль лежит в
сборке, и не приехать он может только вместе со всем плагином.

**Что осталось.** 69 мест, и все они в файлах под З3 — то есть требуют вашего
слова после разбора, а не просто правки. **Две находки там видны уже сейчас:**
два файла держат ту же заглушку, что снята здесь. Разбор целиком — PRD 15.2,
второй отчёт по Д-4.

**Чем закреплено.** Сторож `silent_failures_tests.ts` держит число пофайлово и
умеет только убывать: новое молчание роняет проверку в том файле, где его
завели, а разобранное обязано уйти из списка тем же коммитом. Самое важное из
сделанного закреплено поведением — `decoration_build_tests.js` на настоящем
`RangeSetBuilder` платформы: пять утверждений, две мутации краснеют.

### 52у. Бриф для LLM на проектирование PKM — **сделано 2026-09-09, третий заход**

**Чего просил заказчик.** Отдельный файл, который он будет загружать в LLM,
чтобы та предложила ему набор Fields, Values, цветов, эмодзи-элементов, YAML и
предусловий под его собственный PKM. Плюс просьба сказать, что в его запросе
упущено.

**Что сделано.** `docs/PKM_Design_Brief_for_LLM.md` — 910 строк, 40 тысяч
знаков, **по-английски**: файл написан для загрузки в LLM, и заказчик выбрал
английский прямо (спрошено интервью, ответ — «сам файл для llm должен быть на
английском, мне нужен от тебя только 1 файл md»). Русской версии не оставлено —
одно правило в одном месте. Не пересказ руководства: правила сняты с кода (`fields_model.ts`,
`texts_blocks.ts`, `starter_config.ts`, `order_deep_editor_state.js`, схема
`pkm.ts` и `visual.ts`), и каждое ограничение названо тем, чем оно является —
отказом панели, а не советом. Сверх запроса в файле пять вещей: интейк из
одиннадцати вопросов до проектирования, бюджеты и десять антипаттернов,
проверенная палитра с посчитанным контрастом, список из 25 самопроверок и
точный формат выходного документа с эталонным примером.

**Чем закреплено.** Числа контраста в файле проверены скриптом по формуле из
`custom/contrast.ts`: 37 пар, расхождений ноль, ни одна пара не ниже 4:1 при
пороге предупреждения 3:1. Мутация (заведомо неверное число в таблице) —
краснеет, то есть проверка не слепа.

**Дописано тем же заходом — доступ к vault (заказ заказчика).** Раздел 1.0:
чтение vault необязательно, но если путь дан, читается до интервью и переводит
открытые вопросы в подтверждения. Двенадцать предметов чтения, у каждого
названо, какое решение он снимает; отдельно — `data.json` плагина, если он уже
настроен (тогда ответ обязан быть **диффом**, а не набором с нуля), и
`generated_rules.md` рядом с ним. Плюс шесть правил обхождения: перенимать
существующий словарь, называть цену каждого переименования числом затронутых
строк, отвечать числами, а не «часто», говорить, что прочёл и чего не читал, не
цитировать заметки сверх примеров строк, и выкладывать противоречие между
vault и ответом человека на стол. Шаг чтения добавлен в порядок работы (раздел
0) и шесть проверок — в самопроверку (26–31).

**Прогон брифа на настоящем случае — заказчик прошёл интервью сам.** Одиннадцать
вопросов заданы тремя заходами интерактивного опросника, ответы собраны, и по
ним выдан документ: `test-vault/PKM Plan.md`, 471 строка. Пять Fields
(`Kind` с дочерними, `Priority` с Bars, `Due`, `Project`, `Who`), палитра из
восьми пар, свойства заметки, четыре Smart Rules, три хоткея, порядок ввода в
панель четырнадцатью шагами и раздел «чего я не стал предлагать» из пяти
пунктов. Контраст в нём тоже посчитан: 17 пар, расхождений ноль.

**Прогон нашёл дефект в самом брифе, и это его главная польза за сессию.**
Лестница «серый → янтарный → красный» стояла в разделе 7.6 под заголовком
«яркость монотонна», а она не монотонна: 0.167 → 0.239 → 0.143, вверх и
обратно, — то есть в оттенках серого средняя ступень оказывается самой светлой.
Лестница заменена на монотонную (`#9ba8b8` → `#b07d10` → `#a8231b`,
0.384 → 0.239 → 0.096), у каждой ступени всех трёх лестниц выписана яркость, а
прежняя версия оставлена рядом **как правило**: цветная лестница меряется, а не
прикидывается на глаз, и три цвета, каждый из которых прошёл 4:1 со своим
текстом, лестницей от этого не становятся. Проверка расширена: теперь она
сверяет и одиннадцать значений яркости, не только контраст.

**Строки в листе приёмки у этого пункта нет, и это правильно** (У-84):
проверить его нажатием нельзя, годность видна только по тому, что выдаёт LLM на
его основе. Отчёт, а не чекбокс.

### Разделы 44у, 45у, 48у, 49у, 51у — **в архиве**

Пять отчётов по строкам `S6` и `S7`, **перекрытых позднейшими заходами**. Обе
строки заказчик закрыл галочками 2026-09-10, и последние отчёты по ним (50у,
53у, 57у) унесены в `docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.
Причины дефектов и то, чем правки закреплены, —
`docs/archive/AWAITING_OWNER_CHECK_sessions27_29.md`.

### 38у. Браузер в наборе — **сделано 2026-09-08, вечер**

Ваш ответ: «добавить сейчас отдельным куском», в CI пока не включать.

**Что теперь есть.** Седьмой шаг проверок — `npm run gate:browser`. Он
открывает прототип панели настоящим Chromium и спрашивает **вычисленные
величины**, а не дерево: видит ли браузер открытую подсказку, во всю ли ширину
своего блока она встала, какой контраст у текста шапки против получившейся
заливки, в одну ли строку встала подпись слайдера. На все семь вкладок — 151
подсказка, 7 шапок, 7 подписей.

**Почему это важно.** Заглушка, на которой работают остальные проверки, не
считает ни одного пикселя. За неделю через неё прошли и были найдены **только
вашими глазами**: шесть подсказок в скрытом узле, подсказки шириной с колонку,
шапки серым по серому и знак `%`, уехавший на вторую строку. Дерево каждый раз
было верным.

**Гейт проверяет сам себя.** Он гоняет свои проверки дважды: на нынешнем виде
они обязаны пройти, а на четырёх подменах — упасть. Подмена возвращает панель
в то состояние, из которого вы уже приносили замечание.

**Что он нашёл в первый прогон — вопрос В-92.** Подсказка шапки списка Fields
раскрыта на 188 точек: она занимает свою колонку целиком, а колонка узкая. И
это та самая подсказка, в которую унесён смысл `Left Block` и `Right Block`.

**Проверять тут нечего — это отчёт.** Смотреть придётся одно: ответ на В-92.

### Разделы 25у–36у — **в архиве**

Унесены 2026-09-08 вечером, по итогу сессии 26. Четыре раздела заказчик
закрыл галочками (30у, 31у, 32у, 35у — подсказки у всех подписей, приглушённая
заливка шапок, три починки и перевод панели), остальные четыре — отчёты, в
которых проверять нечего: список правил каталога (29у), закрытие фазы 6 (33у),
три расхождения с каталогом (34у) и разбор мёртвого вне З3 (36у).
Всё вместе — `docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 28у. Решено и не начато

Ни одна строка ниже проверки не ждёт: они ждут работы. Осталось то, у чего срок
назначен его же словом.

- **Минификация — в самом конце**, перед выходом наружу: «нужно упростить
  работу по отлавливанию багов». Вопрос В-95 отвечен 2026-09-10: бету **не
  минифицировать**. Срок остаётся за ним. **Не начато.**
- **Мобильные устройства** — отложены его словом, реализовать позже. **Не
  начато.**
- **Снятие инлайн-элементов при экспорте заметки наружу** — первый пункт его
  файла идей; разбор назвал его хорошим кандидатом в ближайший релиз. Решения
  ещё не было. **Не начато.**

**Долгов, закреплённых числом, больше нет.** Тихие отказы (Д-4, было 105 → 69)
закрыты целиком 2026-09-10: список `DEBT` в `silent_failures_tests.ts` пуст, и
теперь роняет проверку любое новое безымянное молчание. Выпуск наружу, который
стоял здесь «не начато», сделан дважды: `0.1.0-beta.5` 2026-09-10 и
`0.1.0-beta.6` 2026-09-11.

**Долг по инлайновому оформлению закрыт** (Р7, 48 из 48) и подтверждён галочкой
на строке `V2` 2026-09-10; отчёт — в архиве проверенного. Абзац про «182
инлайновых стиля» удалён, а не унесён: две трети того числа лежали в мёртвой
функции, а остаток перенесён (У-31, У-117).

### Разделы 1у–24у — **в архиве**

Всё, что заказчик закрыл галочками до сессии 24, унесено в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`. Открывать, когда нужно
вспомнить, чем закреплена старая правка или когда её сделали.

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него сделаны и подтверждены заказчиком; разборы — в архиве
реестра: `Floating button`, симметричный цикл `Move right`, подсветка
обработанной строки, три пункта 1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа
свойства через `app.metadataTypeManager`.

**Остатка у фазы 6 больше нет.** Последний пункт закрыт 2026-09-08: манифест и
снятие команды `Open settings` были сделаны 2026-09-06, `dist/` вышел из
репозитория тогда же, а половина пункта про `main.js` потеряла предмет — после
разбора это 159 строк исходника, а не артефакт сборки. Живого в пункте
оказалась одна вещь, и её в задании не было: последняя заглушка на месте
модуля, `fields_editor_legacy.js`. Разбор — PRD, раздел 11.

Абзац про «остаток фазы 6 — снятие моста модулей» удалён 2026-09-08, а не
унесён в архив: он описывал состояние, которого больше нет (У-31). Моста нет
с 2026-09-07 — проверено поиском: файла `vault_module_bridge.js` не
существует, вызовов моста в рантайме ноль. В архиве этот абзац продолжал бы
врать.

**Остальное решённое и не начатое — раздел 28у выше**, и оно перечислено там,
а не здесь: этот раздел про работу, о которой заказчик не просил, а тот — про
ту, которую он назначил.
