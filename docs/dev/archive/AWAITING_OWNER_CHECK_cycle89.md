# Приняты его галочками в цикле 90 (тесты 2, 4, 5 цикла 89)

### 350у. Тексты панели выровнены — **сделано 2026-09-24, день** (его пункт «исправить опечатки» и ответ интервью «выровнять всё, включая спорное», тест 8)

Вычитка всех 1 590 строк каталога и справочника команд. Исправлено: 11 ошибок в подсказках; `Standard commands` → `Main commands` (его слово); header → heading в подписях; `Separator`, `Prefix`, `Left Block` с заглавной; британское написание → американское; один вид апострофа; `/` вместо `\` в заголовках; подсказки называют настройки их нынешними именами; снят «голос журнала изменений» и обращения к нему из подсказок. **Не тронуто нарочно:** имена, которые он дал сам (`Show when press Alt`, `Only custom name`, `Custom+Default name`, `How to show Value in YAML`, описание шага элемента), имена под пределом пяти слов гейта Г10 и два имени под правилом регистра предложения (`Smart paste`, `Expanded 'Ctrl+A'`). Откат — коммит `42aabb6`.

### 349у. Цвет рамки `Side` — **сделано 2026-09-24, день** (его пункт из «Новое пишите сюда», тест 7)

Колонка `Side` между `Text` и `Preview` — в таблице Values и в `Color custom tags`; ключ `borderColor` рядом с `fillColor` и `textColor`. Пусто — рамка темы; свой цвет — рамка этого цвета и там, где тема своей не рисует (не тоньше точки); `#FFFFFF` — рамка прозрачна, место её остаётся. Сброс «↺» возвращает и её. **Цена, названная ему интервью и принятая:** таблица Values шире на 32 точки, в панели уже 665 точек она прокручивается вбок. Закреплено: `tag_visual_render_tests.ts`, карта записей, `fields_editor_view_tests.ts`, `user_tags_tests.ts`.

### 348у. Тег без своих цветов — вид тега темы; `#FFFFFF` — прозрачно — **сделано 2026-09-24, день** (его пункт из «Новое пишите сюда», тест 7)

Его слово: «по умолчанию цвет заливки и текста тегов был как в используемой теме… при fill = `#FFFFFF` цвет был прозрачным». Пузырь без своей заливки берёт `--tag-background`, `--tag-color`, `--tag-border-width`/`--tag-border-color` и `--tag-weight` — те же переменные, которыми Obsidian рисует `a.tag` и `.cm-hashtag` (`app.css` 1.13.7). Отменяет его решение 2026-09-12 (акцентная заливка): с цикла 88 пузырь у всякого тега, и одинаковыми их делает тема. Пузырь-образец панели рисуется так же. **Кегль не менялся:** он берётся у темы с 2026-09-21, стенд `tools/tag_size_probe.js` на его Minimal даёт 12.8 точки у обоих; на его снимке буквы пузыря крупнее примерно в 1,1 раза — это ждёт его взгляда после перезапуска (тест 7, шаг 2). Закреплено: браузерный шаг, подмена `plain-tag-accent`.

### 347у. `Add Block` и `Add Field` на всю ширину столбца — **сделано 2026-09-24, день** (его `💬` к тесту 2)

`flex: 1 1 0` у обеих кнопок, в прототипе и в `styles.css`.
