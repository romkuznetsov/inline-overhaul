# Реестр проверенного: сессия 60 (2026-09-17)

Отчёт, закрытый его галочкой. Живой реестр — `docs/AWAITING_OWNER_CHECK.md`;
разбор правки — PRD 10.13.176, исключение к З3 № 127.

### 202у. Значения правого Block уезжали в слово человека, а ссылка не переезжала вовсе — **подтверждено им 2026-09-17** (галочка под `G3`)

**Это ваше замечание `G3`.** «Я переместил все fields в right block и на строке `11` активировал все values и получил `- [ ] 11 [[test]] ::  :: #/1 #todo #work #new #123 📅2026-09-16 20:10 🤣HXxhUv #aaa`, а ожидал `- [ ] 11 :: #/1 #todo [[test]] #work #new #123 📅2026-09-16 20:10 🤣HXxhUv #aaa` — по какой-то причине field=links отображаются в left block, несмотря на то, что я перенёс их в right block».

**Воспроизведено на вашем `data.json` до знака.** Стенд прогнал те же восемь нажатий подряд и выдал ровно вашу строку. Дефектов в ней оказалось **два**, и видны они по отдельности.

**Первый — деление строки на зоны, и он про ваши одинаковые разделители.** У доводки строки было своё объявление «где кончается зона значений», и оно считало единственный разделитель в строке **первым**: `- [ ] 11 :: #/1 #todo` для неё означало «слева `- [ ] 11`, текст `#/1 #todo`, правый Block пуст». Поэтому нажатие на элемент вырывало значения правого Block из зоны и приклеивало их к вашему слову, а следующее нажатие растаскивало получившееся по строке. Разбор строки — тот, которым плагин её читает, — отвечает на ту же строку иначе и верно: единственный разделитель это **второй**. Теперь оба спрашивают один дом; при разных разделителях позиция отвечает сама, и там ничего не менялось.

**Второй — поле типа link не переставлялось по Order ни одной командой.** Списки правил делят поля по роду, а не по Block: теги в одном списке, ссылки и элементы в другом. Правилу «поставь значение в его Block» обе дороги команд подавали **только** список тегов, и ссылку не переставлял никто. Панель дефекта не знала: она строку собирает заново и каждое значение кладёт в Block его поля — отсюда и вид «панель и команда дают разное».

**Как проверить, за две минуты.** На строке со словом (у вас это `11`) нажать подряд команды нескольких полей правого Block, обязательно включая поле-ссылку и поле с датой. Все значения должны встать за одним разделителем в порядке Order, а ваше слово остаться слева от него.

**Чем закреплено.** Обход строки — `node tools/line_matrix.js` — получил две формы, которых в нём не было: «несколько значений за единственным разделителем» и «ссылка поля без предусловия». Обе взяты из вашего конфига, а не написаны в стенде. Обе были красными до правки (расхождений 19 из 207) и зелёные после (7 из 207); каждую правку я снимал по отдельности и смотрел, какие формы от неё краснеют.

**Что осталось расходиться и почему я не стал это трогать.** Семь пар из 207 — строка, где стоит значение поля с **невыполненным** предусловием (у вас это `Project`: он ждёт `#work`). Команда такое значение считает вашим словом и не трогает, панель переносит его в Block поля. Это не про ваше замечание, и правильного ответа тут два; вопрос заведён отдельной строкой.


### 210у. Написанное в Block встало по середине полосы — **сделано 2026-09-17, второй заход**

**Это ваш ответ на `G4`: «по прежнему не по центру», со скриншотом.** Я обмерил ваш скриншот декодировщиком, а не глазом: полоса 27 точек, и всё написанное в ней — и пузыри, и ссылка, и дата — стоит на 3 точки ниже её середины. То же число дал браузерный стенд на ваших настройках: 3,1.

**Причина оказалась не в значениях, а в самой полосе, и их две.**

**Первая: высота полосы считалась от слова человека.** Слагаемым была высота написанного в **строке**, а слово человека набрано обычным кеглем — у вас же `Tags text size` = 60 %. Поэтому при ползунке высоты 20 % полоса всё равно занимала всю строку: свободного места, которое ползунок делит, не оставалось вовсе. Теперь высота считается от того, что в полосе и лежит, и ваши 20 % стали видны: полоса 16,4 вместо 21,6.

**Вторая: вертикаль полосы считалась по ряду строки.** Середина ряда и середина значений — разные величины, и расходятся они ровно на ту разницу кеглей. Теперь вертикаль считается по написанному **в самом куске**.

**Что стало, числами.** На ваших настройках: пузыри 0,01…0,5 точки от середины полосы (было до 3,1), ссылка и дата — до 1,2 (было до 5). Это уже не «по нижней границе», а попадание в пределах точки.

**Цена названа.** Опорой служит меньший из ящиков краёв куска — и выбран он ради вашего же прежнего замечания: ящик ссылки выше соседнего **только сверху**, и объединение ящиков подняло бы полосу на строке со ссылкой. Ваше условие «полоска одинаковая во всех строках» осталось в силе и проверяется отдельно.

**Как проверить, за минуту.** Поставить `Tags text size` поменьше и посмотреть на строку, где в Block есть и тег, и ссылка, и дата: все три должны стоять на одной высоте внутри полосы, а сама полоса — заметно тоньше, чем была, если ползунок высоты у вас невысокий.

**Чем закреплено.** Браузерный шаг мерит вашей мерой — расстоянием до границ полосы; порог 2 точки, при здоровой странице 1,2 и при снятой мере 3,1. Две подмены, которые после этой правки перестали что-либо показывать, сняты с объяснением, а не оставлены зелёными.

### 209у. Строка с одним разделителем: свойства заметки, имя и место ссылки — **сделано 2026-09-17, второй заход**

**Это ваши два замечания по `G2`.** «В transformed заметке `[[dsf]]` не заполнились yaml properties» и «после трансформации исходная линия выглядит `- dsf :: #/1 #source 📅2026-09-16 22:42 [[222/dsf]] :: #processed`, а ожидалось `- [[222/dsf]] :: #/1 …`».

**Оба — одно и то же, и причина третья по счёту за сутки.** Ваша строка `## dsf :: #/1 #source 📅…` несёт **один** разделитель. Правило «единственный разделитель — это второй» живёт в разборе строки, и сегодня я уже сводил к нему доводку (это было `G3`). У Transform оказался **свой** разбор, тоже позиционный: всё, что после первого разделителя, он считал вашим словом. Отсюда сразу три следствия — значения полей не попадали в свойства новой заметки, не подчинялись `Fields to keep`, и именем заметки становились они сами; а слово `dsf` числилось «левой зоной», поэтому ссылка вставала в конец, а не на его место.

**Что стало.** Transform спрашивает тот же дом, что и все остальные: на вашей строке имя — `dsf`, свойства заполняются (`Importance`, `type`, `date_due`), ссылка встаёт на место имени.

**Одно отличие от вашей записи, и оно ваше же решение.** В ожидаемой строке вы оставили `#source`, но поле `type` не отмечено в `Fields to keep` — по вашему ответу В-132 такие значения со строки уходят. Теперь оно и уходит; захотите оставить — отметьте `type` в списке.

**Как проверить, за две минуты.** Строка с одним разделителем и значениями за ним: `Inline to note`. В новой заметке должны заполниться свойства, именем стать ваше слово, а на строке — ссылка на его месте и значения, отмеченные в `Fields to keep`.

### 207у. Ссылка и элемент в Block встали по середине полосы — **сделано 2026-09-17**

**Это ваше замечание `G4`, второй заход.** «Ты сделал не то. Я говорил не про текст тега, а про всё, что находится в технических блоках left/right (tags, wikilinks, emoji-elements) — я хочу, чтобы при уменьшении текста все values в технических блоках были выровнены по центру строки, а не по нижней границе (т.е. сверху и снизу от values в технических блоках должно оставаться одинаковое расстояние до границ полоски tags-block-fill)».

**Вы правы: прошлая правка починила треть предмета.** Пузырь тега — наш узел, и выравнивание досталось ему. Ссылке и эмодзи-элементу пузырь не рисуется нарочно: подменить `[[Note]]` своим узлом значит забрать у ссылки щелчок, наведение и перетаскивание. Им достаётся пометка стилем — и у неё выравнивания не было вовсе.

**Обмерено браузером относительно самой полосы, как вы и сказали.** При 50 %: ссылка и элемент стояли на 5 точек ниже середины полосы (сверху 13, снизу 3); стало — ссылка 1,9, элемент 3,1. При 80 %: было 3,5, стало 1,4. При 100 % и до правки, и после — в пределах полутора точек.

**В ноль не ушло, и я снова говорю это прямо.** Выравнивание идёт по середине букв, а не по середине полосы: это две разные величины, и остаток между ними — те самые три точки в худшем случае при полосе в 24. Убрать его совсем можно только одним способом — мерить строку в момент отрисовки и двигать каждое значение своим числом; это отдельная работа, и она меняет механизм, а не строчку стиля. Скажите, если видно глазом, — сделаю.

**Цена, о которой надо знать.** Значение стало неразрывным куском: `📅2026-09-08 12:36` на переносе теперь уезжает на следующий ряд целиком, а не половиной.

**Как проверить, за минуту.** Поставить `Tags text size` поменьше и посмотреть на строку, где в Block есть и тег, и ссылка, и дата: все три должны стоять на одной высоте внутри полосы.

**Чем закреплено.** Браузерный шаг теперь мерит **его мерой** — расстоянием до границ полосы, а не до написанного в строке; прежняя мера отвечала на другой вопрос и покраснела бы от любой правки вида. Проверка требует, чтобы на странице были оба рода — пузырь и значение, — иначе правило про ссылку проверялось бы отсутствием предмета. Подмен стало 53: к «пузырь обратно по базовой линии» добавилась такая же для значений.

### 203у. Текст заголовка, ставший именем заметки, уходит со строки — **сделано 2026-09-17**

**Это ваше замечание `G2`.** «Source-text = leave it but not the name. Исходное название `## #/1 :: dfdf :: 📅2026-09-16 20:03`, после трансформации стало `- #/1 :: dfdf [[222/dfdf]] :: 📅2026-09-16 20:03 #processed`. Мне казалось, мы ранее решали вопрос с результатом исходной строки после трансформации, должно было быть `- #/1 :: [[222/dfdf]] :: 📅2026-09-16 20:03 #processed`».

**Вы правы, и правило у вас выбрано верное.** У вас стоит `Source line → What happens with your text` = «текст остаётся, кроме слов названия», и для строки-списка оно работало: слова, ставшие именем, уходили, а ссылка вставала на их место. Для строки-заголовка — нет.

**Причина в том, что правило было объявлено дважды и обе записи устарели в один день.** Пока именем из заголовка была строка целиком, вместе с разделителями, снимать со строки было нечего — таких слов в тексте просто не находилось. С правкой прошлой сессии именем стал **текст** заголовка, то есть слово, которое на строке стоит; записи же остались прежние, и каждая — своя: одна у переноса, другая у предпросмотра. Теперь ответ один на обоих.

**Как проверить, за минуту.** `Inline to note` на строке-заголовке с полями. Имя заголовка должно **исчезнуть** со строки, а на его месте встать ссылка на новую заметку; значения полей, отмеченные в `Fields to keep`, остаются.

