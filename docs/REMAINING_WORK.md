# Что осталось до полного завершения технических работ

> **Что здесь есть и чего нет.** Только техническая работа по нынешнему
> плагину: новых функций в списке нет — они живут в `test-vault/Ideas.md` и
> разбираются отдельно. Список собран 2026-09-11 обходом документов, а не по
> памяти: у каждой строки назван источник, чтобы её можно было перепроверить.
>
> **Строка отсюда уходит двумя способами:** работа сделана и подтверждена, или
> заказчик её снял. Зелёная проверка приёмкой не является.

---

> **Где мы стоим после сессии 67 (2026-09-19, вечер).** Сессия прошла четырьмя
> заходами и закрыла его очередь **целиком**: четыре назначенных пункта
> (`З-10`…`З-13`), одиннадцать замечаний трёх заходов (`З-14`…`З-23`) и заказ
> из интервью (`З-24`). Отчёты `252у`…`263у`, разборы PRD
> 10.13.204…10.13.215, исключения к З3 № 131…№ 133.
>
> **Последним заходом — два куска.** Панель больше не дописывает значение
> дочернего Field без родителя (обход строки **6 из 234 → 0**), и появился
> контрол `Sub-field works without parent`: ряд `Child Field` стал выбором из
> трёх, а при `Show always` под ним — ряд `Parent Value`. Третий контрол его
> заказа снят им самим, когда мера показала, что описанный им револьвер уже
> работает.
>
> **Открытых вопросов нет** (`В-150`…`В-153` отвечены). **Назначенной работы
> нет**, но предмет следующей сессии задан его правилом: пока в листе приёмки
> есть пустой чекбокс, сессия идёт в доработку по его комментариям `💬`, а не
> выбирает работу из этого файла. Пустых чекбоксов на закрытии — тринадцать
> (`S1`, `S4`…`S11`).
>
> **Выкладка остановлена его словом** («пока не выкладывай на gh, хочу
> оттестировать перед релизом»): работа лежит локальными коммитами, шаг 4
> завершения сессии не выполняется до его нового слова.
>
> **Открытые находки:** `Ф-7` (подложка Block, пересекающего зрительные строки,
> 1,79 мимо середины) и `Ф-8` (четыре расхождения из 234 при разведённых
> разделителях; совпадающие — его основной случай — нулевые).

---

> **Где мы стояли после сессии 66 (2026-09-19).** Сессия была одной работой:
> разобрать его лист — девять новых пунктов подряд и одно замечание под `C1`.
> **Все десять закрыты**, семь шагов зелёные, обход строки **0 расхождений из
> 234** обеими дорогами.
>
> **Что он подтвердил галочками:** `C2` (память вкладки), `C3` (настройка
> отрисовки доезжает до заметки сразу — этим закрыта находка `Ф-4`), `R2` и `R3`
> (релизы `0.3.1` и `0.3.2`), `P1` (плагин из релиза на чистом vault).
>
> **Что сделано этой сессией.** `C1`: диск сильнее и тогда, когда платформа
> промолчала — причина была не в починке, а в том, что Obsidian зовёт плагин
> только у файла новее его записи, а копия переносит время источника. Пункт 1:
> в корне репозитория не осталось ни одного файла плагина, `docs` похудела с 27
> записей до 21 (исключение к З3 № 131, расхождение измерено сборкой — 28 строк
> из 33 тысяч, все метки esbuild). Пункт 2: кегль Block разведён на две стороны.
> Пункт 3: `Line view` и `Tag view`. Пункт 4: `Global hotkeys` с тремя
> субхедерами. Пункт 5: снята строка про нажатие под предпросмотром прыжка.
> Пункт 6: ссылка в предпросмотре слушает кегль. Пункт 7: заголовок окна больше
> своих подзаголовков. Пункт 8: окно «что изменилось» разбирает markdown.
> Пункт 9: автор в манифесте.
>
> **Отчёты сессии — `242у`…`251у`**, все ждут его слова. Новых строк листа
> девять: `N1`…`N9`, плюс переписанная `C1`.
>
> **Он проверил той же ночью и закрыл шесть строк из девяти галочками** — `N1`,
> `N2`, `N3`, `N6`, `N7`, `N8`. Открытыми остались `N4` (окно «что изменилось»
> показывается один раз на версию), `N9` (страница репозитория — смотреть после
> выкладки) и `N5`, под которым замечание.
>
> **И дописал четыре новых пункта — они и есть очередь следующей сессии**,
> раздел **1н**. Слова дословно — `docs/OWNER_REMARKS.md`, сессия 66, второй
> заход (З-10…З-13).
>
> **Открытых вопросов нет**, но у `З-11` (автоматическая копия `data.json`)
> решение не принято: он сам сказал «я просто набрасываю варианты». Там сначала
> разбор, потом один вопрос интервью, и только потом код.

## 1я. Закрытое: указатель, а не пересказ

Разборы живут в PRD 10.13.* и в `docs/archive/`; здесь только адрес, чтобы
строку можно было найти, не помня её (У-31: пересказ состояния, которого больше
нет, врёт и в архиве).

| Что было строкой очереди | Когда закрыто | Где разбор |
|---|---|---|
| `В-141`: ссылка, которую `Inline to note` ставит вместо его текста, — слово человека, а не значение Block; и найденный тем же обходом дефект панели | 2026-09-18 | 10.13.189, З3 № 129, отчёты `223у`, `224у` |
| Его замечания по листу сессии 62: цвет выбранного значения не доезжал до заметки, кегль Block доставался его тексту, имя строки задержки | 2026-09-18 | 10.13.190 и 10.13.191, отчёты `220у`…`222у` |
| `Н5`: подсветка места, куда прыгнул курсор — шесть строк панели, свой слой над заметкой и живой предпросмотр; исключения к З3 не понадобилось | 2026-09-17 | 10.13.186, отчёт `215у` |
| `Н4`: ссылка на новую заметку в тех заметках, на которые ссылается строка (автоматический MOC), и найденный ею дефект: заметка в корне vault не создавалась вовсе | 2026-09-17 | 10.13.184 и 10.13.185, отчёты `213у`, `214у` |
| Его замечания `Н7` и `Н8` (`H1` и `H2` листа): значение поля со случайным видом не уходило со строки и не попадало в свойства; знак задачи за номером списка становился именем новой заметки | 2026-09-17 | 10.13.182 и 10.13.183, отчёты `211у`, `212у` |
| `Р14`: окно «что изменилось» при обновлении плагина | 2026-09-17 | 10.13.179, отчёт `208у` |
| Его замечания `G2`, `G3`, `G4` из листа приёмки: имя заголовка на строке, правый Block за единственным разделителем и Block поля-ссылки, выравнивание написанного в Block | 2026-09-17 | 10.13.176…10.13.178, З3 № 127, отчёты `202у`, `203у`, `207у` |
| Находки `Н1`…`Н3` сессии 59, закрытые его ответами В-132…В-134 | 2026-09-17 | 10.13.177, отчёты `204у`…`206у` |
| Очередь сессий 51…56 целиком: формы ссылки и тега, настройка человека в коде, имена, объявленные дважды (23 → 1), значение чужого поля в своём Block, зависимое поле при смене родителя | 2026-09-14…16 | З3 № 85…№ 119, отчёты `149у`…`191у`, PRD 10.13.122…10.13.165 |
| Пять мест молчания «охрана наоборот» и два, найденных тем же обходом | 2026-09-16 | 10.13.166, З3 № 120, отчёт `192у` |
| Пятая форма: положительная охрана без иначе, одиннадцать мест (В-119) | 2026-09-16 | 10.13.167, З3 № 121, отчёт `193у` |
| `Р10`: пятислойная передача нормализатора ключа Order (плюс три копии в форме присваивания, сторож на неё по свойству) | 2026-09-16, вечер | 10.13.168, З3 № 122, отчёт `194у`, урок У-204 |
| `Р11`, первая половина: мёртвая `normalizeImportanceTokenShape` | 2026-09-16, вечер | 10.13.169, З3 № 123, отчёт `195у` |
| `Р13`: недостижимый ход команд тегов, 52 строки, и два его жителя | 2026-09-16, вечер | 10.13.170, З3 № 124, отчёт `196у` |
| `Р6`, `Р7`, `Р8`, `Р9`: четыре расхождения между дорогами | 2026-09-16, вечер, его решением | В-123 «оставить под сторожами»; меры — `node tools/form_divergence.js`, пины в `runtime_unified_parity_tests.js` |
| Строка `"- "` не переживала собственный разбор (было 6 из 154) | 2026-09-15 | 10.13.151 и 10.13.155, З3 № 110, отчёт `180у` |
| Строка-заголовок: панель и команда давали разное (было 4 из 180 и 6 из 162) | 2026-09-15 | 10.13.156, З3 № 111, отчёт `181у` |
| «Что такое наша метка»: шесть объявлений, двое отвечали пустотой | 2026-09-15, ночь | 10.13.158, З3 № 113, отчёт `183у`, строка листа `S47` |
| Четыре кандидата долга копий: один сведён, три названы числом | 2026-09-15, ночь | 10.13.159, З3 № 114, отчёт `184у` |
| `composeToken` объявлен четырежды (73 расхождения из 136 пар) | 2026-09-15 | 10.13.152, З3 № 107, отчёт `177у` |
| `normalizeCheckboxToken`: два вопроса под одним именем (В-118) | 2026-09-15 | 10.13.144 и 10.13.153, З3 № 108, отчёт `178у`, правило 117 |
| Ревизия кода 2026-09-14: шесть строк долга, все закрыты | 2026-09-14…15 | `docs/CODE_REVIEW_2026-09-14.md`, раздел 6 |
| Снятие служебного файла правил, все четыре шага | 2026-09-11…14 | PRD 10.13.52, П-8…П-13 |
| Браузерный шаг в CI: падало у них, зелено у меня | 2026-09-13 | PRD 15.7, У-176 |
| Сессии 45…48: подложка, `Ctrl+Z`, знаки начала строки | 2026-09-13…14 | `docs/archive/AWAITING_OWNER_CHECK_session45.md` и `…session47.md` |
| Решённое им 2026-09-11 (подсветка строки, гифки, мобильные, шум линтера, шов в `tagwheel.js`) | 2026-09-11 | `docs/archive/ANSWERED_QUESTIONS.md`, PRD 15.7 |
| `Ctrl+Z` после сессии панели возвращал то, чего не было (Р5) | 2026-09-13 | закрыто его галочкой под `S15`; стенд `node tools/undo_bench.js` |

## 1к. Новых функций в очереди нет

**Обе его новые функции сделаны 2026-09-17** (сессия 61). Разборы до кода,
лежавшие здесь, сняты: они описывали состояние, которого больше нет, и в архиве
врали бы так же (У-31). Где читать теперь:

| № | Что | Где разбор |
|---|---|---|
| Н4 | Ссылка на новую заметку в тех заметках, на которые ссылается строка | PRD 10.13.184, отчёт `213у`, строка листа `N1` |
| Н5 | Подсветка места, куда прыгнул курсор | PRD 10.13.186, отчёт `215у`, строка листа `N3` |

**Р14 закрыт 2026-09-17** (окно «что изменилось» при обновлении): разбор —
10.13.179, отчёт `208у`.

**Исключений к З3 обе функции не потребовали ни одного.** У Н5 разбор его
обещал, и это оказалось гипотезой: сигнал «курсор переехал прыжком» проходит
через обёртку команд вне запрета (У-210, правило 132).

## 1н. Назначенная работа: его ночь на 2026-09-19

**Очередь пройдена целиком 2026-09-19.** Четыре его пункта (`З-10`, `З-13`,
`З-12`, `З-11`) и оба замечания третьего захода (`З-14`, `З-15`) — ночью; пять
замечаний второго захода (`З-16`…`З-20`) — днём, тем же порядком: разбор,
измерение, правка, мутация.

**Что дал второй заход.** `З-16`: порядок в `Line view` вернулся к его пункту 3 —
его «над» я прочёл наоборот (У-232). `З-17`: автокопия перестала реагировать на
переключение вкладки панели — причина была в теле `selectParts` (У-233). `З-18`:
уведомление о снятой копии. `З-19`: его «правка `data.json` не видна» оказалась
мёртвой ветвью `pkm.fields.taxonomy.tagWheelConfig`, и она снята (У-234).
`З-20`: подсказка в заметке копии — коллаутом. Отчёты `257у` и `258у`, разборы
10.13.205 и 10.13.210. У `З-11`
решение было продуктовым — он ответил интервью той же сессией («при открытии
плагина, если файл изменился»), и работа сделана тем же заходом. `З-15` он закрыл
словом «оставить как есть»: служебные файлы остаются в корне.

| № | Что | Состояние |
|---|---|---|
| ~~`З-10`~~ | `tags-block-fill` со своими дочерними встал **над** `tags-text-size-left` | **сделано 2026-09-19**, отчёт `252у`, разбор 10.13.205, строка листа `S3` |
| ~~`З-13`~~ | пять имён контролов полосы | **сделано 2026-09-19**, отчёт `253у`, разбор 10.13.206, строка листа `S2`. Слово `Stripe` стало сущностью — с заглавной везде; прежние подписи ушли в `searchTerms` и запрещены в документах |
| ~~`З-12`~~ | новый контрол `Stripe direction`: `Left`, `Right`, `Both` | **сделано 2026-09-19**, отчёт `254у`, разбор 10.13.207, строка листа `S1`. Правило «получает ли сторона полосу» объявлено один раз на обе отрисовки |
| ~~`З-11`~~ | автоматическая копия `data.json`, контрол `Autosave` в `settings-backup` | **сделано 2026-09-19** по его ответу на `В-147` («при открытии плагина, если файл изменился»): отчёт `256у`, разбор 10.13.209, строка листа `S5`. Прежняя цена была неверной и поправлена: запись отложена на 250 мс, протяжка слайдера доходит до диска одной записью, а не сотнями |
| ~~`З-14`~~ | окно «что изменилось»: нумерация, одна строка на пункт, пропущенные выпуски, ссылка | **сделано 2026-09-19**, отчёт `255у`, разбор 10.13.208, строка листа `S4`. Открытым остался только адрес ссылки — `В-149` |
| ~~`З-15`~~ | убрать ли из корня `.json` и `.mjs` | **закрыто его словом 2026-09-19: «оставить как есть».** Четыре файла из шести переехать не могут, и выигрыш от двух движимых — две строки на странице против флага в каждой команде |

## 1р. Пройденная очередь ревизии: восемь строк, три из них разрешены его словом

**Перечень целиком — `docs/AUDIT_2026-09-18.md`, раздел 5.** Здесь только
очередь и состояние, чтобы не держать два списка (У-32). У каждой строки там
названы цель, ценность, цена, риск регрессии и чем закрепляется.

**Очередь:** ~~`Р-2`~~ → ~~`Р-13`~~ → ~~`Р-7`~~ → ~~`Р-10`~~ → ~~`Р-11`~~ → ~~`Р-8`~~ → ~~`Р-12`~~ → `Р-6` (начат, три файла осталось).

| № | Что | Состояние |
|---|---|---|
| `Р-2` | `onExternalSettingsChange`: внешняя правка `data.json` больше не затирается | **сделано 2026-09-19**, отчёт `233у`, разбор 10.13.196, строка листа `C1` |
| `Р-13` | панель настроек помнит открытую вкладку | **сделано 2026-09-19**, отчёт `234у`, разбор 10.13.197, строка листа `C2` |
| `Р-7` | покрытие снимать и на браузерной дороге | **сделано 2026-09-19**, отчёт `235у`, разбор 10.13.198: 230 одной дорогой против 203 двумя |
| `Р-10` | разделить `CLAUDE.md`: целиком читается меньшая часть | **сделано 2026-09-19**, отчёт `236у`, разбор 10.13.199: 151 889 → 116 037 знаков |
| `Р-11` | свести две копии списка с перетаскиванием | **сделано 2026-09-19**, отчёт `237у`, разбор 10.13.200: расхождение измерено, ноль |
| `Р-8` | выровнять ответ «надо ли перерисовать» в слое оформления | **сделано 2026-09-19 объяснением, а не выравниванием**: измерение показало, что выравнивание сломало бы доставку настроек. Отчёт `238у`, разбор 10.13.201 |
| `Р-12` | найти, где панель теряет время на очень длинной строке | **сделано 2026-09-19**: место названо профилём и снято, 1263 → 49 мс на 20 000 знаков. Отчёт `239у`, разбор 10.13.202 |
| `Р-6` | проверка типов на JavaScript вне З3 | **начато 2026-09-19**: включена на четырёх файлах ядра (30 правок, живых дефектов ноль), отчёт `240у`, разбор 10.13.203. Осталось по мере: `shared_utils.js` 126, `config_normalize.js` 51, `config_write.js` 24 |

**Сделано ревизией той же ночью:** `Р-1` (правка настроек не пропадает при
выключении плагина), `Р-3` (сторож на объявленное и незваное), `Р-4` (снятие
мёртвых имён), `Р-5` (снят недостижимый запасной ход, исключение к З3 № 130).

**В-141 исполнен 2026-09-18.** Признак для разведения он назвал верно — ссылка
считается значением поля, только когда она значением названа. Исключение к З3
№ 129, отчёты `223у` и `224у`, обход строки 0 расхождений из 234. Второй отчёт —
дефект, которого никто не искал: панель теряла из правого Block всё, чего не
строила сама, и нашёл его обход в первый же прогон с новыми формами (У-185).

## 1ф. Найдено по ходу очереди — не назначено, разбор сделан

**Три находки сессии 65, все на дороге `Р-2` и `Р-13`.** Ни одна не
починена: у первой цена видна на экране, вторая и третья — указатели, которые
стоит держать до своей работы. Пишутся сюда, чтобы следующая сессия не начинала
разбор заново (У-31).

| № | Что нашлось | Чем снято | Что с этим делать |
|---|---|---|---|
| `Ф-1` | **Перерисовка панели по подписке на хранилище мертва опечаткой.** `plugin_bootstrap.js` пишет вкладку в `plugin._settingTab`, а подписка читает `plugin._settingsTab` — имени с этим написанием не пишет никто. То есть `renderSettingsTab` не делал ничего ни дня, а вся обвязка вокруг него (откладывание перерисовки, пока фокус в поле ввода) исполнялась впустую | грепом по репозиторию: писатель один, читатель один, имена разные | **Не чинить опечаткой.** Цена починки видна на экране: `setControlValue` нарочно **не** пересобирает определения на каждой записи — пересборка заменяет узел слайдера и обрывает перетаскивание, — а подписка сработала бы на **каждую**. То есть «когда перерисовывать панель» объявлено дважды, и оживление мёртвой копии сломало бы живую (У-150). Решать надо не опечатку, а какая из двух копий права; предмет живой: восстановление копии настроек и записи редактора Fields меняют **определения**, и панель их сегодня не пересобирает |
| `Ф-2` | **`isUiOnlyReason` стережёт причины, которых никто не называет.** В списке стоят `settings:tab`, `settings:visual-subtab` и `settings:hotkeys-subtab` — имена старой панели; подвкладок нет, а вкладку новая панель пишет путём `settings:ui.activeSettingsTab`. И шире: обычная запись из панели идёт `store.update` напрямую, мимо `applyPatch`, то есть до этого списка не доходит вовсе | чтением шва `ConfigStoreAdapter.set` и грепом по причинам записи | Разобраться вместе с `Ф-1`: оба про то, что делает запись из панели помимо самой записи. Сегодняшнего дефекта нет — список просто не срабатывает |
| `Ф-5` | **Строка в 40 000 знаков роняет команды важности**: `Invalid regular expression: too large` — где-то целая строка уходит в выражение как один токен | пробой на прочность: форма дописана в обход, падений 2 из 342. Старше правки `Р-12` — воспроизведено на рабочем дереве до неё (У-187) | Дорога под З3 (`status_*`), цена не измерена. Порядок обычный: полный разбор → измерение → его слово либо В-120 при нулевом расхождении. Край, а не повседневность: сорок тысяч знаков в одной строке |
| ~~`Ф-4`~~ | **Закрыто его галочкой 2026-09-19** («ок, сразу» под строкой `C3`): настройка отрисовки доезжает до открытой заметки сразу, чинить шов не нужно. Разбор оставлен, потому что `Ф-1` и `Ф-2` про то же место. **Обычная запись контрола до заметок не доходит.** У панели две дороги записи: контрол пишет `store.update` напрямую, свой блок — через `setConfigPatch`. Пересборку открытых заметок зовёт только вторая | пробоем со счётчиком: ноль вызовов против одного, контроль внутри | Цвета доезжают переменными CSS и живы; всё, что живёт в самой отрисовке, ждёт ближайшего касания заметки. Видно ли это на его экране — **решает его глаз**, строка в разделе 3. Чинится там же, где `Ф-1` и `Ф-2`: все три про то, что делает запись из панели помимо самой записи |
| ~~`Ф-6`~~ | **Панель дописывала значение дочернего Field без родителя в правый Block** | обход строки 6 из 234 | **закрыто 2026-09-19 его словом**: исключение к З3 № 132, разбор 10.13.214, отчёт `262у`. Обход строки 0 из 234 |
| `Ф-8` | **При разведённых разделителях остаются четыре расхождения из 234** (`node tools/line_matrix.js --split`). До правки исключения № 132 их было десять | тот же обход с ключом `--split` | Другой род: совпадающие разделители — его основной случай, и там ноль. Разбор не начат |
| `Ф-7` | **Подложка Block, который сам пересекает зрительные строки, стоит на 1,79 точки мимо середины написанного.** Прежде было 3,06 — правка уровня 2026-09-19 уменьшила промах, но не сняла | браузерный шаг, строка 13 страницы; порог утверждения 2 назван измерением | Отдельный предмет: у такого Block два ряда, и «своё написанное» у него считается по первому. Цена не измерена |
| `Ф-3` | **`SETTINGS_TABS` в `config_normalize.js` — список вкладок старой панели:** `hotkeys` там, где у новой `keyboard` | прогоном: `migrateConfig` на файле версии 2 имя `keyboard` не трогает, потому что кламп живёт только в первой ступени | Достижимого случая у расхождения нет, и новая панель отбирает вкладки сама (10.13.197). Снимать список — уборка, и делать её стоит вместе с `Р-4`, где решается судьба ключей без контролов |
## 2. Ждёт решения заказчика — работа начнётся после ответа

**Здесь только то, у чего цена измерена, а решение продуктовое.** Ни одна
строка не начата: начинать без его слова значило бы менять поведение там, где
об этом никто не просил (его рамка — «уборка, но не хочу риска дефектов»).

**Четыре строки ушли отсюда его решением, а не работой** (В-123, 2026-09-16):
`Р6`, `Р7`, `Р8` и `Р9` — те, где две половины плагина отвечают по-разному, то
есть сведение меняло бы поведение. Его слово: «оставить под сторожами». Ни одна
из них не может разойтись молча: расхождения закреплены пинами в
`runtime_unified_parity_tests.js`, а мера — `node tools/form_divergence.js`
печатает их числом каждый прогон. Строки перечислены в разделе **1я** со
ссылками на меры; возвращаются сюда только его новым словом.

**Ни одной строки здесь больше нет.** Последняя — `Р4`, минификация сборки —
ушла 2026-09-18 его ответом на В-144: «да, минифицируем перед подачей в
каталог». Решение принято, срок назван, и работа переехала в раздел 4, к
подаче. Прежнее слово В-95 («бету не минифицировать — нужно упростить работу по
отлавливанию багов») этим не отменено, а получило срок.

Новые вопросы из ревизии сюда не попали нарочно: он ответил на них той же
ночью, и работа по ним стоит очередью в разделе **1н**, а не ожиданием здесь.

## 3. Ждёт его глаза — проверить и сказать слово

| № | Что | Как проверить |
|---|---|---|
| П1 | **`README.md` и `instructions.md`** (строка `F3` листа приёмки) | Открыть оба и посмотреть, не обещают ли они того, чего нет, и не молчат ли о том, что есть |
| П2 | **`FEATURES.md`** (строка `F4`) | То же: лист возможностей читают те, кто проверить написанное не может |
| П3 | **Вычитка заметки-руководства** (В-2) | Черновик написан 2026-08-29, девять разделов. Заметка принадлежит человеку — плагин её больше не переписывает |
| П4 | **Работа плагина на настоящем Obsidian после `0.3.2`** | **Через BRAT:** последний обычный релиз — `0.3.2` (2026-09-19), и вложениями он совпадает с локальной сборкой побайтно. Всё, что копилось с `0.2.0` — две новые функции, его починки и уборка, — теперь в выложенном релизе. Чистый vault имеет смысл проверить именно сейчас: здесь никакая ревизия глаз не заменит |
| П5 | **Отчёты 52у–88у реестра** | Работа 9–11 сентября, у которой нечего нажать: ревизия кода, тихие отказы, мёртвый код, две беты, лист возможностей. Строка на отчёт — в `docs/AWAITING_OWNER_CHECK.md`, разборы — в `docs/archive/AWAITING_OWNER_CHECK_reports_2026-09-09_11.md`. Подтверждаются словом, а не галочкой |
| ~~П6~~ | **Закрыто его галочкой 2026-09-19:** «ок, сразу». Настройка отрисовки доезжает до открытой заметки сразу, шов чинить не нужно | — |
| ~~П8~~ | **Закрыто его словом 2026-09-19:** «да, поменять на Roman Kuznetsov». В `LICENSE` теперь то же имя, что в манифесте | — |
| ~~П9~~ | **Закрыто его словом 2026-09-19:** «оставить в корне». Папки `tests` и `tools` остаются там, где их ищут все инструменты | — |
| П7 | **Снять русское зерно текстов** (`В-145`, его слово «тексты панели должны быть на английском») | Решение продуктовое и стоит одной фразы. Сегодня зерно не видно нигде: плагин кладёт только английский `default.js`. Снятие заморозит старый `ru.js` у того, у кого он ещё лежит от сборок до `0.1.0-beta.4` — то есть, возможно, только у вас. Скажете «снимай» — сниму за один заход |

## 4. Отложено его словом — в списке, чтобы не потерялось

- **Мобильные устройства** — «реализовать позже». Флаг «только для компьютера» решено оставить (2026-09-11), и снимать его имеет смысл только вместе с проверкой на телефоне.
- **Переключатель `AND` / `OR` внутри одного типа Field в Smart Rules** (C51) —
  «в дальнейшем»: сегодняшнее поведение подтверждено галочкой. Формально это
  пункт фазы 5.3, по существу — новая функция.
- **Подача в каталог Obsidian.** Блокеры фазы 6 закрыты, правила каталога
  сходятся все; сама подача — его решение и его аккаунт.

  **В подачу входит минификация сборки** — его слово 2026-09-18 (В-144): «да,
  минифицируем перед подачей в каталог». Раньше этого не делаем: прежний довод
  «легче ловить баги» (В-95) остаётся верным до самой подачи. Замер, на котором
  стоит решение: разбор сборки 19.1 мс против 13.7 мс, размер 1845 КБ против
  957 КБ — то есть выигрыш в размере, а не в скорости.

  **И вместе с флагом `minify: true` подача обязана ответить, чем ловить баги
  после.** Вся отладка его дефектов идёт через номера строк в журнале консоли, а
  минификация их обессмысливает. Это разбор при подготовке подачи, и начинать
  его раньше незачем.
- **Две ссылки в карточке плагина** — отложены 2026-09-11 до его адресов.
- **`TagWheel` в произвольном месте строки** (пункт 8 его файла идей). Его слово
  2026-09-13: «это из старой версии плагина функция по вызову tagwheel в
  произвольном месте строки… однажды я хочу вернуться к этой функции, но пока
  можешь снять». Старая реализация — `tests/TriggerWheel`, 464 строки; она
  **снята шагом 4 в ночь на 2026-09-14** и остаётся в истории git: коммит снятия
  назовёт, откуда её достать. Возвращаться к ней как к коду не стоит: расхождений с нынешним
  TagWheel накопилось больше, чем сходств, — это заказ на функцию, а не на
  восстановление файла.

## 5. Долгов, закреплённых числом, не осталось

Проверено прогоном 2026-09-15, а не памятью:

- настройка человека, записанная в коде готовой — **0 мест**
  (`config_literals_tests.js`, список долга пуст, домов умолчания два и оба
  названы поимённо);
- тихие отказы без объяснения — **0** (`silent_failures_tests.ts`, список
  `DEBT` пуст);
- мёртвые экспорты — **0 из 866 имён** (`dead_exports_tests.js`). **Число имён
  выросло с 738 не от работы, а от починки самого сторожа:** его маска давала
  обратной кавычке искать пару до конца файла, а такая кавычка стоит внутри
  регулярного выражения — сторож видел 20 227 знаков из 85 362 и проверял три
  четверти кода, не зная об этом (У-192, PRD 10.13.137);
- имена, объявленные в рантайме больше одного раза — **28** против 71.
  **Чем опущено с 30 (У-118):** одно сведением — склейка приставки со
  значением, четыре тела в один дом (10.13.152); два разведением имён, где
  копии не было вовсе — знак задачи (10.13.153) и шаг по циклу (10.13.154).
  **И это нижняя граница, а не число:** мера видит только объявления верхнего
  уровня — 1217 из 1522; вложенные функции и стрелочные замыкания в счёт не
  попадают, а самые крупные находки ночи на 2026-09-15 жили именно там
  (10.13.148). Прогон говорит это сам.
  **И мера видит не всё:** одно правило бывает объявлено под разными именами,
  и тогда она считает их разными предметами. Так было с ключом Order — шесть
  тел под тремя именами, из которых мера видела четыре (10.13.146). Такие
  находятся поиском **по телу**, а не по имени
  (`node tools/rule_copies.js`): точных копий не осталось ни одной, а признак
  делегата научился видеть **четыре** формы обращения к дому — четвёртую,
  «связать, охранить отказом, вернуть», он не видел до 2026-09-15 и держал
  два честных делегата настоящими телами (У-191, второй раз);
- `no-unused-vars`, `no-undef` — **0 ошибок**;
- инлайновое оформление — **0** (`catalog_rules_tests.ts`);
- `var` вне движков — **0**;
- правила каталога Obsidian: расходится **ни одного**, четыре по букве с
  названной причиной;
- открытых вопросов заказчику — **ни одного**: В-114, В-115 и В-117 отвечены и
  исполнены, разборы унесены в архив.

**Одно новое место, найденное 2026-09-13 и уже решённое им:** под `tests/`
лежит `TriggerWheel` — 464 строки, копия движка со своим разбором заметки
правил. В продукт не входит, но это ровно то дублирование, ради снятия которого
задан порядок 1а. Его ответ: снять вместе с шагом 4, саму функцию («TagWheel в
произвольном месте строки») записать в задачи — сделано, раздел 4.

**Числа, которые растут от обычной работы, живут в прогоне, а не здесь**
(У-145): перепись записей в консоль, `var` под З3 и размер `styles.css`
печатает `catalog_rules_tests.ts`, число подмен — `npm run gate:browser`.
