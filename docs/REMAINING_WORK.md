# Что осталось до полного завершения технических работ

> **Что здесь есть и чего нет.** Только техническая работа по нынешнему
> плагину: новых функций в списке нет — они живут в `test-vault/Ideas.md` и
> разбираются отдельно. Список собран 2026-09-11 обходом документов, а не по
> памяти: у каждой строки назван источник, чтобы её можно было перепроверить.
>
> **Строка отсюда уходит двумя способами:** работа сделана и подтверждена, или
> заказчик её снял. Зелёная проверка приёмкой не является.

---

## 1. Моя работа: можно делать прямо сейчас

| № | Что | Почему это осталось | Цена |
|---|---|---|---|
| Т1 | **Ревизия, заход 6: фикстуры, которые не могут увидеть дефект** (У-47) | Обходом такое не ищется: признак не в форме, а в смысле — фикстура, где два имени совпадают, слепа к их расхождению. Только чтение по одной | заход, ~56 файлов набора |
| Т2 | **`showcase.md`: 17 гифок показывают старую панель** | Отчёт `## What is out of date` лежит в самом файле с 2026-08-31. Гифки — материал заказчика, перезаписывать без его слова нельзя, но **подписи** к 16 записям, называющие снятое, поправить можно | час на подписи; гифки — его решение |
| Т3 | **`tagwheel.js`: четыре модуля берутся со шва внутри главного входа** | Два из них подключены литерально строкой выше, а сам файл их в шов и публикует. Отказ везде громкий — это ясность, а не дефект (PRD 15.7, заход 3) | правка под З3, спрашивается отдельно |
| Т4 | **`npm run gate:browser` не в CI** | Решение заказчика 2026-09-08: Chromium — 115 МБ на машину. Седьмой шаг гоняется руками, и это значит, что он может быть забыт | день + зависимость в CI, если решит включить |

## 2. Ждёт решения заказчика — работа начнётся после ответа

| № | Вопрос | Что стоит за ним | Где разбор |
|---|---|---|---|
| Р1 | **Обрамление `==` у TagWheel: вернуть или признать ушедшим** | Ключ `rules.ui.activePanel.useHighlight` в движке есть, контрола нет нигде, ветки нет в умолчаниях. Либо настройка описывается и попадает в схему, либо ключ вычищается вместе с мёртвой веткой. Заказчик приносил это замечанием («при активации TagWheel строка не выделяется цветом») | PRD Приложение A, В8; разбор — Н-4 |
| Р2 | **Умолчания приоритета Prefix: схема против движка** | `Field order source` — схема `auto`, движок `manual`; `Parent or child wins` — схема `tag-over-subtag`, движок `subtag-over-tag`. Либо схема правится под движок (ничьё поведение не меняется), либо умолчание меняется сознательно, и часть строк у части людей начнёт писаться иначе | PRD Приложение A, В11; разбор — 8.3 |
| Р3 | **Служебный файл правил `generated_rules.md`** | Разбор написан целиком и кода по нему нет по его же решению (В-72): движки на прямое чтение конфига не переводятся. Файл остаётся мостом между конфигом и движками | PRD 10.13.52 |
| Р4 | **Минификация сборки** | Бету решено **не** минифицировать (В-95, 2026-09-10): «нужно упростить работу по отлавливанию багов». Срок — перед выходом наружу, и он за ним | реестр 28у |
| Р5 | **Две ссылки в карточке плагина** (`authorUrl`, `fundingUrl`) | Решено «пока не заводить» (В-75, 2026-09-07): пустая строка на экране неотличима от отсутствующего поля, а `fundingUrl` сразу рисует кнопку `Donate`. Пин держит запрет и говорит, чем он снимается | PRD фаза 6, пункт 4 |
| Р6 | **`isDesktopOnly: true` при нулевом использовании API Node** | Каталог просит ставить флаг только тем, кто и правда зовёт Node. Мы не зовём, но мобильные не проверялись ни разу — снять флаг значит пообещать то, что не проверено | каталог, Б4 |
| Р7 | **17 гифок `showcase.md`** | Перезаписывать его материал без слова нельзя | PRD фаза 5.1, пункт 4 |

## 3. Ждёт его глаза — проверить и сказать слово

| № | Что | Как проверить |
|---|---|---|
| П1 | **`README.md` и `instructions.md`** (строка `F3` листа приёмки) | Открыть оба и посмотреть, не обещают ли они того, чего нет, и не молчат ли о том, что есть |
| П2 | **`FEATURES.md`** (строка `F4`) | То же: лист возможностей читают те, кто проверить написанное не может |
| П3 | **Вычитка заметки-руководства** (В-2) | Черновик написан 2026-08-29, девять разделов. Заметка принадлежит человеку — плагин её больше не переписывает |
| П4 | **Работа плагина на настоящем Obsidian после `0.1.0-beta.6`** | Поставить обновление через BRAT и убедиться, что включается и работает как работал. Здесь никакая ревизия глаз не заменит |
| П5 | **Разделы 64у–71у реестра** | Отчёты о работе, у которой нечего нажать; подтверждаются словом, а не галочкой |

## 4. Отложено его словом — в списке, чтобы не потерялось

- **Мобильные устройства** — «реализовать позже». Связано с Р6.
- **Переключатель `AND` / `OR` внутри одного типа Field в Smart Rules** (C51) —
  «в дальнейшем»: сегодняшнее поведение подтверждено галочкой. Формально это
  пункт фазы 5.3, по существу — новая функция.
- **Подача в каталог Obsidian.** Блокеры фазы 6 закрыты, правила каталога
  сходятся все; сама подача — его решение и его аккаунт.

## 5. Долгов, закреплённых числом, не осталось

Проверено прогоном 2026-09-11, а не памятью:

- тихие отказы без объяснения — **0** (`silent_failures_tests.ts`, список
  `DEBT` пуст);
- мёртвые экспорты — **0** (`dead_exports_tests.js`);
- `no-unused-vars`, `no-undef` — **0 ошибок**;
- инлайновое оформление — **0** (`catalog_rules_tests.ts`);
- `var` вне движков — **0**;
- правила каталога Obsidian: расходится **ни одного**, четыре по букве с
  названной причиной;
- открытых вопросов сессий — **нет**.

**Числа, которые растут от обычной работы, живут в прогоне, а не здесь**
(У-145): перепись записей в консоль, `var` под З3 и размер `styles.css`
печатает `catalog_rules_tests.ts`, число подмен — `npm run gate:browser`.
