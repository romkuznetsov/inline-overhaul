# Сделано, но заказчиком не проверено

> **Проверка ведётся в `test-vault/Приёмка.md`.** Этот файл — реестр: что
> сделано, где смотреть и чем закреплено машиной. Заполненный лист я разбираю,
> подтверждённое убираю отсюда, а из самого листа вычищаю подтверждённые
> пункты, чтобы там оставалось только живое.

**Реестр на 2026-09-08, десятый заход.** Девятым заходом заказчик закрыл
галочками **семь строк из восьми** — подсказки у всех подписей (`T1`, `T2`),
перевод панели (`T3`), приглушённую заливку шапок (`V1`) и все три починки
(`D1`, `D2`, `D3`), — замечаний под ними не написал. Эти разделы (30у, 31у,
32у, 35у) вместе с четырьмя отчётами уехали в архив уборкой того же вечера.

**Ждут проверки три строки листа — `F3`, `S3` и `S4`.** `F3` — README и
руководство («не смотрел» второй заход), `S3` — четвёртый и пятый режимы
`Ctrl+A`, `S4` — вставка под заданный заголовок в Transform; обе сделаны
сессией 27 (разделы 41у и 42у). Всё остальное прошлой сессии он
закрыл галочками в тот же вечер: `K1`, `K2`, `S1`, `S2` — разделы 37у, 39у и
40у. Раздел 38у — отчёт про браузер в наборе, проверять в нём нечего.

**Открыто два вопроса — В-92 и В-93.** В-92: подсказка шапки списка Fields
раскрывается в колонку 188 точек; нашёл его браузерный гейт в первый же прогон,
и это та самая подсказка, в которую унесён смысл `Left Block` и `Right Block`.
В-93: чтобы `Ctrl+A` выделял слово, тронут один из защищённых файлов — на одну
строку, — и это обычное «оставить ли сделанное».

**Из семи его новых задач сделаны четыре**, все семь разобраны (разборы —
`docs/OWNER_REMARKS.md`, сессия 26). Осталось три, и по каждой уже есть его
решение по спорному месту: `Advanced settings` у Smart Rules, сворачивание
правил и заливка Left и Right Block своим слоем за текстом.

**Одно его уточнение стоит держать на виду:** маркер, который панель TagWheel
оставляет на строке, обязан соответствовать **префиксу этой строки**, а не
быть литеральным `- `. У него для тега `#todo` префикс `- [ ] `, и панель
обязана оставлять именно его. Так и работает; закреплено утверждением.

### Разделы 37у, 39у, 40у — **подтверждены заказчиком 2026-09-08, вечер**

Четыре решения про каталог Obsidian (`K1`, `K2`), `Del` на пустой строке
(`S1`) и тумблер `Step out of the word` (`S2`) — все четыре строки закрыты
галочками, замечаний под ними нет. Отчёты унесены в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 42у. Transform: вставка под заданный заголовок — **сделано 2026-09-08, поздний вечер**

Ваша четвёртая задача из семи. Строка листа — `S4`.

**Что теперь есть.** В `Transform → Note content → Where to put the text`
третий пункт — **`At custom header`**, и под ним две строки, которые появляются
только при нём:

- **`Type name of header`** с плейсхолдером `# Header name`;
- **`If header not found`** — `At the beginning` или `At the end`.

**Куда именно ложится запись.** В **конец секции** названного заголовка — ваше
решение. Всё, что уже стоит под этим заголовком, остаётся выше, а следующий
заголовок ниже не двигается.

**Как пишется имя.** Так, как заголовок стоит в заметке. Решётки — часть
запроса, а не украшение: `## Log` найдёт заголовок ровно второго уровня, `Log`
— любого. Регистр не важен. Два одноимённых заголовка — берётся первый.

**`Line above the text` работает по-прежнему.** При `Fixed text` и
`Date and time` строка над записью создаётся, только теперь внутри найденной
секции — как вы и просили.

**И то же самое при дописывании в существующую заметку.** Если `If the name
already taken` = `Add to the existing one`, запись тоже ложится в конец секции,
а не в конец файла. Без этого положение работало бы только на новых заметках.

**Как проверить, за пять минут.** Заведите заметку с двумя заголовками и
что-нибудь под каждым. Поставьте `At custom header`, впишите имя первого
заголовка, нажмите inline2note на строке — запись должна встать последней в
секции первого заголовка, а не в конце заметки. Потом впишите имя, которого в
заметке нет, и проверьте оба положения `If header not found`.

**Чем закреплено машиной.** Двенадцать проверок движка: найденный заголовок,
имя с решётками и без, заголовок последней строкой, два одноимённых, оба
запасных положения, пустое имя, строка над текстом внутри секции, тот же путь
при дописывании, решётка внутри frontmatter, нормализация. Плюс проверка на
сам шов: запись зовётся с поддельным vault, и она отвечает на вопрос, доехала
ли настройка от конфига до заметки. Одиннадцать мутаций краснеют.

### 41у. `Ctrl+A`: режимы `word` и `Custom` — **сделано 2026-09-08, поздний вечер**

Ваша третья задача из семи. Строка листа — `S3`.

**Что теперь есть.** В `Keyboard → Expanded 'Ctrl+A' → Selection steps` два
новых пункта:

- **`Word, line, tree, heading, then note`** — первое нажатие берёт слово у
  курсора, дальше как раньше;
- **`Custom`** — под списком открывается табличка из пяти галочек
  (`word`, `line`, `tree`, `heading`, `note`), видом та же, что `Fields to
  keep` в Transform. Отмеченные и есть ступени цикла.

**Порядок ступеней всегда один** — `word`, `line`, `tree`, `heading`, `note`, —
а галочки выбирают, на каких из них нажатие остановится. Ваш пример
(`word`, `line`, `note`) идёт ровно этим порядком.

**Слово у каретки** берётся так: стоит курсор внутри слова или вплотную к нему
— это слово; стоит на пустом месте — ближайшее, а при равном расстоянии левое.
Слов в строке нет вовсе — ступень пропускается.

**Сняв все галочки, вы возвращаете клавишу Obsidian:** одно нажатие, вся
заметка. Блок так и говорит, а не молчит.

**Как проверить, за пять минут.** Включите `Expanded 'Ctrl+A'`, выберите
четвёртый режим, поставьте курсор в середину слова и нажмите `Ctrl+A` пять раз
подряд: слово → строка → дерево → секция заголовка → заметка. Потом выберите
`Custom`, снимите `tree` и `heading` и повторите: должно стать три шага.

**Чем закреплено машиной.** Последовательности всех пяти режимов на одной
заметке, `Custom` с тремя галочками, с одной и без единой, слово у каретки на
краях строки и на знаке препинания; галочки переживают миграцию конфига;
браузер открывает панель, переключает режим на `Custom` и меряет получившийся
список. Девять мутаций краснеют, десятая — на браузерном гейте.

**Одно место требует вашего слова — В-93.** Правило «где кончается слово» уже
жило в плагине, в защищённом файле навигации. Оно переехало в общий файл, и в
защищённом изменилась одна строка. Поведение навигации не менялось; откат —
одна строка.

### 38у. Браузер в наборе — **сделано 2026-09-08, вечер**

Ваш ответ: «добавить сейчас отдельным куском», в CI пока не включать.

**Что теперь есть.** Седьмой шаг проверок — `npm run gate:browser`. Он
открывает прототип панели настоящим Chromium и спрашивает **вычисленные
величины**, а не дерево: видит ли браузер открытую подсказку, во всю ли ширину
своего блока она встала, какой контраст у текста шапки против получившейся
заливки, в одну ли строку встала подпись слайдера. На все семь вкладок — 151
подсказка, 7 шапок, 7 подписей.

**Почему это важно.** Заглушка, на которой работают остальные проверки, не
считает ни одного пикселя. За неделю через неё прошли и были найдены **только
вашими глазами**: шесть подсказок в скрытом узле, подсказки шириной с колонку,
шапки серым по серому и знак `%`, уехавший на вторую строку. Дерево каждый раз
было верным.

**Гейт проверяет сам себя.** Он гоняет свои проверки дважды: на нынешнем виде
они обязаны пройти, а на четырёх подменах — упасть. Подмена возвращает панель
в то состояние, из которого вы уже приносили замечание.

**Что он нашёл в первый прогон — вопрос В-92.** Подсказка шапки списка Fields
раскрыта на 188 точек: она занимает свою колонку целиком, а колонка узкая. И
это та самая подсказка, в которую унесён смысл `Left Block` и `Right Block`.

**Проверять тут нечего — это отчёт.** Смотреть придётся одно: ответ на В-92.

### Разделы 25у–36у — **в архиве**

Унесены 2026-09-08 вечером, по итогу сессии 26. Четыре раздела заказчик
закрыл галочками (30у, 31у, 32у, 35у — подсказки у всех подписей, приглушённая
заливка шапок, три починки и перевод панели), остальные четыре — отчёты, в
которых проверять нечего: список правил каталога (29у), закрытие фазы 6 (33у),
три расхождения с каталогом (34у) и разбор мёртвого вне З3 (36у).
Всё вместе — `docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`.


### 28у. Решено и не начато

Ни одна строка ниже проверки не ждёт: они ждут работы. Из шести пунктов,
стоявших здесь утром 2026-09-08, **четыре сделаны в тот же день** и уехали
разделами 29у–33у; осталось то, у чего срок назначен его же словом.

- **Минификация — в самом конце**, перед выходом наружу: «нужно упростить
  работу по отлавливанию багов». **Не начато и раньше срока не делается.**
- **Мобильные устройства** — отложены его словом, реализовать позже. **Не
  начато.**
- **Проверка вёрстки браузером** — после того, как он примет нынешний вид
  (15.4). **Не начато**, и раньше его приёмки начинать нельзя: первый прогон
  закрепит то, что он ещё не видел.

**И два долга, заведённых этой сессией.** Они не «решено и не начато» — они
закреплены проверкой, которая не даст им вырасти, и разбираются по частям:

- **110 тихих отказов без объяснения** (Д-4). Число закреплено **по файлам** в
  `silent_failures_tests.ts`: новое молчание роняет проверку в том файле, где
  его завели, а разобранное обязано уйти из списка тем же коммитом.
- **182 инлайновых стиля** (Р7 списка расхождений). Целиком брать я не
  советовал: часть из них не оформление, а вычисленная величина, и её в класс
  не перенести. Сначала разбор «величина или оформление», потом правка.

### Разделы 1у–24у — **в архиве**

Всё, что заказчик закрыл галочками до сессии 24, унесено в
`docs/archive/AWAITING_OWNER_CHECK_sessions1_23.md`. Открывать, когда нужно
вспомнить, чем закреплена старая правка или когда её сделали.

## Что заведомо не сделано

Замечанием это не является — работа известна и ждёт своей очереди. Перечислено,
чтобы вы не тратили на неё внимание во время проверки.

**Список выверен 2026-09-01 поиском по схеме, прототипу и коду, а не чтением.**
Пять пунктов из него сделаны и подтверждены заказчиком; разборы — в архиве
реестра: `Floating button`, симметричный цикл `Move right`, подсветка
обработанной строки, три пункта 1.6.4.1 / 1.6.6.1 / 1.6.6.4 и колонка типа
свойства через `app.metadataTypeManager`.

**Остатка у фазы 6 больше нет.** Последний пункт закрыт 2026-09-08: манифест и
снятие команды `Open settings` были сделаны 2026-09-06, `dist/` вышел из
репозитория тогда же, а половина пункта про `main.js` потеряла предмет — после
разбора это 159 строк исходника, а не артефакт сборки. Живого в пункте
оказалась одна вещь, и её в задании не было: последняя заглушка на месте
модуля, `fields_editor_legacy.js`. Разбор — PRD, раздел 11.

Абзац про «остаток фазы 6 — снятие моста модулей» удалён 2026-09-08, а не
унесён в архив: он описывал состояние, которого больше нет (У-31). Моста нет
с 2026-09-07 — проверено поиском: файла `vault_module_bridge.js` не
существует, вызовов моста в рантайме ноль. В архиве этот абзац продолжал бы
врать.

**Остальное решённое и не начатое — раздел 28у выше**, и оно перечислено там,
а не здесь: этот раздел про работу, о которой заказчик не просил, а тот — про
ту, которую он назначил.
