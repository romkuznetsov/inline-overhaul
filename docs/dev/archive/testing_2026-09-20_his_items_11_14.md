# Его пункты 11–14 из заметки тестов — снято 2026-09-20, ночь

**Откуда это.** Раздел «Новое пишите сюда» заметки `test-vault/testing.md`, его
текст ночью 2026-09-20. Все четыре пункта сделаны в 69-й сессии и заведены
отдельными тестами; раздел очищен под новые записи. **Адрес замечания — его
текст**, поэтому он снят сюда слово в слово, а не пересказан (У-30).

```
11. при move-lines up\down нумерованного списка - при перемещении вверх и вниз нумерация не восстанавливается - пример - я двигал sub-lines и у меня нумерация под п.10 у дочерней строки начинается с .3 (а должна с 1) и т.д.
12. в таблицу command-reference 
	3. добавить кнопку в хедеры всех уровней, чтобы при нажатии на нее открывались хоткеи Obsidian с фильтром на все команды, входящие в этот хедера (например для `tags & pkm` отфильтрует все команды внутри, а io-cmd-part-standard-1 - только те, что входят в него. Сделай кнопку там же где у команд `not set` - назови `to hotkeys (визуально должна отличаться от контролов обычных команд - например, цветом - для каждого хедера должен отличаться )
	4. not set - сделать серым шрифтом (чтобы сразу было видно, что хоткей не назначен)
13. в настройках плагина все дефолтные значения контролов должны в названиях иметь приписку `_default` (или лаконичнее - чтобы пользователь понимал, какой вариант контрола является стандартным (например в выпадающем списке 3 значения и т.д.))
14. добавить для field=link в таблицы io-values values колонку - io-values-col-show. У него две опции - default и custom. Custom работает как для fields=tag io-values-col-show (т.е. подмена названия на значение пользователя (эмодзи и т.д.)). При нажатии на этот замененный элемент должна отрываться соответствующая wikilink (т.е. это wikilink, которой может выглядеть как например эмодзи)
```

**Чем каждый кончился.**

| Его пункт | Отчёт | Что стало |
|---|---|---|
| 11 | `271у`, 10.13.219 | перенос строк пишет окно изменившихся строк, и нумерацию возвращает сама Obsidian; исключение к З3 № 135 |
| 12.3 | `269у` | кнопка `to hotkeys` в заголовке каждого уровня, запрос считается по его списку команд |
| 12.4 | `269у` | `not set` без заливки, пунктиром и курсивом — пустое место под клавишу |
| 13 | `270у` | `(default)` у стандартного значения во всех списках строк настроек; списки своих блоков — вопрос к нему |
| 14 | `273у`, 10.13.220 | колонка `Show` у поля-ссылки, подмена текстом и переход по нажатию |

**Его замечание к тесту 3 (changelog)** и **к тесту 4 (вид репозитория)** той
же ночи сняты отчётами `268у` и `272у`; сами тесты остались в заметке — их
проверяет он.
