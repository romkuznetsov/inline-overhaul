# Принятые отчёты циклов 77–81

Унесены из `docs/dev/AWAITING_OWNER_CHECK.md` 2026-09-23, при завершении цикла 82. У каждого сказано, чьим словом принят.

### 319у. Голый адрес — адрес, и выпуск `0.7.0` — **сделано 2026-09-22**

**Ваши слова:** «я передумал, теперь я хочу, чтобы `[подпись](адрес)` (часть адреса) и просто ссылка в тексте управлялись одним контролом `hyperlink-address-color`».

**Сделано.** У голого адреса больше нет подписи вовсе: весь он — кусок адреса, и красит его `Hyperlink address color`. `Hyperlink target color` остался тексту между квадратными скобками. Подписи обоих контролов переписаны — прежние обещали ровно обратное.

**Выпуск `0.7.0` выложен вашим словом.** Второй знак: в разделе есть новые возможности. Проверен обратной загрузкой — `main.js`, `manifest.json` и `styles.css` совпали с локальной сборкой побайтно. Релиз обычный, не предрелиз.

**Одно я называю прямо:** в заметке на момент выпуска было три непомеченных теста, а ваше правило `В-126` велит выпускать при заполненных чекбоксах. Вы сказали выкладывать — я выложил, и говорю об этом, чтобы решение осталось вашим.

**Как проверить:** тесты 2 и 3 в заметке.

**Принято:** тесты 2 и 3 цикла 81, его галочки.

### 317у. У адреса ссылки свой цвет — **сделано 2026-09-22**

**Ваши слова:** «мне не нравится что в `[hyper](link)` цвет `link` управляется hyperlink-brackets-color — сделай отдельный контрол на него».

**Строк цвета в `Link view` стало пять.** Новая — `Hyperlink address color`: она красит адрес между круглыми скобками. `Hyperlink brackets color` остался только знакам — `[`, `](` и `)`.

**Ничего не изменится, пока вы сами не разведёте цвета:** новая строка заводится от цвета скобок, которым адрес красился до неё.

**Голый адрес остаётся на цвете подписи** — ваш ответ `В-191`: там адрес не разметка, а то, что вы читаете. Это сказано в подсказках обоих полей.

**Как проверить:** тест 2 в заметке.

**Принято:** тест 2 цикла 81, его галочка.

### 316у. Скобка — шаг своего права — **сделано 2026-09-22**

**Ваши слова:** «при выделении одного слова перемещение работает нормально, но если выделить несколько слов, то перемещение сбоит — перепрыгивает через скобки».

**Условие оказалось не в числе слов, и я проверил это до правки.** На одном слове шаг перепрыгивал группу так же: `слово2 слово3 (слово1)`, выделено `слово3`, вправо — и сразу `слово2 (слово1) слово3`. Ваш пример описывал форму строки, а не условие.

**Причина — моё правило, а не ваше.** В переносе стояло «края парные — сосед целый», и завёл его я, чтобы шаг не рвал `[[test1]]`. Написано оно было по форме, и форма накрыла всё, что вы обернули в скобки.

**Что теперь.** Целой остаётся **ссылка**, а скобки вокруг обычного текста стали обычным краем. Ваши три нажатия совпали знак в знак: `слово2 слово3 (слово1)` → `(слово2 слово3 слово1)` → `(слово1 слово2 слово3)` → `(слово1) слово2 слово3`. Влево — то же в обратную сторону.

**Заодно перестала рваться разметка ссылки.** `[подпись](адрес)` под прежнее правило не попадала, и шаг давал `[текст подпись](адрес)` — сломанную запись. Теперь она целая. Цена: единица шага — то, что стоит между пробелами, поэтому `[a link](адрес)` с пробелом в подписи по-прежнему два соседа.

**Как проверить:** тест 1 в заметке.

**Принято:** тест 1 цикла 81, его галочка.

### 308у. `Ctrl+Y` после `Ctrl+Z`: измерено, ваш случай не воспроизвёлся — **измерено 2026-09-22**

**Ваш пункт 13** и ваш ответ в интервью — «сначала измерить».

**Стенд научен нажимать `Ctrl+Y`.** Он и раньше гонял настоящую историю CodeMirror и нажимал `Ctrl+Z` до упора; теперь после отмены он столько же раз возвращает и спрашивает одно: стала ли строка снова той, что была перед первой отменой.

**Что он показал.** На всех четырёх сценариях — команда поля, дата, панель слева, оба Block, в обоих режимах противоположного Block — `Ctrl+Y` возвращает строку полностью: четыре отмены, четыре возврата, строка та же. То есть вашими словами «ctrl+y не возвращает обратно» эти шаги не воспроизводятся.

**Контроль на сам шаг возврата стоит:** если первое нажатие подменить отменой, стенд говорит «НЕТ (отмен 3, возвратов 1)» — то есть он умеет краснеть, и «да» выше означает «вернул», а не «мерить было нечем».

**Что это значит для вас.** Ловить осталось **условие**: вы написали «иногда сбоит». Когда поймаете — назовите, что делали до `Ctrl+Z` (какая команда, панель или набор руками), и я повторю это шагами стенда: он принимает свой порядок шагов строкой команды.

**Второе, что стенд сказал сам.** Прежняя форма записи — та, из-за которой замечание про `Ctrl+Z` и возникло, — сегодня даёт чистую историю во всех четырёх случаях. Значит сценарии стенда устарели относительно продукта: они больше не воспроизводят то, ради чего заведены, и на новом замечании их придётся дописывать, а не доверять их «нулю».

**Принято:** его слово 2026-09-22: «убери этот таск, я не могу его воспроизвести».
